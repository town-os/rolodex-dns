# 変更履歴

> 言語：[English](CHANGELOG.md) ｜ [繁體中文](CHANGELOG.zh-TW.md) ｜ [简体中文](CHANGELOG.zh-CN.md) ｜ [Español (España)](CHANGELOG.es-ES.md) ｜ [Español (México)](CHANGELOG.es-MX.md) ｜ **日本語**

## 未リリース

### 破壊的変更

- **RBL は削除されました。DNSBL は残り、それが今やブロックリストです。** 逆引き IP によるプロバイダー照会——逆引き DNS クエリのたびに、反転させたアドレスをプロバイダーゾーンに問い合わせるもの——は無くなりました。あわせて、スコープごとの RBL プロバイダー、`rbl:` 設定セクション、`SetRblConfig`/`GetRblConfig`、`AddScopeRblProvider`/`RemoveScopeRblProvider`/`ListScopeRblProviders`、そして `set-rbl-config`/`get-rbl-config`/`*-scope-rbl` の各 CLI サブコマンドも無くなりました。`src/rbl.rs` は `src/dnsbl.rs` になり、二つのリストが共有していた仕組み——プロバイダー、拒否コード、ローテーション、結果キャッシュ、再帰リゾルバー——は `DnsblChecker`/`DnsblProvider`/`DnsblResolver` として生き残っています。リストが一つになったので、`set_dnsbl_config` の類はただの `set_config` です。

  **アドレスは今も引き続きブロックできます**。プロバイダーではなくローカルリストによってです：運用者がそのアドレス（あるいは `dig -x` が表示する逆引き名）を指定すれば、どちらの綴りでも NXDOMAIN になります。そもそもそれは、プロバイダーがここで決してうまくできなかったことです——プロバイダーが訊かれるのは「解決しようとしている*名前*」であり、逆引きにおいてそれは、誰も評判を公開していない名前だからです。

  **ローカルリストは改名され、その行も移行されます**：`local_rbl_entries` は `local_blocklist_entries` になり、エントリは起動時にそちらへ移されます。それは運用者自身のリストであり、アップグレードでブロックリストが黙って空になったマシンは、ブロックリストが動いていないのと見分けがつきません。`scope_rbl_providers` はそのまま破棄されます——それが設定していた照会自体がもう存在しないからです。gRPC 上では、`LocalRblEntry` とその Add/Remove/List メソッドは `LocalBlocklistEntry` / `AddLocalBlocklistEntry` / `RemoveLocalBlocklistEntry` / `ListLocalBlocklistEntries` になりました。Town OS がそれらを提供している HTTP パス（`/dns/rbl/local*`）は歴史的な綴りのままです。それは UI との間で公開済みの契約だからです。

  proto3 は*フィールド*番号を予約できますが、メッセージやメソッドの**名前**は予約できません。そして引退した部品はどれもメッセージかメソッドの全体でした——そこで proto は代わりに明示的な「引退した名前」ブロックを持ち、それらの名前はいずれも再利用してはいけません。

- **ブロックリストのメトリクスラベルが変わりました。** `blocklist_blocks_total{kind}` は今や `local` か `dnsbl_provider` です。`rbl_provider`、`rbl_local`、`rbl_scope_provider` は無くなりました。`answers_total{source}` は、以前 `rbl` を報告していたところで `reverse_blocklist` を報告します。古いラベル値を名指ししているダッシュボードは更新が必要です。`kind` で合計しているものはそのまま動きます。

### 機能

- **ACME は証明書が担うすべてのエンドポイントに DANE-TA レコードを公開するようになりました。一つだけではありません。** `acme.tlsa_port`／`acme.tlsa_proto` は単一のスカラー対なので、発行は TLSA レコードをちょうど一件——既定では `_443._tcp.<name>`——置くだけでした。TLSA レコードが指すのは証明書ではなく*サービスのエンドポイント*であり、暗号化 DNS は一枚の証明書の上のその二つです：`853/tcp` の DoT と `853/udp` の DoQ。公開されなかった側は DANE を確かめるクライアントに対して閉じる方向で失敗し、それはサーバーに最初から DANE が無かった場合と見分けがつきません。`acme.tlsa_endpoints` は `"<port>/<proto>"` 項目の一覧で、スカラー対と併せて公開され、重複は取り除かれ、プロトコルは小文字に正規化されます。そのため `853/UDP` は誰も問い合わせない `_853._UDP.` という名前ではなく、実際に効くレコードになります。書式の誤った項目は読み飛ばさず起動時に拒否されます。理由は同じで、黙って現れないレコードこそ、この仕組みが防ぐために存在する失敗だからです。

- **暗号化トランスポートが実行時に設定できるようになり、`Set*Config` が嘘をつかなくなりました。** `SetDotConfig`、`SetDohConfig`、`SetDoqConfig` は要求をログに書き、何も保存しないまま `success: true` を返していました——受け取って、捨てていたのです。オーケストレーターにはそれと「動いている」との区別がつかず、そのため暗号化 DNS を設定する唯一の方法は設定ファイルを書いてプロセスを再起動することでした。rolodex が唯一のリゾルバーである機体では、それはその上のすべてにとっての DNS の停止です。

  新しい `TransportSupervisor`（`src/transports.rs`）が DoT/DoH/DoQ のリスナーをプロセスの生涯にわたって所有し、**起動の経路と RPC はいまや一つのコード経路**になりました。`main.rs` は各トランスポートを、RPC が呼ぶのと同じ `apply()` で立ち上げます。したがって起動時に働く設定は、あとから届く設定を適用するのとまったく同じコードによって適用され、両者が別々のことをするように離れていくことはありえません。`:53` は終始触れられません——これらは互いに独立したリスナーであり、片方を設定し直す代価は、その外側には及びません。

  古いリスナーがポートを手放すまで新しいリスナーは起動できないので、古い設定を捨てる前に新しい設定がバインドできることを証明する手立てはありません。代わりに行うのはこうです。まずポート**なしで**確かめられることをすべて確かめ（バインド一覧が解決すること、TLS の材料が読めるか生成できること）、打ち間違えたアドレスや読めない証明書は、古いリスナーがまだ提供している状態で拒否します。次に古いリスナーを止め、**その終了を待ちます**。待たずに abort すると、新しいバインドが古いソケットの閉鎖と競走するからです。そしてそれでもバインドが失敗したら、前の設定を戻し、呼び出し側にはそのトランスポートが落ちていると伝えます。空のバインド一覧はエラーではなく停止です——設定の節を省くことがすでに意味しているのは、それだからです。

  `Get*Config` が報告するのは実際に**バインドされた**アドレスであり、要求がポート 0 を名指していれば、それは要求されたものとは異なります。メッセージには `binds` の一覧が、`TlsConfig` には `self_signed_sans` が加わりました。古い単一の `bind` 文字列も引き続き尊重されるので、一覧より前の呼び出し側はそのまま動きます。`doh.bind` も `dot.bind` と同じ条件で一覧を受け取ります。トランスポートの監督を持たずに組み立てられたサーバー——プロセス内のテスト用の仕掛け——は、持ってもいないリスナーを設定したと称するのではなく `FailedPrecondition` を返します。

- **SVCB と HTTPS のレコード（RFC 9460）、そしてそれに伴う DDR（RFC 9462）。** SVCB のレコード型が無かったため、リゾルバーには `_dns.resolver.arpa. SVCB` に答えるものが何もありませんでした——それこそ、クライアントが自分のリゾルバーの暗号化された接続先を見つける方法です。そのため暗号化 DNS は端末ごとに手で打ち込むしかなく、しかもそれは運用者がその存在を知っていればの話でした。

  値は RFC 9460 §2.1 の表現形式の一行（`1 dns.home. alpn=dot port=853`）であり、`alpn`、`no-default-alpn`、`port`、`dohpath`（RFC 9461）、そして `keyNNNNN=` という逃げ道に対応します。パラメータは、ワイヤ形式が要求する厳密に増加する鍵の順に並べ替えられます。おかげで運用者は好きな順に書いてよく、それでもクライアントが受け入れるレコードになります——検証するクライアントがことごとく捨てるレコードにはなりません。解析は**厳密で、書き込む側で行われます**。解析できない値は保存されてから提供時に飛ばされるのではなく、`AddRecord` が拒否します。あらゆる一覧に現れながら何も答えないレコードこそ、このコードが何度も見つけてきた種類の障害だからです。DNSSEC の正規化は、提供の経路が発するのと同じ rdata を符号化するので、署名されるバイトとワイヤ上のバイトが食い違うことはありえません。

  `arpa.` は今もこの機体の外で解決されることがありません——しかしその拒否はあらゆるローカルの照会の*下*に位置するので、このサーバー自身が持つ指定は自身のレコードから答えられ、機体を出ることは決してありません。それこそ DDR が必要とする性質です。あるリゾルバー自身の指定に答えるのは、そのリゾルバーだけです。

- **まだ発行されていない証明書を名指せるようになりました。** `cert_path`／`key_path` が存在しないファイルを指していると、以前は起動時の強い失敗でした。そのため、これらの経路はファイルが揃ってからでなければ書けず——そして CA がリゾルバーの起動の*あと*に作られる機体では、そこへ至る道は設定を書き直して機体で唯一のリゾルバーを再起動することでした。いまは `auto_self_signed: true` と並べておけば、名指されたが不在の証明書は生成された材料で起動し、既存の 30 秒の巡回が、本物の組が現れたその時にそれを引き継ぎます。再起動は要らず、調整すべきことも何もありません。`auto_self_signed: false` のもとでは今も強い失敗です。それは運用者が「この証明書を出すか、さもなくば何も出すな」と言っているのであり、黙って生成したものを出すのは、起動を拒むよりも悪いからです。

- **上流の解決モードを実行時に切り替えられるようになりました。** `resolution.mode` は起動時に一度読まれるきりで、そのためこれは、オーケストレーターが設定ファイルを書き直してプロセスを再起動しない限り変えられない唯一の上流の挙動でした —— そして機体で唯一のリゾルバーの再起動は、その上のすべてにとっての DNS の停止です。いまは `SetResolutionMode` / `GetResolutionMode`（proto、`src/grpc_service.rs`、Go クライアント、そして `set-resolution-mode` / `get-resolution-mode` の二つの CLI 動詞）がこれを稼働中に変更し、読み出します。ファイルは**起動時の種**です。ゲッターが報告するのは実際に問い合わせを解決しているモードなので、両者は食い違いうるのであって、拠り所は動いているサーバーです。

  この RPC は、ファイルの経路のように警告して `auto` へ落とすのではなく、認識できないモードを `InvalidArgument` で**拒否**します。ファイルは、警告を読めるオペレーターが起動時に一度読むものです。しかし RPC の向こうには答えを待つ呼び出し側がおり、頼んでもいないモードで解決しながら「成功」と告げることこそ、`:53` を濾すネットワーク上で機体が `recursive` に落ち、あらゆる名前が失敗する理由がログのどこにも無い、という事態の始まりです。

  `auto` へ**切り替える**と、起動時の経路と同じ階層の暖機が走るので、切り替え直後の最初の問い合わせが冷えた階層の代償を払うことはありません。また `recovery_probe_loop` はいまや無条件に起動されるので（`auto` の外では何もしません）、実行時に `auto` へ切り替えられたモードでも、回復した階層を取り戻せます。

- **`dot.bind` と `doq.bind` がアドレスの一覧を受け取るようになり、DoT と DoQ が両方のアドレスファミリを覆えます。** それぞれ単一のバインド文字列しか取らず、一つの文字列で両方を名指すことはできません。`0.0.0.0` は IPv4 だけを指し、`[::]` は可搬な代わりにはなりません —— Linux の既定である `net.ipv6.bindv6only=0` のもとでは `[::]` のソケットは v4 射影のトラフィックも受け取るため、同じポートの `0.0.0.0` のソケットと衝突し、あとからバインドしたほうが `EADDRINUSE` で失敗します。この二つのフィールドはいまや**裸の文字列でも一覧でも**受け取り、各項目はいつもの四つの書き方（`ip:port`、`[ipv6]:port`、`primary:port`、`interface:port`）をそれぞれ通り、重複は二度バインドされるのではなく取り除かれます。裸の文字列も引き続き受け取られるので、一覧の書き方が存在する前に書かれた設定はすべてそのまま解析されます。

- **更新された TLS 証明書が、サーバーを再起動せずに提供されます。** すべての TLS リスナー——DoT、DoH、DoQ、ACME 発行者、登録ポータル——は起動時に自分の `rustls::ServerConfig` のスナップショットを一度だけ取っており、`TlsManager::reload()` は存在しテストもされていたのに、どこからも呼ばれていませんでした。したがって証明書の更新は DNS の障害でした：リスナーはプロセスが再起動されるまで期限切れのものを提供し続けます——そして rolodex が*リゾルバーそのもの*であるマシンで、ACME リスナー自身が発行する証明書を更新するために再起動するというのは、断ち切る価値のある循環です。

  リスナーはスナップショットではなく `watch` チャネルに追随するようになり、各マネージャーは自分の証明書ファイルを 30 秒ごとにポーリングして、内容が変わったら組み直します。すでに確立された接続は、ハンドシェイクしたときの証明書のまま終わります。次に到着した接続が新しいものを受け取ります。何も再バインドしないので、ポートが閉じることは一度もありません。DoT は接続ごとに acceptor を構築し、DoQ は accept ループの中で `set_server_config` によって差し替えを適用し、三つの HTTPS リスナーは axum-server が接続ごとに読み直す `RustlsConfig` へ書き込みます。

  検出はファイルの**内容**のポーリングで、それを解析するのと同じパスでハッシュします。inotify の監視では、実際の更新がふつう取る二つの形——古いパスへのリネーム上書き、あるいはバージョン付きディレクトリへのシンボリックリンクの張り替え——を取り逃がします。どちらも監視対象の inode に書き込まないからです。ポーリングが失敗したときは、直前の証明書を提供したまま再試行します。フィンガープリントは読み込みに成功した後にのみ記録されるからです：ACME クライアントは二つのファイルを書き、三十秒のタイマーはいずれその間に落ちます。`rustls` は不揃いな組を拒否し、揃った組は次のティックで拾われます。生成された材料（`auto_self_signed`）を提供するマネージャーはポーリングされません——タイマーで再生成すれば、一分に二度、どのクライアントにも違う証明書を渡すことになります。

### 修正

- **gRPC で有効にされたブロックリストにも、外向き `:53` の到達性の探索が届くようになりました。** 提供者への参照は素の `:53` で出ていくので、それを濾すネットワークでは時間切れになって遅延を足すだけです。その探索は、それに気づいて参照を飛ばし、目立つ印をログに残すために存在します。ところがそれは**設定ファイルの** `dnsbl.enabled` が真のときにしか起動されませんでした —— しかしこれを動かすオーケストレーター（Town OS）は `SetDnsblConfig` でブロックリストを書き込み、設定ファイルをいっさい書きません。そのため、そうした機体では探索は一度も走らず、`resolver_available` は `true` の既定のまま留まり、提供者への参照はことごとく時間切れになり、その理由はログのどこにも残りませんでした。このループはいまや無条件に起動され、チェッカーの*実行時の*有効フラグを読みます —— `recovery_probe_loop` がすでに持っていたのと同じ形です。ブロックリストが切れている間はネットワークの探索をまるごと飛ばし —— 探すべきものが無いからです —— フラグを読み直すだけにします。それはアトミックな読み出しなので十分に頻繁に走り、実行時に有効にされたブロックリストが探索の間隔をまるまる待たされることはありません。

- **DoT リスナーが `dot` という ALPN トークンを広告します。** 以前は何も広告していませんでした：`main.rs` は DoT リスナーの TLS 設定を空の ALPN リストで組み立てていたので、`dot` を提示するクライアント——IANA が DNS-over-TLS に割り当てたトークンであり、スタブリゾルバーが、そのポート上の他のあらゆる TLS サービスと DoT リスナーを見分けるために送るもの——は、その問いに何の答えも得られず、推測するしかありませんでした。今このトークンは提示され、しかも**それだけ**が提示されます：`dot` を求めるクライアントはそれを折衝でき、他のプロトコルしか提示しないクライアントは黙って提供されるのではなく拒否され、ALPN 拡張をまったく送らないクライアント（Android の Private DNS、日和見モードの systemd-resolved）は以前とまったく同じように提供されます。TLS がハンドシェイクを失敗させるのは、クライアントがプロトコルを提示していて、そのどれとも一致しないときだけだからです。

- **生成された TLS 証明書が、そのリスナーのバインド先アドレスを名乗るようになりました。** `auto_self_signed` は `localhost`、`127.0.0.1`、`::1` だけを覆う証明書を、どのトランスポートでも作っていました。LAN に面した DoT リスナーでは、その証明書は自分が接続した名前を確認するどのクライアントにも使えません——それは認証名を設定したすべてのクライアントであり、生の公開鍵ピンニングを除けば自己署名証明書に許される唯一の検証でもあります。各リスナー自身のバインドアドレスは今、自動的に証明書へ畳み込まれ、`<transport>.tls.self_signed_sans`（新規。`dot`、`doh`、`doq`、`acme` にあります）が、バインドアドレスでは名乗れない識別子を運びます：マシンのホスト名、その `.local` 名、LAN のエイリアス。ワイルドカードのバインド（`0.0.0.0`、`::`）は何も寄与しません。誰も接続先に指定する識別子ではないからです——ワイルドカード上のリスナーにはこのリストが要ります。重複する綴りは畳まれ（`[::1]` と `::1`、`DNS.Home.` と `dns.home`）、`cert_path`／`key_path` が設定されているときはこれらは一切適用されません。これは名前の確認であり、まず最初に失敗します。その証明書を信頼することは、依然として別の行為です。

- **どの委任も告げないゾーンカットが、拒否ではなく踏み越えられるようになりました。** 一台のネームサーバーが親ゾーンと、その署名済みの子ゾーンの*両方*に権威を持つとき、子ゾーン内の名前へのクエリは子ゾーンから直接答えられます——委任は一度も送られません——そのため「最後にたどった委任」から鍵を選ぶリゾルバーは、子の署名を親の鍵で検証してしまい、まったく正しい答えを bogus と判定していました：

  ```
  answer for cdnjs.cloudflare.com. A is bogus: RRSIG over cdnjs.cloudflare.com. A
  is signed by cdnjs.cloudflare.com., which is not the zone cloudflare.com.
  ```

  これは v0.5.1 が名指ししたまま未修正で残した、あの検証器の欠陥です。RFC 4035 §5.3.1 が決着をつけます：どの鍵が適用されるかを述べるのは RRSIG の署名者名です。答え、CNAME のホップ、あるいは否定応答の不在証明を検証する前に、リゾルバーは署名が現在のゾーンより下のゾーンを名指ししていないかを確認し、信頼の連鎖をそこまで、一度に一つのカットずつ延ばすようになりました——委任が決して届けなかった DS を取得しながら。各カットの確立のしかたは、委任によるものとまったく同じです——DS が親の鍵の下で検証を通ること、子の DNSKEY RRset がそれと一致すること、DS の不在は*証明*されなければならないこと——そして確立できないカットは、やはり答えを差し止めます。

  同じインフラ上にサブゾーンをホストしているプロバイダーの背後にある名前は、すべて影響を受けていました。これを見つけたのは `cdnjs.cloudflare.com` で、その SERVFAIL は、そこからアセットを読み込むページを真っ白なまま描画させます。

  この降下は応答側から操縦できません：署名者を追うのは、そのセクション内のすべての RRSIG が同じ署名者を名指ししていて、その署名者が現在のゾーンの厳密に内側にあり、かつ自分が署名したすべての所有者名を含んでいるときだけです。最後の条件が無ければ、偽造された答えが、本当に未署名の兄弟ゾーンを自分の署名者に指名し、親にその委任が DS を持たないことを正直に証明させ、本物のゾーンが署名しているデータを insecure として受け入れさせることができてしまいます——`a_signer_below_the_zone_that_does_not_enclose_its_owner_is_refused` がその攻撃であり、この確認を外すとテスト群が失敗します。

- **親のネームサーバー上にある*未署名の*子ゾーンが、見えるようになりました。** それは同じ隠れたカットで、追うものが何も無い場合です：応答に署名が一切無いので、上記の降下がたどるべき署名者名が存在せず、拒否されたままになります——そのパケットは、転送中にすべての RRSIG を剥ぎ取ったときに生じるものと寸分違わず、リゾルバー側からは両者を区別できません。`dnssec_unsigned_responses_total{evidence}` がそれを一件ずつ数えるようになりました。権威セクションの SOA が現在のゾーンより下のゾーンを名指ししているとき（未署名の子ゾーンの否定応答が持つもの）は `child_apex_soa`、それ以外は `none` というラベルが付きます。その SOA は未署名であり偽造可能なので、あくまで診断用で何も決めません。これ以前は、このケースは他のどの SERVFAIL とも区別できない SERVFAIL 以外に痕跡を残しませんでした。

- **新しいメトリクスが二つ**、公開ファミリー数は 80 になりました：`rolodex_dns_dnssec_hidden_zone_cuts_total` は、問い合わせたゾーンより下のゾーンによって署名された応答を数えます（つまり上記の DS 照会が支払われた回数）。もう一つは上で説明した `rolodex_dns_dnssec_unsigned_responses_total{evidence}` です。

### テスト

- **リゾルバー可用性の探索ループが覆われました。** `src/dnsbl.rs` の二つの事例で、注入した探索とミリ秒の間隔で駆動するため、ネットワークには一切触れません。切れているブロックリストは決して探索してはならず（それは理由もなく公開のリゾルバーへ流すトラフィックです）、`SetDnsblConfig` が有効にするのと同じやり方で —— 設定ファイルを介さずに —— 有効にされたブロックリストは探索され始めねばならず、その探索の判定が実際にチェッカーへ届かねばなりません。呼ばれはしたが答えが捨てられる探索は、同じ障害そのものです。

- **`BindList` は、回帰が静かに起きうる箇所で覆われました。** `src/config.rs` にて：裸の文字列の `bind:` が引き続き解析されること（一覧の書き方が存在する前に書かれた設定はすべてこれを使うので、これを落とせば更新時に壊れます）、一覧が解析されて両方のファミリを解決すること、項目が一つの一覧が一要素の一覧ではなく裸の文字列として書き戻されること（さもなければ世にあるスカラーの `bind:` がすべて一要素の一覧に変わってしまいます）、重複したアドレスが一度だけ解決されること、空の項目が飛ばされること、そして誤った項目がエラーの中で自らを名指すこと。

- **証明書リロードの経路がエンドツーエンドで覆われました。** `tests/tls_reload_test.rs` はポーリング側の半分を得ました：変化していない組は再読み込みされないこと（ポーラーは一日 2,880 回走るので、無条件に再読み込みする版はその頻度で再公開してしまいます）、入れ替えられた組は検出され提供されること、自己署名のマネージャーはポーリングするものが無いのでポーリングタスクを与えられないこと、そしてそのタスク自体が、他に何も呼ばれないまま入れ替えを拾うこと。牙のあるケースは**書き込みの途中**で捕まえた更新です——それは失敗し、古い証明書を提供したままにし、その後で完成した組を拾わなければなりません。読み込んだものではなく見たものを記録するポーラーは、その引き裂かれた状態を新しい常態として扱い、再起動まで古い証明書を提供し続けます——それこそがこの仕組みが取り除くために存在する失敗そのものであり、しかも正常に動いているように見えてしまいます。`tests/dot_test.rs` はリスナー側を固定します：稼働中の `serve_dot` の下で入れ替えられた証明書は次の接続に届き、すでに開いている接続は乱されず、リスナーは今までどおり答えます。

- **`tests/dot_test.rs` は DoT が*しなければならないこと*を覆います。** `src/dot_server.rs` にはコンパイルのスモークテストが一つあるだけで、`tests/security_dot_limits_test.rs` はリスナーが拒否すべきことしか覆っていませんでした。トランスポートの折衝、メッセージのフレーミング、応答、次のクエリのためにセッションを開いたまま保つこと——どれも動かされていませんでした。二つの半分に分かれているのは、それぞれ別の問いに答えるからです。プロセス内では、本物の `tokio-rustls` クライアントが `serve_dot` に対して：上記三通りの ALPN の結末、2 バイトの長さプレフィックスとそれに続く本体の整合、書き込んだ名前には答えが返り書き込んでいない名前には NXDOMAIN が返ること、そして一本の接続が四つのクエリを運び、その ID と質問が突き合わされること。プロセス外では、本物の `rolodex-dns` バイナリが `dot:` セクションを持つ設定ファイルに対して——上記どちらの修正も捕まえられたのはこれだけです。プロセス内のハーネスは自前の `rustls::ServerConfig` を組み立てるので、ALPN トークンを一度も要求しない、あるいは自分のバインドアドレスを名指ししない `main.rs` を見ることができません。この半分は管理ソケット越しにレコードを書き込み、それを DoT 越しに引き直し、サーバーが実際に提示した証明書からサブジェクト代替名をデコードします。バインド先を `127.0.0.2` にしているのは意図的です：ループバックの集合は生成されるすべての証明書に焼き込まれているので、その集合の外側のアドレスだけが、バインドアドレスが既定値ではなく導出されたことを証明できます。

### ドキュメント

- **文書一式がすべての言語で揃いました。** `README.md`、`DESIGN.md`、`CHANGELOG.md` にヨーロッパスペイン語、メキシコスペイン語、日本語の翻訳が加わり、五つの文書すべてが六言語で存在するようになりました。存在しないファイルを指す言語ナビ行はもうありません。英語は引き続き唯一の真実です：まず英語を変え、翻訳は後続の作業であって、編集する二つめの場所ではありません。両者が一致することは何も検証していません——`tests/promql_docs_test.rs` が読むのは英語の `README.md` と `DESIGN.md` だけなので、翻訳の中の PromQL ブロックやファミリー数はドキュメントであって、検査されたアサーションではありません。

  日本語のサフィックスは `.ja.md` ではなく `.ja-JP.md` になりました。これで一式のどの言語も地域コードになり、サフィックスの読み方がどこでも同じになります。変わったのはファイル名と言語ナビ行だけです。

  中国語の翻訳も、一リリース分ずれていたところを英語と同じ水準まで戻しました：`arpa.` サブツリーの規則、v0.5.1 のルート階層での拒絶とルートサーバーへの帰責、そしてメトリクスファミリー数です。

## v0.5.1（2026-08-12）

### 破壊的変更

- **`arpa.` はこのマシンの外で解決されることが決してありません。** その下のあらゆる名前は、ローカルデータ——保存済みの PTR、スコープ付きレコード、管理下または権威のある逆引きゾーン——から答えられるか、そうでなければ **REFUSED** です。これはすべての解決モードで同じです。このサブツリーの中身がルートサーバー、フォワーダー、暗号化上流に届くことはありません。NXDOMAIN ではなく REFUSED なのは、サーバーがある名前空間について答えることを辞退しているのであって、その名前が存在しないと主張しているのではないからです。

  このゲートは二つの層に置かれています：クエリ経路は「このマシンが持っているデータ」と「取りに行かなければならないデータ」の境界で拒否します——上流応答キャッシュより前なので、古いポリシー下でキャッシュされたものが提供されることはありません——そして反復リゾルバーはパケットを送らずに拒否するので、そのサブツリーを指す CNAME のターゲットやグルー無しの NS ホスト名も同じ確認で覆われます。所属は最後の**ラベル**で判定され、文字列サフィックスでは決してないので、`notarpa.` と `arpa.example.com` は普通の名前として正常に解決されます。

  これにより、この作業のきっかけとなった `ipv4only.arpa`（RFC 7050 の NAT64 探索）の SERVFAIL が消えます：ルートと `arpa.` が同居して提供されるゾーンカット——一つのクエリが二つのカットを跨ぎ、委任の NSEC は `arpa.` によって署名されているのに、走査はまだルートの鍵に対して確認している——には、もはやまったく到達できません。根底にある検証器の欠陥——委任の証明を、RRSIG 署名者のゾーンではなく想定した親に対して確認すること——は手つかずで、依然として未解決です。

  後から発見するのではなく先に述べておくべき帰結が二つあります：このマシンがデータを持たないアドレスの逆引きは、インターネットから答えられるのではなく拒否されます（`dig -x 8.8.8.8`）。そして `ipv4only.arpa` は拒否され、NAT64 を探索しているクライアントはそれを「ここに NAT64 は無い」と読みます。逆引きツリー全体をローカルデータからきちんと提供することは、別の、先送りにされた作業です。

- **検証できないルートゾーンは、静かな降格ではなく障害になりました。** `fetch_dnskeys` は `Unreachable`（トランスポート）と `Invalid`（暗号）を区別します。ルートでは `Invalid` は結果を差し止め、連鎖はそこで止まります。`Unreachable` は意図的に下へ落ちます——到達不能は無効ではないからです。この二つを平らに潰していたために、ルート DNSKEY の取得を確実に壊せる者は、bogus の判定を一つも生じさせずに検証を経路から外すことができていました——フォールバック連鎖がそのエラーを「ルートに到達できない」と読み、検証をしない上流から答えていたのです。

  トレードオフは本物です：このビルドが知らないトラストアンカー（KSK ロールオーバー）は、DoH への静かな降格ではなく DNS 障害になります。アンカーを直すまでの脱出口は `dnssec.validate: false` です。

### 機能

- **無効な DNSSEC を提供するルートサーバーはルート集合から外されます**。最初は 15 分、再犯ごとに倍増し、上限 24 時間です。判断の根拠は、誰にも訊かずに確認できる唯一の主張——ローカルのアンカーに対するそのルート DNSKEY——だけです。この帰責はサーバーが素早く応答しても消えず（`note_success` はトランスポート系のフィールドだけを消すようになりました）、**検証を通った**答えによってのみ解除され——待つことでは決して解除されず——最後に残った一台には決して適用されません。すべてのルートが同時に失敗するのは、十三台の不正なサーバーではなく、ゾーンかアンカーの問題だからです。ルートサーバー限定です：ルートより下では、検証の失敗はたいていそのゾーン自身の署名ミスであり、それらはすでに安全に失敗します。帰責はメモリ上にあるので、再起動すればすべてのルートを再び信頼します。

- **新しいメトリクスが一つ、`rolodex_dns_dnssec_blamed_roots`**。公開ファミリー数は 78 になりました。ルート集合の一部を長期間、無言で除外し続けることは、この件のうち既存のカウンターが何も報告しない唯一の部分です。ファミリー数と PromQL のレシピ集は `README.md` と `DESIGN.md` で更新済みです。

### バグ修正

- **Bogus で終わった DNSSEC 走査が、もう何も残しません。** 委任とそのグルーは、`extend_trust` が使える信頼状態を返した後にのみキャッシュされるようになりました。以前は先に書き込んでいたため、DS/NSEC の証明に失敗した委任でも、その NS セットはすでに確定済みでした——しかもディスクに永続化され、再起動を越えて生き残っていました。

### ドキュメント

- **機能仕様が `CLAUDE.md` から `DESIGN.md` へ移りました。** `CLAUDE.md` は今や開発ルールだけです。ソフトウェアが何をするかは仕様書にあり、それこそが振る舞いの変更が着地すべき文書です。

- **翻訳は地域ロケールコードで並びます。** 中国語の翻訳は BCP 47 の用字サブタグ `zh-Hant`/`zh-Hans` から `zh-TW`/`zh-CN` に移りました。スペイン語が地域で分かれた今、文書一式のどのロケールサフィックスも同じ問いに答えるようになります。`CLAUDE.md` と `CONFIGURATION.md` にヨーロッパスペイン語（`.es-ES.md`）、メキシコスペイン語（`.es-MX.md`）、日本語（`.ja.md`）の翻訳が加わり、`Cargo.toml` の `include` リストがそれらを配布します。

  `README.md`、`DESIGN.md`、`CHANGELOG.md` はこの三つの新しいロケールに**まだ翻訳されていない**ので、それらの言語ナビ行は今のところ存在しないファイルにリンクしています。英語は引き続き唯一の真実であり、翻訳が一致することは何も検証していません——`tests/promql_docs_test.rs` が読むのは英語の `README.md` と `DESIGN.md` だけです。

### テスト

- **`tests/arpa_refusal_test.rs`**（新規。Makefile の `rust-integration-test` レシピにも記載済み）は rcode ではなく*パケット*についてアサートし、ラベル境界とローカルデータ経路を対照に据えて、三つの解決モードすべてを掃きます。何でも拒否する `arpa.` ゲートは「このサブツリーは拒否される」を満たし、何も拒否しないゲートは「`notarpa.` は今も解決する」を満たします——対にしてはじめて何かを語ります。

- **`tests/security_dnssec_test.rs`** はこれらの拒絶規則を得ました。動くカウント付きフォワーダーを備えた本物の `DnsServer` を通して駆動します。「クライアントが SERVFAIL を受け取った」と「フォワーダーは一度も問い合わされなかった」は別の性質であり、発見そのものは後者だけだからです。

## v0.5.0（2026-08-10）

### バグ修正

- **クライアントのクエリが、降格した階層の探索に費やされることはもうありません。** `auto` モードでは、クエリ経路が `resolution.recovery_probe_secs`（既定 60 秒）ごとに一つの検索を選び、階層チェーンをルートからやり直させて、復旧したより望ましい階層を見つけられるようにしていました。それ自体は望んで当然のことですが、その代金を一人の呼び出し元に請求するのは最悪です：外向きの `:53` を落とすネットワークではルート階層は答えられないので、選ばれたクエリは反復走査の全費用——最大で `MAX_QUERIES_PER_RESOLUTION` 回の上流クエリ、それぞれネームサーバーごとのタイムアウト分——を支払ってから、はじめから答えるはずだった階層へ落ちていました。毎分一回、運の悪い検索が数十秒止まり、たいていクライアントの方が先に諦めます。それはユーザーには「探索中」ではなく「DNS が固まる」と読めます。

  `auto_start_tier` は常に確定済みの階層そのものになり、探索クエリを選出していた compare-exchange もそれとともに無くなりました——今や探索者は常に一つだけなので、選出は不要です。

### 機能

- **非同期の階層復旧。** 復旧は `recovery_probe_loop` へ移りました。確定済み階層より上の階層を `resolution.recovery_probe_secs` ごとに、自前の使い捨てカナリアで再試験するバックグラウンドタスクです。その結果は捨てられます——この探索は確定階層を動かすために存在するのであって、何かに答えるためではありません——ので、超過してもどのクライアントの答えも損なわれません。それこそが、下記の時間上限を「待っている人の忍耐」ではなく「正しさ」で選べた理由です。間隔は前回パスの終了時ではなく前回の探索から測られるので、遅い探索が忙しい探索に化けることはなく、毎パス読み直されるので実行中の設定変更が再起動なしで効きます。

- **ルート階層の奪還には DNSSEC 検証済みの答えが要ります。** 階層 0 が昇格するのは、ルートゾーン自身の `DNSKEY` が `Verdict::Secure` として解決されたときだけで、単に到達できただけでは昇格しません。`:53` に居座る傍受ミドルボックスやキャプティブポータルは到達可能で素早く応答します——好きな内容を答えるだけです——ので、このゲートが無ければ、ポート 53 を乗っ取るネットワークは最も信頼される階層として自分を据え、マシンが正しくフォールバックしていた暗号化上流を静かに自動で押しのけられました。検証を通った答えは、そうしたミドルボックスに偽造できない唯一のものであり、ルート自身の DNSKEY は実際に問われている問いの最も狭い形です：このマシンはルートサーバーに到達して、そこから信頼の連鎖を築けるか。

  `dnssec.validate: false` ではゲートにできる判定がありません——リゾルバーは設計上すべてを `Insecure` と報告します——ので、代わりに決定的な答えが要求されます。そこで `Secure` を要求すると、意図的に検証しないマシンがフォールバック階層に永久に取り残され、既定かつ望ましいモードである再帰解決を一度も使えなくなってしまいます。

- **ルート階層は、クエリ数だけでなく実時間でも縛られます。** 反復リゾルバーは自らを*クエリ数*で予算化します。それは「この名前にどれだけの労力が見合うか」には答えますが、「呼び出し元をどれだけ待たせてよいか」には答えられません——そして待っている人にとって重要なのは後者だけです。ブラックホール化した `:53` はそれらのクエリを一つ残らずタイムアウトさせるので、回数予算だけでは一回の検索が一分以上走ることを許してしまいます。この階層は今、クエリ経路で 8 秒、復旧探索で 2 秒の上限を持ちます。

  他の階層が使う 1.5 秒ではなく 8 秒なのは意図的です：コールドな反復解決は複数の逐次ラウンドトリップ（ルート、TLD、ゾーン、加えて検証に必要な DNSKEY 取得）ですが、セキュア階層と転送階層は一台へのラウンドトリップ一回です。実測したコールド検索は公開名で 0.6〜1.9 秒、RFC 1918 の PTR でおよそ 2.7 秒なので、その辺りの上限は病的な状態を縛るのではなく——健全な再帰を壊し、遅いが正常な検索をすべて失敗させ、動いているリゾルバーを DoH へ降格させてしまいます。探索の 2 秒がずっと厳しいのは、問いがずっと小さいからです：一台への一クエリ、たどる委任なし。

### テスト

- **`tests/recovery_probe_test.rs`** は、DNSSEC 署名済みのモック階層に対して、意図的に両方向を覆います：検証を通ったルートは*奪還され*、到達はできるが未署名・他者署名・期限切れ署名を提供するルートは*奪還されません*。決して開かないゲートはすべての否定テストを通り、常に開くゲートはすべての肯定テストを通ります——対にしてはじめて何かを語ります。クライアントのクエリが探索に費やされないという防御も固定し、実時間の上限と、探索が何もしないケース（すでに最上位階層、あるいは `auto` モードでない）をユニットテストが覆います。

## v0.4.6（2026-08-09）

### バグ修正

- **DoH コネクションプールのテストが互いに競合しなくなりました。** `DOH_POOL` はプロセスグローバルな `DashMap` で、四つのプールテストは一つのバイナリ内で並行に走るのに、それぞれが前後で**グローバルな** `DOH_POOL.clear()` を呼んでいました。どれか一つが、他が入れたばかりのものを消してしまうので、`test_get_doh_connection_reuses_pooled` はエントリを挿入した次の行で `None` を見つけることがありました——再現可能な失敗ではなく、たまに起きる説明のつかないパニックです。だからこそ何度も緑の実行を生き延びてきました。

  各テストは自分のキーだけを削除するようになりました。直列化のためのロックは不要です：各テストは自分のエフェメラルポートをバインドするので、同時に走る二つが同じアドレスを持つことはなく、テスト間で再利用されたポートは、それを使った側がすでに削除しています。プール検索に対する二つの裸の `unwrap()` は、実際に何が見つかったかを報告する形に置き換えたので、次にここで失敗したときは匿名でパニックする代わりに問題を名指しします。

## v0.4.5（2026-08-09）

### 機能

- **実際にどのリストが一致したかでブロックリストの帰属を分割。** `blocklist_allowlisted_total` に `kind` ラベル——`forward_name`、`reverse_name`、`ip_literal`——が加わり、`rbl_scope_provider` がブロック種別に加わりました。

  許可リストのラベルが名指しするのはリストではなく*一致経路*で、それはこの確認が置かれた場所が強いています：免除はプロバイダー照会が発行される前に短絡するので、それが発火した時点では何も問い合わせておらず、「どのリストが一致したはずか」を記録しようがありません。代わりにゲートを名指しすることは、知り得ることであり、かつより有用な軸でもあります——正引き名での免除（ステップ 7）と逆引きでの免除（ステップ 2）を分け、後者の中で `in-addr.arpa` 綴りと IP リテラルを分けます。両者は異なる規則（サフィックス対完全一致）で一致するからです。

  `rbl_scope_provider` が存在するのは、あるネットワークが自分で選んだプロバイダーと、マシン全体のリストとが、影響範囲の異なる別々の運用判断だからです。まとめてしまうと「このネットワーク自身のブロックリストがこのネットワークを壊した」と「グローバルのリストが全員を壊した」が区別できません。あるネットワークから逆引き DNS が動かなくなったと報告されたとき、まず知りたいのはそこです。分割の費用はゼロです：二つの確認は互いに素なプロバイダー集合を覆い、結果キャッシュは `<ip>/<zone>` ごとのキーです。新しい種別は末尾に追加され、決して挿入されません——`BLOCK_*` 定数は配列上の位置なので、挿入は既存のカウンターすべてを黙って付け替えてしまいます。

- **トラフィック量。** `traffic_bytes_total{direction=rx|tx}` と `records_served_total` はいずれも、すべてのトランスポートが漏斗のように通る唯一の計装済み出口で記録されるので、クエリが数えられてそのバイト数が数えられないということが起こりません。レコード数は、サーバーが直前にシリアライズしたメッセージを再解析するのではなく、応答ヘッダーの ANCOUNT フィールドから読みます。クエリ数だけではこれは見えません：百万件の NXDOMAIN と百万件の中身のある答えは同じクエリ数でありながら仕事量はまったく違い、tx/rx 比は外から到達できるリスナーで見張る価値のある増幅率です。

- **TLD ごとの分離。** `queries_by_tld_total{tld}` はクエリの流れを TLD で分けます。それが、分割視域デプロイのネットワーク同士を、そして公開インターネットとを、区別可能にします。

  この次元は運用者が所有する集合で縛られています。問い合わせられる名前を選ぶのはクライアントだからです：縛りの無い `tld` ラベルは、`a.zzz1`、`a.zzz2`、… と掃くスキャナーに、レジストリがプロセスを食い尽くすまで系列を鋳造させてしまいます。三つの供給源が和集合として流し込みます——ネットワークスコープが所有するあらゆる TLD（各スコープの暗黙の `.home` ドメインを含む）は**自動的に**追跡されます。ネットワーク自身の名前空間を二度名指しさせるのは、系列が黙って欠けるという形で表れる罠だからです。次に新しい設定リスト `metrics.tracked_tlds`。これは固定されていて再起動を越えて残り、API では消せません。そして新しい `SetTrackedTlds`/`ListTrackedTlds` RPC が管理する保存済みリスト。CLI サブコマンド `set-tracked-tlds`/`list-tracked-tlds` と、対応する Go クライアントメソッドもあります。追跡外のものはすべて `other` に畳まれます。

  エントリ `common` は組み込みの一般 TLD 集合に展開されるので、よくある公開 TLD は二十行ではなく一行です。しかも**展開せずに**保存されます——読み戻すと運用者が求めたものが返り、後日その組み込みリストが変わっても各デプロイが呼び出しを出し直す必要がありません。`refusal_codes` の `none` と同じ形です。一致判定は名前のサフィックスを最も具体的なものから順に歩き、名前のスライスを返すので、`home.` と `lab.home.` の両方を追跡しているデプロイは `box.lab.home.` をより具体的な方に帰属させ、追跡外の名前は何も確保しません。ルートゾーンは `InvalidArgument` で拒否されます：`.` はあらゆる名前のサフィックスなので、追跡すればすべての系列が一つに潰れ、受け皿に到達できなくなります。

### 破壊的変更

- **DHCP のメトリクスラベルはサブシステム名で限定されました。** `rolodex_dns_dhcp_messages_total{type}` は `{message_type}` に、`rolodex_dns_dhcp_leases{state}` は `{lease_state}` になりました。汎用的なラベル名こそが、両サブシステムに跨る集約——記録ルールの `sum by (type)` など——に DHCP の ACK 数を DNS のそれに黙って混ぜ込ませます。古い名前で選択しているダッシュボードとアラートは更新が必要です。DNS の総計（`queries_total`、`traffic_bytes_total`、`records_served_total`、`queries_by_tld_total`）も同様に DNS だけを数えます：DHCP の `:67` トラフィックが DNS トラフィックになることは決してなく、DHCP で登録された名前がこれらのメトリクスに現れるのは、誰かが実際にそれを解決したときだけです。

### テスト

- **ドキュメントに載った PromQL は二層でテストされます。** `tests/promql_docs_test.rs` は `README.md` と `CLAUDE.md` からすべての ```promql ブロックを取り出し、メトリクス名とラベルマッチャーを抽出して、それぞれを生の公開出力に対して解決します。ドキュメントは、メトリクス変更のうち他に検証するものが何も無い唯一の部分です：上記の DHCP 改名はコードをコンパイル可能なまま、他のすべてのテストを緑のまま保ちながら、ドキュメント化された各ダッシュボードクエリをデータを返さないものへ静かに変え、運用者はインシデントの最中にパネルが真っ白になって気づきます。またドキュメント上のファミリー数を、レジストリが実際に出す数に対して固定します——これはすでにずれていました。文書上 73 に対し出力は 74 です——さらにフェンス自体も守ります。` ```bash ` に付け替えられたブロックは、ファイル全体を静かに何も確認しないものにしてしまうからです。

  `tests/prometheus_integration_test.rs` は同じクエリを、稼働中のサーバーをスクレイプする本物の Prometheus に通し、部分文字列スキャナーには捕まえられないものを捕まえます：存在しない系列を名指ししているだけでなく、*PromQL として*不正なクエリです。`rate(sum(x)[5m])` は実在の系列しか名指ししていませんが、貼り付けた瞬間に拒否されます。これは `make prometheus-test` から走り、`make test` がそれに依存するようになりました。podman が無ければ失敗ではなく大きな声でスキップするので、コンテナランタイムの無いマシンでも緑の実行が得られますが、クエリを確認したふりは決してしません。`ROLODEX_PROMETHEUS_REQUIRED=1` は CI 向けにそのスキップを失敗へ格上げします。

- 新しい各次元にユニットテストと結合テストがあり、それぞれ対照と対になっています——とりわけ、追跡外の TLD が系列を**一つも**鋳造しないこと。肯定側だけをアサートするテストは、カーディナリティの縛りを完全に外しても通ってしまうからです。

### パッケージング

- **クレートは公開可能で、自身のドキュメントを配布します。** `readme` と `homepage` を宣言し、明示的な `include` リストが「省略によるパッケージング」を置き換えます：公開パッケージはソース、proto、ベンチマーク、テストとともに `README.md`、`CONFIGURATION.md`、`CHANGELOG.md`、`CLAUDE.md`、`LICENSE` を運びます。

  `exclude` ではなく `include` なのは、作業ツリーには Go クライアント、JavaScript クライアント、ブラウザー拡張、コンテナ関連のツールなど、クレート利用者には用の無いものも入っており、除外リストでは新しく増えるたびに静かに配布し始めてしまうからです。`proto/` と `build.rs` は構造上必須です（gRPC バインディングはビルド時に生成されるので、それらの無いパッケージはコンパイルできません）。`benches/` が配布されるのは `[[bench]]` がターゲットを名指ししているからです——宣言されたターゲットのファイルが無いと cargo は失敗するので、省略はパッケージを小さくするのではなく壊します。

  `tests/` が配布されるのは、公開されたクレートを受け取った側が検証できるようにするためで、AGPL のネットワークサービスにとってはそれこそが要点です。`CLAUDE.md` をリポジトリ専用ツールとして扱わずに含めているのも同じ理由です：`tests/promql_docs_test.rs` が `README.md` と並べてそれを読むので、テストを運びながらその入力を運ばないパッケージは、走らせられないテストです。すべてのドキュメントファイルがチェックアウトでも公開パッケージでも必ず存在すると保証されたので、このテストは読み込めないものを飛ばすのではなく、一つ一つを見つけることを要求します——飛ばすというのは、確認が静かに何も検証しなくなる道筋そのものです。

### ドキュメント

- **`CONFIGURATION.md` — 設定ガイド。** README の設定セクションはフィールドリファレンスで、「このオプションは何をするか」には答えますが「私の設定はどうあるべきか」には答えません。ガイドはタスク指向です：ファイルがどう読み込まれるか（無いことはエラーではないこと）、動く最小の設定、四つの実例デプロイ形態（家庭用リゾルバー、純粋に権威、分割視域のオーバーレイノード、`:53` を落とすネットワーク上のリゾルバー）、そしてサブシステムごとに一節。どの設定が*そもそも設定ではないか*——レコード、スコープ、ゾーン、ブロックリストのエントリ、DNSSEC 鍵、ACME の CA は SQLite 上の実行時状態です——をはっきり述べ、再起動が必要なものの表、サーバーが意図的に起動を拒む四つの条件、症状で引くトラブルシューティング表を添えます。

  二つの CIDR リストは並べて比較されます。`overlay_cidrs`（誰にスコープが*強制*されるか）と `recursion_cidrs`（誰がこのサーバーに上流へ問い合わせさせてよいか）は、似て見える別々の問いだからです。

- **README を v0.4.4 まで追随。** DNSSEC は両側を記述するようになりました——署名側と上流の検証側、四つの判定、そして Insecure 対 Bogus こそがセキュリティを担う区別である理由。ブロックリストの拒否コードとプロバイダーのローテーション、すべてのリストと両方のゲートに及ぶ許可リストの射程、そして再帰アクセス制御（すでに出荷済みだったが文書化されていなかった）に、それぞれ一節が与えられました。設定例とオプション表に `dnssec.*`、`security.recursion_cidrs`、`metrics.bind`、プロバイダーごとの拒否フィールドが加わり、CLI のドキュメントに拒否フラグと現行の `get-rbl-config` 出力が加わり、RFC 表に DNSSEC 一族（4033/4034/4035、5155、6605/8080、6840、9276）と 7766 が加わりました。

## v0.4.4（2026-08-08）

### 機能

- **上流の DNSSEC 検証。** Rolodex は自分のゾーンには署名しながら、自分が解決したものは何も検証していませんでした：トラストアンカーも、外向き反復クエリの DO ビットも無く、`verify_rrsig` は意図的に未接続でした。`src/dnssec_validate.rs` が検証側の半分で、署名側とコードを共有していません——署名側は自分たちが書いたデータベース行を扱いすべてのバイトを制御しますが、検証側は正直さそのものが問われている相手から届いたものを扱います。両者は食い違えなければなりません。

  連鎖は**リゾルバーがすでに行っている委任走査と並んで上から下へ**築かれるので、DS は追加のクエリを一切要しません——委任に相乗りしてきて、鍵をすでに持っている親によって署名されています。検証済みの鍵集合（および証明済みで insecure な委任）は `src/key_cache.rs` にゾーンごとにキャッシュされます。クエリのたびに連鎖を導出し直せば、委任キャッシュのコールドスタート問題を一つ上の層で再発させてしまいます。そこでの検索は完全一致名であり、決してサフィックスではありません：親の鍵は子の鍵について何も語らず、そのために DS レコードがあるのです。

  RFC 4033 §5 の四つの状態がすべて区別されます。セキュリティを担うのは Insecure 対 Bogus です：「署名が無い」ことは Insecure では*ありません*。経路上の攻撃者はどんな応答からでも署名を剥ぎ取れるからです。上位の委任に DS が無いことを署名済みの NSEC/NSEC3 が証明したときにのみ Insecure であり、攻撃者は親の鍵無しにそれを偽造できません。NSEC/NSEC3 の否定証明の仕組みがそもそも存在するのはそのためで、それを省いた検証器は、攻撃者に検証器ゼロへ降格させられる検証器です。

  Bogus と Indeterminate のデータは、肯定でも否定でも、提供されることもキャッシュされることもありません——キャッシュされた bogus な否定は、その TTL のあいだ本物の名前を封じてしまいます。`auto` モードでは検証の失敗は階層の失敗ではなく**決定的な**答えなので、壊れた署名を、検証しない上流を経由して洗浄することはできません。AD が立つのは Secure のときだけ、しかも要求したクライアント（クエリに DO か AD）に対してだけです。DO を立てていないクライアントには RRSIG/NSEC/NSEC3 が取り除かれます。署名済みの A レコードはおよそ三倍の大きさになり、小さな問いに対する大きな答えは、`security.recursion_cidrs` が塞ぐために存在する増幅の形だからです。

  検証は**反復経路のみ**に適用されます——`recursive` モードと `auto` のルート階層です。転送されてきた応答は誰か他人の再帰結論の要約であり、それを検証するとは連鎖を自分で解決し直すことであって、それこそがルート階層そのものです。したがって階層 0 より下に降格した連鎖は未検証であり、AD を立てないことでそう表明します。

  `dnssec.validate`（既定 `true`）と `dnssec.trust_anchors`（既定：IANA のルート鍵）で設定します。アンカーの上書きは IANA の鍵に追加するのではなく**置き換え**、すべてのアンカーのすべてのフィールドが起動時に検証されます——不正な形式のアンカーは静かなフォールバックではなくハードな失敗です。外向きの反復クエリは、検証時に DO を立てた EDNS0 と 1232 バイトのペイロードサイズを持つようになりました（以前は OPT がまったく無かったので、サーバーは応答を 512 バイトに切り詰める権利があり、署名済みの答えは事実上そこに収まりません）。検索ごとのクエリ予算に `VALIDATION_QUERY_BUDGET`（基礎の 64 に加えて 32）が加わるので、検証を有効にしても名前の深さの許容が静かに縮むことはありません。NSEC3 の反復回数が 100 を超える場合は計算せずに insecure として扱います（RFC 9276）——そのハッシュ計算は、攻撃者が選び我々の側で支払う仕事だからです。このビルドが検証できないアルゴリズムでのみ検証されるレコードは Bogus ではなく Insecure です（RFC 6840 §5.11）：我々にアルゴリズムが無いことは、そのゾーンの障害ではありません。

  型付きの DNSSEC RDATA と RSA 検証のために hickory の `dnssec-ring` フィーチャーを有効にします。`ring` は RSA 鍵を*生成*できず、だからこそ署名はアルゴリズム 8 を拒みます——しかし検証は別の問いであり、RSA を確認できない検証器はルートを確認できません。

- **ブロックリストの拒否コードとプロバイダーのローテーション。** DNSxL は「掲載されている」ことと*問い合わせ元*への苦情とを同じ方法で返します——`127.0.0.0/8` の下の `A` レコードです——そして両者を区別するのはアドレスだけです：`zen.spamhaus.org` は `127.0.0.2` で「掲載」と言い、`127.255.255.254` で「あなたは公開リゾルバー経由で問い合わせている」と言います。したがって、どんな `A` レコードも掲載と読むことは、ブロックリストが我々に答えるのをやめると決めた瞬間を、そのプロバイダーに対して確認された*すべての*名前の NXDOMAIN に変えていました。しかもそれはクエリ量がプロバイダーの閾値を越えたときに始まります——問題なさそうに見えたデプロイの数時間後、あるいは数週間後にです。Spamhaus は直接こう述べています：それらのコードは「いかなる種類の評判とも解釈されるべきではない」。

  拒否コードが認識されるようになり、拒否は掲載でも否定でもありません：何もキャッシュされません。問い合わせた名前について何も学んでいないからです。応答のどこかにある拒否は、同じ応答の中の掲載に勝ちます：苦情を言っているプロバイダーが同時に評判を報告しているはずはなく、この向きに誤ることは*開いた側*に倒れるのに対し、逆の順序はすべての名前で閉じた側に倒れます。プロバイダーはその後、リクエストのたびに問い直されるのではなく、クールダウンのあいだ照会ローテーションから外されます（`refusal_cooldown_secs`、既定 3600 秒、プロバイダーごとの上書き可）。ローテーションは新しい照会だけを飛ばし、すでにキャッシュされた判定はそのまま残し、自然に失効するので一時的なクォータ超過は運用者の手を借りずに治り、`FlushCache` または任意の `SetRblConfig`/`SetDnsblConfig` で解除されます——再設定こそがその拒否の修正であることが多いからです。

  組み込みの集合は、一般的なプロバイダーの文書化されたコードを覆います：`127.255.255.0/24`（今日の三つのコードではなく Spamhaus のエラー範囲全体。明日の四つ目が静かに掲載と読まれないように）、`127.0.1.255` と `127.0.2.255`（Spamhaus DBL/ZRD の「IP クエリ非対応」）、そして `127.0.0.1` / `127.0.0.255`（URIBL/SURBL の「クエリはブロックされました」）。空は組み込み集合を意味します——「コード無し」を意味することはできません。この機能が存在する前に書かれた設定はすべて空だからです——そして単一エントリ `none` は、実際の掲載値がそれらのいずれかと衝突する私的ブロックリストのために検出を無効化します。明示的なリストは既定を拡張するのではなく置き換えるので、書き下した運用者はそれを狭めることもできます。解析できないコードは起動時に、あるいは RPC からの `InvalidArgument` で拒否され、決して飛ばされません：静かに適用されないコードは、掲載として読まれる拒否であり、しかも設定は成功を報告してしまっています。

  proto、CLI、Go クライアント、設定ファイルを通じて、プロバイダーごとにもリスト全体にも設定できます。RBL と DNSBL のリストは独立した既定クールダウンを持ちます。`GetRblConfig`/`GetDnsblConfig` は解決済みの**実効**コード（設定上の空リストは空ではなく組み込み集合として読み戻ります）と、現在どのプロバイダーがローテーションから外れていてあと何秒かを報告します。`rolodex_dns_blocklist_refusals_total{kind}` と `rolodex_dns_blocklist_rotated_out` が同じものを Prometheus に公開します。それらが無ければ「ブロックリストが黙った」と「ブロックリストが綺麗だ」は外から見て同じであり、運用者が想定するのは後者です。クールダウンを `0` にすることは「クールダウン無し」ではなく「既定値を使う」を意味します——ゼロのクールダウンは、たった今やめてくれと言ってきたプロバイダーに問い直すことだからです。

### バグ修正

- **ブロックリストの一致はすべて NXDOMAIN になり、それを抑えられるのは許可リストのエントリだけになりました。** 三つの穴があり、いずれもリストが「掲載」と言ったのにクエリが解決してしまう、あるいは運用者の脱出口が届かないケースでした：

  - DNSBL の許可リストは正引き名の確認（ステップ 7）しかゲートしていなかったので、*アドレス*に対する誤検出は解除不能でした——誤って掲載された IP は、正常に動いているホストの `dig -x` を壊し、プロバイダーを無効にする以外の手立てがありませんでした。今は逆引き/IP 経路（ステップ 2）も、どちらの綴りでもゲートします：`in-addr.arpa`/`ip6.arpa` 名は他の DNS 名と同様にサフィックス一致するので、一エントリで /24 全体が解除されます。IP リテラルは**完全一致**です——オクテットは最上位から並ぶので、`1.100` は `192.168.1.100` の親ではなく、親として扱えば誰も指定していないアドレスまで免除してしまいます。
  - スコープごとの RBL プロバイダーは保存され、API から読み戻せたのに、一度も参照されていませんでした。クエリ経路は素の `is_listed` を呼んでいたので、あるスコープ自身のリストによる一致は普通に解決されていました。
  - 逆引き名として書かれたローカル RBL エントリは何にも一致しませんでした——IP ゲートは `ip.to_string()` しか引いていなかったからです。`dig -x` は逆引き名を表示するので、運用者が貼り付けるのはそれであり、ブロックのように読めるのに黙って何にも一致しないエントリは、拒否されるエントリより悪いものです。今はどちらの綴りでも一致します。

  逆引き側の二つの呼び出し箇所は一つのゲート（`ip_blocklist_kind`）へ、すべてのゲートは一つの免除（`blocklist_exempt`）へ漏斗のように集まるので、将来の出口がずれることはありません。スコープごとのプロバイダーは、外向きの平文 `:53` に到達できないときは飛ばされます——そのフラグはポリシーのスイッチではなく、その照会はタイムアウトしかしないと述べているだけです。ローカルレコードは今も意図的にブロックリストに勝ちます：逆にすると第三者の掲載が内部サービスを潰せてしまい、それを固定するテストがあります。

- **ゾーン署名は検証できない署名を作り、読めない鍵を公開していました。** DNSKEY、DS、RRSIG は保存された文字列を載せた **TXT レコード**として提供されていたので、DNSKEY クエリには TXT が返り、公開されたすべての署名は使い物になりませんでした。今はそれぞれ固有のタイプコードで、署名側がハッシュするのと同じ正規エンコーダーの RDATA とともに提供されるので、線上に出るものは署名されたものとバイト単位で同一です。URI と ZONEMD も同じ方法でエンコードされます。署名されるバイト自体も RFC 4034 §3.1.8.1 の正規形になりました——正規化された所有者名、元の TTL、重複を落とした RFC 4034 §6.3 の順序付け。これで SQLite からレコードが出てくる順序が署名を変えることはありません——加えて RFC 4035 §2.1 の KSK/ZSK 役割分担、ラベル境界でのゾーン限定、そして蓄積ではなく置換する再署名（前回以降にレコードが削除された名前も含む）です。署名不能な型（NSEC、NSEC3、NSEC3PARAM、ANAME、不正な値）は、でっち上げのエンコーディングの上に署名するのではなく飛ばして応答に名前を挙げます——他の誰も再現しないバイト列に対する署名は、あらゆる検証器で閉じた側に倒れ、それは名前を未署名のままにするより悪いからです。

- **RSA/SHA-256（アルゴリズム 8）は鍵生成時に拒否されます**。受け入れてから差し替えるのではありません。`ring` は RSA 鍵を生成できません。Ed25519 の鍵素材の上にアルゴリズム 13 を名乗る DNSKEY は、互いに食い違う DS と DNSKEY と RRSIG 一式を生み、その失敗はローカルではなく検証するリゾルバーの側で表面化します。関連して、ファイルされているアルゴリズムとしてバイト列が読み込めない保存済み鍵は、それで署名せずに警告つきで飛ばされます。

- **管理ゾーンの権威にはゾーン*頂点*のレコードが必要でした。** レコードがすべてサブドメインにあるゾーン——`example.com` には何も無く `www.example.com` がある、というゾーンの普通の形——は決して権威になれず、その不一致は上流へ転送され、内側の表現が優先されなくなっていました。今はゾーン内のどのレコードでも数えられ、`remove_records` はゾーンとその下に何も残らなくなった時点で `managed_zones` キャッシュを刈るので、キャッシュを信頼して安全です（古いエントリは無害ではありません——もう存在しないゾーンについて NXDOMAIN を返し続けます）。逆引きツリーは除外されています：末尾二ラベルのヒューリスティックは常に `in-addr.arpa.` 自体を導くので、保存された PTR 一つがグローバルな逆引きツリー全体を主張してしまうところでした——しかも `dns.auto_ptr` が有効なら、A レコード一つがその PTR を作ります。`home.arpa`（RFC 8375）は意図的に除外*しません*。そこではヒューリスティックが正しく、不一致を転送することは RFC 8375 §4 が禁じているからです。

- **ゾーンのサフィックス一致が三箇所で裸の `ends_with` を使っていました**。`notexample.com` が `example.com` の内側に入ってしまいます——証明書一覧、スコープ付き権威、ゾーン署名の三つです。ラベル境界で一致する `db::name_in_zone` に引き上げました。

- **`SetDns64Config` はスタブ**で、引数をログに出し、何も保存せず、成功を報告していました。`GetDns64Config` はハードコードされた既定値を返していました。有効フラグとプレフィックスは独立に保存されるようになったので、合成を無効にしても設定済みのプレフィックスは失われず、解析できないプレフィックスは差し替えではなく拒否されます。

- **`GetTtlDriftConfig` が往復しませんでした。** `1h30m` として設定した調整値が `5400s` として読み戻されていました。この応答は運用者が自分の設定を確認するために読むものであり、read-modify-write の自動化には往復が要るので、設定したときと同じ複合表記で整形されるようになりました（`ttl_drift::format_duration_secs`、パーサーの逆関数）。

- **`add-scope-rbl --enabled` は飾りでした。** `default_value = "true"` の裸の `bool` なので、clap はこのフィールドに `SetTrue` アクションを与えます。clap 4.6 は既定値を適用するため、このフラグは `false` を表現できず、無効化されたスコープごとのプロバイダーには到達不能でした。今は値を取ります（`--enabled false`）。このフラグは両方向で間違っていたので、両方向を固定しています。

### 変更

- ドキュメントと例から `dnsbl.sorbs.net` を削除——SORBS は閉鎖しました。

### テスト

- **`tests/signed_hierarchy/mod.rs`** — 本物の UDP ソケット上の、DNSSEC 署名済みモック階層です：署名済みルート → 署名済み TLD → 署名済みゾーン。各ゾーンが自分の Ed25519 鍵を持ち、DNSKEY RRset を公開し、署名済みの子ごとに DS を渡します。`tests/mock_hierarchy` がクエリ*回数*を証明するのに対し、こちらは*判定*を証明します。DNSSEC を間違えるほとんどすべての方法が、それでも正しいレコードを返すからです——有効期限の確認を飛ばす検証器、未署名の NSEC を信じる検証器、どんな署名者名でも受け入れる検証器は、誰かが攻撃してくるその瞬間まで、インターネット全体を正しく解決します。その `Tamper` 列挙型は、ゾーンが正しく構築された**後**、応答がシリアライズされる時点で各攻撃を適用するので、どのテストも「無効なデプロイが拒否された」ではなく「有効なデプロイが攻撃された」になります。

  `tests/dnssec_validation_test.rs` は動き続けなければならない経路を覆います（完全な連鎖が Secure と検証されること、NSEC で証明された insecure な委任、証明済みの NXDOMAIN と NODATA、鍵キャッシュがルートへの二度目のクエリを省くこと、検証しないリゾルバーが Secure ではなく Insecure を報告すること）。`tests/security_dnssec_test.rs` は攻撃を覆います（剥ぎ取られた署名、DS もその不在証明も無い委任、期限切れおよび発効前の署名、未公開の鍵による署名、他所の署名者名、署名後に改変されたデータ、証明されていない否定、どのルート鍵とも一致しないトラストアンカー、不正な形式のアンカー）。両者は同じ理由で等しく重要です：何でも拒否する検証器はすべての攻撃テストを通り、何でも受け入れる検証器はすべての正常系テストを通ります。

- **`tests/dnssec_signing_test.rs`** は、署名が単に存在するだけでなく*確認できる*ことを固定します——中心となるテストは、秘密鍵の行からではなく**公開された DNSKEY RRset** から署名入力を導出し直します。検証器が持っているのは DNSKEY だけだからです。

- **`tests/blocklist_nxdomain_test.rs`** は、運用者がするのと同じやり方でブロックリストの契約を駆動します：gRPC による変更、そして本物の UDP または TCP ソケット越しのクエリ。五つのリスト、二つのゲート、二つのコード経路、それぞれに固有の対照つきです——何でもブロックするゲートは契約の半分を満たし、何もブロックしないゲートはもう半分を満たします。

- **`tests/rbl_refusal_test.rs`** は、モックのブロックリストゾーンに対して本物の UDP DNS 越しに拒否コードを駆動し、フォワーダーのフォールバックと分類を経て `handle_query` に至ります。その間のどの層も、掲載と拒否の区別が失われうる場所だからです。どのテストも、本物の `127.0.0.2` の掲載が同一の経路をたどってもなお NXDOMAIN を返す、という対照と対になっています。

- **`tests/doq_test.rs`、`tests/proxy_test.rs`、`tests/tls_reload_test.rs`** は、以前はコンパイルのスモークテストか設定解析のユニットテストしか無かった三つの面を覆います：`serve_doq` に対する本物の `quinn` クライアント。缶詰の応答を返すのではなくサーバーが送るものを**解析する**、HTTP CONNECT・SOCKS5・DoH のモックプロキシ（到達不能なプロキシが直接接続へフォールバック*しない*ことも固定）。そして本物の TLS ハンドシェイク越しに観測される `TlsManager::reload()`。

- **`tests/zonemd_test.rs`** は RFC 8976 の RDATA レイアウトをフィールドごとに固定します。エンコーダーから取るのではなく手書きです——エンコーダーを自分自身と比べれば、バグを追認してしまいます。

- **`tests/acme_admin_test.rs`** は五つの管理 RPC を、`success` フラグではなくその性質で覆います：`EnsureZoneCa` の冪等性（作り直せば、古い中間証明書に連なるすべての証明書と、公開済みの DANE-TA レコードまで壊れます）、共有された一つのルートの下でのゾーンごとの中間証明書、EAB のスコープと鍵の往復、正直な削除、そしてラベル境界での証明書一覧。

- **`tests/cli_integration_test.rs`** はすべてのサブコマンドの `--help` を歩くようになりました。clap はパーサー構築時に短オプションを検証し、重複があると**パニック**します。したがって、グローバルオプションが取っている文字を再利用したサブコマンドは、引数を一つも読まないうちに落ちます——`generate-dnssec-key`（`-a`/`--algorithm`）と `set-ttl-drift`（`-a`/`--adjustment`）が両方ともグローバルの `--address` と衝突し、まったく実行できなくなっていたのがまさにそれです。どちらも今は長形式のみです。

## v0.4.3（2026-08-07）

### セキュリティ

- **Do53 フォワーダーは届いた任意のデータグラムを受け入れていました。** `forward_udp` はクエリを送り、返ってきたものをそのまま返していました：トランザクション ID は一度も比較されず、プールされたソケットは connect されていないのでカーネルは*どの*送信元からのデータグラムも配送し、応答の質問はクエリの質問と照合されず、唯一存在した確認——0x20 の QNAME 大小文字——は `warn!` を出すだけで、その応答をそのまま返していました。`security.qname_case_randomization` は既定で有効、キャッシュポイズニング耐性として文書化されていましたが、何も強制していませんでした。プールされた 8 つのソケットは一度バインドされてプロセスの生涯にわたり再利用されるので、その送信元ポートは一度観測されれば安定します。つまり経路外のポイズニングには推測がまったく不要で、しかも `cache_from_wire` がその偽造をその後永続化していました。

  応答は今、問い合わせたアドレスから来ていて、送ったトランザクション ID を持ち、同じ質問——名前、タイプ、クラス、しかも**大小文字を区別して**比較——に答えている場合にのみ受け入れられます。それがようやく 0x20 に仕事をさせます。拒否されたデータグラムは捨てられ、最初のパケットがクエリを決めるのではなく、期限まで本物の答えを待ち続けます：それが、注入された一パケットで検索を妨害されることを防ぎ、以前のクエリへの遅れて届いた応答をプールされたソケットから排出します。

  運用上の注意：質問をそのまま返さない上流リゾルバーは、静かに解決するのではなく失敗するようになります。行儀の良いリゾルバーは要求どおりそのまま返しますが、大小文字を正規化するものを指しているなら、それについては `security.qname_case_randomization` を切ってください。プロキシ経由（`proxy.mode`）の転送経路は変わりません——その応答は既知のプロキシへの TCP 接続で届くからです。

- **反復リゾルバーは任意の送信元からの応答を、任意の質問への答えとして受け入れていました。** `query_one` は UDP ソケットをバインドしても一度も connect していなかったので、カーネルは*どの*ピアからのデータグラムも配送していました——経路外の攻撃者に権威サーバーの IP を詐称させる、あの送信元アドレス確認がまるごと欠けていて、残るのはトランザクション ID とエフェメラルポートだけでした。別途、`validate_question` は応答の質問の*名前*しか比較していなかったので、リゾルバーが一度も訊いていない質問——別のレコードタイプ、別の DNS クラス——に答える応答が検証を通り、そのレコードが取り込まれ、キャッシュされ、提供されていました。

  クエリ用ソケットはクエリを送る前にネームサーバーへ connect されるようになり、パケットごとの費用ゼロでカーネル内の送信元確認が戻ります。そして `validate_question` は質問全体を比較します：名前（リゾルバーは 0x20 のランダム化をしないので大小文字を区別しない）、タイプ、クラス。この経路は二つの上流経路のうち元々防御が良い方でした——トランザクション ID は確認していました——が、両方の穴が UDP でも TCP でも等しく塞がれました。

- **反復リゾルバーは管轄外の委任とグルーを受け入れていました。** `classify` は権威セクションで最初に現れた NS レコードの所有者名から委任ゾーンを取り、それを解決中の名前とも答えたゾーンとも一度も照合せず、`walk` がそれをそのまま委任キャッシュへ書き込んでいました。リゾルバーがこれまでに話した任意のネームサーバー——広告ドメイン一つ、リンク一つ——が、自分のゾーンについてのクエリに `AUTHORITY: com. NS ns.attacker.example` とグルーで答えられ、`best_match` はサフィックスを歩くので、以降のあらゆる `.com` 検索が攻撃者のサーバーから始まっていました。TTL が `resolution.delegation_persist_min_ttl` を超える委任は `delegation_cache` テーブルに書かれ起動時に再読み込みされるので、この乗っ取りは再起動を越えて生き残りました。`.` でもまったく同様に動きました。別途、`cache_glue` は追加セクションのアドレスをその所有者名の下にキャッシュしていたので、同じ応答が他所のネームサーバーホスト名に対するアドレスを植え付けることもできました。

  委任は今、答えたゾーンから**厳密に下へ**進み、*かつ*解決中の名前を覆う場合にのみ、たどられ、キャッシュされます（`referral_in_bailiwick`）。それ以外は静かに無視されるのではなく検索を失敗させるので、敵対的な委任が前進を装うことはできません。グルーは**答えている**ゾーン内の名前に絞られます——意図的に、委任先のゾーンではありません。`com.` に対するルートからの委任は `a.gtld-servers.net.` のグルーを正当に運びますが、それは `com.` の外側で、`.` の十分内側だからです。破棄は `rolodex_dns_resolver_out_of_bailiwick_total` が数えます。

- **DHCP のホスト名が検証されないまま DNS 名に埋め込まれていました。** `register_dns_hostname` は DHCP オプション 12——LAN 上の任意の未認証デバイスがそのまま供給するもの——から `format!("{}.lan.{}.", hostname, tld)` でレコード名を組み立てていました。自分を `*` と名乗ったクライアントは `*.lan.<tld>.` を登録でき、`make_wildcard_name` はそれを本物のワイルドカードにします：`lookup_scoped` は完全一致が外れるたびにそこへフォールバックするので、一台のノートパソコンがそのスコープ内の未登録の名前すべてに答えていました。ドットはクライアントに、割り当てられていない深さへ自分を置かせ、長さも確認されていなかったので、線上の 63 バイト上限を超えるラベルがデータベースに届き、到着時に一度失敗する代わりに、後のシリアライズで失敗していました。

  ホスト名は今、単一の LDH ラベル（RFC 1123 §2.1）——1〜63 バイト、英数字とハイフン、先頭末尾のハイフン不可——として検証され、小文字化され、**サニタイズではなく拒否**されます。ですから、規則が拒む名前を送ったクライアントに、黙って別の名前が割り当てられることはありません。登録は警告つきで飛ばされ、登録解除も同じ検証を通るので、削除される名前は追加された名前と同じです。

- **DNS over TCP と DoT の接続に上限も時間制限もありませんでした。** `serve_tcp` と `serve_dot` は接続ごとに上限なしでタスクを起動し、どちらの読み取りループも時間制限なしに `read_exact` を待っていたので、接続して何も送らないクライアント——あるいは長さプレフィックスの半分、あるいは決して届けない 65535 バイトの予告——がタスクとファイルディスクリプタを無期限に占有していました。`dns.bind` の既定は `0.0.0.0:53` です：ルーティング可能なインターフェースでは、これは認証前のリモート資源枯渇であり、ディスクリプタが尽きれば正規のクライアントに対しても `accept` が失敗します。DoT はもっと悪く、`acceptor.accept()` にも時間制限が無かったので、TLS 実装が一切無い素の `connect()` が、DNS のやり取りが始まる前に、DNS 読み取りループ向けのどんな修正も届かない場所でタスクを占有していました。

  どちらのリスナーも今は、メッセージ間に 10 秒のアイドルタイムアウト、予告されたメッセージ本体に 5 秒の期限、リスナーごとに 1024 接続の上限を適用します。DoT はさらに 10 秒の TLS ハンドシェイクタイムアウトを加えます。アイドルタイムアウトは最後の活動から測られるので、RFC 7766 の接続再利用は今も機能します——再接続が新しいハンドシェイクの費用になる DoT ではとくに重要です。DoQ にはすでに `max_idle_timeout` があり、変更ありません。

- **gRPC の共有シークレットは `==` で比較され、失敗は絞られていませんでした。** `String: PartialEq` は長さを比べてから `memcmp` に委ね、それは最初に異なるバイトで戻ります——つまり所要時間が、呼び出し元がシークレットの先頭何バイトを当てたかを漏らし、シークレット全体の探索を一バイトずつの探索に変えてしまいます。別途、失敗した試行を何も制限していませんでした：gRPC ポートに到達できるクライアントは、接続を開ける速さで推測できました。弱い共有シークレットが単に軽率であるにとどまらず致命的になるのはそのためです。

  比較は `subtle::ConstantTimeEq` を使うようになりました。（`ring` 自身の `verify_slices_are_equal` は非推奨で、サイドチャネルの保証が無い内部専用と文書化されているので、すでに推移的にツリーに入っていた `subtle` を直接の依存にしました。）失敗した認証は**送信元アドレスごとに**絞られます：連続 5 回の失敗でその送信元を 30 秒ロックアウトし、連続するロックアウトごとに倍増して上限 15 分。ロックアウト中はすべての試行が `ResourceExhausted` で拒否され、*トークンの比較はまったく行われません*。ロックアウト自体が「今の推測は当たりだったか？」のオラクルにならないようにするためです。

  グローバルではなく送信元ごとにキー付けしているので、攻撃者が運用者を自分の管理プレーンから締め出すことはできません。カウンターはリクエストではなく*失敗*を数え、成功はその送信元の履歴を消すので、ループで呼び続ける正当な自動化が絞られることはありません。5 分静かだった連続はリセットされるので、たまのタイプミスが積み上がることもありません。テーブルは 65536 送信元で上限が付いています：上限を超えると、アイドルかつロックされていないエントリが刈られ、それ以上の新規送信元は追跡されなくなります。分散型の洪水がテーブルを無制限に育てるのではなく、です。

- **gRPC の Unix ソケットが誰でも接続可能でした。** Unix 接続は認証を完全に迂回します——それは意図的で、そのためソケットのファイルモードが管理プレーンの*唯一の*アクセス制御になります。`main.rs` は素の `UnixListener::bind` をして結果の権限を一度も変えていなかったので、umask のまま——よくあるのは `0755`——になり、マシン上のすべてのローカルユーザーが未認証の管理権限を持っていました：任意の DNS レコードの書き換え、EAB 資格情報の発行、ゾーン CA の作成。ソケットは今 `0660` で作られます——一時的な兄弟パスにバインドし、権限を絞ってから、所定の位置へリネームします。公開パスでバインド後に chmod すると、ソケットが存在しかつ誰でも接続できる窓が残るからで、アトミックなリネームは同じ inode を保つのでリスナーには影響しません。`0600` ではなく `0660` なのは、ソケットの所有グループを変えることでデプロイが専用の管理グループにアクセスを与えられるようにするためです。

- **データベースが誰でも読める状態でした。** それは鍵庫です：ルート CA の秘密鍵、ゾーンごとの中間鍵すべて、DNSSEC の秘密鍵、EAB の HMAC シークレットが、そこに素の行として入っています。`set_permissions` を誰も呼んでいなかったので、SQLite は素の umask——よくある既定では `0644`——で作成し、任意のローカルユーザーがルート CA 鍵を読んで、登録済みのすべてのクライアントが信頼する任意の名前の証明書を偽造できました。`Database::open` は今それを `0600` に絞り、しかも WAL を有効にする*前*にそうします。SQLite が作る `-wal`/`-shm` のサイドカーには本体ファイルのモードがコピーされるからです。既存のサイドカーも締められます。

- **共有シークレット無しでルーティング可能な gRPC バインドが静かに起動していました。** 空の `grpc.shared_secret` は、すべての TCP RPC について `check_auth` を即座に `Ok(())` にします。それはループバック上では文書化された開発用設定であり、それ以外では管理プレーンの完全な露出です——しかもサーバーは健全そうに立ち上がり、異常なログを何も出しませんでした。この組み合わせは今、起動時に拒否されます（`config::check_grpc_exposure`）：ループバックの場合は今も起動し、`0.0.0.0` と `::` は正しくループバックではないと判定され、`インターフェース:ポート` のバインドはそのインターフェース上のルーティング可能なアドレス一つで有罪となります。

- **既定のデプロイはオープンな再帰リゾルバーでした。** `dns.bind` の既定は `0.0.0.0:53` で、送信元の分類は `security.overlay_cidrs` の外側をすべて信頼されたローカルクライアントとして扱うので、ルーティング可能なインターフェースではインターネット上のあらゆるホストが完全な再帰サービスを受けていました。それは古典的な反射／増幅の資産であり——詐称された小さなクエリが、詐称された被害者へ向けた大きな答えを返し、外向きの解決トラフィックはこのマシンに請求されます——しかもレート制限が何も無かったので、洪水は届く速さのまま処理されていました。

  新しい `security.recursion_cidrs` が、誰が**上流**解決を駆動してよいかを決めます。既定はインターネットからルーティング不能なすべての範囲です：ループバック、RFC 1918、リンクローカル、ULA、CGNAT。これで LAN、コンテナブリッジ、WireGuard オーバーレイ（`10.64.0.0/10` は `10.0.0.0/8` の内側）が覆われるので、このサーバーを正当に使っていたものがサービスを失うことはありません。これは、誰にスコープが*強制*されるかを決める `overlay_cidrs` とは別の軸です。

  この確認はローカル／リモートの境界に置かれています——このサーバーが持つデータから答えるすべてより後、持たないデータを取りに行くすべてより前——ので、見知らぬ相手も今までどおりこのサーバーの権威ある答えと NXDOMAIN を受け取りますが、自分の代わりにインターネットを解決させることはできません。応答キャッシュより*前*に走ります。キャッシュ済みの答えも新鮮な答えと同じく増幅し、キャッシュを温めることこそ攻撃の仕込みだからです。そして拒否は応答セクションが空の REFUSED で、それを引き起こした質問より大きくなることは決してありません。DoH は今、ピアアドレスが分類まで届くよう接続情報つきで提供されます。それが無ければ、`:443` が `:53` の閉じた穴を開け直していました。

- **IPv4 マップされた送信元アドレスがスコープ強制を迂回していました。** 送信元の分類はソケットが報告したとおりのアドレスを検査し、`IpCidr::contains` は意図的にアドレスファミリーを跨いで一致しません。デュアルスタックのリスナー——`[::]:53`、文書化されたバインド形式であり、`net.ipv6.bindv6only=0` の Linux では既定——では、IPv4 のオーバーレイピアは `::ffff:10.64.0.1` として到着し、それは `IpAddr::V6` なので `is_overlay_peer` は偽を返し、そのピアは*信頼されたローカル送信元*として分類されていました。分割視域の両半分が壊れます：どのネットワークにも参加していないオーバーレイピアが REFUSED されなくなり、分離されているはずのグローバル名前空間に到達し、`JoinNetwork` を呼んで*いた*ピアは静かにスコープを失いました。関連付けは素の IPv4 形式で保存されていたからです。WireGuard ピアにそもそもスコープが強制されるかどうかが、リスナーがたまたまどうバインドされたかで決まっていたのです。

  `handle_query_on` は今、何かが分類する前に、送信元アドレス（およびリスナーのローカル IP）を一度だけ正規化します。ですからマップ形式と素の形式は、以降のすべて——CIDR 判定、関連付けの検索、入口リスナーの照合——で同じアドレスです。IPv4 互換アドレス（`::1.2.3.4`、非推奨）は意図的に畳みません。真に IPv4 マップされたものだけです。

- **登録ポータルは、インターネット上のどのゾーンの CA にもなり得ました。** `create_account_inner` は `zone` 文字列をそのまま受け取り、それに対して `ensure_zone_intermediate` を呼び、そのスコープの EAB を発行していました。ゾーンを、このサーバーが実際に管理している何かに結びつけるものが何も無かったので、到達できるクライアントは `windowsupdate.com` の中間証明書を作れました——ローカル DNS に DANE-TA レコードとして公開され、登録済みのすべてのクライアントが信頼するルートに連なります——そしてそれに対して発行できました。「到達できる者は誰でも登録してよい」は意図的な設計判断で、これは変わりません。それは「名前空間全体の CA になってよい」を意味したことは一度もありません。登録は今、このサーバーが関係を持つゾーンに限定されます：あるスコープが TLD として所有している（スコープの暗黙の `.home` ドメインを含む）、ローカルデータベースにレコードがある、宣言された権威ゾーンである、あるいは運用者が `EnsureZoneCa` を実行したのですでに中間 CA がある。それぞれサフィックス一致なので、管理ゾーンのサブゾーンも登録できます。そして `acme.issuance_scope: any` は制限を完全に解除します——発行者にとってその設定がすでに意味していたのと同じことです。

- **ポータルは通りすがりの CSRF に開いていました。** `POST /api/account` は本文をバイト列として読み、JSON の content-type を要求せず `Origin` も確認せずに JSON を解析していました。`Content-Type: text/plain` のクロスオリジンなフォーム POST は CORS の*単純リクエスト*です——プリフライトが無いのでブラウザーは送信し、副作用が着地します。攻撃者は発行された EAB を読み戻せませんでしたが、CA の作成と DNS への公開はとにかく起こりました：LAN の任意のユーザーのブラウザー上のページが、ローカルの PKI を作り変えられたのです。このエンドポイントは今 `application/json` を要求し（それがポータルの答えないプリフライトを強制します）、このサーバー以外の `Origin` を拒否します。比較は authority 単位なので、TLS を終端するプロキシも今までどおり動きます。ブラウザー拡張のオリジンは免除です——同梱の拡張は一級のクライアントであり、そのオリジンはユーザーがホスト権限を与えた後にしか付きません。ブラウザーでないクライアント（CLI、`js/` のポータルクライアント、ローカル UI のプロキシ）は `Origin` を送らないので影響を受けません。

- **ACME 発行者は CSR が求めた名前をそのまま署名していました。** `ca::issue_leaf` はクライアントの CSR をそのまま rcgen に渡し、rcgen はその `dNSName` SAN と subject を発行証明書へ写します。それらを、そのオーダーが実際に検証した識別子と比べるものが何も無かったので、ある名前の管理を証明すれば*任意の*名前の証明書が買えました——他テナントのゾーンの名前も含めて——しかも、登録済みのすべてのクライアントが信頼する Rolodex のルートに連なる中間証明書で署名され、DANE-TA アンカーとして公開されます。`issue_leaf` は今、そのオーダーの検証済み識別子を受け取り、その外側を求める CSR を拒否します（`badCSR`）。subject の common name も確認しますが、ホスト名として読める場合だけです。明示的な DN 無しで作られた CSR には rcgen が `"rcgen self signed cert"` を刻むからです。

- **ACME のエンドポイントは呼び出し元を認証しても、認可していませんでした。** `order`、`authz`、`challenge`、`finalize`、`cert` はオブジェクトを id で引くだけで、それが要求元アカウントのものかを一度も確認していませんでした（RFC 8555 §7.4）。他人の ready なオーダーを finalize すれば、被害者の名前の証明書が攻撃者の持つ鍵の下で得られました。証明書は連番の rowid でアドレス指定されるので、どのアカウントも、その CA が発行したすべての証明書を列挙してダウンロードできました。五つすべてが今は所有権を検証します。証明書の経路は発行したオーダー経由です。

- **`url` の束縛が任意でした。** `verify_request` は JWS の保護ヘッダーの `url` を、存在するときにしか確認していなかったので、省略すればリクエストはどのエンドポイントにも束縛されず、エンドポイント跨ぎのリプレイが可能でした。今は必須です（RFC 8555 §6.4）。

- **EAB 資格情報が永久に再利用できました。** `used` 列は `mark_eab_used` が書き込むだけで一度も読まれていなかったので、漏洩した登録資格情報一つで、そのゾーンに対して無制限にアカウントを登録できました。今は強制されます。

- **オーダーと認可の有効期限が一度も確認されていませんでした。** `expires_at` は保存されて無視されていたので、一度行った検証が無期限に有効でした——運用者がチャレンジのレコードを削除した後や、その名前を手放した後もずっとです。`finalize` とチャレンジ応答の経路が今それを強制します。

- **リプレイ防止 nonce が無制限に溜まっていました。** nonce はすべての応答で発行され永続化されており、未認証の `GET /acme/directory` も含みます。そして消費されたものだけが削除されていました——未認証のリモートクライアントがディスクが埋まるまでテーブルを育てられ、DNS のホットパスが使うミューテックスを奪い合わせられました。nonce は今、一時間で期限切れになり、*かつ*テーブルは未消費 1024 件で上限が付き、古いものから追い出されます。TTL だけでは縛れませんでした：一秒以内の洪水は完全に窓の内側だからです。

- **`revoke-cert` は失効させずに成功を報告していました。** JWS を検証したあと `200 OK` を返し、何も変わっていないのに、危殆化した証明書が無効になったとクライアントに伝えていました。失効が実装されるまでは `501` を返します。

### 機能

- **Prometheus メトリクス。** 任意の `metrics` 設定セクションが、平文 HTTP の `/metrics` エンドポイント（既定 `127.0.0.1:9153`）を起動し、サーバー全体にわたる 66 のメトリクスファミリーを公開します：トランスポート/rcode/タイプごとのクエリ、トランスポートごとのエンドツーエンド遅延ヒストグラム、メッセージサイズのヒストグラム、応答キャッシュのヒット/ミス/期限切れ/フラッシュの会計、ブロックリストのブロック数とプロバイダー照会の結果、`auto` 階層チェーン（有効階層、階層ごとの試行/勝利/失敗、確定した切り替え、復旧探索、上流ごとの交換回数）、反復リゾルバーの内部（委任、CNAME ホップ、予算枯渇、TCP 再試行、ルートのプライミング、ネームサーバーごとの EMA 遅延）、分割視域の状態（レコード、スコープ、関連付け、所有 TLD、入口リスナー）、アドレスファミリーの抑制、DHCP のメッセージと状態別のリース、ACME の発行と dns-01 検証、そしてメソッドごとの gRPC 呼び出し数。

  このセクションは**既定では存在しない**ので、アップグレードで新しいポートが開くことはありません。既定のバインドがループバックなのは、このエンドポイントが未認証だからです。運ぶのは集計された数値だけです——クエリ名もレコード値もありません。

  レジストリは手書きで（`src/metrics.rs`）、サーバーの他の部分がすでに使っているのと同じロックフリーの部品の上に作られ、**依存関係を一つも増やしません**：ホットパスでのカウンター加算は、あらかじめ確保された系列に対する緩和された `fetch_add` 一つで、テキストの公開形式は直接書き出されます。どのラベル次元も固定の列挙型か設定で縛られています——クライアントが吹き飛ばせる唯一のもの、クエリタイプは、認識できないタイプを `OTHER` に畳むので、`TYPE4242` のクエリの洪水が無制限に系列を鋳造することはできません。

  `rolodex_dns_answers_total{source}` メトリクスは、解決順序のどの段階が答えたかを報告します——キャッシュ、ローカルレコード、スコープ、LAN→スコープのフォールバック、TLD ピア、ブロックリスト、RBL、DNS64、上流、権威 NXDOMAIN、拒否——それが分割視域のパイプラインを外から読めるものにします。既定値が上流経路であるタグによって `resolve_query` の約 30 の出口に通され、すべてのトランスポートが共有する唯一の計装済み出口で記録されるので、新しい早期リターンが静かにメトリクスをすり抜けることはありません。

- **DNSBL 許可リスト——特定のホストをブロックリストの確認から免除できます。** ある名前で誤検出するブロックリストプロバイダーに対して、以前の運用者にはそのプロバイダーか DNSBL 全体を無効にする以外の手立てがありませんでした。許可リスト上の名前（理由つきで `dnsbl_allowlist` テーブルに保存）は、解決のステップ 7 を完全に飛ばします。

  エントリは**ラベル境界でサフィックス一致**するので、`example.com` を許可すると `www.example.com` も免除されますが `notexample.com` はされません。そして正規化して保存される（小文字、末尾ドット）ので、どんな綴りでも同じエントリを追加・削除します。この確認は名前ベースのステップ全体を短絡するので——設定済みの DNSBL プロバイダーもローカルの RBL ブロックリストも、免除された名前をブロックできません——しかもプロバイダー照会より*前*に走るので、免除された名前はブロックリストのクエリを一切発行しません。逆引き DNS による IP ブロックには影響しません。検索はテーブルを鏡写しにして起動時に読み込む `DashSet` に対して O(ラベル数) なので、許可リストが未設定ならホットパスの費用は空集合の確認一回です。

  `AddDnsblAllowlistEntry` / `RemoveDnsblAllowlistEntry` / `ListDnsblAllowlistEntries`（gRPC と Go クライアント）、あるいは CLI の `add-dnsbl-allow` / `remove-dnsbl-allow` / `list-dnsbl-allow` で管理します。

## v0.4.2（2026-08-06）

### バグ修正

- **上流の応答キャッシュはほぼ完全に無効でした。** 二つの独立したキーのバグにより、答えは、どの検索も決して再現できないキーの下に書かれていました。そのため、同じ名前をどれだけ最近解決していようと、ほとんどすべてのクエリが上流への完全なラウンドトリップを支払っていました。

  `cache_answers` は各エントリを、質問ではなく最初の*応答レコード*でキー付けしていました。CNAME の背後にあるどんな名前にとってもそれは誤りです：`index.crates.io A` は連鎖として返ってきます——`index.crates.io CNAME` → `fastly-index.crates.io CNAME` → 三つ目の名前上の A レコード——その最初のレコードは CNAME なので、エントリは `index.crates.io.:CNAME` の下に着地する一方、どの検索も `index.crates.io.:A` を訊いていました。これらのキーは決して出会わないので、その名前は*恒久的に*キャッシュ不能でした。それは CDN の前面に立つインターネットのほとんどです（Fastly、CloudFront、S3）。気づかれなかったのは、`example.com` のような名前は自分自身の A レコードで答えるため、誤ったキーがたまたま正しくなり、しかもすべてのテストがその形の名前を使っていたからです。

  `cache_key` は別途、呼び出し元が名前を正規化済みであることを信頼していました。キャッシュの両側は同じ大小文字を見ていません：読み取りはクライアントが送った大小文字を使い、書き込みは質問が返ってきたときの大小文字を使います。そして 0x20 エンコーディング（`security.qname_case_randomization`、既定で有効）では、転送されるクエリは `eXaMpLe.CoM` として出ていき、応答はそれをそのまま返します。したがってすべての上流の答えは、ランダムな大小文字のキーの下にファイルされ、それがキャッシュを丸ごと無効にしていました——連鎖した名前だけでなく、すべての名前です。

  どちらの経路も今は質問——`question.name()` と質問のタイプ——でキー付けします。それが読み取り経路が引くキーであり、`cache_negative` がすでに従っていた規則です。CNAME の連鎖を含む答え一式が、その一つのキーの下に保存されます。質問を持たない応答は正しくキー付けできないので、誰も見ない場所にキャッシュされるのではなく、まったくキャッシュされなくなりました。`cache_key` はキーを埋めながら小文字化しますが、その `String` はどのみち確保されるので追加の確保費用はゼロです。

### ビルド

- **両アーキテクチャは `make` を走らせるどのホストでもクロスコンパイルされ、ビルダー VM は無くなりました。** マルチアーキテクチャイメージは以前、ホストが異アーキテクチャを実行できないときは常に x86_64 の QEMU VM（`make/amd64-vm.sh`）を立ち上げて作られていました——ツールチェーンの代わりに仮想マシン一台、です。公開される二つのイメージは今や、作られ方ではなくターゲットトリプルだけが違います。

  `cargo-zigbuild`（`make/cross.sh`）が、`rusqlite`（同梱の SQLite ソース）と `ring` が必要とする C のクロスコンパイラーを供給し——`rustup target add` だけでは `cc` の段階で死にます——固定した glibc（`GLIBC_VERSION`、既定 `2.36`、`debian:bookworm` に一致）にリンクするので、バイナリはビルドホスト自身の glibc に関係なくランタイムのベースイメージ上で動きます。ツールチェーン一式は root 無しで入ります：`rustup target add`、`cargo install cargo-zigbuild`、そして `.cache/zig/` 以下に展開する zig の tarball。ですからディストリ固有の cross-gcc パッケージに頼らず、`make deps` がどこでも用意できます。

  ランタイムの `Containerfile` は今や `COPY` するだけです——クロスコンパイルされたバイナリと、ビルドホストから取った CA バンドルを。`RUN` ステップがゼロなので、`podman build --platform linux/<arch>` がターゲットアーキテクチャのものを*実行*する必要は一度もなく、したがって異アーキテクチャのイメージはユーザーモードエミュレーションをまったく必要としません。それが問題を移動させるのではなく VM を無くします：Fedora Asahi のようなホストでは、x86 エミュレーションは FEX + `binfmt-dispatcher` + `muvm` を通り、`podman build` のサンドボックス内では使い物になりません。`Containerfile.build` もそれとともに削除され、イメージのビルド中に DNS を解決するものが何も無いので `--network=host` も既定では渡さなくなりました。

- `TARGET` が**すべての**コンテナターゲット（`image`、`push-arch`、`push-rc`、`push-release`）でアーキテクチャを選ぶようになり、`install` リポジトリのモデルを踏襲するので、一つの `TARGET=` の値が town-os の各リポジトリを跨いで通ります。ボードの種類（`rpi`、`rg35xxpro`、`anbernic`、…）はそのアーキテクチャに解決され、認識できない値は解析時のハードエラーです。どのホストでもどのアーキテクチャでもビルドできます——`CROSS` は導出値であって、ユーザーが回すつまみではありません。`push-rc-all` / `push-release-all` は両アーキテクチャを公開し、どちらか一方のホスト一台からマニフェストを組み立てます。

## v0.4.1（2026-08-06）

### 性能

- **UDP リスナーが `SO_REUSEPORT` ソケットにシャーディングされました。** 単一の UDP ソケットがリスナー全体を直列化していました：一つのタスクが `recv_from` でそれを汲み出し、すべての返信も同じソケットから出ていくので、受信はシングルスレッドで、カーネルは両方向でソケットごとのロックを取っていました。クエリごとにタスクを起動しても*処理*はコアに広がりますがソケットは広がらないので、マシンが遊んでいるのにスループットは頭打ちになりました——16 コアのホストでサーバーは 1 コア未満を燃やしながら約 104k qps で頭打ちになり、それを超えるとクライアント側の並行度を上げてもスループットは増えず遅延が膨らむだけでした（並行 32 クライアントで p99 1.70 ms）。各 UDP 待ち受けアドレスは今、同じ `アドレス:ポート` に `dns.udp_shards` 個のソケットをバインドし、それぞれが自分の受信ループと返信用ソケットを持ち、カーネルが到着したデータグラムをハッシュで振り分けます。16 コア上でローカルレコードに対して実測、前 → 後：並行 4 クライアントで 68k → 101k qps、16 で 99k → 168k、32 で 104k → 257k（p99 1.70 ms → 432 µs）。古い経路は 32 クライアントですでに頭打ちでしたが、シャーディングされた方は 128 でもまだ伸びており（477k qps）、サーバーは 16 コア中 2.87 コア。残る天井は生成側であってサーバーではありません。

  `SO_REUSEPORT` は、シャードを二つ以上要求したときに**だけ**設定されます。Linux がポートを共有するのは、そこにバインドされたすべてのソケットがこのオプションを持つときだけなので、単一シャードのリスナーは今も混雑したポートで衝突します——入口のバインド失敗処理はそれに依存しています。無条件に設定していたら、失敗した入口バインドが他プロセスの所有するポートに対して「成功」し始め、そのトラフィックの一部を静かに横取りしていました。シャードは呼び出し元ではなく `serve_udp` の future が（`JoinSet` で）所有するので、`stop_ingress_listener` は今までどおり単一の abort ハンドルですべてを畳めます。そしてポート `0` のバインドは一シャードに強制されます。カーネルは各ソケットに*異なる*エフェメラルポートを渡すので、共有すべき筋の通ったものが何も無いからです。

### 設定

- `dns.udp_shards`（既定 `0`）— UDP 待ち受けアドレスごとにバインドする `SO_REUSEPORT` ソケットの数。`0` は利用可能なコアごとに一シャード、`1` は以前の単一ソケットリスナーに戻します。

### ドキュメント

- 並行モデルの節は DNS の UDP クエリを「単一タスク上で逐次処理される」と記述していましたが、クエリがそれぞれタスクに移された時点でそれは古くなっていました。今はシャードのファンアウトと上記の三つの制約を記述しています。

## v0.4.0（2026-08-05）

### 変更

- **入口 DNS リスナーは今や自分の TLD だけでなく名前空間全体を解決します。** スコープの選択は問い合わせられた*名前*でキー付けされていました：`ingress_target` は `find_tld_owner(qname)` から始まるので、所有 TLD の下に無い名前は `None` を返し、そのクエリは到着したリスナーとの関連付けを失っていました。そのまま送信元 IP の分岐へ落ち、そこでは WireGuard のピアは `security.overlay_cidrs` の内側にありながら `JoinNetwork` されていない（参加するのはマシン自身の入口アドレスだけで、ピアは決して参加しません）ので、関連付けの無いオーバーレイピアとして REFUSED されていました。したがってこのリスナーは自分の TLD にだけ答え、他には何も答えませんでした：ネットワーク上のクライアントは `gitea.default.fart` は解決できるのに `google.com` では REFUSED を受け取り、そのネットワークの DNS サーバーが、自らがリゾルバーであるはずのインターネットを解決できなかったのです。

  スコープは今、名前からではなく**リスナー**から選ばれます（`db::scope_for_ingress_ip`、TLD ごとの入口マッピングの逆）。入口リスナーに届いたクエリは、名前が何であれそのリスナーを所有するスコープに属します：所有 TLD は分離されたまま（兄弟ネットワークの TLD は今も権威 NXDOMAIN）で、それ以外のすべての名前はグローバル解決と上流転送へ落ちます。入口 IP への答えの**書き換え**は今も名前で条件付けられているので、素通りする名前は解決された値を保ちます。入口リスナー以外でのスコープ強制は変わりません。

### ドキュメント

- `CLAUDE.md` と `README.md` を v0.3.x で着地したリゾルバーの作業に追随させました：`auto` 階層チェーン、反復リゾルバーのサーバー選択／バックオフ／上限、委任キャッシュとレコードキャッシュ（`flush_cache` と `flush_upstream_state` がそれぞれ何を消すかを含む）、TTL の意味論、そして上記の入口リスナーのスコープ規則。

## v0.3.3（2026-07-14）

### バグ修正

- **失敗した入口バインドが、その IP をプロセスの生涯にわたって汚すことはもうありません。** `spawn_ingress_listener` は UDP+TCP の abort ハンドルを、どちらのタスクもバインドを試みる前に無条件で記録していたので、バインドに失敗したリスナーは、そのアドレスが提供されていると主張するエントリを残しながら、実際には何も待ち受けていませんでした——そして「存在するか」だけを見るガードのせいで、以降の再追加はすべてその死骸の上で即座に戻っていました。これは一般的なケースで噛みつきました：あるネットワークの TLD の入口 IP は WireGuard オーバーレイのアドレスで、`sync_ingress_listeners` が起動時にデータベースから再生しますが、それはトンネルインターフェースが存在する*前*なので、両方のタスクが `EADDRNOTAVAIL` で失敗して終了していました。ネットワークを設定した状態で再起動したマシンは、そのオーバーレイ上で二度と DNS を提供できませんでした。タスクがすべて終了したエントリは今、存在しないものとして扱われ——破棄されて起動し直され——ますので、再追加はインターフェースが上がった後に実際にバインドを再試行します。

## v0.3.2（2026-07-11）

### 性能

- **反復リゾルバーのための委任キャッシュ** — ルートサーバーから降りていく途中で学んだ ゾーン → ネームサーバー の委任がキャッシュされるようになったので、コールドな名前が検索のたびに ルート → TLD を歩き直すことはもうありません。長寿命の委任（ルートと TLD の NS セットは数日の TTL を持ちます）は新しい `delegation_cache` テーブルに永続化されます。`resolution.delegation_persist_min_ttl`（既定 300 秒）が条件で、再起動しても温まった状態で戻ります。
- **委任だけでなく再帰の全体がキャッシュされます** — グルー、グルー無しの NS 名の検索、CNAME のホップは TTL つきで届くのに、以前は一度使って捨てられていました。今は `(名前, タイプ)` でキー付けして保持し、*残り*の寿命とともに返すので、提供されたレコードが上流で満期の TTL で再キャッシュされることはありません。権威ある NXDOMAIN/NODATA の答えは別の否定マップにキャッシュされるので、肯定側の経路は「レコード無し」をミスとして扱えます。
- **負荷をルートサーバーに分散** — サーバー選択は、純粋な RTT 順ではなく `hits * ema_latency` で採点するようになりました。純粋な RTT 順は、すべてのクエリを最速のルート一台へ寄せていました。この積はグループ内の均等化へ向かい、クエリを `hits ∝ 1/遅延` の比で配分します：速いサーバーが多く担い、健全なサーバーはすべて何かしら担います。未照会のサーバーは 0 点で最初に試され、どのみち発生するクエリから自分の遅延を学びます——事前計測も探索確率の分岐も不要です。
- **ルートのプライミング** — 生きたルート NS セットを起動時に一度取得し、`.` の委任として本物の TTL でキャッシュします。ハードコードされた `ROOT_HINTS` は本来あるべきものになります：ブートストラップであり、プライミングが失敗したときのフォールバックです。

### バグ修正

- **失敗した上流サーバーが実際に復帰するようになりました。** 失敗は、合成的な 10 秒の RTT として遅延の EMA に畳み込むのではなく、明示的な指数バックオフ（2 秒、倍増、上限 300 秒、最初の成功で解除）として追跡されます。畳み込んでいたせいで、復帰の速さが健全なピアのたまたまの速さに縛られていました——0.3 ms のピアに対して 10 秒のペナルティは、死んだサーバーに 33,000 分の 1 の取り分しか与えないので、再試行されることも復帰することもありませんでした。バックオフ中のサーバーは同じアドレスファミリー内で健全なものの後ろに並びますが、決して除去されないので、すべてが失敗していても解決は続きます。
- **DNS キャッシュの起動時ロードが常に 0 件だった** — 空の名前で `cache_lookup` を呼んでいました。新しい `cache_load_all` が、本来行うべきだったクエリを行います。
- **`dns_cache` の行の重複** — 応答を再キャッシュすると、更新ではなく行が追加されていました。既存の重複は畳まれ、一意インデックスが `cache_insert` を upsert にします。
- **`flush_cache` が委任を捨てなくなりました。** gRPC のレコード変更はすべてこれを呼ぶので、ローカルレコードを変えるたびに上流の状態を捨てていました。階層の切り替えは別の `flush_upstream_state` を使います。

### 設定

- `resolution.delegation_persist_min_ttl`（既定 300）— 委任が再起動を越えて永続化されるための最小 TTL。
- `resolution.default_ttl`（既定 300）— レコードや応答が自前の TTL をまったく持たない場面（SOA の無い NXDOMAIN/NODATA、TTL ゼロの委任やグルーレコード）で適用される TTL。*存在する* TTL は、SOA の否定 TTL も含め、送られたとおり厳密に尊重され、決して切り詰められません。

## v0.3.1（2026-07-10）

### 新機能

- **LAN → 所有スコープへの解決フォールバック** — 信頼されたローカル送信元（ループバック／LAN、どのスコープにも関連付けられていない）が、所有されている**すべての**ネットワーク TLD をその所有スコープから解決するようになったので、レコードがスコープ付きで保存されていても、すべてのネットワーク TLD が LAN から見えます。フォールバックはグローバル検索の後（デュアルホームな名前の LAN 向けグローバルレコードは今も勝ちます）、上流転送の前に走ります：スコープにしか存在しない名前（例えばあるネットワークのゾーン頂点）は所有スコープから保存された値で提供され、次にその TLD のピアフォワーダーが参照され、すべて失敗すると**権威ある NXDOMAIN** が返ります——私的に所有された TLD が LAN から上流へ転送されることは決してありません。オーバーレイピアは影響を受けず厳密に分離されたまま（あるネットワークに参加したピアは自分の TLD だけを見て、兄弟ネットワークの TLD には NXDOMAIN を受け取ります）なので、WireGuard オーバーレイを何も結びつけずに、TLD を*所有する*ためだけのスコープ（ピアからは分離され、LAN からは解決可能）を作れます。

### ビルド

- **`go test` の出力がライブに** — Makefile の `go-test` と `go-integration-test` ターゲットは `go test` をローカルディレクトリモードで（`./...` のパッケージパターンを落として）走らせるようになったので、詳細出力（`-v`）がパッケージ単位で完了までバッファされるのではなく、テストの進行に合わせて流れます。Go のテストはすべてルートの `go/` パッケージにあるので、カバレッジは変わりません。

## v0.3.0（2026-07-02）

### 新機能

- **DoH を優先するセキュア階層を備えた、粘り強い `auto` 上流解決** — リゾルバーはルートのみではなく段階的なフォールバックチェーン（ルート → DoH/DoT → ローカルフォワーダー → 公開 :53）をたどるようになったので、外向きの :53（および DoT の :853）を落とすネットワークでも解決を続けます。:443 上の DoH が優先されます。有効階層を粘着させることで死んだ階層でのクエリごとのタイムアウトを避け、定期的な復旧探索が階層の復帰を捉え、階層の切り替えは失敗の猶予期間で条件付けられ、確定した切り替えのたびに DNS キャッシュを、階層跨ぎのキャッシュポイズニング対策としてフラッシュします。`resolution` セクションで設定します（`mode = auto`、`secure_upstreams`、`public_fallback`、`switch_grace_failures`、`recovery_probe_secs`）。
- **アドレスファミリーの経路到達性プローブ** — 新しい `address_family` 設定セクションとバックグラウンドのプローブが、公開の v4/v6 ターゲットの :443 へ定期的に TCP connect し、実際のインターネット到達性を試験します。`auto` モード（既定）では、ホストが到達できないファミリーについてサーバーは A または AAAA の答えを返すのをやめるので、クライアントは死んだファミリーのアドレスで止まる代わりに、動いているスタックへフォールバックします。`off` は常に両ファミリーに答えます（従来動作）。`force4`/`force6` はプローブせずに一方に固定します。失敗の閾値がばたつきを抑え、復旧は最初の成功で即座に効き、起動時の最初のプローブは決定的（猶予なし）なので、死んだファミリーのリンクで起動した場合は最初のクエリからそれが抑制されます。

### バグ修正

- **RBL/DNSBL の照会がローカルスタブを経由してループしなくなりました。** ブロックリストのクエリ（`<名前>.<ゾーン>`）は以前システムリゾルバー経由で出ていましたが、典型的なホストではそれが rolodex 自身を指しており——クエリハンドラーに再入し、また DNSBL 判定され、ゾーンをもう一度付け足し、`<名前>.<ゾーン>.<ゾーン>…` と永久にループして、解決全体を詰まらせていました。`RecursiveRblResolver` は今、rolodex が他のすべてを解決するのと同じやり方でブロックリスト名を解決します：自前のソケットでルートから再帰的に（決してローカルスタブは使わず）、ルートに到達できないときは設定済みフォワーダーへ UDP でフォールバックします。完全な失敗は開いた側に倒れます（未掲載として扱う）。
- **外向きの :53 がフィルタされているとき（バックグラウンドのプローブで検出）、RBL/DNSBL の確認はリゾルバー依存のプロバイダーを飛ばします**。フィルタされたネットワークで解決が止まらないようにするためです。データベース由来のローカル RBL エントリは影響を受けません。

### 性能

- **RBL/DNSBL 確認のための、投げっぱなし非同期キャッシュ充填** — ブロックリストのキャッシュ投入が解決の経路をブロックしなくなりました。

### インフラ

- **再現可能なイメージの保証** — `make/build.sh` が `SOURCE_REV`（git の短い HEAD、ツリーが変更されていれば `+dirty`）を計算し、両方のイメージビルドに通します：ビルダーステージは `cargo build` の直前でそれを参照するので、コミットが変われば必ずコンパイル層が無効化されます（再利用されたキャッシュ層から古いバイナリが出荷されません）。そしてランタイムイメージは `org.opencontainers.image.revision` を刻むので、公開されたイメージは `skopeo inspect` で自分の素性を名乗ります。

### コード品質

- ライブの DoH/DoT 上流テストは、無条件に無視されるのではなく到達性で条件付けられるようになったので、ネットワークが許すところでは実際にセキュア階層を動かします。

## v0.2.4（2026-06-28）

### 新機能

- **DNSBL（ドメインのブロックリスト）** — IP ベースの RBL とは別の、ドメイン名によるブロックリスト機構です。DNSBL プロバイダー（例：Spamhaus DBL、SURBL、URIBL）へは、調べる名前をゾーンの前に付けて（`<名前>.<ゾーン>`）問い合わせます。RBL が使う反転 IP 形式とは対照的です。DNSBL の掲載は、**外部で解決されたどの答えよりも優先されます**——転送されたもの、ルートからの反復、上流キャッシュのいずれであっても——一方でローカルレコードと管理／権威ゾーンは常に勝ちます。起動時は `dnsbl` 設定セクションで、実行中は新しい `SetDnsblConfig`/`GetDnsblConfig` gRPC エンドポイント、`set-dnsbl-config`/`get-dnsbl-config` CLI サブコマンド、Go クライアントの `SetDnsblConfig`/`GetDnsblConfig` メソッド（`DnsblConfig`/`DnsblStatus` 型）で設定します。
- **解決キャッシュのパイプライン内での RBL/DNSBL** — ローカル RBL と DNSBL の確認が、ローカル／ゾーン解決の後、上流キャッシュとフォワーダーの前で正引きのドメイン名に適用されるようになったので、上流の答えが以前にキャッシュされていても、ブロックリスト掲載の名前は NXDOMAIN で拒否されます。DNS キャッシュの「ローカルレコード優先」段階は権威あるローカルエントリだけを提供するようになり（`lookup_local_only` 経由）、上流キャッシュのエントリはブロックリストのゲートの後で提供されます。

### 変更

- **RBL と DNSBL のプロバイダーリストは既定で空になりました。** 以前は RBL が五つのゾーンを既定で同梱していました。運用者がプロバイダーを設定する（設定ファイルまたは `Set{Rbl,Dnsbl}Config` で）まで、外部のブロックリストには一切問い合わせません。有効だが空のブロックリストは何もしません。

### コード品質

- `make lint` が `cargo clippy --all-targets` を走らせるようになり、ライブラリに加えてテストとベンチマークも検査します。これで表面化した既存の指摘を修正しました：ベンチマークで `await` を跨いで保持された `RefCell` の借用、テストでの `Copy` に対する clone、`assert!(true)` のプレースホルダー、設定テストの `field_reassign_with_default`。
- DNSBL の解決と優先順位（上流キャッシュ済みの答えに対する優先を含む）、空のブロックリストが何もしないこと、そして gRPC/CLI/Go のプログラム可能なエンドポイントを覆う、新しいユニット・結合・CLI・Go の各テスト。

## v0.2.3（2026-06-28）

### 新機能

- **ルートからの反復解決**が既定の上流モードになりました——クエリはルートサーバーを起点に再帰的に解決され、従来の `forward` モードも引き続き選択できます。
- **自動の逆引き PTR レコード** — オプトインの `dns.auto_ptr` により、gRPC 管理インターフェース経由で追加された A/AAAA レコードが、対応する `in-addr.arpa`/`ip6.arpa` の PTR レコードを自動的に維持します。
- **ACME 発行者／認証局**（RFC 8555、サーバー側） — Rolodex は自身の CA として振る舞います：自己署名のルートがゾーンごとの中間証明書に署名し、それが ACME を通じてリーフ証明書を発行します。検証は Rolodex 自身の DNS データに対する dns-01 です。External Account Binding、DANE-TA TLSA の自動公開、信頼されたネットワーク向けの Web 登録ポータル、ブラウザー拡張、そして DANE 取得を備えた JavaScript クライアントライブラリを含みます。
- **DNS 経由の CA 配布** — ルートおよび中間 CA のチェーンが CERT（RFC 4398）と分割された TXT レコードとして DNS データベースに公開されるので、そのゾーンを解決できるクライアントはポータルにアクセスせずに CA を取得して信頼できます。

### インフラ

- ネイティブなマルチアーキテクチャ（amd64/arm64）コンテナビルドを `quay.io/town/rolodex` へ公開。arm64 ホスト向けの amd64 ビルダー VM と、`uname -m` を接尾辞に付けたアーキテクチャごとのイメージタグを含みます。
- `primary`（外向き IP を自動検出）と `インターフェース:ポート`（名前付きインターフェース上のすべてのアドレスにバインド）のキーワードによる、統一された DNS バインド設定。
- `Cargo.toml` に `repository` フィールドを追加。

## v0.2.0-alpha（2026-03-27）

### 新機能

- **DHCP サーバー**。統合された IPAM、DNS ホスト名の自動登録（スコープ付きの A + PTR レコード）、MAC と IP の粘着的な結びつけ、バックグラウンドのリース掃除、そして DISCOVER/OFFER/REQUEST/ACK の完全なフロー
- **スコープごとの RBL プロバイダー** — ネットワークスコープは、グローバル設定に加えて追加の RBL プロバイダーを選べます
- **DHCP による証明書配送** — サイト固有の DHCP オプション（コード 224〜254）でクライアントへ証明書を配送
- 上流 DNS 転送のための **SOCKS5 プロキシ**対応（RFC 1928）。既存の HTTP CONNECT と DoH プロキシモードに加えて
- `enable_h3` 設定フラグによる DNS-over-HTTPS の **HTTP/3（QUIC）トランスポート**
- QNAME ランダム化、キャッシュ操作、DB 検索、ゾーン一致、クエリのパイプライン、UDP/TCP のラウンドトリップを覆う **Criterion ベンチマーク**（`make bench`）

### 性能

- **その場での QNAME ランダム化** — DNS メッセージ全体を解析・複製・再構築・再シリアライズするのではなく、DNS ワイヤ形式のバイト列を直接操作します（0x20 ビットの反転）。転送クエリごとに約 6 回の確保を削減
- **DB 検索のバッチ化** — `lookup_with_fallbacks()` が完全一致・ワイルドカード・CNAME・ANAME の検索を単一の SQL `UNION ALL` クエリにまとめ、クエリごとのミューテックス取得を 4 回以上から 1 回へ削減
- **O(ラベル数) のゾーン一致** — `find_managed_zone()` と `find_authoritative_zone()` が、`ends_with()` による O(ゾーン数) の線形反復ではなく、`DashSet` に対して DNS のラベルサフィックスを歩きます
- **Arc で包まれたキャッシュレコード** — DNS キャッシュは `Arc<Vec<DnsRecord>>` を保持し、キャッシュ挿入時とローカルキャッシュヒット時の複製をなくします
- **キャッシュ永続化のバッチ化** — 挿入ごとの `tokio::spawn` を、上限付き `mpsc` チャネル（容量 1024）と単一のバックグラウンドワーカーに置き換え、一度に最大 64 件の書き込みを排出します
- **キャッシュキーの最適化** — 冗長な `to_lowercase()` を省いた、あらかじめサイズを与える `String::with_capacity`（名前はすでに正規化済み）
- **UDP バッファのサイズ調整** — 受信バッファはループの外で一度だけ確保し、複製は `Vec::with_capacity` + `extend_from_slice` で実際のパケット長に合わせます
- **DoH プロキシのコネクションプーリング** — プロキシアドレスごとの `DashMap` プール（アドレスあたり最大 8）による再利用可能な TCP 接続と HTTP/1.1 keep-alive

### コード品質

- コードベース全体から `unwrap()` 呼び出し、`let _ = expr;` による抑制、デッドコードをすべて排除
- Makefile に `lint` ターゲットを追加（`cargo fmt --check` + `cargo clippy -D warnings`）
- lib.rs と main.rs の両方でクレートレベルの `#![deny(dead_code)]` と `#![deny(unsafe_code)]` を強制
- 最適化されたすべてのコード経路を覆う 40 件の新しいユニットテスト
- 新機能、性能上のパターン、ベンチマークのドキュメントをすべて反映した包括的な CLAUDE.md 仕様の更新

### インフラ

- コンテナイメージを `quay.io/town/rolodex` へ変更
- すべての依存関係を最新の互換バージョンへ更新

## v0.1.0

最初のリリース。

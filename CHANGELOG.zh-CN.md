# 变更日志

> 语言：[English](CHANGELOG.md) ｜ [繁體中文](CHANGELOG.zh-TW.md) ｜ **简体中文** ｜ [Español (España)](CHANGELOG.es-ES.md) ｜ [Español (México)](CHANGELOG.es-MX.md) ｜ [日本語](CHANGELOG.ja-JP.md)

## 未发布

### 功能

- **DoH 开始说 HTTP/3，`doh.enable_h3` 不再是一个什么都不做的开关。** 这个键能被解析、`SetDohConfig` 返回 `success: true`，而机器只在 TCP 上提供 h2，别无其他——一个被接受的设置与一个被实现的设置无从分辨，这正是它能存续这么久的原因。现在它会开启第二个监听器（`doh_h3_server.rs`）：相同地址、相同端口、相同证书，以 QUIC 取代 TCP。客户端因此得到的是队头阻塞的消失——在 TCP 上，一个丢失的分段会卡住该连接上复用的每一次查询，在 QUIC 上只会卡住一次——以及对曾与这台机器交谈过的客户端而言的零往返恢复。`GetDohConfig` 报告的是实际运行中的状态，而不是写死的 `false`；而失败的 QUIC 绑定会让整次应用一并失败，而不是悄悄留下一个承诺更多却只有 h2 的名字。

  证书不会被加载两次：QUIC 配置派生自 TCP 监听器自己的配置，只是把 ALPN 列表换成 `h3`。两个管理器共用同一对文件，会在某次轮询注意到更新之前一直漂移；而在自动生成的材料上，它们永远不会一致——各自会铸造自己的自签证书，于是同一个名字会依客户端所选的传输而由两把不同的密钥提供服务。

- **客户端会从两条途径得知 HTTP/3 端点的存在。** 已经持有 h2 连接的客户端，会从每个 DoH 响应上的 `Alt-Svc` 标头得知；完全还没连接过的客户端，则从 DDR 指定记录得知——它现在会为 DoH 端点发布 `alpn=h2,h3`。两者都到不了对方的客户端，这正是两者都存在的理由——而在 HTTP/3 关闭时两者都不作声，因为宣告一个无人应答的端点，会让每个相信它的客户端在退回之前付出一次完整的超时。

- **ACME 不再于由其他证书提供服务的端点上发布 DANE 记录。** `acme.tlsa_endpoints` 的存在，是为了让一张覆盖多个端点的证书在每个端点都有一条记录——但那些监听器未必提供的就是 ACME 的证书，而在 Town OS 机器上，DoT 与 DoQ 读取的是机器自有 CA 签发的叶证书。此时 ACME 的关联数据所固定的，是该端点从未出示过的证书；而 RFC 7671 规定，找到 TLSA 记录却无一相符的客户端必须拒绝该连接；若完全没有记录，同一个客户端会退回 PKIX 并持续工作。这样的端点现在会在启动时被剔除——并且大声说明，指出是哪个监听器、它实际提供的是哪张证书，以及两种修正方式——而不是被发布或被悄悄跳过。

### 测试

- **`tests/doh_h3_test.rs`**（新增，并已列入 Makefile 的 `rust-integration-test` 配方）以真实的 HTTP/3 客户端对真实的监听器发起：ALPN 令牌是从一份以 `h2`／`http/1.1` 播种的配置协商而来，因此被测试的正是那次替换；RFC 8484 的两种请求形式都由同一条已存记录作答；`Cache-Control` 带的是该答案自己的 TTL，一个刻意挑选的非整数，好让写死的默认值无法蒙混过关；每一次拒绝都会结束它的流，因为送出标头却永不关闭的那种，在客户端读起来像是一台卡死的解析器，而不是一次被拒的请求；而四个同时在途的请求会被各自作答且不交错，这是顺序测试无法与串行处理的服务器区分开来的。

- **`tests/ddr_follow_test.rs`**（同样是新增）是没有人写过的端到端测试：它通过真实的 UDP 套接字向运行中的解析器询问其加密端点在哪里，解析 SVCB 答案，并且只用记录所说的内容——目标、端口、ALPN 令牌、URI 模板——建出一个 DoH 请求，然后在那里解析一个名字并检查地址。DDR 的每个部分此前都各自被覆盖，而整条链仍可能因为一个无人提供的模板或一个指向不存在监听器的端口而断裂。对照组会在指定记录未指名的路径上发问并要求 404；没有它，遵循模板什么也证明不了。

### 文档

- `DESIGN.md` 在 DoH 传输之下新增 HTTP/3 监听器，并在测试套件章节下新增这两个新套件；`README.md` 与 `CONFIGURATION.md` 新增这两种发现机制；而 `acme.tlsa_endpoints` 现在会说明它拒绝发布什么以及为什么。六个语言同步更新。
## v0.6.1（2026-08-15）

### 功能

- **运行期自身的阻塞工作现在会被测量，并以两个新的指标系列输出**——`rolodex_dns_blocking_duration_seconds{site}`（直方图）与 `rolodex_dns_blocking_stalls_total{site}`——使输出的系列数来到 82 个。服务器整体是 `async` 的，但它必须做的几件事并不是：SQLite 位于单个 `std::sync::Mutex<Connection>` 之后、证书文件要从磁盘读出来、而签名运算就是运算。这些每一件都会在它执行的整段期间占住那条线程，而在 Tokio 工作线程上，那就是一条没有在轮询其他任何东西的线程——一句慢的语句不只让它自己的调用者等，也让那条工作线程正在多路复用的每一个查询一起等。`query_duration_seconds` 只会把它显示成摊在互不相干的名字上的延迟，没有办法归因到真正的成因。

  八个站点，一个只会往后追加、永远不会插入的固定枚举，因为这些值是预先分配数组里的位置：`db_lock_wait`、`db_locked`、`db_open`、`metrics_collect`、`tls_reload`、`dnssec_sign`、`dnssec_verify`、`config_load`。`db_lock_wait` 与 `db_locked` 之所以分开，是因为它们的意思恰好相反——等待时间是*别人*让你付出的代价，持有时间是你让别人付出的代价——而且两者都记录在 `Database::lock`，也就是每一个碰 SQLite 的方法都会经过的那个唯一咽喉点，这正是让之后新增的方法不需要任何人记得去测量就已经被测量的原因。持有时间是在守卫的 `Drop` 里取的，所以提前带着错误返回的路径，会以它实际持有连接的时间被计入，而不是完全没被计入。

  `blocking_stalls_total` 计算的是达到或超过 10ms 的观测值（`metrics::BLOCKING_STALL_NANOS`），这样一条告警就能直接问“多常发生”，而不必再重述一次某个桶的边界；分布本来就已经在直方图里了。那些桶是自己的一组，从 100ns 起算，而不是查询直方图的 50µs 下限——一次没有争用的互斥锁获取只有数十纳秒，而一个第一格就已经装下每一个健康样本的直方图，看不出它不再健康的那一天。有三个站点刻意不在工作线程上：`tls_reload` 跑在阻塞线程池上，而 `db_open` 与 `config_load` 是在任何监听器绑定之前跑的。它们仍然被测量，因为“这够快了，不重要”是一个值得有一条系列去撑的主张，而不是一句断言它的注释。

  服务器应答的方式没有任何改变。没有新的配置、没有新的依赖，任何路径上也没有新的工作——这份检测只是在本来就是同步的区段外面加上一个计时器与一个 `Drop` 守卫。

### 测试

- **`tests/blocking_metrics_test.rs`**（新增，并已列进 Makefile 的 `rust-integration-test` 配方）为每一条被测量的路径，都配上一条必须什么都不记录的对照路径：通过真实的 `Database` 做一次插入与一次查询，对照一次完全由内存中关联缓存服务的读取，后者两条系列都不得移动——一次从未取锁的读取若记下 `db_locked` 样本，就是在报告没有人正在经历的争用。停顿阈值从两侧各检查一次，比 10ms 少一纳秒，以及正好等于 10ms，因为 `>=` 与 `>` 的差别恰好就是针对那个边界所写的告警会踩到的情况。站点索引以名称对索引逐一钉住，旁边再加一条长度断言，因为 `BLOCK_SITE_*` 常量就是数组的位置，插入一个值会悄悄把它之后每一个已记录的样本改标签。而且在任何观测发生之前，每个站点就必须已经以零值输出：一个要等到非零才出现的标签值，会让刚重启的进程上的 `rate()` 悄悄变成空的，也会让“我们从未在这里阻塞”与“这个站点不存在”无法区分。

### 文档

- `DESIGN.md` 在指标章节下新增了一节 **运行期阻塞**——站点表格、每个区段跑在哪一种线程上、为什么两个数据库站点要分开，以及那三个不在工作线程上的站点为什么还是要测量——并在“指标测试”下加上新测试套件的说明。`README.md` 新增了五条针对新系列的食谱查询。文档里的系列数在两份文档中都从 80 改为 82，而 `tests/promql_docs_test.rs` 会把这个数字钉在注册表实际输出的内容上，新区块里的每一条查询也都由同一套测试对着实际的输出格式解析验证。六个语言版本同步更新。

## v0.6.0（2026-08-15）

### 破坏性变更

- **RBL 已移除。DNSBL 留下来了，而且它现在就是那份封锁列表。** 反向 IP 的提供方查找——在每一次反向 DNS 查询时，拿一个反转过的地址去查提供方区域——已经不存在了，一并消失的还有逐范围的 RBL 提供方、`rbl:` 配置区段、`SetRblConfig`／`GetRblConfig`、`AddScopeRblProvider`／`RemoveScopeRblProvider`／`ListScopeRblProviders`，以及 `set-rbl-config`／`get-rbl-config`／`*-scope-rbl` 这几个 CLI 命令。`src/rbl.rs` 现在是 `src/dnsbl.rs`，而两份列表共用的那套机制——提供方、拒答码、轮换、结果缓存、递归解析器——则以 `DnsblChecker`／`DnsblProvider`／`DnsblResolver` 的名义存活下来。既然只剩一份列表，`set_dnsbl_config` 之流就只叫 `set_config`。

  **地址仍然封锁得了**，只是改由本地列表而不是提供方来做：运维人员指名那个地址（或 `dig -x` 打印出来的那个反向名称），两种写法都会是 NXDOMAIN。反正那也正是提供方在这里永远做不好的事——它被问的是「正在被解析的那个*名称*」，而在一次反向查找里，那是一个没有人会为它公布信誉的名称。

  **本地列表改了名字，数据行也一并迁移**：`local_rbl_entries` 变成 `local_blocklist_entries`，条目会在启动时搬到它上面。那是运维人员自己的列表，而一台升级之后封锁列表无声清空的机器，看起来会跟封锁列表坏掉一模一样。`scope_rbl_providers` 则是直接丢弃——它所配置的那些查找已经不存在了。在 gRPC 上，`LocalRblEntry` 及其 Add／Remove／List 方法现在是 `LocalBlocklistEntry`／`AddLocalBlocklistEntry`／`RemoveLocalBlocklistEntry`／`ListLocalBlocklistEntries`；Town OS 用来提供它们的那些 HTTP 路径（`/dns/rbl/local*`）维持历史上的拼法，因为那是与 UI 之间一份已经公布的契约。

  proto3 能保留一个*字段*编号，却无法保留一个消息或方法**名称**，而每一项被退役的东西都是一整个消息或方法——所以 proto 改为携带一个明确的「已退役名称」区块，那些名称一律不得再被使用。

- **封锁列表的指标标签变了。** `blocklist_blocks_total{kind}` 现在是 `local` 或 `dnsbl_provider`；`rbl_provider`、`rbl_local` 与 `rbl_scope_provider` 都不见了。`answers_total{source}` 在原本报告 `rbl` 的地方改为报告 `reverse_blocklist`。凡是指名了旧标签值的仪表板都需要更新；任何以 `kind` 汇总的东西则照常运作。

### 功能

- **ACME 现在会在证书所服务的每一个端点发布 DANE-TA 记录，而不只一个。** `acme.tlsa_port`／`acme.tlsa_proto` 是一组标量，因此签发只会放上一条 TLSA 记录——默认是 `_443._tcp.<name>`。TLSA 记录指的是*服务端点*而非证书，而加密 DNS 在同一张证书上就是两个端点：`853/tcp` 的 DoT 与 `853/udp` 的 DoQ。没被发布的那一半，对任何检查 DANE 的客户端都会失效阻断，其表现与服务器根本没有 DANE 完全相同。`acme.tlsa_endpoints` 是一份 `"<port>/<proto>"` 列表，与标量那组一并发布并去除重复，协议会规范化为小写，因此 `853/UDP` 会成为一条有效的记录，而不是无人查询的 `_853._UDP.` 名称。格式错误的条目会在启动时被拒绝而不是跳过，理由相同：一条悄悄从未出现的记录，正是这套机制存在的目的所要避免的失败。

- **加密传输可以在运行期配置，`Set*Config` 不再说谎。** `SetDotConfig`、`SetDohConfig` 与 `SetDoqConfig` 从前只是把请求打进日志，然后什么也不存就返回 `success: true`——照单全收，随手丢弃。编排方无法把这与"成功了"区分开，于是配置加密 DNS 的唯一办法就是写配置文件再重启进程，而在一台 rolodex 就是唯一解析器的机器上，那是它上面一切的一次 DNS 停摆。

  新的 `TransportSupervisor`（`src/transports.rs`）在整个进程生命周期里持有 DoT/DoH/DoQ 的监听器，而且**启动路径与 RPC 现在是同一条代码路径**：`main.rs` 通过 RPC 所调用的那同一个 `apply()` 把每一种传输拉起来，因此一份在启动时可用的配置，是由恰恰处理运行期到来的那份配置的同一段代码来应用的，两者不可能各自漂移。`:53` 从头到尾没被碰过——这些是彼此独立的监听器，重新配置其中一个，在它自身之外不付出任何代价。

  在旧监听器让出端口之前，新监听器无法启动，所以没有办法在放弃旧配置之前证明新配置能绑定。于是改为：先做完所有**不需要端口**就能做的检查（bind 列表可解析、TLS 材料可加载或可生成），这样一个打错的地址或一份读不出来的证书会在旧监听器仍在服务时被拒绝；然后停掉旧监听器**并等待它们真的结束**，因为只 abort 而不等待，会让新的绑定与旧套接字的关闭赛跑；而如果绑定仍然失败，就把先前的配置放回去，并告诉调用方这个传输已经下线。空的 bind 列表是关停而不是错误——那本来就是省略一个配置段的含义。

  `Get*Config` 报告的是**实际绑定**的地址，只要请求写的是端口 0，它就与请求的不同。这些消息新增了 `binds` 列表，`TlsConfig` 新增了 `self_signed_sans`；旧的单个 `bind` 字符串仍然被采纳，因此早于列表写法的调用方照常工作。`doh.bind` 同样接受列表，条件与 `dot.bind` 一致。一个在构造时没有装配传输监管的服务器——也就是进程内的测试夹具——会回 `FailedPrecondition`，而不是声称自己配置好了一个它根本没有的监听器。

- **SVCB 与 HTTPS 记录（RFC 9460），以及随之而来的 DDR（RFC 9462）。** 从前没有 SVCB 记录类型，于是解析器面对 `_dns.resolver.arpa. SVCB` 无以作答——而那正是客户端发现自己那台解析器的加密端点的方式。因此加密 DNS 只能被手工敲进每一台设备，前提还得是运维知道它存在。

  值是一行 RFC 9460 §2.1 的表示格式（`1 dns.home. alpn=dot port=853`），支持 `alpn`、`no-default-alpn`、`port`、`dohpath`（RFC 9461）以及 `keyNNNNN=` 这个逃生舱口。参数会被排成线路格式所要求的严格递增的键序，因此运维可以按任意顺序书写，仍然得到一条客户端会接受的记录，而不是一条每个做校验的客户端都会丢弃的记录。解析是**严格的，而且发生在写入的那一侧**：无法解析的值会被 `AddRecord` 拒绝，而不是先存下来、再在服务时被跳过——因为一条出现在每一份列表里却什么也答不出的记录，正是这套代码反复找到的那种故障。DNSSEC 的规范化编码的是服务路径所发出的同一份 rdata，因此被签名的字节与线路上的字节不可能不一致。

  `arpa.` 依然从不在这台机器之外解析——但那道拒绝坐落在每一次本地查找的*下方*，所以本服务器自己持有的那份指定记录会由它自己的记录作答，永远不出这台机器。这恰恰是 DDR 需要的性质：为一台解析器自己的指定记录作答的，只能是那台解析器。

- **可以指名一份尚未签发的证书。** `cert_path`／`key_path` 指向一个并不存在的文件，从前是一次硬性的启动失败，因此这两个路径只能在文件已经就位之后才写得下去——而在一台 CA 是在解析器启动*之后*才被创建的机器上，要走到那一步就意味着重写配置并重启这台机器唯一的解析器。如今只要与 `auto_self_signed: true` 并列，一份被指名但尚不存在的证书会先用生成的材料起步，而既有的 30 秒轮询器会在真正的那一对出现的当下把它接过去，不需要重启，也没有什么要协调。在 `auto_self_signed: false` 之下它仍然是硬性失败：那是运维在说"要么给我这份证书，要么什么都不要"，而悄悄改用一份生成的证书，比拒绝启动更糟。

- **上游解析模式可以在运行期切换。** `resolution.mode` 从前只在启动时读一次、此后再也不读，这让它成了编排方无法在不重写配置文件并重启进程的前提下改动的唯一一项上游行为——而重启一台机器唯一的解析器，就是让它上面的一切都断一次 DNS。现在 `SetResolutionMode` / `GetResolutionMode`（proto、`src/grpc_service.rs`、Go 客户端，以及 `set-resolution-mode` / `get-resolution-mode` 两个 CLI 动词）可以实时改动并读取它。文件只是**启动时的种子**；读取接口报告的是真正在解析查询的那个模式，所以两者可能不一致，而以正在运行的服务器为准。

  该 RPC 会以 `InvalidArgument` **拒绝**无法识别的模式，而不是像文件那条路径那样发出告警再退回 `auto`。文件是由能看见告警的运维在启动时读一次的；而 RPC 那头有个调用方在等答复，告诉它"成功"却在用它没要的模式解析，正是一台机器在过滤 `:53` 的网络上落进 `recursive`、而日志里对每个名称为何解析失败只字未提的由来。

  切换**进入** `auto` 会派生与启动路径相同的层级预热，因此切换之后的头几次查询不必付冷层级的代价；而 `recovery_probe_loop` 现在是无条件派生的（在 `auto` 之外它什么也不做），所以在运行期切换进 `auto` 的模式仍然能夺回一个已恢复的层级。

- **`dot.bind` 与 `doq.bind` 接受一个地址列表，于是 DoT 与 DoQ 能同时覆盖两个地址族。** 它们此前各自只收一个绑定字符串，而一个字符串无法同时点名两者：`0.0.0.0` 只管 IPv4，而 `[::]` 并不是能同时顶替两者的可移植写法——在 `net.ipv6.bindv6only=0`（Linux 的默认值）之下，`[::]` 的套接字也会收下 v4 映射的流量，于是它会与同一端口上 `0.0.0.0` 的套接字相撞，后绑定的那个以 `EADDRINUSE` 失败。这两个字段现在**既收裸字符串，也收一个列表**，每一项都各自走那四种常规写法（`ip:port`、`[ipv6]:port`、`primary:port`、`interface:port`），重复的会被丢弃而不是绑定两次。裸字符串仍然被接受，所以在列表写法出现之前写下的每一份配置都照样能解析。

- **更新后的 TLS 证书不必重启服务器就会被提供出去。** 每一个 TLS 监听器——DoT、DoH、DoQ、ACME 签发者与注册门户——都在启动时对自己的 `rustls::ServerConfig` 取了一份一次性的快照，而 `TlsManager::reload()` 虽然存在也有测试，却没有任何地方调用它。于是证书更新就等于一次 DNS 中断：监听器会一直提供那张过期的证书，直到进程被重启为止——而在一台由 rolodex **担任**解析器的机器上，为了更新那张由 ACME 监听器自己签发的证书而去重启它，是一个值得打破的循环。

  监听器现在跟随一个 `watch` 通道而不是快照，而每个管理器每 30 秒轮询一次自己的证书文件，并在内容改变时重建。已经建立的连接会在它握手时所用的那张证书下走完；下一条抵达的连接拿到新的。没有任何东西重新绑定，所以端口一次都不会关闭。DoT 逐连接构造它的 acceptor，DoQ 在它的 accept 循环里用 `set_server_config` 套用这次替换，而三个 HTTPS 监听器则写入 axum-server 逐连接重新加载的那个 `RustlsConfig`。

  检测方式是轮询文件的**内容**，并在解析它们的同一趟里做哈希。一个 inotify 监视会漏掉真实更新通常采取的那两种形态——把新文件重命名盖过旧路径，或把一个符号链接移向某个带版本的目录——因为两者都不会写入正在被监视的那个 inode。轮询失败时会让此前那张证书继续提供并重试，因为指纹只在成功加载之后才记录：一个 ACME 客户端会写两个文件，而一个三十秒的定时器终究会落在两者之间，`rustls` 会拒绝那对不相符的组合，而完整的那一对会在下一个节拍被捡起。提供生成式材料（`auto_self_signed`）的管理器不会被轮询——按定时器重新生成，等于每分钟两次递给每个客户端一张不同的证书。

### 缺陷修复

- **通过 gRPC 启用的封锁列表，现在也会拿到出站 `:53` 的可达性探测。** 提供方查询是走明文 `:53` 出去的，因此在过滤该端口的网络上它们只会超时并徒增延迟；那个探测的存在就是为了察觉这一点并跳过它们，同时打出一条醒目的标记。它此前只在**配置文件里** `dnsbl.enabled` 为真时才被派生——但驱动这件事的编排方（Town OS）是通过 `SetDnsblConfig` 编程封锁列表的，根本不写配置文件，于是在那些机器上探测从来没跑过：`resolver_available` 一直停在它 `true` 的默认值上，而每一次提供方查询都在超时，日志里对原因只字未提。现在这个循环是无条件派生的，读的是检查器*运行期*的启用标志——正是 `recovery_probe_loop` 早就有的那种形状。在封锁列表关着的时候它完全跳过网络探测——没有什么可探测的——只重新读一次那个标志，而那是一次原子读取，因此跑得足够勤，让运行期启用的封锁列表不必等满一整个探测周期。

- **DoT 监听器会宣告 `dot` 这个 ALPN 标记。** 它此前什么都没宣告：`main.rs` 以一份空的 ALPN 列表构造 DoT 监听器的 TLS 配置，因此一个提出 `dot` 的客户端——那是 IANA 指派给 DNS-over-TLS 的标记，也是一个 stub 解析器用来把 DoT 监听器与该端口上其他任何 TLS 服务区分开来所发送的东西——对这个问题得不到任何答案，只能自己猜。现在这个标记会被提供，而且**只**提供它：一个要求 `dot` 的客户端会协商成功，一个只提出其他协议的客户端会被拒绝而不是被默默服务，而一个根本不发 ALPN 扩展的客户端（Android 的 Private DNS、处于机会模式的 systemd-resolved）会与此前完全一样地被服务——因为 TLS 只有在客户端提出了协议而没有任何一个相符时才会让握手失败。

- **生成出来的 TLS 证书现在会指名它的监听器所绑定的地址。** `auto_self_signed` 生成的证书只涵盖 `localhost`、`127.0.0.1` 与 `::1`，别无其他，而且在每一种传输上都如此。在一个面向局域网的 DoT 监听器上，那张证书无法被任何会检查自己拨号名称的客户端使用——那是每一个配置了认证名称的客户端，而且那也是一张自签证书除了原始公钥固定之外唯一容许的验证。每个监听器自己的绑定地址现在会被自动折进证书里，而 `<transport>.tls.self_signed_sans`（新增，位于 `dot`、`doh`、`doq` 与 `acme`）承载那些绑定地址无法指名的身份：机器的主机名、它的 `.local` 名称、一个局域网别名。通配绑定（`0.0.0.0`、`::`）不贡献任何东西，因为它们不是任何人会拨号的身份——一个绑在通配地址上的监听器需要那份列表。重复的写法会折叠在一起（`[::1]` 与 `::1`、`DNS.Home.` 与 `dns.home`），而在配置了 `cert_path`／`key_path` 时这一切都不适用。这是一次名称检查，而且它最先失败；信任那张证书仍然是另一件独立的事。

- **一个没有任何转介宣告过的区切点，现在会被跨越，而不是被拒绝。** 当同一台名称服务器同时对某个父区*以及*它底下一个已签名的子区具有权威时，一个落在子区里的名称会直接由该子区作答——完全不会发送任何转介——于是一个从「最后一次跟随的委派」挑选密钥的解析器，会拿父区的密钥去验证子区的签名，并把一个好答案判成 bogus：

  ```
  answer for cdnjs.cloudflare.com. A is bogus: RRSIG over cdnjs.cloudflare.com. A
  is signed by cdnjs.cloudflare.com., which is not the zone cloudflare.com.
  ```

  这正是 v0.5.1 点名指出、却留着没修的那个验证器缺陷。RFC 4035 §5.3.1 给了裁决：RRSIG 的签名者名称说明了该套用哪些密钥。在验证一个答案、一次 CNAME 跳转，或一个否定答案的否认证明之前，解析器现在会检查那些签名是否指名了一个位于当前这个区之下的区，并把信任链一次一个切点地往下延伸过去，把转介从未交付的那条 DS 取回来。每一个切点的建立方式，与转介所建立的完全相同——DS 要在父区的密钥之下通过验证、子区的 DNSKEY RRset 要与它相符、一条不存在的 DS 必须被*证明*不存在——而一个无法建立起来的切点，依然会扣住答案不发。

  每一个名称，只要它背后的提供方把某个子区托管在同一套基础设施上，就都受影响。找出这件事的那一个是 `cdnjs.cloudflare.com`，它的 SERVFAIL 会让那些从它加载资源的页面渲染成一片空白。

  这趟下降无法被响应本身操纵：只有在该区段里的每一条 RRSIG 都指名同一个签名者、它严格位于当前这个区之内，而且它涵盖了它所签名的每一个拥有者名称时，才会去追那个签名者。少了最后那个条件，一个伪造的答案就可以指派一个确实未签名的兄弟区当作它的签名者，让父区诚实地证明该委派不带 DS，然后让真实区确实有签名的数据被当成 insecure 接受下来——`a_signer_below_the_zone_that_does_not_enclose_its_owner_is_refused` 就是那个攻击，把该项检查拿掉，这套测试就会失败。

- **一个位于其父区名称服务器上的*未签名*子区，现在看得见了。** 那是同一个隐藏切点，只是没有东西可以追：响应不带任何签名，因此上面那趟下降没有签名者名称可循，它仍然会被拒绝——而那个数据包同时也正是「在传输途中把每一条 RRSIG 都剥掉」会产生的东西，站在解析器这一侧，两者分不出来。`dnssec_unsigned_responses_total{evidence}` 现在会逐一计数：当授权区段的 SOA 指名了一个位于当前这个区之下的区时（那正是一个未签名子区的否定答案所带的东西）标成 `child_apex_soa`，其余标成 `none`。那条 SOA 未经签名因此可被伪造，所以它纯属诊断用途、什么都不决定；在此之前，这个情况除了一个与其他任何 SERVFAIL 无从区分的 SERVFAIL 之外，不留下任何痕迹。

- **两项新指标**，使导出的系列族数来到 80：`rolodex_dns_dnssec_hidden_zone_cuts_total`，计数那些由「位于被查询的区之下的区」所签名的响应（也就是上面那趟 DS 查找被付出的次数），以及上面说明过的 `rolodex_dns_dnssec_unsigned_responses_total{evidence}`。

### 测试

- **解析器可用性探测循环已被覆盖。** `src/dnsbl.rs` 里的两个用例，用注入的探测函数与毫秒级的间隔来驱动，因此完全不碰网络：关着的封锁列表绝不能发起探测（那会是毫无理由地向一个公共解析器发流量），而以 `SetDnsblConfig` 的方式启用的封锁列表——不牵涉任何配置文件——必须开始被探测，并且探测的判定必须真正抵达检查器。一个被调用了、但答案被丢掉的探测，与那场故障并无二致。

- **`BindList` 在回归会悄无声息的地方已被覆盖。** 在 `src/config.rs` 里：裸字符串的 `bind:` 仍然能解析（在列表写法出现之前写下的每一份配置用的都是它，丢掉这一点会在升级时把它们弄坏）、列表能解析并解出两个地址族、只有一项的列表会序列化回裸字符串而不是把世上每一处标量 `bind:` 都搅成一个单元素列表、重复的地址只解出一次、空项会被跳过，以及出错的那一项会在错误信息里点名自己。

- **证书重新加载的路径已端到端涵盖。** `tests/tls_reload_test.rs` 加入了轮询的那一半：一对没有变动的证书不会被重新加载（轮询器一天跑 2,880 次，所以一个无条件重新加载的版本会以那个频率重新发布），一对轮换过的证书会被检测到并被提供出去，一个自签管理器没有东西可轮询因而不会被赋予轮询任务，而那个任务自己也会在什么都没被调用的情况下捡起一次轮换。真正有牙齿的案例是**写入途中**被撞见的更新——它必须失败、让旧证书继续提供，然后捡起那对完成的证书；一个记录“自己看到什么”而不是“自己加载了什么”的轮询器，会把那个撕裂的状态当成新的常态，并一路提供旧证书直到重启为止——而那正是这套机制存在要消除的失败，而且它看起来会像是在正常运作。`tests/dot_test.rs` 钉住监听器那一侧：在一个运行中的 `serve_dot` 底下轮换过的证书会抵达下一条连接，而一条已经开启的连接不受打扰，且监听器仍然作答。

- **`tests/dot_test.rs` 涵盖 DoT 必须*做到*的事。** `src/dot_server.rs` 只带着一个编译用的冒烟测试，而 `tests/security_dot_limits_test.rs` 只涵盖监听器必须拒绝的东西；没有任何东西演练过协商该传输、把消息框起来、作答，或是把会话留着给下一次查询。分成两半，因为它们回答不同的问题。进程内，一个真实的 `tokio-rustls` 客户端对着 `serve_dot`：上述三种 ALPN 结果、2 字节长度前缀与其后的主体是否相符、一个已写入的名称得到答案而一个未写入的名称得到 NXDOMAIN，以及一条连接承载四次查询并把它们的 ID 与问题一一对回。进程外，真正的 `rolodex-dns` 可执行文件对着一份带有 `dot:` 段的配置文件——那是唯一能抓到上面任一项修正的东西：一个进程内的测试框架会自己构造 `rustls::ServerConfig`，因此看不见一个从不索取 ALPN 标记、或从不指名自己绑定地址的 `main.rs`。那一半会经由管理套接字写入一条记录，再通过 DoT 查回来，然后把服务器实际提供的证书里的主体备用名称解码出来。它刻意绑定 `127.0.0.2`：loopback 那一组地址被烘进了每一张生成出来的证书里，所以只有那一组之外的地址才能证明绑定地址是被推导出来的、而不是套用了默认值。

### 文档

- **这组文档在每一种语言下都齐全了。** `README.md`、`DESIGN.md` 与 `CHANGELOG.md` 新增了欧洲西班牙语、墨西哥西班牙语与日语译本，因此五份文档现在在六种语言下全都存在，也没有任何一条语言导航栏指向不存在的文件。英文仍是唯一的真实来源：先改英文，译本是后续工作，而不是第二个可以编辑的地方。没有任何东西会验证它们彼此一致——`tests/promql_docs_test.rs` 只读英文的 `README.md` 与 `DESIGN.md`，因此译本里的 PromQL 区块或族计数是文档，而不是一项被检查的断言。

  日语的后缀现在是 `.ja-JP.md` 而不是 `.ja.md`，如此一来这组文档里的每种语言都是地区代码，后缀在各处的读法也一致。除了文件名与语言导航栏之外没有任何东西改变。

  中文译本也被拉回与英文同步，此前它落后了一个版本：`arpa.` 子树规则、v0.5.1 的根服务器层级拒绝与根服务器归责，以及指标族计数。

## v0.5.1（2026-08-12）

### 破坏性变更

- **`arpa.` 一律不在本机之外解析。** 其下的每个名称，都只会由本地数据回答——一条已存的 PTR、一条带范围的记录、一个受管理或具权威的反解区——否则就是 **REFUSED**，在所有解析模式下皆然。这个子树中没有任何东西会发送到根服务器、转发器或加密上游。返回 REFUSED 而不是 NXDOMAIN，是因为服务器是拒绝为某个命名空间作答，而不是宣称该名称不存在。

  这道闸门设在两层：查询路径在「本机持有的数据」与「必须外出获取的数据」的边界上拒绝——位于上游响应缓存之前，因此旧策略下缓存的东西一律不会被提供——而迭代解析器则会在不发送数据包的情况下拒绝，这也涵盖了指向该子树的 CNAME 目标或无粘合的 NS 主机名。判定依据是最后一个**标签**，绝不是字符串后缀，因此 `notarpa.` 与 `arpa.example.com` 都是普通名称，会正常解析。

  这消除了促成这项工作的 `ipv4only.arpa`（RFC 7050 的 NAT64 探测）SERVFAIL：根与 `arpa.` 共同服务的区切点——一次查询跨越两个切点，转介的 NSEC 由 `arpa.` 签名，而遍历仍在对照根的密钥检查——现在根本无法抵达。底层的验证器缺陷（拿转介的证明去对照假定的父区，而不是 RRSIG 签名者的区）并未改动，仍然存在。

  有两个后果值得先讲明，而不是日后才发现：对于本机没有任何数据的地址，反解会被拒绝，而不是从互联网取得答案（`dig -x 8.8.8.8`）；而 `ipv4only.arpa` 会被拒绝，正在探测 NAT64 的客户端会把这读成「这里没有 NAT64」。用本地数据妥善地服务整棵反解树，是另一项延后处理的工作。

- **无法验证的根区现在会造成中断，而不是无声降级。** `fetch_dnskeys` 现在会区分 `Unreachable`（传输层）与 `Invalid`（密码学层）。在根这一层，`Invalid` 会扣住结果、信任链就此停住；`Unreachable` 则仍会往下掉，这是刻意的，因为「连不上」不等于「无效」。把两者混为一谈，会让任何能稳定破坏根 DNSKEY 获取的人，在完全不产生任何 bogus 判定的情况下，把验证整个移出路径——回退链会把该错误读成「根连不上」，然后改由一个不做验证的上游回答。

  这个取舍是真实存在的：这个构建不认识的信任锚点（一次 KSK 轮转）会变成 DNS 中断，而不是悄悄降级到 DoH。在修好锚点之前，`dnssec.validate: false` 就是逃生口。

### 功能

- **提供无效 DNSSEC 的根服务器会被移出根集合** 15 分钟，每再犯一次就翻倍，上限 24 小时；依据的是唯一一项不必问别人就能检查的主张：它的根 DNSKEY 对照本地锚点。这个归责不会因为该服务器响应迅速而消失（`note_success` 现在只清除传输层字段），只会被一个**通过验证**的回答清除——绝不会靠等待清除——而且绝不会套用在最后仅存的那一台根上，因为所有根同时失败，代表问题出在区或锚点，而不是十三台流氓服务器。仅适用于根服务器：在根以下，验证失败通常是该区自己的签名错误，而那些本来就会安全地失败。归责只存在内存中，因此重启后会重新信任每一台根。

- **一项新指标 `rolodex_dns_dnssec_blamed_roots`**，使导出的系列族数来到 78。长时间无声地把部分根集合排除在外，是这件事情里唯一没有任何既有计数器会报告的部分。族数与 PromQL 食谱已在 `README.md` 与 `DESIGN.md` 中同步更新。

### 缺陷修复

- **以 Bogus 收场的 DNSSEC 遍历不再留下任何东西。** 委派与其粘合记录现在只有在 `extend_trust` 返回可用的信任状态之后才会缓存。此前是先写入的，因此一个 DS/NSEC 证明失败的转介，其 NS 集合早已被写进去——而且持久化到磁盘，并在重启后存活。

### 文档

- **功能规格已从 `CLAUDE.md` 移入 `DESIGN.md`。** `CLAUDE.md` 现在只剩开发规则；软件做什么写在规格里，而规格正是行为变更必须落地的那份文档。

- **译本改以地区语言代码标识。** 中文译本从 BCP 47 的文字子标签 `zh-Hant`／`zh-Hans` 改为 `zh-TW`／`zh-CN`，如此一来，在西班牙语已按地区拆分之后，这组文档里的每个语言后缀回答的都是同一个问题。`CLAUDE.md` 与 `CONFIGURATION.md` 新增了欧洲西班牙语（`.es-ES.md`）、墨西哥西班牙语（`.es-MX.md`）与日语（`.ja.md`）译本，并由 `Cargo.toml` 的 `include` 列表一并发布。

  `README.md`、`DESIGN.md` 与 `CHANGELOG.md` **尚未**翻译成这三种新语言，因此它们的语言导航栏目前会链接到不存在的文件。英文版仍是唯一的真实来源，而且没有任何东西会验证译本彼此一致——`tests/promql_docs_test.rs` 只读英文的 `README.md` 与 `DESIGN.md`。

### 测试

- **`tests/arpa_refusal_test.rs`**（新增，并已写入 Makefile 的 `rust-integration-test` 配方）断言的是**数据包**而不是 rcode，并以标签边界与本地数据路径作为它的对照，横跨全部三种解析模式。一道拒绝一切的 `arpa.` 闸门，满足「这个子树被拒绝」；一道什么都不拒绝的闸门，满足「`notarpa.` 仍然解析得到」——只有成对出现才说明得了什么。

- **`tests/security_dnssec_test.rs`** 加入了这些拒绝规则，并通过一台搭配可运作之计数转发器的真实 `DnsServer` 来驱动，因为「客户端收到 SERVFAIL」与「转发器从未被询问」是两种不同的性质，而只有后者才是那项发现本身。

## v0.5.0（2026-08-10）

### 缺陷修复

- **再也不会有客户端查询被拿去探测降级中的层级。** 在 `auto` 模式下，查询路径会每隔 `resolution.recovery_probe_secs`（默认 60 秒）选出一次查询，让它从根服务器重新开始整条层级链，以便发现已恢复且更受偏好的层级。想达成这件事是合理的，但把代价算在某位调用者头上就很糟糕：在会过滤对外 `:53` 的网络上，根服务器层级根本无法回答，于是被选中的那次查询要付出整趟迭代走访的代价——最多 `MAX_QUERIES_PER_RESOLUTION` 次上游查询，每次都是逐名称服务器的超时——之后才落到那个本来就会回答它的层级。每分钟就有一次倒霉的查找卡上数十秒，而客户端通常先放弃了，这在用户眼中读起来是“DNS 卡住了”，而不是“在做探测”。

  现在 `auto_start_tier` 一律就是已提交的层级，没有例外，而那个用来选出探测查询的 compare-exchange 也随之移除——现在永远只有一个探测者，不需要任何选举。

### 特性

- **异步的层级恢复。** 恢复逻辑移到 `recovery_probe_loop`，这是一个后台任务，每隔 `resolution.recovery_probe_secs` 以自己的一次性探针，重新测试位于已提交层级之上的那些层级。它的结果会被丢弃——探测的存在是为了移动已提交的层级，而绝不是为了回答任何东西——所以就算它跑过头，也不会害任何客户端拿不到答案；正因如此，下面的时间界限才得以按“正确性”来挑选，而不是按“等待者的耐心”。间隔是从上一次探测起算，而不是从上一轮结束起算，所以一次缓慢的探测不会演变成忙碌的探测；而且每一轮都会重新读取配置，因此运行期的变更不需重启即可生效。

- **重新夺回根服务器层级需要一个通过 DNSSEC 验证的答案。** 第 0 层只有在根区域自身的 `DNSKEY` 得到 `Verdict::Secure` 的解析结果时才会被提升，而不是仅凭“连得上”。`:53` 上的拦截型中间盒或强制门户是连得上的，也会迅速响应——它只是响应它想响应的任何东西——所以少了这道关卡，任何劫持 53 端口的网络都能把自己安装成最受信任的层级，悄悄自动取代这台机器原本正确退守到的加密上游。一个通过验证的答案，正是这类中间盒唯一伪造不来的东西；而根区域自己的 DNSKEY，是这个问题最精简的形式：这台机器能不能连到某台根服务器，并从它建立起一条信任链。

  当 `dnssec.validate: false` 时没有判定可供把关——按设计，解析器对所有东西都报告 `Insecure`——所以此时改为要求一个确定性答案。在那里要求 `Secure` 会让一台刻意不做验证的机器永远被困在后备层级上，再也用不到那个本来就是默认且首选的递归解析模式。

- **根服务器层级同时受挂钟时间限制，而不只受查询次数限制。** 迭代解析器以**查询次数**编列自己的额度，那回答的是“这个名称值得投入多少工作量？”，而回答不了“一位调用者可以被晾多久？”——但对正在等待的人来说，只有第二个问题重要。被黑洞掉的 `:53` 会让其中每一次查询都超时，因此光靠次数额度，就允许单次查找跑上一分多钟。现在这个层级在查询路径上带有 8 秒的上限，在恢复探测上则是 2 秒。

  之所以是 8 秒而不是其他层级用的 1.5 秒，是刻意的：一次冷启的迭代解析是好几趟顺序的往返（根、TLD、区域，再加上验证所需的 DNSKEY 获取），而安全层级与转发层级只是对单一服务器的一次往返。实测的冷查找对公开名称是 0.6–1.9 秒，对 RFC 1918 的 PTR 约 2.7 秒，所以把上限定在这些数字附近并不会限制住病态情况——它只会弄坏健康的递归，让每一次“慢但正常”的查找失败，并把一台运作正常的解析器降级到 DoH 上。探测用的 2 秒之所以紧得多，是因为它问的问题小得多：一次查询、一台服务器、没有委派要走。

### 测试

- **`tests/recovery_probe_test.rs`** 基于 DNSSEC 已签名的模拟层级，刻意涵盖两个方向：通过验证的根服务器**会**被夺回，而那些连得上但未签名、由外来密钥签名、或提供已过期签名的根服务器则**不会**。一道永不开启的关卡能通过所有负面测试，一道永远开启的关卡能通过所有正面测试——只有两者成对才说明得了什么。它也钉住了“绝不把客户端查询拿去探测”这道保障，而单元测试则涵盖挂钟界限与探测的空操作情形（已在最上层，或不在 `auto` 模式下）。

## v0.4.6（2026-08-09）

### 缺陷修复

- **DoH 连接池的测试不再互相竞争。** `DOH_POOL` 是进程级的全局 `DashMap`，而那四个连接池测试在同一个测试可执行文件中并发运行，但每一个都在头尾调用**全局**的 `DOH_POOL.clear()`。任一个测试都会把其他测试刚放进池里的东西抹掉，于是 `test_get_doh_connection_reuses_pooled` 可能在插入一个条目的下一行就找到 `None`——这是偶发、无法解释的 panic，而不是可复现的失败，也正因如此它撑过了好几次全绿的运行。

  现在每个测试只移除自己的键。不需要序列化用的锁：每个测试都绑定自己的临时端口，所以同时运行的两个测试不可能持有相同地址，而在测试之间被回收再用的端口，早已被上一个使用者移除了。两处对连接池查找的裸 `unwrap()` 也换成了会报告“实际找到了什么”的写法，因此下一次这里失败时会指出问题，而不是匿名地 panic。

## v0.4.5（2026-08-09）

### 特性

- **封锁归因按“实际命中的是哪份列表”拆开。** `blocklist_allowlisted_total` 新增了 `kind` 标签——`forward_name`、`reverse_name`、`ip_literal`——而 `rbl_scope_provider` 则加入了封锁种类。

  允许列表的标签命名的是**命中路径**，而不是列表，这是由检查所处的位置所决定的：豁免会在任何提供方查询发出之前就短路，所以在它触发的那一刻什么都还没问过，也就不存在“本来会命中哪份列表”可供记录。改为指名那道关卡，既是可知的，也是更有用的轴向：它把发生在正向名称上的豁免（步骤 7）与发生在反向查找上的豁免（步骤 2）分开，而在后者之内又把 `in-addr.arpa` 的写法与 IP 字面量分开——两者是用不同规则匹配的，一个是后缀、一个是精确。

  `rbl_scope_provider` 之所以存在，是因为“某个网络自己选用的提供方”与“整台机器共用的列表”是两种不同的运维决策，波及范围也不同。折在一起时，“这个网络自己的封锁列表弄坏了这个网络”与“全局列表弄坏了所有人”无从分辨，而当某个网络报告反向 DNS 不能用时，这正是第一件值得知道的事。拆开的代价是零：两次检查涵盖的是互不重叠的提供方集合，而结果缓存是以 `<ip>/<zone>` 为键的。新的种类一律追加在末尾，绝不插入中间——那些 `BLOCK_*` 常量是数组中的位置，插入会悄悄地把已有的每个计数器重新贴上别的标签。

- **流量体积。** `traffic_bytes_total{direction=rx|tx}` 与 `records_served_total`，两者都记录在每个传输方式都会汇流经过的那一个受检测出口上，因此一次查询不可能被计数而它的字节数没被计数。记录数量是从响应首部的 ANCOUNT 字段读出的，而不是去重新解析一个服务器才刚序列化完的消息。光看查询数量看不出这些：一百万次 NXDOMAIN 与一百万次有内容的答案，查询数相同、工作量却天差地别，而 tx/rx 比值就是任何对外可达的监听器上值得盯着的放大倍数。

- **逐 TLD 隔离。** `queries_by_tld_total{tld}` 把查询流按 TLD 分开，这正是让分割视域部署中的各个网络，彼此之间、以及与公网之间得以分辨的关键。

  这个维度是由运维人员拥有的集合所限制的，因为被查询的名称是由客户端选的：一个无界的 `tld` 标签，会让一台扫描 `a.zzz1`、`a.zzz2`…… 的扫描器不断产生新系列，直到注册表把进程的内存吃光。有三个来源汇入它并取并集——每个网络范围所拥有的 TLD（包含各范围隐含的 `.home` 域名）会**自动**被追踪，因为要求一个网络自己的命名空间被写两次是个坑，而它会以“某个系列悄悄不见”的形式浮现；新的 `metrics.tracked_tlds` 配置列表，它被钉住，因此能撑过重启且无法通过 API 移除；以及一份由新的 `SetTrackedTlds`/`ListTrackedTlds` RPC 管理的存储列表，并附有 `set-tracked-tlds`/`list-tracked-tlds` CLI 子命令与对应的 Go 客户端方法。所有未被追踪的都折叠进 `other`。

  条目 `common` 会展开成一组内置的常见 TLD 集合，让常见的公开 TLD 变成一行配置而不是二十行；而且它是以**未展开**的形式存储的——读回来时报告的是运维人员当初要求的内容，而日后若内置列表有所变动，也不需要每个部署都重新调用一次即可生效，形状与 `refusal_codes` 中的 `none` 相同。匹配会由最具体者开始走访名称的各个后缀，并返回该名称的一个切片，因此同时追踪 `home.` 与 `lab.home.` 的部署会把 `box.lab.home.` 归因到较具体的那个，而未被追踪的名称不会产生任何分配。根区域会被以 `InvalidArgument` 拒绝：`.` 是每个名称的后缀，追踪它会把所有系列坍缩成一个，并让兜底值永远取不到。

### 破坏性变更

- **DHCP 的指标标签现在带有子系统限定。** `rolodex_dns_dhcp_messages_total{type}` 改为 `{message_type}`，`rolodex_dns_dhcp_leases{state}` 改为 `{lease_state}`。通用的标签名称，正是让横跨两个子系统的聚合——例如某条记录规则里的 `sum by (type)`——悄悄把 DHCP 的 ACK 计数混进 DNS 计数的原因。选用旧名称的仪表板与告警需要更新。DNS 的汇总指标（`queries_total`、`traffic_bytes_total`、`records_served_total`、`queries_by_tld_total`）同样只计 DNS：DHCP 的 `:67` 流量从来就不是 DNS 流量，而一个由 DHCP 注册的名称，只有在真的有人去解析它时才会进到这些指标。

### 测试

- **文档中的 PromQL 是被测试的，分两层。** `tests/promql_docs_test.rs` 从 `README.md` 与 `CLAUDE.md` 中解析出每一个 ```promql 块，抽取其中的指标名称与标签匹配条件，并逐一对照实际输出的内容解析。文档是指标变更中唯一没有其他东西会去验证的部分：上面那次 DHCP 改名让代码照样编译、其他测试照样全绿，却悄悄把每一条文档中的仪表板查询变成查不到数据的查询，而运维人员要等到事故当中某块面板空白时才会发现。它同时把文档中声称的系列数量钉在注册表实际输出的数量上——那个数字早就已经飘掉了，文档写 73、实际输出 74——并且保护那个代码围栏本身，因为一个被改标成 ` ```bash ` 的块会让整个文件悄悄地不再检查任何东西。

  `tests/prometheus_integration_test.rs` 把同一批查询送进一台真正的、正在抓取实际服务器的 Prometheus 运行，抓到子串扫描器抓不到的东西：一个**作为 PromQL 就格式错误**的查询，而不只是指名了不存在的系列。`rate(sum(x)[5m])` 指名的全是真实系列，却在被粘贴的那一刻就会被拒绝。它由 `make prometheus-test` 运行，而 `make test` 现在依赖它；缺少 podman 时会大声跳过而不是失败，所以没有容器运行环境的机器仍能得到绿灯，同时绝不会假装那些查询已被检查过，而 `ROLODEX_PROMETHEUS_REQUIRED=1` 会把那个跳过提升为失败，供 CI 使用。

- 为每个新维度补上单元与集成覆盖，每一项都配有它的对照组——特别是“未被追踪的 TLD 不会产生**任何**系列”这一项，因为一个只检查正面情况的测试，在基数限制被整个移除之后照样会通过。

### 打包

- **这个 crate 可以发布，并且会带上它的文档。** 声明了 `readme` 与 `homepage`，并以一份明确的 `include` 列表取代“靠省略来决定打包内容”的做法：现在发布出去的包会在源码、protos、benches 与 tests 之外，一并带上 `README.md`、`CONFIGURATION.md`、`CHANGELOG.md`、`CLAUDE.md` 与 `LICENSE`。

  之所以用 `include` 而非 `exclude`，是因为工作目录里还放着一个 Go 客户端、一个 JavaScript 客户端、一个浏览器扩展与容器工具，这些对 crate 的使用者毫无用处——而一份排除列表会在每次新增这类东西时悄悄开始把它打包进去。`proto/` 与 `build.rs` 是承重的（gRPC 绑定是在构建时生成的，所以少了它们的包根本编不起来）；`benches/` 之所以随附，是因为 `[[bench]]` 指名了一个目标——cargo 会在被声明的目标文件缺失时失败，所以省略它是弄坏包，而不只是把它变小。

  `tests/` 随附，是为了让收到这个 crate 的人能够验证它，而对一个 AGPL 的网络服务来说，这正是重点所在。这也是为什么 `CLAUDE.md` 是被纳入而非被当成仅限仓库的工具：`tests/promql_docs_test.rs` 会连同 `README.md` 一起读取它，而一个带着测试却没带测试输入的包，等于带了一个跑不起来的测试。既然每个文档文件在检出版本与发布包中都保证存在，那个测试就坚持一定要找到每一份，而不是跳过它读不到的——跳过正是一项检查悄悄开始什么都不验证的方式。

### 文档

- **`CONFIGURATION.md`——一份配置指南。** README 的配置章节是字段参考，它回答“这个选项做什么”，而回答不了“我的配置文件应该长什么样”。这份指南是任务导向的：配置文件如何加载（以及文件不存在并不是错误）、最小可用配置、四种实现过的部署形态（家用解析器、纯权威服务器、分割视域叠加节点、位于会过滤 `:53` 的网络上的解析器），接着每个子系统各一节。它明确指出哪些配置根本不算配置——记录、范围、区域、封锁列表条目、DNSSEC 密钥与 ACME 证书颁发机构都是 SQLite 中的运行期状态——并附上一份“什么需要重启”的表格、服务器刻意拒绝启动的四种情况，以及一份按症状索引的故障排查表。

  两份 CIDR 列表有并排比较，因为 `overlay_cidrs`（谁会被强制**套用范围**）与 `recursion_cidrs`（谁可以让这台服务器去问上游）是两个读起来很像的不同问题。

- **README 更新至 v0.4.4。** DNSSEC 现在两半都写到了——签名者与上游验证器，含四种判定，以及为什么“非安全 vs 伪造”才是承载安全性的那个区分。封锁列表的拒答码与提供方轮换、允许列表横跨所有列表与两道关卡的涵盖范围，以及递归访问控制（此前已随版本推出却从未写进文档），各自有了专属章节。配置示例与选项表新增了 `dnssec.*`、`security.recursion_cidrs`、`metrics.bind` 与各提供方的拒答字段；CLI 文档新增了拒答相关标志与目前的 `get-rbl-config` 输出；而 RFC 对照表新增了 DNSSEC 家族（4033/4034/4035、5155、6605/8080、6840、9276）与 7766。

## v0.4.4（2026-08-08）

### 特性

- **上游 DNSSEC 验证。** Rolodex 过去会签自己的区域，却对它解析回来的东西不做任何验证：没有信任锚点、对外的迭代查询不置 DO 位，而 `verify_rrsig` 则是刻意没接上。`src/dnssec_validate.rs` 是负责验证的那一半，而且它与签名者不共用任何代码——签名者处理的是我们自己写入的数据库行，每一个字节都在掌控之中；验证器处理的是来自“其诚实与否正是待证问题”的一方所发来的东西，而这两者必须有能力彼此不同意。

  信任链是**自上而下、与解析器本来就在执行的委派走访并行**建立的，因此获取一条 DS 不需要额外查询——它就搭在转介里，由我们已经持有其密钥的父区域所签。已验证的密钥集合（以及已被证明为非安全的委派）会在 `src/key_cache.rs` 中逐区域缓存；若每次查询都重新推导整条链，等于把委派缓存所解决的冷启动问题往上搬一层再重演一次。那里的查找是精确名称匹配，绝不用后缀：父区域的密钥对子区域什么都没说，而那正是 DS 记录存在的意义。

  RFC 4033 §5 的四种状态全都被区分开来。承载安全性的是“非安全 vs 伪造”：“没有签名”**不等于**非安全，因为路径上攻击者能从任何响应中剥掉签名——只有当一份已签名的 NSEC/NSEC3 证明了上层委派处确实没有 DS 时，它才是非安全，而攻击者没有父区域的密钥就伪造不出这种证明。这就是那整套 NSEC/NSEC3 否定证明机制存在的理由；略过它，剩下的验证器会被任何攻击者降级成不存在的验证器。

  伪造与无法判定的数据永不提供、也永不缓存，无论正面或负面——一条被缓存的伪造否定响应，会在其整个 TTL 期间压住真正的名称。在 `auto` 模式下，验证失败是一个**确定性**答案而非层级失败，所以一个坏掉的签名不可能被拿去经由某个不做验证的上游洗白。AD 只在 Secure 时置位，且只对有询问的客户端置位（查询中带 DO 或 AD）；对没有设置 DO 的客户端会剥除 RRSIG/NSEC/NSEC3，因为一条已签名的 A 记录体积大约会变成三倍，而“小问题换大答案”正是 `security.recursion_cidrs` 存在要堵住的那种放大形状。

  验证**只适用于迭代路径**——`recursive` 模式，以及 `auto` 的根服务器层级。转发回来的响应是别人的递归结论摘要，要验证它就意味着我们自己把整条链重新解析一遍，而那恰恰就是根服务器层级本身；因此一条降到第 0 层以下的链是未经验证的，而它会以“不置 AD”如实表明。

  由 `dnssec.validate`（默认 `true`）与 `dnssec.trust_anchors`（默认：IANA 根密钥）配置。锚点覆盖是**取代**IANA 密钥而非追加，而且每个锚点的每个字段都在启动时验证——格式错误的锚点是硬性失败，而不是悄悄退回。启用验证时，对外的迭代查询现在会带上 EDNS0，置 DO 并声明 1232 字节的载荷大小（此前完全没有 OPT，于是服务器有权把响应压在 512 字节内，而一个已签名的答案基本上塞不进去）。逐次查找的查询额度新增了 `VALIDATION_QUERY_BUDGET`（在基础的 64 之上再加 32），所以启用验证不会悄悄缩短一个名称可以有多深。NSEC3 迭代次数超过 100 时会被视为非安全而不去计算（RFC 9276）——那是由攻击者选定、却花在我们这一侧的运算工作。只能对照本构建无法验证的算法来验证的记录，是非安全而非伪造（RFC 6840 §5.11）：我们缺少某个算法，不是那个区域的故障。

  启用了 hickory 的 `dnssec-ring` 特性，以获得带类型的 DNSSEC RDATA 与 RSA 验证能力。`ring` 无法**生成** RSA 密钥，这正是签名拒绝算法 8 的原因——但验证是另一回事，而一个检查不了 RSA 的验证器，也就检查不了根。

- **封锁列表拒答码与提供方轮换。** 一份 DNSxL 响应“已列入”与抱怨**发问者**用的是同一种方式——`127.0.0.0/8` 之下的一条 `A` 记录——而唯一能区分两者的就是地址本身：`zen.spamhaus.org` 用 `127.0.0.2` 说“已列入”，用 `127.255.255.254` 说“你正通过公共解析器发问”。因此，把任何 `A` 记录都读成“已列入”，就等于在封锁列表决定不再回答我们的那一刻，把**每一个**对照该提供方检查过的名称都变成 NXDOMAIN，而这会在查询量越过该提供方阈值时开始发生——可能是一次看起来一切正常的部署之后好几小时或好几周。Spamhaus 讲得很直接：那些码“不应被解读为任何形式的信誉评价”。

  现在拒答码会被识别出来，而一次拒答既不是列入也不是否定——什么都不会被缓存，因为我们对被查询的名称什么都没学到。同一个响应中，任何位置的拒答都胜过同一个响应中的列入：一个正在抱怨的提供方不会同时在报告信誉，而往这个方向犯错会**失效放行**，反过来的顺序则会在每一个名称上失效阻断。接着该提供方会被移出查询轮换一段冷却时间（`refusal_cooldown_secs`，默认 3600 秒，可逐提供方覆盖），而不是每次请求都再去问它一遍；轮换只跳过新的查找，已缓存的判定维持不变；它会自行失效，因此短暂的超额配额期不需要运维人员动手就会自愈；而 `FlushCache` 或任何 `SetRblConfig`/`SetDnsblConfig` 都会清除它——重新配置往往正是那次拒答的修正动作。

  内置集合涵盖了常见提供方已载明的那些码：`127.255.255.0/24`（整个 Spamhaus 错误范围，而不是今天的那三个码，这样明天的第四个码才不会被悄悄读成列入）、`127.0.1.255` 与 `127.0.2.255`（Spamhaus DBL/ZRD 的“不支持 IP 查询”），以及 `127.0.0.1`／`127.0.0.255`（URIBL/SURBL 的“查询已被封锁”）。空值代表内置集合——它不可能代表“没有任何码”，因为在这项功能存在之前写的每一份配置都是空的——而单一条目 `none` 则为那些真实列入值恰好与其中之一相撞的私有封锁列表禁用检测。明确列出的列表是取代而非扩充默认值，所以把它写出来的运维人员也可以借此缩小范围。无法解析的码会在启动时被拒绝，或由 RPC 回以 `InvalidArgument`，绝不跳过：一个悄悄失效的码，就是一个会被读成“已列入”的拒答，而且配置当时还报告了成功。

  可通过 proto、CLI、Go 客户端与配置文件逐提供方及全列表配置，且 RBL 与 DNSBL 两份列表各自带有独立的冷却默认值。`GetRblConfig`/`GetDnsblConfig` 报告的是**实际生效**的解析后结果（一份配置为空的列表会读回内置集合，而不是读回空值），外加目前有哪些提供方被移出轮换、以及还剩多久；`rolodex_dns_blocklist_refusals_total{kind}` 与 `rolodex_dns_blocklist_rotated_out` 把同样的信息输出给 Prometheus。少了它们，“封锁列表安静了”与“封锁列表很干净”从外面看起来一模一样，而运维人员默认会认为是第二种。把冷却设为 `0` 代表“使用默认值”，而不是“不冷却”——零冷却等于去重问那个刚刚叫我们别问了的提供方。

### 缺陷修复

- **现在每一次封锁列表命中都会是 NXDOMAIN，而允许列表条目是唯一能压下它的东西。** 三个漏洞，每一个都是“某份列表说了‘已列入’而查询照样解析成功”，或是“运维人员的逃生口够不到”的情况：

  - DNSBL 允许列表只守住正向名称检查（步骤 7），所以针对**地址**的误判是解不掉的——一个被错误列入的 IP 会让一台运作正常的主机 `dig -x` 失败，除了禁用该提供方之外别无他法。现在它同时守住反向／IP 路径（步骤 2），且两种写法皆可：`in-addr.arpa`／`ip6.arpa` 名称，像任何 DNS 名称一样以后缀匹配，因此一个条目就能解除整个 /24；或是 IP 字面量，以**精确**匹配——地址是最高位在前的，所以 `1.100` 不是 `192.168.1.100` 的父节点，把它当成父节点会豁免掉没有人指名过的地址。
  - 逐范围的 RBL 提供方被存储了、能被 API 列出来，却从来没被查询过。查询路径调用的是单纯的 `is_listed`，所以来自某个范围自有列表的命中照样正常解析。
  - 写成反向名称的本地 RBL 条目什么都匹配不到——IP 那道关卡只查 `ip.to_string()`。`dig -x` 打印出的是反向名称，所以那正是运维人员会粘贴进去的东西，而一个读起来像封锁、实际上什么都没匹配到的条目，比一个被拒绝的条目更糟。现在两种写法都能匹配。

  两处反向调用点都汇流进同一道关卡（`ip_blocklist_kind`），而每一道关卡都汇流进同一个豁免判断（`blocklist_exempt`），因此日后新增的出口不会偏离。当对外的明文 `:53` 不可达时，逐范围提供方会被跳过——那个标志不是策略开关，它说的是这次查找只可能超时。本地记录仍然胜过封锁列表，这是刻意的：反过来会让第三方的列入弄掉一个内部服务，而且有测试钉住它。

- **区域签名产生出无法被验证的签名，以及读不出来的已发布密钥。** DNSKEY、DS 与 RRSIG 过去是以承载存储字符串的 **TXT 记录**提供的，所以一次 DNSKEY 查询拿回来的是 TXT，而每一个已发布的签名都无法使用。现在它们以自己的类型码提供，RDATA 来自签名者拿去做哈希的同一个规范编码器，因此送上线路的东西与被签的东西逐字节相同；URI 与 ZONEMD 也以同样的方式编码。被签的字节本身现在是 RFC 4034 §3.1.8.1 的规范形式——规范化的所有者名称、原始 TTL、RFC 4034 §6.3 的排序并去除重复，因此记录从 SQLite 出来的顺序无法改变签名——并具备 RFC 4035 §2.1 的 KSK/ZSK 角色分工、以标签边界界定的区域范围，以及“取代而非累积”的重新签名（包括在那些自上次运行以来记录已被删除的名称处）。无法签名的类型（NSEC、NSEC3、NSEC3PARAM、ANAME，以及格式错误的值）会被跳过并在响应中指名，而不是用一种杜撰出来的编码去签——对一串没有别人重现得出来的字节所做的签名，会在每一个验证器上失效阻断，那比让那个名称维持未签名更糟。

- **RSA/SHA-256（算法 8）在生成密钥时就被拒绝**，而不是先接受再偷偷替换。`ring` 无法生成 RSA 密钥；一个声称算法 13 却承载 Ed25519 密钥材料的 DNSKEY，会产出彼此都对不上的 DS、DNSKEY 与一整组 RRSIG，而那个失败会在某个做验证的解析器上浮现，而不是在本地。相关地，若某个已存储密钥的字节无法以它被归档的算法加载，签名时会跳过它并记录警告，而不是拿它去签。

- **受管区域的权威判定过去要求区域**顶点**有记录。** 一个记录全都位于子域名的区域——例如有 `www.example.com` 而 `example.com` 上什么都没有，也就是区域的正常样貌——永远不会变成权威，于是它的落空查询被转发到上游，而内部的表述也就不再优先。现在区域内任何位置的记录都算数，而 `remove_records` 会在该区域及其之下什么都不剩时把 `managed_zones` 缓存剪除，因此信任这份缓存是安全的（过期的条目并非无害——它会持续为一个已经不存在的区域回答 NXDOMAIN）。反向树被排除在外：最后两个标签的启发式永远会推导出 `in-addr.arpa.` 本身，所以一条存储的 PTR 就会声称整棵全球反向树——而在 `dns.auto_ptr` 开启时，单单一条 A 记录就会产生那条 PTR。`home.arpa`（RFC 8375）则刻意**不**排除，因为那套启发式在那里是对的，而把落空查询转发出去正是 RFC 8375 §4 所禁止的。

- **区域后缀匹配在三个地方用了裸 `ends_with`**，把 `notexample.com` 算进 `example.com` 里——分别发生在证书列表、范围内权威判定与区域签名。已提取为 `db::name_in_zone`，改以标签边界匹配。

- **`SetDns64Config` 是个桩函数**，它只把参数记录下来、什么都不存储、然后报告成功；而 `GetDns64Config` 返回的是写死的默认值。现在启用标志与前缀会被独立存储，因此禁用合成不会丢弃已配置的前缀，而一个解析不出来的前缀会被拒绝而不是被替换掉。

- **`GetTtlDriftConfig` 无法往返。** 以 `1h30m` 设置的调整值读回来是 `5400s`。那个回复正是运维人员用来确认自己配置了什么的东西，而读取－修改－写回的自动化也需要它能往返，所以现在它会以配置时所用的同一种复合写法格式化（`ttl_drift::format_duration_secs`，解析器的反函数）。

- **`add-scope-rbl --enabled` 形同虚设。** 作为带 `default_value = "true"` 的裸 `bool`，clap 会给这个字段 `SetTrue` 行为；clap 4.6 会套用该默认值，所以这个标志永远表达不了 `false`，而一个被禁用的逐范围提供方也就无从配置。现在它接受一个值（`--enabled false`），并且在两个方向上都被钉住，因为这个标志在两个方向上都曾经是错的。

### 变更

- 文档与示例中移除了 `dnsbl.sorbs.net`——SORBS 已经关闭。

### 测试

- **`tests/signed_hierarchy/mod.rs`**——一套在真实 UDP 套接字之上、经过 DNSSEC 签名的模拟层级：已签名的根 → 已签名的 TLD → 已签名的区域，每一层持有自己的 Ed25519 密钥、发布一组 DNSKEY RRset，并为每个已签名的子节点交付一条 DS。`tests/mock_hierarchy` 证明的是查询**次数**，而这一套证明的是**判定**，因为几乎每一种把 DNSSEC 做错的方式，仍然会返回正确的记录——一个略过过期检查、或相信未签名 NSEC、或接受任何签名者名称的验证器，会把整个互联网解析得好好的，直到有人来攻击它为止。它的 `Tamper` 枚举会在响应**被序列化**时、也就是区域已被正确构造之后，才施加每一种攻击，因此每个测试都是“一个有效的部署，遭到攻击”，而不是“一个无效的部署，被拒绝”。

  `tests/dnssec_validation_test.rs` 涵盖那些必须持续正常运作的路径（完整的链验证为 Secure、由 NSEC 证明的非安全委派、经证明的 NXDOMAIN 与 NODATA、密钥缓存让已预热的区域省下对根的第二次查询、不做验证的解析器报告 Insecure 而非 Secure）；`tests/security_dnssec_test.rs` 涵盖那些攻击（被剥除的签名、既无 DS 也无其不存在证明的委派、已过期与尚未生效的签名、来自未发布密钥的签名、外来的签名者名称、签名后被篡改的数据、未经证明的否定响应、对不上任何根密钥的信任锚点、格式错误的锚点）。两者同等重要，且理由相同：一个什么都拒绝的验证器能通过所有攻击测试，一个什么都接受的验证器能通过所有正常路径测试。

- **`tests/dnssec_signing_test.rs`** 钉住的是签名**可被检查**，而不只是存在——核心测试会从**已发布的 DNSKEY RRset** 重新推导出签名输入，绝不从私钥行，因为验证器手上只有 DNSKEY。

- **`tests/blocklist_nxdomain_test.rs`** 以运维人员的方式驱动封锁列表契约：先做一次 gRPC 变更，再通过真实的 UDP 或 TCP 套接字发出查询。五份列表、两道关卡、两条代码路径，每一项都有自己的对照组——一道什么都封锁的关卡满足契约的一半，一道什么都不封锁的关卡满足另一半。

- **`tests/rbl_refusal_test.rs`** 以真实 UDP DNS 对着一个模拟封锁列表区域驱动拒答码，经过转发器后备与分类逻辑一路进到 `handle_query`，因为中间的每一层都是“列入／拒答”的区分可能丢失的地方。每个测试都配有一个对照组：一条货真价实的 `127.0.0.2` 列入沿着完全相同的路径走，仍然必须返回 NXDOMAIN。

- **`tests/doq_test.rs`、`tests/proxy_test.rs`、`tests/tls_reload_test.rs`** 涵盖三个此前只有编译冒烟测试或配置解析单元测试的界面：一个真正的 `quinn` 客户端对上 `serve_doq`；会**解析**服务器所发内容而不是回以罐头响应的模拟 HTTP CONNECT、SOCKS5 与 DoH 代理（并钉住“连不上的代理**不会**退回直连”）；以及通过真实 TLS 握手观察到的 `TlsManager::reload()`。

- **`tests/zonemd_test.rs`** 逐字段钉住 RFC 8976 的 RDATA 布局，而且是手写出来的而不是从编码器取得——拿编码器跟自己比对只会把缺陷盖章认可。

- **`tests/acme_admin_test.rs`** 以性质而非 `success` 标志来涵盖那五个管理 RPC：`EnsureZoneCa` 的幂等性（重新铸造会弄坏每一张已经链到旧中间证书的证书，以及随之而来的已发布 DANE-TA 记录）、同一个共享根之下的逐区域中间证书、EAB 的范围限定与密钥往返、诚实的移除行为，以及以标签边界进行的证书列表。

- **`tests/cli_integration_test.rs`** 现在会走访每个子命令的 `--help`。clap 在构造解析器时就会验证短选项，并在重复时**panic**，所以一个重用了全局选项字母的子命令会在读到任何一个参数之前就中止——`generate-dnssec-key`（`-a`／`--algorithm`）与 `set-ttl-drift`（`-a`／`--adjustment`）就是这样双双与全局的 `--address` 相撞，变成根本无法运行。现在两者都只保留长选项。

## v0.4.3（2026-08-07）

### 安全

- **Do53 转发器接受任何抵达的数据报。** `forward_udp` 发出查询后就把回来的任何东西当成答案：事务 ID 从未被比对、池中的套接字未经 connect 因此内核会投递来自**任何**来源的数据报、响应中的问题从未与查询比对，而唯一存在的那道检查——0x20 QNAME 大小写——只发出 `warn!` 然后照样返回该响应。`security.qname_case_randomization` 默认开启，且文档记载它是缓存投毒的防护；它其实什么都没强制。由于那 8 个池中套接字绑定一次后就在整个进程生命周期内重复使用，它们的源端口只要被观察一次就固定了，因此路径外的投毒攻击根本不需要猜测——而 `cache_from_wire` 接着会把伪造的内容持久化。

  现在一个回复必须来自被查询的地址、带有发出时的事务 ID，并回答同一个问题——名称、类型与类别，且以**大小写敏感**方式比对，这才终于让 0x20 发挥它的作用——才会被接受。被拒绝的数据报会被丢弃，并且仍会等到截止时间为止以获取真正的答案，而不是由第一个包决定这次查询：这既能避免单一注入包否决掉查找，也能把池中套接字上迟到的、属于较早查询的回复排掉。

  运维上的影响：一个不会逐字回送问题的上游解析器现在会失败，而不是悄悄解析成功。行为良好的解析器会按规定回送，但如果你指向的是一个会把大小写归一化的解析器，请为它关掉 `security.qname_case_randomization`。经由代理（`proxy.mode`）的转发路径不受影响——它的响应是通过一条到已知代理的 TCP 连接抵达的。

- **迭代解析器接受来自任何来源、回答任何问题的回复。** `query_one` 绑定了它的 UDP 套接字却从未 connect，所以内核会投递来自**任何**对端的数据报——那道通常会迫使路径外攻击者必须伪冒权威服务器 IP 的源地址检查根本不存在，只剩下事务 ID 与临时端口。另外，`validate_question` 只比对响应问题的**名称**而不比对其他，所以一个回答了解析器从未问过的问题的回复——不同的记录类型，或不同的 DNS 类别——会通过验证，而它的记录会被采纳、缓存并提供出去。

  现在查询用的套接字会在查询发出前先 connect 到该名称服务器，这在内核中恢复了源检查且不增加每包成本；而 `validate_question` 会比对完整的问题：名称（大小写不敏感，因为解析器不做 0x20 随机化）、类型与类别。这条路径本来就是两条上游路径中防护较好的——它确实检查了事务 ID——但现在两个缺口在 UDP 与 TCP 上都补上了。

- **迭代解析器接受超出管辖范围的转介与粘合记录。** `classify` 从权威段中排在最前面的那条 NS 记录的所有者名称获取转介区域，从不去对照正在解析的名称或作答的区域，而 `walk` 直接把它写进委派缓存。任何解析器曾经对话过的名称服务器——一个广告域名、一个链接——都能在回答关于它自己区域的查询时附上 `AUTHORITY: com. NS ns.attacker.example` 加上粘合记录，而 `best_match` 会走访后缀，于是往后每一次 `.com` 查找都从攻击者的服务器开始。若某个转介的 TTL 超过 `resolution.delegation_persist_min_ttl`，它会被写进 `delegation_cache` 表并在开机时重新加载，于是这场劫持能活过重启。`.` 的情况一模一样。另外，`cache_glue` 把附加段的地址以它们自己的所有者名称缓存起来，所以同一个响应还能为一个外来的名称服务器主机名植入地址。

  现在只有当一个转介**严格向下**离开作答的区域**并且**涵盖正在解析的名称时（`referral_in_bailiwick`），它才会被跟随与缓存；其他情况会让这次查找失败，而不是被默默忽略，因此一个恶意委派无法伪装成进展。粘合记录被过滤到**作答**区域之内的名称——刻意不是被委派的那个区域，因为一个关于 `com.` 的根转介理所当然会带着 `a.gtld-servers.net.` 的粘合记录，它在 `com.` 之外，却稳稳地在 `.` 之内。被丢弃的数量由 `rolodex_dns_resolver_out_of_bailiwick_total` 计数。

- **DHCP 主机名未经验证就被插进 DNS 名称。** `register_dns_hostname` 用 `format!("{}.lan.{}.", hostname, tld)` 从 DHCP 选项 12 构造出记录名称——那是由局域网上任何未认证设备逐字提供的。一个把自己命名为 `*` 的客户端会让 `*.lan.<tld>.` 被注册进去，而 `make_wildcard_name` 让那成为一个真正的通配符：`lookup_scoped` 在精确匹配落空时就会退回到它，于是一台笔记本替它所在范围中每一个未注册的名称作答。点号让客户端能把自己放到一个它从未被分配到的深度，而且没有任何长度检查，所以超过 63 字节线路限制的标签会进到数据库，并在稍后序列化时失败，而不是在抵达的当下一次挡掉。

  现在主机名会被验证为单一 LDH 标签（RFC 1123 §2.1）——1 至 63 字节、字母／数字／连字符、开头与结尾不得为连字符——并转为小写，且是**被拒绝而非被清洗**，所以一个发出了规则不允许之名称的客户端，不会被悄悄指派成另一个名称。注册会被跳过并记录警告；注销会执行同一套验证，因此被移除的名称就是当初被加入的名称。

- **DNS-over-TCP 与 DoT 连接既无上限也无计时。** `serve_tcp` 与 `serve_dot` 为每条连接生成一个任务且没有上限，而两者的读取循环都在没有超时的情况下 await `read_exact`，因此一个连上来却什么都不发的客户端——或发半个长度前缀、或声明了 65535 字节却从不交付的客户端——会无限期地占住一个任务与一个文件描述符。`dns.bind` 默认是 `0.0.0.0:53`：在可路由的接口上，那就是一个认证前的远程资源耗尽问题，而一旦描述符耗尽，`accept` 对每一个正当客户端也都会失败。DoT 更糟：`acceptor.accept()` 同样没有计时，所以一个裸的 `connect()`、完全不需要任何 TLS 实现，就能在任何 DNS 被交换之前占住一个任务，而那个位置是任何针对 DNS 读取循环的修正都够不到的。

  现在两个监听器都会在消息之间套用 10 秒空闲超时、对已声明消息的主体套用 5 秒截止时间，并为每个监听器设下 1024 条连接的上限；DoT 另加 10 秒的 TLS 握手超时。空闲超时是从最后一次活动起算，因此 RFC 7766 的连接重用仍然可行——这在 DoT 上更重要，因为重新连接要付出一次全新的握手代价。DoQ 本来就有 `max_idle_timeout`，维持不变。

- **gRPC 共享密钥是用 `==` 比对的，且失败不受限流。** `String: PartialEq` 先比长度，再交给 `memcmp`，而 `memcmp` 在第一个相异字节处就返回——所以所花的时间泄漏了调用方猜对了密钥前面几个字节，把对整个密钥的搜索变成逐字节的搜索。另外，没有任何东西限制失败尝试：一个连得到 gRPC 端口的客户端，能以它开得动连接的速度不断猜测，而这正是让一个弱的共享密钥从“不明智”升级为“致命”的原因。

  现在比对使用 `subtle::ConstantTimeEq`。（`ring` 的 `verify_slices_are_equal` 已被弃用，且文档注明仅供内部使用、不对侧信道做任何承诺，所以本来就已在依赖树中的 `subtle` 现在成为直接依赖。）认证失败会**按源地址**限流：连续 5 次失败会把该来源锁定 30 秒，每次连续锁定加倍直到 15 分钟的上限；而在锁定期间，每一次尝试都会被以 `ResourceExhausted` 拒绝，且**完全不去比对令牌**，因此锁定本身不会变成“刚刚那次猜对了吗？”的预言机。

  它以来源为键而非全局，因此一个攻击者无法把运维人员锁在自己的管理平面之外；计数器算的是**失败**而不是请求，且一次成功会清除该来源的历史记录，所以循环调用的正当自动化永远不会被限流；一连串失败若安静 5 分钟就会重置，所以偶尔打错一次令牌绝不会累积起来。这张表上限为 65536 个来源——超过上限时会剪除空闲且未锁定的条目，再多的新来源则不予追踪，而不是让一场分布式洪水把它无界地撑大。

- **gRPC Unix 套接字对全世界都可连。** Unix 连接完全跳过认证——那是刻意的，而这也让套接字的文件模式成为管理平面上**唯一**的访问控制。`main.rs` 做的是裸的 `UnixListener::bind`，从未对结果 chmod，所以它落在 umask 之下、通常是 `0755`，于是这台机器上的每一个本地用户都握有未认证的管理权限：改写任何 DNS 记录、铸造 EAB 凭据、创建区域证书颁发机构。现在这个套接字以 `0660` 创建——先绑定在一个临时的同层路径上、限制权限、再改名到位，因为在已发布路径上“先绑定后 chmod”会留下一个套接字已存在且全世界可连的空窗，而原子性的改名保留同一个 inode，所以监听器不受影响。用 `0660` 而不是 `0600`，是为了让部署可以通过对套接字执行 chgrp 来授权给一个专属的管理组。

- **数据库对全世界可读。** 它就是密钥库：根证书颁发机构的私钥、每个区域的中间证书密钥、DNSSEC 私钥与 EAB HMAC 密钥，都是它里面的普通行。没有任何地方调用 `set_permissions`，所以 SQLite 在裸 umask 之下创建它——常见默认下是 `0644`——而任何本地用户都能读出根证书颁发机构的密钥，并为任何名称伪造一张每个已注册客户端都信任的证书。现在 `Database::open` 会把它限制为 `0600`，而且是在启用 WAL **之前**做，因为 SQLite 会把主文件的模式复制到它创建的 `-wal`／`-shm` 附属文件上；已有的附属文件也会一并收紧。

- **一个可路由的 gRPC 绑定搭配没有共享密钥的组合会静默启动。** 空的 `grpc.shared_secret` 会让 `check_auth` 对每一个 TCP RPC 提前返回 `Ok(())`。那在 loopback 上是文档记载的开发配置，在其他任何地址上则是管理平面的彻底暴露——而服务器会看起来健康地启动，日志里也没有任何异状。这个组合现在会在启动时被拒绝（`config::check_grpc_exposure`）：loopback 的情况仍会启动，`0.0.0.0` 与 `::` 正确地不算 loopback，而一个 `interface:port` 绑定只要该接口上有单一个可路由地址就会被判定不合格。

- **默认部署就是一台开放递归解析器。** `dns.bind` 默认为 `0.0.0.0:53`，而来源分类把 `security.overlay_cidrs` 之外的一切都当成受信任的本地客户端，所以在可路由的接口上，互联网上的每一台主机都获得完整的递归服务。那就是经典的反射／放大攻击资产——一个小的伪冒查询会返回一个大的答案打向被伪冒的受害者，而对外的解析流量算在这台机器头上——而且没有任何速率限制，所以洪水来得多快就服务得多快。

  新的 `security.recursion_cidrs` 决定谁可以驱动**上游**解析，默认为每一个从互联网不可路由的范围：loopback、RFC 1918、link-local、ULA 与 CGNAT。那涵盖了局域网、容器网桥与 WireGuard 叠加网络（`10.64.0.0/10` 在 `10.0.0.0/8` 之内），因此任何正当使用这台服务器的东西都不会失去服务。它与 `overlay_cidrs` 是分开的一个轴向，后者决定的是谁会被**强制套用范围**。

  这道检查位于本地／远程的边界上——在所有“从这台服务器持有的数据作答”的路径之后，在所有“去获取它没有的数据”的路径之前——因此一个陌生人仍然拿得到这台服务器的权威答案与 NXDOMAIN，却没办法让它代为解析互联网。它在响应缓存**之前**运行，因为一个被缓存的答案放大效果跟新鲜的一样好，而把缓存预热正是这种攻击的准备手法；而拒绝的形式是 REFUSED 搭配空的答案段，它永远不会比引发它的问题更大。DoH 现在会带着连接信息提供服务，让它的对端地址能进到分类逻辑；没有那一步，`:443` 会把 `:53` 关上的东西重新打开。

- **IPv4-mapped 的源地址绕过了范围强制。** 来源分类是完全按照套接字报告的地址去测试的，而 `IpCidr::contains` 刻意不跨地址族匹配。在双栈监听器上——`[::]:53`，一种文档记载的绑定形式，也是 Linux 在 `net.ipv6.bindv6only=0` 下的默认——一个 IPv4 的叠加网络对等节点会以 `::ffff:10.64.0.1` 抵达，那是一个 `IpAddr::V6`，所以 `is_overlay_peer` 返回 false，而该对等节点被归类为**受信任的本机来源**。分割视域的两半都因此坏掉：一个没有加入任何网络的叠加对等节点不再被 REFUSED，并进到了它本应被分隔开来的全局命名空间；而一个**确实**调用过 `JoinNetwork` 的对等节点会悄悄失去它的范围，因为那条关联是以纯 IPv4 形式存储的。一个 WireGuard 对等节点究竟会不会被强制套用范围，最后取决于监听器碰巧是怎么绑的。

  现在 `handle_query_on` 会在任何分类发生之前，先把源地址（以及本地监听器 IP）归一化一次，因此一个地址的 mapped 写法与纯写法在下游所有地方都是同一个地址——CIDR 测试、关联查找，以及入口监听器匹配。IPv4-compatible 地址（`::1.2.3.4`，已弃用）刻意不折叠；只折叠真正的 IPv4-mapped 地址。

- **注册门户会成为互联网上任何区域的证书颁发机构。** `create_account_inner` 逐字接受 `zone` 字符串、对它调用 `ensure_zone_intermediate`，并铸造一个以它为范围的 EAB。没有任何东西把那个区域与这台服务器实际管理的东西绑在一起，所以一个连得上的客户端可以为 `windowsupdate.com` 创建一张中间证书——以 DANE-TA 记录发布进本地 DNS，并链到每个已注册客户端都信任的那个根——然后据以签发证书。“任何连得上的人都可以注册”是刻意的设计决策且维持不变；它从来就不该意味着“可以成为整个命名空间的证书颁发机构”。注册现在被限制在这台服务器与之有关系的区域上：某个范围以 TLD 形式拥有它（这涵盖了范围隐含的 `.home` 域名）、它在本地数据库中有记录、它是一个已声明的权威区域，或它已经因为运维人员运行过 `EnsureZoneCa` 而有了中间证书颁发机构。每一项都是后缀匹配，所以受管区域的子区域也能注册，而 `acme.issuance_scope: any` 会完全解除这项限制——那也正是这个配置对签发者本来就代表的意思。

- **门户对“路过式 CSRF”门户洞开。** `POST /api/account` 把主体读成字节并解析 JSON，既不要求 JSON 内容类型，也不检查 `Origin`。一个带 `Content-Type: text/plain` 的跨源表单 POST 是 CORS 的**简单请求**——不会有预检，所以浏览器会发出它，副作用也就落地了。攻击者读不回被铸造的 EAB，但证书颁发机构的创建与 DNS 发布照样发生：任何局域网用户浏览器中的一个页面，就能重塑本地的 PKI。现在这个端点要求 `application/json`（这会强制一次门户不予响应的预检），并拒绝任何不是这台服务器的 `Origin`，比对是以 authority 进行的，因此终止 TLS 的反向代理仍然可用。浏览器扩展的来源被豁免——随附的扩展是一级客户端，而它的来源只有在用户授予主机权限之后才会被附上。非浏览器客户端（CLI、`js/` 的门户客户端、本地 UI 代理）不发 `Origin`，因此不受影响。

- **ACME 签发者会把 CSR 要求的任何名称都签下去。** `ca::issue_leaf` 把客户端的 CSR 直接交给 rcgen，而 rcgen 会把它的 `dNSName` SAN 与主体复制到签发出的证书里。没有任何东西把它们与该订单实际验证过的标识符做比对，所以对某一个名称的控制权证明，可以换到**任何**名称的证书——包括另一个租户区域里的名称——由一张链到每个已注册客户端都信任的 Rolodex 根的中间证书签发，并被当作 DANE-TA 锚点发布。现在 `issue_leaf` 会接收该订单已验证的标识符，并拒绝任何要求超出范围的 CSR（`badCSR`）。主体通用名称也会被检查，但只在它读起来像主机名时，因为 rcgen 会把 `"rcgen self signed cert"` 盖进任何没有明确 DN 就构造出来的 CSR。

- **ACME 端点认证了调用者，却从不授权他们。** `order`、`authz`、`challenge`、`finalize` 与 `cert` 都以 id 查出对象，却从不检查它是否属于发出请求的账号（RFC 8555 §7.4）。把别人账号中已就绪的订单 finalize 掉，会拿到一张受害者名称、由攻击者持有密钥的证书；而证书是以连续的 rowid 寻址的，所以任何账号都能枚举并下载这个证书颁发机构签发过的每一张证书。这五个现在都会验证所有权，证书路径则通过签发它的订单来验证。

- **`url` 绑定是可选的。** `verify_request` 只在 JWS 受保护首部中的 `url` 存在时才检查它，所以省略它就等于这个请求没有绑定到任何端点，并容许跨端点重放。现在它是必填的（RFC 8555 §6.4）。

- **EAB 凭据可以永久重复使用。** `used` 列由 `mark_eab_used` 写入却从未被读取，所以一份泄漏的注册凭据可以在它的区域上注册无限多个账号。现在它会被强制执行。

- **订单与授权的到期从未被检查。** `expires_at` 被存储了却被忽略，所以一次执行过的验证可以无限期有效——远在运维人员移除那条挑战记录或失去那个名称之后仍然有效。现在 `finalize` 与挑战响应路径会强制执行它。

- **防重放的 nonce 无界地累积。** 每一个响应都会铸造并持久化一个 nonce，包括未认证的 `GET /acme/directory`，而只有被用掉的才会被移除——一个未认证的远程客户端可以把这张表撑大到磁盘塞满，并在 DNS 热路径所使用的那个互斥锁上造成争用。现在 nonce 会在一小时后过期，**而且**这张表被限制在 1024 个未使用条目，并以最旧优先淘汰。光靠 TTL 是限制不住的：一秒之内的一场洪水完全落在那个时间窗之内。

- **`revoke-cert` 在没有吊销的情况下报告成功。** 它在验证 JWS 之后返回 `200 OK`，告诉客户端一张已遭泄露的证书已经作废，而其实什么都没变。现在它会返回 `501`，直到吊销功能实现为止。

### 特性

- **Prometheus 指标。** 一个可选的 `metrics` 配置段会启动一个纯 HTTP 的 `/metrics` 端点（默认 `127.0.0.1:9153`），输出横跨整台服务器的 66 个指标系列：按传输方式／rcode／类型分类的查询、逐传输方式的端到端延迟直方图、消息大小直方图、响应缓存的命中／未命中／过期／清空账目、封锁列表封锁数与提供方查询结果、`auto` 层级链（当前层级、逐层级的尝试／胜出／失败、已提交的切换、恢复探测、逐上游的交换次数）、迭代解析器的内部状态（转介、CNAME 跳数、额度耗尽、TCP 重试、根服务器预热、逐名称服务器的 EMA 延迟）、分割视域状态（记录、范围、关联、专属 TLD、入口监听器）、地址族抑制、按状态分类的 DHCP 消息与租约、ACME 签发与 dns-01 验证，以及逐方法的 gRPC 调用次数。

  这个段**默认不存在**，所以升级不会开出新的端口；默认绑定在 loopback，因为这个端点不做认证。它只承载汇总计数——没有查询名称，没有记录值。

  这个注册表是手工打造的（`src/metrics.rs`），建立在服务器其余部分本来就在用的同一批无锁原语上，**不新增任何依赖**：热路径上的一次计数器递增，是对一个预分配好的系列做一次 relaxed `fetch_add`，而文本输出格式是直接写出来的。每个标签维度要么是固定枚举、要么由配置所限制——唯一可能被客户端撑爆的那个，也就是查询类型，会把无法识别的类型折叠进 `OTHER`，因此一场 `TYPE4242` 查询的洪水无法产生无界的系列。

  `rolodex_dns_answers_total{source}` 指标报告的是解析顺序中的哪个阶段作答——缓存、本地记录、范围、LAN→范围后备、TLD 对等节点、封锁列表、RBL、DNS64、上游、权威 NXDOMAIN、被拒绝——这正是让分割视域流水线从外面看得懂的关键。它以一个默认值为“上游路径”的标记，贯穿 `resolve_query` 的约三十个出口，并记录在一个由每个传输方式共用的、受检测的单一出口上，因此新增的提前返回不可能悄悄逃出指标之外。

- **DNSBL 允许列表——特定主机可以被排除在封锁列表检查之外。** 一个对某个名称误判的封锁列表提供方，过去让运维人员除了禁用该提供方或整个 DNSBL 之外别无他法。位于允许列表上的名称（存储在 `dnsbl_allowlist` 表中，附带一个理由）会完全跳过解析的步骤 7。

  一个条目是**以标签边界进行后缀匹配**的，所以把 `example.com` 加进允许列表也会豁免 `www.example.com`，但不会豁免 `notexample.com`；而且它是归一化存储的（小写、结尾带点），因此任何写法都会新增或移除同一个条目。这道检查会整个短路掉以名称为基础的那一步——不管是已配置的 DNSBL 提供方还是本地 RBL 封锁列表，都封锁不了一个被豁免的名称——而且它在提供方查询**之前**运行，所以一个被豁免的名称根本不会发出任何封锁列表查询。反向 DNS 的 IP 封锁不受影响。查找是对一个从表镜像而来、并在开机时重新加载的 `DashSet` 做 O(标签数) 的操作，所以在没有配置任何允许列表时，热路径的代价只是一次空集合检查。

  通过 `AddDnsblAllowlistEntry`／`RemoveDnsblAllowlistEntry`／`ListDnsblAllowlistEntries`（gRPC 与 Go 客户端），或 CLI 上的 `add-dnsbl-allow`／`remove-dnsbl-allow`／`list-dnsbl-allow` 管理。

## v0.4.2（2026-08-06）

### 缺陷修复

- **上游响应缓存几乎完全是个空操作。** 两个彼此独立的键值缺陷，使得答案被写在任何查找都重现不出来的键之下，于是几乎每一次查询都要付出完整的上游往返代价，无论同一个名称多久之前才刚解析过。

  `cache_answers` 是以第一条**答案记录**而不是以问题来作为每个条目的键。对任何位于 CNAME 之后的名称来说那都是错的：`index.crates.io A` 回来的是一条链——`index.crates.io CNAME` → `fastly-index.crates.io CNAME` → 第三个名称上的 A 记录——它的第一条记录是 CNAME，所以条目落在 `index.crates.io.:CNAME` 之下，而每一次查找问的都是 `index.crates.io.:A`。这两个键永远碰不到，所以那个名称**永久**无法缓存。那涵盖了 CDN 前置的互联网的大部分（Fastly、CloudFront、S3）。它之所以没被注意到，是因为像 `example.com` 这样的名称会用它自己的 A 记录作答，让错误的键碰巧变成正确的——而每一个测试用的都是那种形状的名称。

  `cache_key` 则另外信任调用方已经把名称归一化了。缓存的两侧看到的大小写并不相同：读取用的是客户端发来的大小写，写入用的是问题回来时的大小写，而在 0x20 编码下（`security.qname_case_randomization`，默认开启）一个转发出去的查询会以 `eXaMpLe.CoM` 发出，响应也照样回送。因此每一个上游答案都被归档在一个随机大小写的键之下，这等于把缓存整个禁用了——是每一个名称，而不只是有链的那些。

  现在两条路径都以问题为键，`question.name()` 加上问题的类型，那正是读取路径会去查的键，也是 `cache_negative` 本来就遵循的规则。整组答案，包含 CNAME 链在内，都存在那一个键之下。一个不带问题的响应无法被正确地建键，因此现在它根本不会被缓存，而不是被缓存到一个没人会去看的地方。`cache_key` 会在填入键的同时转小写，这不增加额外分配，因为那个 `String` 本来就要分配。

### 构建

- **两种架构都在运行 `make` 的那台主机上交叉编译；构建用的虚拟机没了。** 多架构镜像此前是在主机无法运行外来架构时，启动一台 x86_64 QEMU 虚拟机（`make/amd64-vm.sh`）来构建的——用一整台虚拟机来代替一套工具链。现在发布出去的两个镜像，只差在它们的目标三元组，而不是差在它们是怎么被产生的。

  `cargo-zigbuild`（`make/cross.sh`）提供了 `rusqlite`（随附的 SQLite 源码）与 `ring` 所需要的 C 交叉编译器——光靠 `rustup target add` 会在 `cc` 那一步就死掉——并链接到一个钉住的 glibc（`GLIBC_VERSION`，默认 `2.36`，与 `debian:bookworm` 相符），因此无论构建主机自己的 glibc 是哪个版本，产出的可执行文件都能在运行期的基础镜像上跑。整套工具链都不需要 root 就能安装：`rustup target add`、`cargo install cargo-zigbuild`，以及解压到 `.cache/zig/` 底下的一个 zig 压缩包，因此 `make deps` 可以在任何地方配置它，而不必依赖各发行版各异的交叉 gcc 包。

  运行期的 `Containerfile` 现在只做 `COPY`——复制交叉编译出的可执行文件与一份取自构建主机的 CA 证书包。由于完全没有 `RUN` 步骤，`podman build --platform linux/<arch>` 从来不需要**执行**任何目标架构的东西，因此一个外来架构的镜像根本不需要任何用户态模拟。那才是真正把虚拟机拿掉，而不是把问题搬个位置：在像 Fedora Asahi 这样的主机上，x86 模拟要经过 FEX + `binfmt-dispatcher` + `muvm`，在 `podman build` 的沙箱里根本用不了。`Containerfile.build` 也随之移除，而 `--network=host` 不再默认传入，因为镜像构建过程中已经没有任何东西需要解析 DNS。

- `TARGET` 现在为**每一个**容器目标（`image`、`push-arch`、`push-rc`、`push-release`）选择架构，比照 `install` 仓库所采用的模型，好让同一个 `TARGET=` 值能在各个 town-os 仓库之间通用。开发板风味（`rpi`、`rg35xxpro`、`anbernic` 等）会解析到它们对应的架构；无法识别的值在解析阶段就是硬性错误。任何主机都能构建任何架构——`CROSS` 是推导出来的，不是用户旋钮。`push-rc-all`／`push-release-all` 可以从任一架构的单一主机发布两种架构并组出清单。

## v0.4.1（2026-08-06）

### 性能

- **UDP 监听器改为跨 `SO_REUSEPORT` 套接字分片。** 单一 UDP 套接字把整个监听器序列化了：一个任务用 `recv_from` 把它抽干，而每一个回复也都从同一个套接字发出去，所以接收是单线程的，而内核在两个方向上都要拿一把逐套接字的锁。为每个查询生成一个任务，把**处理**扩散到各核心，却从未扩散那个套接字，于是吞吐量在机器空闲的情况下就打平了——在一台 16 核主机上，服务器在约 104k qps 处到顶，而 CPU 用不到一核，超过那个点之后，增加客户端并发度只会拉高延迟（32 个并发客户端时 p99 为 1.70ms）而不会增加吞吐量。现在每个 UDP 监听地址会把 `dns.udp_shards` 个套接字绑到同一个 `addr:port`，每个都有自己的接收循环与自己用来回复的套接字，并由内核把抵达的数据报哈希分散到它们之间。在 16 核上针对一条本地记录实测，前 → 后：4 个并发客户端时 68k → 101k qps、16 个时 99k → 168k、32 个时 104k → 257k（p99 1.70ms → 432µs）。旧路径在 32 个客户端时就已经打平；分片后的版本到 128 个时仍在爬升（477k qps），而服务器只用了 16 核中的 2.87 核，所以剩下的天花板是负载生成器而不是服务器。

  `SO_REUSEPORT` **只**在请求超过一个分片时才设置。Linux 只有在绑到某个端口的每一个套接字都带着这个选项时才会共享该端口，所以单分片的监听器在繁忙的端口上仍然会相撞——而那正是入口绑定失败处理所依赖的行为。无条件设置它，会让一次失败的入口绑定开始对一个属于其他进程的端口“成功”，并悄悄分走它的一部分流量。分片由 `serve_udp` 这个 future 拥有（放在一个 `JoinSet` 里）而不是由调用方拥有，所以 `stop_ingress_listener` 仍然能通过它那唯一的 abort handle 把它们全部拆掉；而绑定到端口 `0` 时会被强制为单一分片，因为内核会给每个套接字一个**不同的**临时端口，那样没有任何一致的东西可以共享。

### 配置

- `dns.udp_shards`（默认 `0`）——每个 UDP 监听地址所绑定的 `SO_REUSEPORT` 套接字数量。`0` 代表每个可用核心一个分片；`1` 恢复此前的单一套接字监听器。

### 文档

- 并发模型章节此前把 DNS UDP 查询描述为“在单一任务上顺序处理”，那自从查询改为一个查询一个任务之后就已经过时了；现在它描述的是分片扇出与上述三项限制。

## v0.4.0（2026-08-05）

### 变更

- **入口 DNS 监听器现在解析整个命名空间，而不只是它自己的 TLD。** 范围选择过去是以被查询的**名称**为键：`ingress_target` 从 `find_tld_owner(qname)` 开始，所以一个不属于任何专属 TLD 的名称会返回 `None`，于是这次查询就失去了它与抵达时所在监听器的关联。它接着落入源 IP 分支，而在那里，一个 WireGuard 对等节点位于 `security.overlay_cidrs` 之内却从来没有被 `JoinNetwork` 过（只有这台机器自己的入口地址会被加入，对等节点从来不会）——于是被当成未关联的叠加对等节点而 REFUSED。因此那个监听器只回答它自己的 TLD 而别无其他：网络上的一个客户端解析得了 `gitea.default.fart`，却对 `google.com` 得到 REFUSED，所以这个网络的 DNS 服务器没办法解析它所服务的那个互联网。

  现在范围是从**监听器**选出的（`db::scope_for_ingress_ip`，逐 TLD 入口映射的反向查找），而不是从名称。一个抵达入口监听器的查询，无论名称是什么，都属于该监听器的拥有范围：专属 TLD 仍然保持分隔（兄弟网络的 TLD 仍然是权威 NXDOMAIN），而其他每一个名称都落入全局解析与上游转发。答案**改写**成入口 IP 这件事仍然是按名称把关的，因此一个穿透过去的名称会保留它解析出来的值。入口监听器之外的范围强制维持不变。

### 文档

- `CLAUDE.md` 与 `README.md` 更新至 v0.3.x 所落地的解析器工作：`auto` 层级链、迭代解析器的服务器选择／退避／界限、委派与记录缓存（包含 `flush_cache` 与 `flush_upstream_state` 各自清掉什么），TTL 语义，以及上面那些入口监听器范围规则。

## v0.3.3（2026-07-14）

### 缺陷修复

- **失败的入口绑定不再让它的 IP 在整个进程生命周期内作废。** `spawn_ingress_listener` 无条件地、在两个任务都还没尝试绑定之前就记下 UDP+TCP 的 abort handle，所以一个绑定失败的监听器会留下一个声称该地址已被服务的条目，而其实没有任何东西在监听——而那个只看“是否存在”的保护判断，会让往后每一次重新加入都在这具尸体上提前返回。这打中的正是常见情况：一个网络 TLD 的入口 IP 是 WireGuard 叠加地址，而 `sync_ingress_listeners` 会在启动时从数据库重放它，那时隧道接口**还不存在**，于是两个任务都以 `EADDRNOTAVAIL` 失败并退出；一台在已配置好网络的情况下重启的机器，就再也无法在那个叠加地址上提供 DNS 了。现在一个任务都已结束的条目会被视为不存在——丢掉并重新生成——因此重新加入确实会在接口起来之后重试绑定。

## v0.3.2（2026-07-11）

### 性能

- **迭代解析器的委派缓存**——从根服务器往下走访时学到的“区域 → 名称服务器”委派现在会被缓存，因此一个冷名称不再每次查找都重走一遍根 → TLD。长寿的委派（根与 TLD 的 NS 集合带有数天的 TTL）会被持久化到新的 `delegation_cache` 表，由 `resolution.delegation_persist_min_ttl`（默认 300 秒）把关，因此重启后回来时是预热过的。
- **整个递归过程都被缓存，而不只是委派**——粘合记录、无粘合记录的 NS 名称查找，以及 CNAME 跳转，这些都带着 TTL 抵达，此前用过一次就被丢弃。现在它们会以 `(name, type)` 为键保留，并以它们**剩余的**寿命交还，因此一条被提供出去的记录绝不会在上游被以完整 TTL 重新缓存。权威的 NXDOMAIN／NODATA 答案被缓存在一个独立的否定映射中，让正面路径得以继续把“没有记录”当成未命中。
- **负载分散到各根服务器之间**——服务器选择现在以 `hits * ema_latency` 评分，而不是纯粹按 RTT 排序，后者会把每一次查询都赶到单一最快的根服务器上。这个乘积会朝向整个组的相等靠拢，把查询分配成 `hits ∝ 1/latency`：快的服务器承担较多，每一台健康的服务器都承担一些。一台未被查询过的服务器评分为 0，会被优先尝试，并从一次本来就得发生的查询中学到它的延迟——不需要预先测量，也不需要探索概率的分支。
- **根服务器预热**——实际的根 NS 集合会在启动时获取一次，并以它真正的 TTL 缓存为 `.` 的委派。写死的 `ROOT_HINTS` 因此回到它们应有的角色：一个启动引导，以及预热失败时的后备。

### 缺陷修复

- **失败的上游服务器现在真的会恢复。** 失败现在被追踪为一个明确的指数退避（2 秒、加倍、上限 300 秒、首次成功时清除），而不是被折进延迟 EMA 当成一个 10 秒的合成 RTT。把它们折进去，会让恢复取决于健康的同伴碰巧有多快——面对一个 0.3ms 的同伴，10 秒的惩罚让一台死掉的服务器只分到三万三千分之一的份额，于是它永远不会被重试，也就永远回不来。处于退避中的服务器在同一地址族内会排在健康的服务器之后，但绝不会被移除，因此即使所有东西都在失败，解析仍会继续。
- **DNS 缓存的开机加载永远加载 0 个条目**——它调用 `cache_lookup` 时给了空名称；新的 `cache_load_all` 会执行它本来就该做的那个查询。
- **重复的 `dns_cache` 行**——重新缓存一个响应时是追加一行而不是更新它。已有的重复行会被合并，而一个唯一索引让 `cache_insert` 变成 upsert。
- **`flush_cache` 不再丢弃委派。** 每一次 gRPC 记录变更都会调用它，这等于在每一次本地记录变动时都把上游状态扔掉；层级切换改用独立的 `flush_upstream_state`。

### 配置

- `resolution.delegation_persist_min_ttl`（默认 300）——一个委派要被跨重启持久化所需的最低 TTL。
- `resolution.default_ttl`（默认 300）——在记录或响应完全没有提供自己的 TTL 时所套用的值（没有 SOA 的 NXDOMAIN／NODATA、TTL 为零的委派或粘合记录）。一个**确实存在**的 TTL 一律按原样采用，包括 SOA 的否定 TTL，它从不被钳制。

## v0.3.1（2026-07-10）

### 新特性

- **LAN → 拥有范围的解析后备**——一个受信任的本机来源（loopback／局域网，未关联任何范围）现在会从拥有它的范围解析**每一个**专属网络 TLD，因此即使记录是以范围方式存储的，所有网络 TLD 在局域网上都看得到。这个后备在全局查找之后运行（一个双栖名称面向局域网的全局记录仍然胜出），并在上游转发之前：仅存在于范围中的名称（例如某个网络的区域顶点）会以其存储值从拥有范围提供，接着会咨询该 TLD 的对等转发器，若全部落空则返回一个**权威 NXDOMAIN**——一个私有拥有的 TLD 绝不会从局域网被转发到上游。叠加对等节点不受影响，仍然严格分隔（一个加入某个网络的对等节点只看得到自己的 TLD，对兄弟网络的 TLD 得到 NXDOMAIN），这使得一个范围可以纯粹为了**拥有**某个 TLD 而创建（与对等节点分隔、可从局域网解析），而完全不需要为它绑定任何 WireGuard 叠加网络。

### 构建

- **实时的 `go test` 输出**——`go-test` 与 `go-integration-test` 这两个 Makefile 目标现在以本地目录模式运行 `go test`（去掉 `./...` 包模式），让详细（`-v`）输出在测试运行时流式输出，而不是逐包缓冲到结束为止。所有 Go 测试都位于根部的 `go/` 包中，因此覆盖范围不变。

## v0.3.0（2026-07-02）

### 新特性

- **具韧性的 `auto` 上游解析，并以 DoH 为首选的安全层级**——解析器现在遵循一条分层的后备链（根服务器 → DoH/DoT → 本地转发器 → 公共 :53）而不是只走根服务器，因此在会过滤对外 :53（以及 DoT 的 :853）的网络上仍能持续解析。:443 上的 DoH 是首选。粘滞的当前层级避免了在死掉的层级上逐查询超时，定期的恢复探测会在某个层级回来时把它夺回，层级切换被一段失败宽限期把关，而每一次已提交的切换都会清空 DNS 缓存作为跨层级的缓存投毒防护。通过 `resolution` 段配置（`mode = auto`、`secure_upstreams`、`public_fallback`、`switch_grace_failures`、`recovery_probe_secs`）。
- **地址族可路由性探测**——新的 `address_family` 配置段与后台探测会定期以 TCP 连到 :443 上的公共 v4/v6 目标，测试真实的互联网可达性。在 `auto` 模式（默认）下，服务器会停止为主机够不到的那一族返回 A 或 AAAA 答案，因此客户端会退到可用的协议栈，而不是卡在一个死掉的地址族上。`off` 一律两族都回答（旧行为）；`force4`／`force6` 则不做探测直接钉住一族。一个失败阈值用来抑制抖动，恢复则在首次成功时立即生效，而开机时的第一次探测是决定性的（没有宽限），因此开机到一个死掉地址族的连接上时，从第一次查询起就会抑制它。

### 缺陷修复

- **RBL/DNSBL 查找不再绕回本地存根。** 封锁列表查询（`<name>.<zone>`）此前是通过系统解析器发出的，而在典型主机上那会指回 rolodex 自己——重新进入查询处理器、再被 DNSBL 检查一次、再追加一次区域，然后永远循环下去（`<name>.<zone>.<zone>…`），把所有解析都卡死。现在 `RecursiveRblResolver` 会用 rolodex 解析其他所有东西的同一种方式来解析封锁列表名称：在它自己的套接字上从根服务器开始递归（绝不走本地存根），并在根服务器不可达时退回以 UDP 走已配置的转发器。彻底失败时失效放行（视为未列入）。
- **当对外 :53 被过滤时（由后台探测检测），RBL/DNSBL 检查会跳过由解析器支撑的提供方**，因此受过滤的网络不会拖住解析；由本地数据库支撑的 RBL 条目不受影响。

### 性能

- **RBL/DNSBL 检查改用发后不理的异步缓存填充**——封锁列表的缓存填充不再阻塞解析路径。

### 基础设施

- **可复现镜像的保证**——`make/build.sh` 会计算 `SOURCE_REV`（git 短 HEAD，工作目录有修改时附上 `+dirty`）并把它贯穿进两个镜像构建：构建阶段在 `cargo build` 之前就引用它，因此变更了的提交一定会让编译层失效（不会从重用的缓存层送出过期的可执行文件），而运行期镜像会盖上 `org.opencontainers.image.revision`，因此推送出去的镜像能通过 `skopeo inspect` 自我识别。

### 代码质量

- 实际联网的 DoH/DoT 上游测试现在是以可达性把关，而不是无条件忽略，因此在网络允许的情况下它真的会操练到安全层级。

## v0.2.4（2026-06-28）

### 新特性

- **DNSBL（域名封锁列表）**——一套域名封锁机制，与以 IP 为基础的 RBL 分开。DNSBL 提供方（例如 Spamhaus DBL、SURBL、URIBL）是以把被查找的名称前置到区域之前（`<name>.<zone>`）来查询的，相对于 RBL 所使用的反转 IP 形式。DNSBL 的列入**优先于任何外部解析出来的答案**——转发的、从根服务器迭代的，或上游缓存的——而本地记录与受管／权威区域一律胜出。可在启动时通过 `dnsbl` 配置段设置，并在运行期通过新的 `SetDnsblConfig`/`GetDnsblConfig` gRPC 端点、`set-dnsbl-config`/`get-dnsbl-config` CLI 子命令，以及 Go 客户端的 `SetDnsblConfig`/`GetDnsblConfig` 方法（`DnsblConfig`/`DnsblStatus` 类型）设置。
- **RBL/DNSBL 进入解析缓存流水线**——本地 RBL 与 DNSBL 检查现在会在本地／区域解析之后、但在上游缓存与转发器之前，套用到正向域名上，因此即使此前已缓存了一个上游答案，被列入封锁的名称仍会被以 NXDOMAIN 拒绝。DNS 缓存的“本地记录优先”阶段现在只提供权威的本地条目（通过 `lookup_local_only`）；上游缓存的条目则在封锁列表关卡之后才提供。

### 变更

- **RBL 与 DNSBL 的提供方列表现在默认为空。** 此前 RBL 随附五个默认区域；在运维人员配置提供方之前（通过配置文件或 `Set{Rbl,Dnsbl}Config`），不会查询任何外部封锁列表。一个已启用但为空的封锁列表是空操作。

### 代码质量

- `make lint` 现在运行 `cargo clippy --all-targets`，除了库之外也 lint 测试与 benches。修正了因此浮现的既有问题：benchmark 中横跨 `await` 持有的 `RefCell` 借用、测试中对 `Copy` 类型的 clone、`assert!(true)` 占位，以及配置测试中的 `field_reassign_with_default`。
- 新增涵盖 DNSBL 解析与优先顺序（包含优先于上游缓存答案）、空封锁列表的空操作行为，以及 gRPC/CLI/Go 可编程端点的单元、集成、CLI 与 Go 测试。

## v0.2.3（2026-06-28）

### 新特性

- **从根服务器开始的迭代解析**现在是默认的上游模式——查询会从根服务器开始递归解析，而此前的 `forward` 模式仍可选用。
- **自动反向 PTR 记录**——可选的 `dns.auto_ptr` 让通过 gRPC 管理界面新增的 A/AAAA 记录自动维护一条对应的 `in-addr.arpa`／`ip6.arpa` PTR 记录。
- **ACME 签发者／证书颁发机构**（RFC 8555，服务器端）——Rolodex 自己就是证书颁发机构：一个自签根签署逐区域的中间证书，而中间证书通过 ACME 签发叶证书，并对照 Rolodex 自己的 DNS 数据进行 dns-01 验证。包含 External Account Binding、自动的 DANE-TA TLSA 发布、一个受信任网络的网页注册门户、一个浏览器扩展，以及一个具备 DANE 获取能力的 JavaScript 客户端库。
- **通过 DNS 分发证书颁发机构**——根与中间证书链会被发布进 DNS 数据库，形式为 CERT（RFC 4398）与分块的 TXT 记录，因此任何解析得到该区域的客户端都能获取并信任该证书颁发机构，而不需要访问门户。

### 基础设施

- 原生的多架构（amd64/arm64）容器构建，发布到 `quay.io/town/rolodex`，包含供 arm64 主机使用的 amd64 构建虚拟机，以及以 `uname -m` 为后缀的逐架构镜像标签。
- 统一的 DNS 绑定配置，具备 `primary`（自动检测对外 IP）与 `interface:port`（绑定指定接口上的每一个地址）关键字。
- 在 `Cargo.toml` 中新增 `repository` 字段。

## v0.2.0-alpha（2026-03-27）

### 新特性

- **DHCP 服务器**，具备集成的 IPAM、自动的 DNS 主机名注册（A + PTR 范围内记录）、粘滞的 MAC 对 IP 绑定、后台租约清扫，以及完整的 DISCOVER/OFFER/REQUEST/ACK 流程
- **逐范围的 RBL 提供方**——网络范围可以在全局配置之外额外选用 RBL 提供方
- **DHCP 证书交付**——通过站点专用的 DHCP 选项（代码 224-254）把证书交付给客户端
- 上游 DNS 转发的 **SOCKS5 代理**支持（RFC 1928），与已有的 HTTP CONNECT 及 DoH 代理模式并存
- DNS-over-HTTPS 的 **HTTP/3（QUIC）传输**，通过 `enable_h3` 配置标志启用
- **Criterion 性能基准测试**（`make bench`），涵盖 QNAME 随机化、缓存操作、数据库查找、区域匹配、查询流水线，以及 UDP/TCP 往返

### 性能

- **原地 QNAME 大小写随机化**——直接在 DNS 线路格式的字节上操作（切换 0x20 位），而不是解析、复制、重建并重新序列化整个 DNS 消息；每次转发查询省下约 6 次分配
- **批量数据库查找**——`lookup_with_fallbacks()` 把精确、通配符、CNAME 与 ANAME 查找合并成单一 SQL `UNION ALL` 查询，把每次查询的互斥锁获取次数从 4 次以上降到 1 次
- **O(标签数) 的区域匹配**——`find_managed_zone()` 与 `find_authoritative_zone()` 会对一个 `DashSet` 走访 DNS 标签后缀，而不是用 `ends_with()` 做 O(区域数) 的线性迭代
- **以 Arc 包装的缓存记录**——DNS 缓存存储 `Arc<Vec<DnsRecord>>`，以消除缓存插入与本地缓存命中时的复制
- **批量缓存持久化**——有界的 `mpsc` 通道（容量 1024）搭配单一后台工作者，取代逐次插入的 `tokio::spawn`，每次最多抽取 64 笔写入
- **优化的缓存键**——预分配大小的 `String::with_capacity`，且不做多余的 `to_lowercase()`（名称已归一化）
- **UDP 缓冲区大小**——接收缓冲区在循环外分配一次；复制时通过 `Vec::with_capacity` + `extend_from_slice` 按实际包长度取大小
- **DoH 代理连接池**——通过逐代理地址的 `DashMap` 池（每个地址最多 8 条）与 HTTP/1.1 keep-alive 重用 TCP 连接

### 代码质量

- 消除了整个代码库中所有的 `unwrap()` 调用、`let _ = expr;` 压制写法与死代码
- 新增 `lint` Makefile 目标（`cargo fmt --check` + `cargo clippy -D warnings`）
- 在 lib.rs 与 main.rs 两处以 crate 级别强制 `#![deny(dead_code)]` 与 `#![deny(unsafe_code)]`
- 40 个新的单元测试，涵盖所有经过性能优化的代码路径
- 全面更新的 CLAUDE.md 规格，含所有新特性、性能模式与 benchmark 文档

### 基础设施

- 容器镜像切换到 `quay.io/town/rolodex`
- 所有依赖更新到最新的兼容版本

## v0.1.0

首次发布。

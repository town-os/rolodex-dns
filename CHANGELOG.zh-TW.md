# 變更紀錄

> 語言：[English](CHANGELOG.md) ｜ **繁體中文** ｜ [简体中文](CHANGELOG.zh-CN.md) ｜ [Español (España)](CHANGELOG.es-ES.md) ｜ [Español (México)](CHANGELOG.es-MX.md) ｜ [日本語](CHANGELOG.ja-JP.md)

## 未發布

### 破壞性變更

- **RBL 已移除。DNSBL 留下來了，而且它現在就是那份封鎖清單。** 反向 IP 的供應商查找——在每一次反向 DNS 查詢時，拿一個反轉過的位址去查供應商區域——已經不存在了，一併消失的還有逐範圍的 RBL 供應商、`rbl:` 設定區段、`SetRblConfig`／`GetRblConfig`、`AddScopeRblProvider`／`RemoveScopeRblProvider`／`ListScopeRblProviders`，以及 `set-rbl-config`／`get-rbl-config`／`*-scope-rbl` 這幾個 CLI 指令。`src/rbl.rs` 現在是 `src/dnsbl.rs`，而兩份清單共用的那套機制——供應商、拒答碼、輪替、結果快取、遞迴解析器——則以 `DnsblChecker`／`DnsblProvider`／`DnsblResolver` 的名義存活下來。既然只剩一份清單，`set_dnsbl_config` 之流就只叫 `set_config`。

  **位址仍然封鎖得了**，只是改由本地清單而不是供應商來做：維運人員指名那個位址（或 `dig -x` 印出來的那個反向名稱），兩種寫法都會是 NXDOMAIN。反正那也正是供應商在這裡永遠做不好的事——它被問的是「正在被解析的那個*名稱*」，而在一次反向查找裡，那是一個沒有人會為它公布信譽的名稱。

  **本地清單改了名字，資料列也一併遷移**：`local_rbl_entries` 變成 `local_blocklist_entries`，項目會在啟動時搬到它上面。那是維運人員自己的清單，而一台升級之後封鎖清單無聲清空的機器，看起來會跟封鎖清單壞掉一模一樣。`scope_rbl_providers` 則是直接丟棄——它所設定的那些查找已經不存在了。在 gRPC 上，`LocalRblEntry` 及其 Add／Remove／List 方法現在是 `LocalBlocklistEntry`／`AddLocalBlocklistEntry`／`RemoveLocalBlocklistEntry`／`ListLocalBlocklistEntries`；Town OS 用來提供它們的那些 HTTP 路徑（`/dns/rbl/local*`）維持歷史上的拼法，因為那是與 UI 之間一份已經公布的契約。

  proto3 能保留一個*欄位*編號，卻無法保留一個訊息或方法**名稱**，而每一項被退役的東西都是一整個訊息或方法——所以 proto 改為攜帶一個明確的「已退役名稱」區塊，那些名稱一律不得再被使用。

- **封鎖清單的指標標籤變了。** `blocklist_blocks_total{kind}` 現在是 `local` 或 `dnsbl_provider`；`rbl_provider`、`rbl_local` 與 `rbl_scope_provider` 都不見了。`answers_total{source}` 在原本回報 `rbl` 的地方改為回報 `reverse_blocklist`。凡是指名了舊標籤值的儀表板都需要更新；任何以 `kind` 加總的東西則照常運作。

### 功能

- **ACME 現在會在憑證所服務的每一個端點發佈 DANE-TA 記錄，而不只一個。** `acme.tlsa_port`／`acme.tlsa_proto` 是一組純量，因此簽發只會放上一筆 TLSA 記錄——預設是 `_443._tcp.<name>`。TLSA 記錄指的是*服務端點*而非憑證，而加密 DNS 在同一張憑證上就是兩個端點：`853/tcp` 的 DoT 與 `853/udp` 的 DoQ。沒被發佈的那一半，對任何檢查 DANE 的用戶端都會失效阻斷，其表現與伺服器根本沒有 DANE 完全相同。`acme.tlsa_endpoints` 是一份 `"<port>/<proto>"` 清單，與純量那組一併發佈並去除重複，協定會正規化為小寫，因此 `853/UDP` 會成為一筆有效的記錄，而不是無人查詢的 `_853._UDP.` 名稱。格式錯誤的項目會在啟動時被拒絕而不是跳過，理由相同：一筆悄悄從未出現的記錄，正是這套機制存在的目的所要避免的失敗。

- **加密傳輸可以在執行期設定，`Set*Config` 不再說謊。** `SetDotConfig`、`SetDohConfig` 與 `SetDoqConfig` 從前只是把請求打進日誌，然後什麼也不存就回 `success: true`——照單全收，隨手丟棄。編排方無法把這與「成功了」區分開，於是設定加密 DNS 的唯一辦法就是寫設定檔再重啟行程，而在一台 rolodex 就是唯一解析器的機器上，那是它上面一切的一次 DNS 停擺。

  新的 `TransportSupervisor`（`src/transports.rs`）在整個行程生命週期裡持有 DoT/DoH/DoQ 的監聽器，而且**啟動路徑與 RPC 現在是同一條程式碼路徑**：`main.rs` 透過 RPC 所呼叫的那同一個 `apply()` 把每一種傳輸拉起來，因此一份在啟動時可用的設定，是由恰恰處理執行期到來的那份設定的同一段程式碼來套用的，兩者不可能各自漂移。`:53` 從頭到尾沒被碰過——這些是彼此獨立的監聽器，重新設定其中一個，在它自身之外不付出任何代價。

  在舊監聽器讓出埠之前，新監聽器無法啟動，所以沒有辦法在放棄舊設定之前證明新設定能綁定。於是改為：先做完所有**不需要埠**就能做的檢查（bind 清單可解析、TLS 材料可載入或可產生），這樣一個打錯的位址或一份讀不出來的憑證會在舊監聽器仍在服務時被拒絕；然後停掉舊監聽器**並等待它們真的結束**，因為只 abort 而不等待，會讓新的綁定與舊通訊端的關閉賽跑；而如果綁定仍然失敗，就把先前的設定放回去，並告訴呼叫方這個傳輸已經下線。空的 bind 清單是關停而不是錯誤——那本來就是省略一個設定區段的含義。

  `Get*Config` 回報的是**實際綁定**的位址，只要請求寫的是埠 0，它就與請求的不同。這些訊息新增了 `binds` 清單，`TlsConfig` 新增了 `self_signed_sans`；舊的單個 `bind` 字串仍然被採納，因此早於清單寫法的呼叫方照常運作。`doh.bind` 同樣接受清單，條件與 `dot.bind` 一致。一個在建構時沒有裝配傳輸監管的伺服器——也就是行程內的測試夾具——會回 `FailedPrecondition`，而不是聲稱自己設定好了一個它根本沒有的監聽器。

- **SVCB 與 HTTPS 記錄（RFC 9460），以及隨之而來的 DDR（RFC 9462）。** 從前沒有 SVCB 記錄型別，於是解析器面對 `_dns.resolver.arpa. SVCB` 無以作答——而那正是用戶端發現自己那台解析器的加密端點的方式。因此加密 DNS 只能被手工敲進每一台裝置，前提還得是維運知道它存在。

  值是一行 RFC 9460 §2.1 的表示格式（`1 dns.home. alpn=dot port=853`），支援 `alpn`、`no-default-alpn`、`port`、`dohpath`（RFC 9461）以及 `keyNNNNN=` 這個逃生艙口。參數會被排成線路格式所要求的嚴格遞增的鍵序，因此維運可以按任意順序書寫，仍然得到一筆用戶端會接受的記錄，而不是一筆每個做校驗的用戶端都會丟棄的記錄。解析是**嚴格的，而且發生在寫入的那一側**：無法解析的值會被 `AddRecord` 拒絕，而不是先存下來、再在服務時被跳過——因為一筆出現在每一份清單裡卻什麼也答不出的記錄，正是這套程式碼反覆找到的那種故障。DNSSEC 的正規化編碼的是服務路徑所發出的同一份 rdata，因此被簽章的位元組與線路上的位元組不可能不一致。

  `arpa.` 依然從不在這台機器之外解析——但那道拒絕坐落在每一次本地查找的*下方*，所以本伺服器自己持有的那份指定記錄會由它自己的記錄作答，永遠不出這台機器。這恰恰是 DDR 需要的性質：為一台解析器自己的指定記錄作答的，只能是那台解析器。

- **可以指名一份尚未簽發的憑證。** `cert_path`／`key_path` 指向一個並不存在的檔案，從前是一次硬性的啟動失敗，因此這兩個路徑只能在檔案已經就位之後才寫得下去——而在一台 CA 是在解析器啟動*之後*才被建立的機器上，要走到那一步就意味著重寫設定並重啟這台機器唯一的解析器。如今只要與 `auto_self_signed: true` 並列，一份被指名但尚不存在的憑證會先用產生的材料起步，而既有的 30 秒輪詢器會在真正的那一對出現的當下把它接過去，不需要重啟，也沒有什麼要協調。在 `auto_self_signed: false` 之下它仍然是硬性失敗：那是維運在說「要麼給我這份憑證，要麼什麼都不要」，而悄悄改用一份產生的憑證，比拒絕啟動更糟。

- **上游解析模式可以在執行期切換。** `resolution.mode` 從前只在啟動時讀一次、此後再也不讀，這讓它成了編排方無法在不重寫設定檔並重啟行程的前提下改動的唯一一項上游行為——而重啟一台機器唯一的解析器，就是讓它上面的一切都斷一次 DNS。現在 `SetResolutionMode` / `GetResolutionMode`（proto、`src/grpc_service.rs`、Go 用戶端，以及 `set-resolution-mode` / `get-resolution-mode` 兩個 CLI 動詞）可以即時改動並讀取它。檔案只是**啟動時的種子**；讀取介面回報的是真正在解析查詢的那個模式，所以兩者可能不一致，而以正在執行的伺服器為準。

  該 RPC 會以 `InvalidArgument` **拒絕**無法辨識的模式，而不是像檔案那條路徑那樣發出警告再退回 `auto`。檔案是由能看見警告的維運在啟動時讀一次的；而 RPC 那頭有個呼叫方在等回覆，告訴它「成功」卻在用它沒要的模式解析，正是一台機器在過濾 `:53` 的網路上落進 `recursive`、而日誌裡對每個名稱為何解析失敗隻字未提的由來。

  切換**進入** `auto` 會派生與啟動路徑相同的層級預熱，因此切換之後的頭幾次查詢不必付冷層級的代價；而 `recovery_probe_loop` 現在是無條件派生的（在 `auto` 之外它什麼也不做），所以在執行期切換進 `auto` 的模式仍然能奪回一個已恢復的層級。

- **`dot.bind` 與 `doq.bind` 接受一個位址清單，於是 DoT 與 DoQ 能同時涵蓋兩個位址族。** 它們此前各自只收一個綁定字串，而一個字串無法同時點名兩者：`0.0.0.0` 只管 IPv4，而 `[::]` 並不是能同時頂替兩者的可移植寫法——在 `net.ipv6.bindv6only=0`（Linux 的預設值）之下，`[::]` 的通訊端也會收下 v4 映射的流量，於是它會與同一個埠上 `0.0.0.0` 的通訊端相撞，後綁定的那個以 `EADDRINUSE` 失敗。這兩個欄位現在**既收裸字串，也收一個清單**，每一項都各自走那四種常規寫法（`ip:port`、`[ipv6]:port`、`primary:port`、`interface:port`），重複的會被丟棄而不是綁定兩次。裸字串仍然被接受，所以在清單寫法出現之前寫下的每一份設定都照樣能解析。

- **更新後的 TLS 憑證不必重啟伺服器就會被提供出去。** 每一個 TLS 監聽器——DoT、DoH、DoQ、ACME 簽發者與註冊入口網站——都在啟動時對自己的 `rustls::ServerConfig` 取了一份一次性的快照，而 `TlsManager::reload()` 雖然存在也有測試，卻沒有任何地方呼叫它。於是憑證更新就等於一次 DNS 中斷：監聽器會一直提供那張過期的憑證，直到行程被重啟為止——而在一台由 rolodex **擔任**解析器的機器上，為了更新那張由 ACME 監聽器自己簽發的憑證而去重啟它，是一個值得打破的循環。

  監聽器現在跟隨一個 `watch` 通道而不是快照，而每個管理器每 30 秒輪詢一次自己的憑證檔案，並在內容改變時重建。已經建立的連線會在它交握時所用的那張憑證下走完；下一條抵達的連線拿到新的。沒有任何東西重新綁定，所以連接埠一次都不會關閉。DoT 逐連線建構它的 acceptor，DoQ 在它的 accept 迴圈裡用 `set_server_config` 套用這次替換，而三個 HTTPS 監聽器則寫入 axum-server 逐連線重新載入的那個 `RustlsConfig`。

  偵測方式是輪詢檔案的**內容**，並在剖析它們的同一趟裡雜湊。一個 inotify 監看會漏掉真實更新通常採取的那兩種形態——把新檔重新命名蓋過舊路徑，或把一個符號連結移向某個帶版本的目錄——因為兩者都不會寫入正在被監看的那個 inode。輪詢失敗時會讓先前那張憑證繼續提供並重試，因為指紋只在成功載入之後才記錄：一個 ACME 用戶端會寫兩個檔案，而一個三十秒的計時器終究會落在兩者之間，`rustls` 會拒絕那對不相符的組合，而完整的那一對會在下一個節拍被撿起。提供產生式材料（`auto_self_signed`）的管理器不會被輪詢——照計時器重新產生，等於每分鐘兩次遞給每個用戶端一張不同的憑證。

### 錯誤修正

- **透過 gRPC 啟用的阻擋清單，現在也會拿到出站 `:53` 的可達性探測。** 提供方查詢是走明文 `:53` 出去的，因此在過濾該埠的網路上它們只會逾時並徒增延遲；那個探測的存在就是為了察覺這一點並跳過它們，同時打出一條醒目的標記。它此前只在**設定檔裡** `dnsbl.enabled` 為真時才被派生——但驅動這件事的編排方（Town OS）是透過 `SetDnsblConfig` 編程阻擋清單的，根本不寫設定檔，於是在那些機器上探測從來沒跑過：`resolver_available` 一直停在它 `true` 的預設值上，而每一次提供方查詢都在逾時，日誌裡對原因隻字未提。現在這個迴圈是無條件派生的，讀的是檢查器*執行期*的啟用旗標——正是 `recovery_probe_loop` 早就有的那種形狀。在阻擋清單關著的時候它完全跳過網路探測——沒有什麼可探測的——只重新讀一次那個旗標，而那是一次原子讀取，因此跑得足夠勤，讓執行期啟用的阻擋清單不必等滿一整個探測週期。

- **DoT 監聽器會宣告 `dot` 這個 ALPN 標記。** 它先前什麼都沒宣告：`main.rs` 以一份空的 ALPN 清單建構 DoT 監聽器的 TLS 設定，因此一個提出 `dot` 的用戶端——那是 IANA 指派給 DNS-over-TLS 的標記，也是一個 stub 解析器用來把 DoT 監聽器與該連接埠上其他任何 TLS 服務區分開來所送出的東西——對這個問題得不到任何答案，只能自己猜。現在這個標記會被提供，而且**只**提供它：一個要求 `dot` 的用戶端會協商成功，一個只提出其他協定的用戶端會被拒絕而不是被默默服務，而一個根本不送 ALPN 擴充的用戶端（Android 的 Private DNS、處於機會模式的 systemd-resolved）會與先前完全一樣地被服務——因為 TLS 只有在用戶端提出了協定而沒有任何一個相符時才會讓交握失敗。

- **產生出來的 TLS 憑證現在會指名它的監聽器所綁定的位址。** `auto_self_signed` 產生的憑證只涵蓋 `localhost`、`127.0.0.1` 與 `::1`，別無其他，而且在每一種傳輸上都如此。在一個面向區域網路的 DoT 監聽器上，那張憑證無法被任何會檢查自己撥接名稱的用戶端使用——那是每一個設定了認證名稱的用戶端，而且那也是一張自簽憑證除了原始公鑰釘選之外唯一容許的驗證。每個監聽器自己的綁定位址現在會被自動折進憑證裡，而 `<transport>.tls.self_signed_sans`（新增，位於 `dot`、`doh`、`doq` 與 `acme`）承載那些綁定位址無法指名的身分：機器的主機名稱、它的 `.local` 名稱、一個區網別名。萬用綁定（`0.0.0.0`、`::`）不貢獻任何東西，因為它們不是任何人會撥接的身分——一個綁在萬用位址上的監聽器需要那份清單。重複的寫法會折疊在一起（`[::1]` 與 `::1`、`DNS.Home.` 與 `dns.home`），而在設定了 `cert_path`／`key_path` 時這一切都不適用。這是一次名稱檢查，而且它最先失敗；信任那張憑證仍然是另一件獨立的事。

- **一個沒有任何轉介宣告過的區切點，現在會被跨越，而不是被拒絕。** 當同一台名稱伺服器同時對某個父區*以及*它底下一個已簽署的子區具有權威時，一個落在子區裡的名稱會直接由該子區作答——完全不會送出任何轉介——於是一個從「最後一次跟隨的委派」挑選金鑰的解析器，會拿父區的金鑰去驗證子區的簽章，並把一個好答案判成 bogus：

  ```
  answer for cdnjs.cloudflare.com. A is bogus: RRSIG over cdnjs.cloudflare.com. A
  is signed by cdnjs.cloudflare.com., which is not the zone cloudflare.com.
  ```

  這正是 v0.5.1 點名指出、卻留著沒修的那個驗證器缺陷。RFC 4035 §5.3.1 給了裁決：RRSIG 的簽署者名稱說明了該套用哪些金鑰。在驗證一個答案、一次 CNAME 跳轉，或一個否定答案的否認證明之前，解析器現在會檢查那些簽章是否指名了一個位於目前這個區之下的區，並把信任鏈一次一個切點地往下延伸過去，把轉介從未交付的那筆 DS 取回來。每一個切點的建立方式，與轉介所建立的完全相同——DS 要在父區的金鑰之下通過驗證、子區的 DNSKEY RRset 要與它相符、一筆不存在的 DS 必須被*證明*不存在——而一個無法建立起來的切點，依然會扣住答案不發。

  每一個名稱，只要它背後的供應商把某個子區託管在同一套基礎設施上，就都受影響。找出這件事的那一個是 `cdnjs.cloudflare.com`，它的 SERVFAIL 會讓那些從它載入資源的頁面渲染成一片空白。

  這趟下降無法被回應本身操縱：只有在該區段裡的每一筆 RRSIG 都指名同一個簽署者、它嚴格位於目前這個區之內，而且它涵蓋了它所簽署的每一個擁有者名稱時，才會去追那個簽署者。少了最後那個條件，一個偽造的答案就可以指派一個確實未簽署的兄弟區當作它的簽署者，讓父區誠實地證明該委派不帶 DS，然後讓真實區確實有簽署的資料被當成 insecure 接受下來——`a_signer_below_the_zone_that_does_not_enclose_its_owner_is_refused` 就是那個攻擊，把該項檢查拿掉，這套測試就會失敗。

- **一個位於其父區名稱伺服器上的*未簽署*子區，現在看得見了。** 那是同一個隱藏切點，只是沒有東西可以追：回應不帶任何簽章，因此上面那趟下降沒有簽署者名稱可循，它仍然會被拒絕——而那個封包同時也正是「在傳輸途中把每一筆 RRSIG 都剝掉」會產生的東西，站在解析器這一側，兩者分不出來。`dnssec_unsigned_responses_total{evidence}` 現在會逐一計數：當授權區段的 SOA 指名了一個位於目前這個區之下的區時（那正是一個未簽署子區的否定答案所帶的東西）標成 `child_apex_soa`，其餘標成 `none`。那筆 SOA 未經簽署因此可被偽造，所以它純屬診斷用途、什麼都不決定；在此之前，這個情況除了一個與其他任何 SERVFAIL 無從區分的 SERVFAIL 之外，不留下任何痕跡。

- **兩項新指標**，使匯出的系列家族數來到 80：`rolodex_dns_dnssec_hidden_zone_cuts_total`，計數那些由「位於被查詢的區之下的區」所簽署的回應（也就是上面那趟 DS 查找被付出的次數），以及上面說明過的 `rolodex_dns_dnssec_unsigned_responses_total{evidence}`。

### 測試

- **解析器可用性探測迴圈已被涵蓋。** `src/dnsbl.rs` 裡的兩個案例，用注入的探測函式與毫秒級的間隔來驅動，因此完全不碰網路：關著的阻擋清單絕不能發起探測（那會是毫無理由地向一個公共解析器送流量），而以 `SetDnsblConfig` 的方式啟用的阻擋清單——不牽涉任何設定檔——必須開始被探測，並且探測的判定必須真正抵達檢查器。一個被呼叫了、但答案被丟掉的探測，與那場故障並無二致。

- **`BindList` 在回歸會悄無聲息的地方已被涵蓋。** 在 `src/config.rs` 裡：裸字串的 `bind:` 仍然能解析（在清單寫法出現之前寫下的每一份設定用的都是它，丟掉這一點會在升級時把它們弄壞）、清單能解析並解出兩個位址族、只有一項的清單會序列化回裸字串而不是把世上每一處純量 `bind:` 都攪成一個單元素清單、重複的位址只解出一次、空項會被跳過，以及出錯的那一項會在錯誤訊息裡點名自己。

- **憑證重新載入的路徑已端到端涵蓋。** `tests/tls_reload_test.rs` 加入了輪詢的那一半：一對沒有變動的憑證不會被重新載入（輪詢器一天跑 2,880 次，所以一個無條件重新載入的版本會以那個頻率重新發布），一對輪替過的憑證會被偵測到並被提供出去，一個自簽管理器沒有東西可輪詢因而不會被賦予輪詢任務，而那個任務自己也會在什麼都沒被呼叫的情況下撿起一次輪替。真正有牙齒的案例是**寫入途中**被撞見的更新——它必須失敗、讓舊憑證繼續提供，然後撿起那對完成的憑證；一個記錄「自己看到什麼」而不是「自己載入了什麼」的輪詢器，會把那個撕裂的狀態當成新的常態，並一路提供舊憑證直到重啟為止——而那正是這套機制存在要消除的失敗，而且它看起來會像是在正常運作。`tests/dot_test.rs` 釘住監聽器那一側：在一個運行中的 `serve_dot` 底下輪替過的憑證會抵達下一條連線，而一條已經開啟的連線不受打擾，且監聽器仍然作答。

- **`tests/dot_test.rs` 涵蓋 DoT 必須*做到*的事。** `src/dot_server.rs` 只帶著一個編譯用的冒煙測試，而 `tests/security_dot_limits_test.rs` 只涵蓋監聽器必須拒絕的東西；沒有任何東西演練過協商該傳輸、把訊息框起來、作答，或是把工作階段留著給下一次查詢。分成兩半，因為它們回答不同的問題。行程內，一個真實的 `tokio-rustls` 用戶端對著 `serve_dot`：上述三種 ALPN 結果、2 位元組長度前綴與其後的本體是否相符、一個已寫入的名稱得到答案而一個未寫入的名稱得到 NXDOMAIN，以及一條連線承載四次查詢並把它們的 ID 與問題一一對回。行程外，真正的 `rolodex-dns` 執行檔對著一份帶有 `dot:` 區段的設定檔——那是唯一能抓到上面任一項修正的東西：一個行程內的測試框架會自己建構 `rustls::ServerConfig`，因此看不見一個從不索取 ALPN 標記、或從不指名自己綁定位址的 `main.rs`。那一半會經由管理 socket 寫入一筆記錄，再透過 DoT 查回來，然後把伺服器實際提供的憑證裡的主體別名解碼出來。它刻意綁定 `127.0.0.2`：loopback 那一組位址被烘進了每一張產生出來的憑證裡，所以只有那一組之外的位址才能證明綁定位址是被推導出來的、而不是套用了預設值。

### 文件

- **這組文件在每一種語言下都齊全了。** `README.md`、`DESIGN.md` 與 `CHANGELOG.md` 新增了歐洲西班牙文、墨西哥西班牙文與日文譯本，因此五份文件現在在六種語言下全都存在，也沒有任何一條語言導覽列指向不存在的檔案。英文仍是唯一的真實來源：先改英文，譯本是後續工作，而不是第二個可以編輯的地方。沒有任何東西會驗證它們彼此一致——`tests/promql_docs_test.rs` 只讀英文的 `README.md` 與 `DESIGN.md`，因此譯本裡的 PromQL 區塊或家族計數是文件，而不是一項被檢查的斷言。

  日文的後綴現在是 `.ja-JP.md` 而不是 `.ja.md`，如此一來這組文件裡的每種語言都是地區代碼，後綴在各處的讀法也一致。除了檔名與語言導覽列之外沒有任何東西改變。

  中文譯本也被拉回與英文同步，先前它落後了一個版本：`arpa.` 子樹規則、v0.5.1 的根伺服器層級拒絕與根伺服器歸責，以及指標家族計數。

## v0.5.1（2026-08-12）

### 破壞性變更

- **`arpa.` 一律不在本機之外解析。** 其下的每個名稱，都只會由本地資料回答——一筆已存的 PTR、一筆帶範圍的記錄、一個受管理或具權威的反解區——否則就是 **REFUSED**，在所有解析模式下皆然。這個子樹中沒有任何東西會送到根伺服器、轉送器或加密上游。回 REFUSED 而不是 NXDOMAIN，是因為伺服器是拒絕為某個命名空間作答，而不是宣稱該名稱不存在。

  這道閘門設在兩層：查詢路徑在「本機持有的資料」與「必須外出取得的資料」的邊界上拒絕——位於上游回應快取之前，因此舊政策下快取的東西一律不會被提供——而迭代解析器則會在不送出封包的情況下拒絕，這也涵蓋了指向該子樹的 CNAME 目標或無膠水的 NS 主機名稱。判定依據是最後一個**標籤**，絕不是字串後綴，因此 `notarpa.` 與 `arpa.example.com` 都是普通名稱，會正常解析。

  這消除了促成這項工作的 `ipv4only.arpa`（RFC 7050 的 NAT64 探測）SERVFAIL：根與 `arpa.` 共同服務的區切點——一次查詢跨越兩個切點，轉介的 NSEC 由 `arpa.` 簽署，而走訪仍在對照根的金鑰檢查——現在根本無法抵達。底層的驗證器缺陷（拿轉介的證明去對照假定的父區，而不是 RRSIG 簽署者的區）並未更動，仍然存在。

  有兩個後果值得先講明，而不是日後才發現：對於本機沒有任何資料的位址，反解會被拒絕，而不是從網際網路取得答案（`dig -x 8.8.8.8`）；而 `ipv4only.arpa` 會被拒絕，正在探測 NAT64 的用戶端會把這讀成「這裡沒有 NAT64」。用本地資料好好地服務整棵反解樹，是另一項延後處理的工作。

- **無法驗證的根區現在會造成中斷，而不是無聲降級。** `fetch_dnskeys` 現在會區分 `Unreachable`（傳輸層）與 `Invalid`（密碼學層）。在根這一層，`Invalid` 會扣住結果、信任鏈就此停住；`Unreachable` 則仍會往下掉，這是刻意的，因為「連不上」不等於「無效」。把兩者混為一談，會讓任何能穩定破壞根 DNSKEY 取得的人，在完全不產生任何 bogus 判定的情況下，把驗證整個移出路徑——退回鏈會把該錯誤讀成「根連不上」，然後改由一個不做驗證的上游回答。

  這個取捨是真實存在的：這個組建不認識的信任錨點（一次 KSK 輪替）會變成 DNS 中斷，而不是悄悄降級到 DoH。在修好錨點之前，`dnssec.validate: false` 就是逃生口。

### 功能

- **提供無效 DNSSEC 的根伺服器會被移出根集合** 15 分鐘，每再犯一次就加倍，上限 24 小時；依據的是唯一一項不必問別人就能檢查的主張：它的根 DNSKEY 對照本地錨點。這個歸責不會因為該伺服器回應迅速而消失（`note_success` 現在只清除傳輸層欄位），只會被一個**通過驗證**的回答清除——絕不會靠等待清除——而且絕不會套用在最後僅存的那一台根上，因為所有根同時失敗，代表問題出在區或錨點，而不是十三台流氓伺服器。僅適用於根伺服器：在根以下，驗證失敗通常是該區自己的簽署錯誤，而那些本來就會安全地失敗。歸責只存在記憶體中，因此重啟後會重新信任每一台根。

- **一項新指標 `rolodex_dns_dnssec_blamed_roots`**，使匯出的系列家族數來到 78。長時間無聲地把部分根集合排除在外，是這件事情裡唯一沒有任何既有計數器會報告的部分。家族數與 PromQL 食譜已在 `README.md` 與 `DESIGN.md` 中同步更新。

### 錯誤修正

- **以 Bogus 收場的 DNSSEC 走訪不再留下任何東西。** 委派與其膠水記錄現在只有在 `extend_trust` 回傳可用的信任狀態之後才會快取。先前是先寫入的，因此一個 DS/NSEC 證明失敗的轉介，其 NS 集合早已被寫進去——而且持久化到磁碟，並在重啟後存活。

### 文件

- **功能規格已從 `CLAUDE.md` 移入 `DESIGN.md`。** `CLAUDE.md` 現在只剩開發規則；軟體做什麼寫在規格書裡，而規格書正是行為變更必須落地的那份文件。

- **譯本改以地區語言代碼標示。** 中文譯本從 BCP 47 的文字子標籤 `zh-Hant`／`zh-Hans` 改為 `zh-TW`／`zh-CN`，如此一來，在西班牙文已按地區拆分之後，這組文件裡的每個語言後綴回答的都是同一個問題。`CLAUDE.md` 與 `CONFIGURATION.md` 新增了歐洲西班牙文（`.es-ES.md`）、墨西哥西班牙文（`.es-MX.md`）與日文（`.ja.md`）譯本，並由 `Cargo.toml` 的 `include` 清單一併發布。

  `README.md`、`DESIGN.md` 與 `CHANGELOG.md` **尚未**翻譯成這三種新語言，因此它們的語言導覽列目前會連到不存在的檔案。英文版仍是唯一的真實來源，而且沒有任何東西會驗證譯本彼此一致——`tests/promql_docs_test.rs` 只讀英文的 `README.md` 與 `DESIGN.md`。

### 測試

- **`tests/arpa_refusal_test.rs`**（新增，並已寫入 Makefile 的 `rust-integration-test` 配方）斷言的是**封包**而不是 rcode，並以標籤邊界與本地資料路徑作為它的對照，橫跨全部三種解析模式。一道拒絕一切的 `arpa.` 閘門，滿足「這個子樹被拒絕」；一道什麼都不拒絕的閘門，滿足「`notarpa.` 仍然解析得到」——只有成對出現才說明得了什麼。

- **`tests/security_dnssec_test.rs`** 加入了這些拒絕規則，並透過一台搭配可運作之計數轉送器的真實 `DnsServer` 來驅動，因為「用戶端收到 SERVFAIL」與「轉送器從未被詢問」是兩種不同的性質，而只有後者才是那項發現本身。

## v0.5.0（2026-08-10）

### 錯誤修正

- **再也不會有用戶端查詢被拿去探測降級中的層級。** 在 `auto` 模式下，查詢路徑會每隔 `resolution.recovery_probe_secs`（預設 60 秒）選出一次查詢，讓它從根伺服器重新開始整條層級鏈，以便發現已復原且更受偏好的層級。想達成這件事是合理的，但把代價算在某位呼叫者頭上就很糟糕：在會過濾對外 `:53` 的網路上，根伺服器層級根本無法回答，於是被選中的那次查詢要付出整趟迭代走訪的代價——最多 `MAX_QUERIES_PER_RESOLUTION` 次上游查詢，每次都是逐名稱伺服器的逾時——之後才落到那個本來就會回答它的層級。每分鐘就有一次倒楣的查找卡上數十秒，而用戶端通常先放棄了，這在使用者眼中讀起來是「DNS 卡住了」，而不是「在做探測」。

  現在 `auto_start_tier` 一律就是已提交的層級，沒有例外，而那個用來選出探測查詢的 compare-exchange 也隨之移除——現在永遠只有一個探測者，不需要任何選舉。

### 功能

- **非同步的層級復原。** 復原邏輯移到 `recovery_probe_loop`，這是一個背景任務，每隔 `resolution.recovery_probe_secs` 以自己的拋棄式探針，重新測試位於已提交層級之上的那些層級。它的結果會被丟棄——探測的存在是為了移動已提交的層級，而絕不是為了回答任何東西——所以就算它跑過頭，也不會害任何用戶端拿不到答案；正因如此，底下的時間界限才得以按「正確性」來挑選，而不是按「等待者的耐心」。間隔是從上一次探測起算，而不是從上一輪結束起算，所以一次緩慢的探測不會演變成忙碌的探測；而且每一輪都會重新讀取設定，因此執行期的變更不需重啟即可生效。

- **重新取回根伺服器層級需要一個通過 DNSSEC 驗證的答案。** 第 0 層只有在根區域自身的 `DNSKEY` 得到 `Verdict::Secure` 的解析結果時才會被提升，而不是僅憑「連得上」。`:53` 上的攔截型中介設備或網路認證入口是連得上的，也會迅速回應——它只是回應它想回應的任何東西——所以少了這道關卡，任何劫持 53 埠的網路都能把自己安裝成最受信任的層級，悄悄自動取代這台機器原本正確退守到的加密上游。一個通過驗證的答案，正是這類中介設備唯一偽造不來的東西；而根區域自己的 DNSKEY，是這個問題最精簡的形式：這台機器能不能連到某台根伺服器，並從它建立起一條信任鏈。

  當 `dnssec.validate: false` 時沒有判定可供把關——依設計，解析器對所有東西都回報 `Insecure`——所以此時改為要求一個確定性答案。在那裡要求 `Secure` 會讓一台刻意不做驗證的機器永遠被困在後備層級上，再也用不到那個本來就是預設且首選的遞迴解析模式。

- **根伺服器層級同時受掛鐘時間限制，而不只受查詢次數限制。** 迭代解析器以**查詢次數**編列自己的額度，那回答的是「這個名稱值得投入多少工作量？」，而回答不了「一位呼叫者可以被晾多久？」——但對正在等待的人來說，只有第二個問題重要。被黑洞掉的 `:53` 會讓其中每一次查詢都逾時，因此光靠次數額度，就允許單次查找跑上一分多鐘。現在這個層級在查詢路徑上帶有 8 秒的上限，在復原探測上則是 2 秒。

  之所以是 8 秒而不是其他層級用的 1.5 秒，是刻意的：一次冷啟的迭代解析是好幾趟循序的往返（根、TLD、區域，再加上驗證所需的 DNSKEY 取得），而安全層級與轉送層級只是對單一伺服器的一次往返。實測的冷查找對公開名稱是 0.6–1.9 秒，對 RFC 1918 的 PTR 約 2.7 秒，所以把上限訂在這些數字附近並不會限制住病態情況——它只會弄壞健康的遞迴，讓每一次「慢但正常」的查找失敗，並把一台運作正常的解析器降級到 DoH 上。探測用的 2 秒之所以緊得多，是因為它問的問題小得多：一次查詢、一台伺服器、沒有委派要走。

### 測試

- **`tests/recovery_probe_test.rs`** 基於 DNSSEC 已簽章的模擬階層，刻意涵蓋兩個方向：通過驗證的根伺服器**會**被取回，而那些連得上但未簽章、由外來金鑰簽章、或提供已過期簽章的根伺服器則**不會**。一道永不開啟的關卡能通過所有負面測試，一道永遠開啟的關卡能通過所有正面測試——只有兩者成對才說明得了什麼。它也釘住了「絕不把用戶端查詢拿去探測」這道保障，而單元測試則涵蓋掛鐘界限與探測的空操作情形（已在最上層，或不在 `auto` 模式下）。

## v0.4.6（2026-08-09）

### 錯誤修正

- **DoH 連線池的測試不再互相競爭。** `DOH_POOL` 是行程層級的全域 `DashMap`，而那四個連線池測試在同一個測試執行檔中並行執行，但每一個都在頭尾呼叫**全域**的 `DOH_POOL.clear()`。任一個測試都會把其他測試剛放進池裡的東西抹掉，於是 `test_get_doh_connection_reuses_pooled` 可能在插入一個項目的下一行就找到 `None`——這是偶發、無法解釋的 panic，而不是可重現的失敗，也正因如此它撐過了好幾次全綠的執行。

  現在每個測試只移除自己的鍵。不需要序列化用的鎖：每個測試都綁定自己的臨時連接埠，所以同時執行的兩個測試不可能持有相同位址，而在測試之間被回收再用的連接埠，早已被上一個使用者移除了。兩處對連線池查找的裸 `unwrap()` 也換成了會回報「實際找到了什麼」的寫法，因此下一次這裡失敗時會指出問題，而不是匿名地 panic。

## v0.4.5（2026-08-09）

### 功能

- **封鎖歸因依「實際命中的是哪份清單」拆開。** `blocklist_allowlisted_total` 新增了 `kind` 標籤——`forward_name`、`reverse_name`、`ip_literal`——而 `rbl_scope_provider` 則加入了封鎖種類。

  允許清單的標籤命名的是**命中路徑**，而不是清單，這是由檢查所處的位置所決定的：豁免會在任何供應商查詢發出之前就短路，所以在它觸發的那一刻什麼都還沒問過，也就不存在「本來會命中哪份清單」可供記錄。改為指名那道關卡，既是可知的，也是更有用的軸向：它把發生在正向名稱上的豁免（步驟 7）與發生在反向查找上的豁免（步驟 2）分開，而在後者之內又把 `in-addr.arpa` 的寫法與 IP 字面值分開——兩者是用不同規則比對的，一個是後綴、一個是精確。

  `rbl_scope_provider` 之所以存在，是因為「某個網路自己選用的供應商」與「整台機器共用的清單」是兩種不同的維運決策，波及範圍也不同。折在一起時，「這個網路自己的封鎖清單弄壞了這個網路」與「全域清單弄壞了所有人」無從分辨，而當某個網路回報反向 DNS 不能用時，這正是第一件值得知道的事。拆開的代價是零：兩次檢查涵蓋的是互不重疊的供應商集合，而結果快取是以 `<ip>/<zone>` 為鍵的。新的種類一律附加在後面，絕不插入中間——那些 `BLOCK_*` 常數是陣列中的位置，插入會悄悄地把既有的每個計數器重新貼上別的標籤。

- **流量體積。** `traffic_bytes_total{direction=rx|tx}` 與 `records_served_total`，兩者都記錄在每個傳輸方式都會匯流經過的那一個受檢測出口上，因此一次查詢不可能被計數而它的位元組數沒被計數。記錄數量是從回應標頭的 ANCOUNT 欄位讀出的，而不是去重新剖析一個伺服器才剛序列化完的訊息。光看查詢數量看不出這些：一百萬次 NXDOMAIN 與一百萬次有內容的答案，查詢數相同、工作量卻天差地別，而 tx/rx 比值就是任何對外可達的監聽器上值得盯著的放大倍數。

- **逐 TLD 隔離。** `queries_by_tld_total{tld}` 把查詢流依 TLD 分開，這正是讓分割視域部署中的各個網路，彼此之間、以及與公開網際網路之間得以分辨的關鍵。

  這個維度是由維運人員擁有的集合所限制的，因為被查詢的名稱是由用戶端選的：一個無界的 `tld` 標籤，會讓一台掃描 `a.zzz1`、`a.zzz2`…… 的掃描器不斷產生新系列，直到登錄表把行程的記憶體吃光。有三個來源匯入它並取聯集——每個網路範圍所擁有的 TLD（包含各範圍隱含的 `.home` 網域）會**自動**被追蹤，因為要求一個網路自己的命名空間被寫兩次是個坑，而它會以「某個系列悄悄不見」的形式浮現；新的 `metrics.tracked_tlds` 設定清單，它被釘住，因此能撐過重啟且無法透過 API 移除；以及一份由新的 `SetTrackedTlds`/`ListTrackedTlds` RPC 管理的儲存清單，並附有 `set-tracked-tlds`/`list-tracked-tlds` CLI 子指令與對應的 Go 用戶端方法。所有未被追蹤的都折疊進 `other`。

  項目 `common` 會展開成一組內建的常見 TLD 集合，讓常見的公開 TLD 變成一行設定而不是二十行；而且它是以**未展開**的形式儲存的——讀回來時回報的是維運人員當初要求的內容，而日後若內建清單有所變動，也不需要每個部署都重新呼叫一次即可生效，形狀與 `refusal_codes` 中的 `none` 相同。比對會由最具體者開始走訪名稱的各個後綴，並回傳該名稱的一個切片，因此同時追蹤 `home.` 與 `lab.home.` 的部署會把 `box.lab.home.` 歸因到較具體的那個，而未被追蹤的名稱不會產生任何配置。根區域會被以 `InvalidArgument` 拒絕：`.` 是每個名稱的後綴，追蹤它會把所有系列塌縮成一個，並讓兜底值永遠取不到。

### 破壞性變更

- **DHCP 的指標標籤現在帶有子系統限定。** `rolodex_dns_dhcp_messages_total{type}` 改為 `{message_type}`，`rolodex_dns_dhcp_leases{state}` 改為 `{lease_state}`。通用的標籤名稱，正是讓橫跨兩個子系統的彙總——例如某條記錄規則裡的 `sum by (type)`——悄悄把 DHCP 的 ACK 計數混進 DNS 計數的原因。選用舊名稱的儀表板與告警需要更新。DNS 的彙總指標（`queries_total`、`traffic_bytes_total`、`records_served_total`、`queries_by_tld_total`）同樣只計 DNS：DHCP 的 `:67` 流量從來就不是 DNS 流量，而一個由 DHCP 註冊的名稱，只有在真的有人去解析它時才會進到這些指標。

### 測試

- **文件中的 PromQL 是被測試的，分兩層。** `tests/promql_docs_test.rs` 從 `README.md` 與 `CLAUDE.md` 中剖析出每一個 ```promql 區塊，抽取其中的指標名稱與標籤比對條件，並逐一對照實際輸出的內容解析。文件是指標變更中唯一沒有其他東西會去驗證的部分：上面那次 DHCP 改名讓程式碼照樣編譯、其他測試照樣全綠，卻悄悄把每一條文件中的儀表板查詢變成查不到資料的查詢，而維運人員要等到事故當中某塊面板空白時才會發現。它同時把文件中宣稱的系列數量釘在登錄表實際輸出的數量上——那個數字早就已經飄掉了，文件寫 73、實際輸出 74——並且保護那個程式碼圍籬本身，因為一個被改標成 ` ```bash ` 的區塊會讓整個檔案悄悄地不再檢查任何東西。

  `tests/prometheus_integration_test.rs` 把同一批查詢送進一台真正的、正在抓取實際伺服器的 Prometheus 執行，抓到子字串掃描器抓不到的東西：一個**作為 PromQL 就格式錯誤**的查詢，而不只是指名了不存在的系列。`rate(sum(x)[5m])` 指名的全是真實系列，卻在被貼上的那一刻就會被拒絕。它由 `make prometheus-test` 執行，而 `make test` 現在依賴它；缺少 podman 時會大聲跳過而不是失敗，所以沒有容器執行環境的機器仍能得到綠燈，同時絕不會假裝那些查詢已被檢查過，而 `ROLODEX_PROMETHEUS_REQUIRED=1` 會把那個跳過提升為失敗，供 CI 使用。

- 為每個新維度補上單元與整合覆蓋，每一項都配有它的對照組——特別是「未被追蹤的 TLD 不會產生**任何**系列」這一項，因為一個只檢查正面情況的測試，在基數限制被整個移除之後照樣會通過。

### 打包

- **這個 crate 可以發佈，並且會帶上它的文件。** 宣告了 `readme` 與 `homepage`，並以一份明確的 `include` 清單取代「靠省略來決定打包內容」的做法：現在發佈出去的套件會在原始碼、protos、benches 與 tests 之外，一併帶上 `README.md`、`CONFIGURATION.md`、`CHANGELOG.md`、`CLAUDE.md` 與 `LICENSE`。

  之所以用 `include` 而非 `exclude`，是因為工作目錄裡還放著一個 Go 用戶端、一個 JavaScript 用戶端、一個瀏覽器擴充功能與容器工具，這些對 crate 的使用者毫無用處——而一份排除清單會在每次新增這類東西時悄悄開始把它打包進去。`proto/` 與 `build.rs` 是承重的（gRPC 綁定是在建置時產生的，所以少了它們的套件根本編不起來）；`benches/` 之所以隨附，是因為 `[[bench]]` 指名了一個目標——cargo 會在被宣告的目標檔案缺失時失敗，所以省略它是弄壞套件，而不只是把它變小。

  `tests/` 隨附，是為了讓收到這個 crate 的人能夠驗證它，而對一個 AGPL 的網路服務來說，這正是重點所在。這也是為什麼 `CLAUDE.md` 是被納入而非被當成僅限儲存庫的工具：`tests/promql_docs_test.rs` 會連同 `README.md` 一起讀取它，而一個帶著測試卻沒帶測試輸入的套件，等於帶了一個跑不起來的測試。既然每個文件檔在檢出版本與發佈套件中都保證存在，那個測試就堅持一定要找到每一份，而不是跳過它讀不到的——跳過正是一項檢查悄悄開始什麼都不驗證的方式。

### 文件

- **`CONFIGURATION.md`——一份設定指南。** README 的設定章節是欄位參考，它回答「這個選項做什麼」，而回答不了「我的設定檔應該長什麼樣」。這份指南是任務導向的：設定檔如何載入（以及檔案不存在並不是錯誤）、最小可用設定、四種實作過的部署形態（家用解析器、純權威伺服器、分割視域疊加節點、位於會過濾 `:53` 的網路上的解析器），接著每個子系統各一節。它明白指出哪些設定根本不算設定——記錄、範圍、區域、封鎖清單項目、DNSSEC 金鑰與 ACME 憑證機構都是 SQLite 中的執行期狀態——並附上一份「什麼需要重啟」的表格、伺服器刻意拒絕啟動的四種情況，以及一份依症狀索引的疑難排解表。

  兩份 CIDR 清單有並排比較，因為 `overlay_cidrs`（誰會被強制**套用範圍**）與 `recursion_cidrs`（誰可以讓這台伺服器去問上游）是兩個讀起來很像的不同問題。

- **README 更新至 v0.4.4。** DNSSEC 現在兩半都寫到了——簽章者與上游驗證器，含四種判定，以及為什麼「非安全 vs 偽造」才是承載安全性的那個區分。封鎖清單的拒答碼與供應商輪替、允許清單橫跨所有清單與兩道關卡的涵蓋範圍，以及遞迴存取控制（先前已隨版本推出卻從未寫進文件），各自有了專屬章節。設定範例與選項表新增了 `dnssec.*`、`security.recursion_cidrs`、`metrics.bind` 與各供應商的拒答欄位；CLI 文件新增了拒答相關旗標與目前的 `get-rbl-config` 輸出；而 RFC 對照表新增了 DNSSEC 家族（4033/4034/4035、5155、6605/8080、6840、9276）與 7766。

## v0.4.4（2026-08-08）

### 功能

- **上游 DNSSEC 驗證。** Rolodex 過去會簽自己的區域，卻對它解析回來的東西不做任何驗證：沒有信任錨點、對外的迭代查詢不設 DO 位元，而 `verify_rrsig` 則是刻意沒接上。`src/dnssec_validate.rs` 是負責驗證的那一半，而且它與簽章者不共用任何程式碼——簽章者處理的是我們自己寫入的資料庫資料列，每一個位元組都在掌控之中；驗證器處理的是來自「其誠實與否正是待證問題」的一方所送來的東西，而這兩者必須有能力彼此不同意。

  信任鏈是**由上而下、與解析器本來就在執行的委派走訪並行**建立的，因此取得一筆 DS 不需要額外查詢——它就搭在轉介裡，由我們已經持有其金鑰的父區域所簽。已驗證的金鑰集合（以及已被證明為非安全的委派）會在 `src/key_cache.rs` 中逐區域快取；若每次查詢都重新推導整條鏈，等於把委派快取所解決的冷啟動問題往上搬一層再重演一次。那裡的查找是精確名稱比對，絕不用後綴：父區域的金鑰對子區域什麼都沒說，而那正是 DS 記錄存在的意義。

  RFC 4033 §5 的四種狀態全都被區分開來。承載安全性的是「非安全 vs 偽造」：「沒有簽章」**不等於**非安全，因為路徑上攻擊者能從任何回應中剝掉簽章——只有當一份已簽章的 NSEC/NSEC3 證明了上層委派處確實沒有 DS 時，它才是非安全，而攻擊者沒有父區域的金鑰就偽造不出這種證明。這就是那整套 NSEC/NSEC3 否定證明機制存在的理由；略過它，剩下的驗證器會被任何攻擊者降級成不存在的驗證器。

  偽造與無法判定的資料永不提供、也永不快取，無論正面或負面——一筆被快取的偽造否定回應，會在其整個 TTL 期間壓住真正的名稱。在 `auto` 模式下，驗證失敗是一個**確定性**答案而非層級失敗，所以一個壞掉的簽章不可能被拿去經由某個不做驗證的上游洗白。AD 只在 Secure 時設置，且只對有詢問的用戶端設置（查詢中帶 DO 或 AD）；對沒有設定 DO 的用戶端會剝除 RRSIG/NSEC/NSEC3，因為一筆已簽章的 A 記錄體積大約會變成三倍，而「小問題換大答案」正是 `security.recursion_cidrs` 存在要堵住的那種放大形狀。

  驗證**只適用於迭代路徑**——`recursive` 模式，以及 `auto` 的根伺服器層級。轉送回來的回應是別人的遞迴結論摘要，要驗證它就意味著我們自己把整條鏈重新解析一遍，而那恰恰就是根伺服器層級本身；因此一條降到第 0 層以下的鏈是未經驗證的，而它會以「不設定 AD」如實表明。

  由 `dnssec.validate`（預設 `true`）與 `dnssec.trust_anchors`（預設：IANA 根金鑰）設定。錨點覆寫是**取代**IANA 金鑰而非追加，而且每個錨點的每個欄位都在啟動時驗證——格式錯誤的錨點是硬性失敗，而不是悄悄退回。啟用驗證時，對外的迭代查詢現在會帶上 EDNS0，設定 DO 並宣告 1232 位元組的酬載大小（先前完全沒有 OPT，於是伺服器有權把回應壓在 512 位元組內，而一個已簽章的答案基本上塞不進去）。逐次查找的查詢額度新增了 `VALIDATION_QUERY_BUDGET`（在基礎的 64 之上再加 32），所以啟用驗證不會悄悄縮短一個名稱可以有多深。NSEC3 迭代次數超過 100 時會被視為非安全而不去計算（RFC 9276）——那是由攻擊者選定、卻花在我們這一側的運算工作。只能對照本建置無法驗證的演算法來驗證的記錄，是非安全而非偽造（RFC 6840 §5.11）：我們缺少某個演算法，不是那個區域的故障。

  啟用了 hickory 的 `dnssec-ring` 特性，以取得具型別的 DNSSEC RDATA 與 RSA 驗證能力。`ring` 無法**產生** RSA 金鑰，這正是簽章拒絕演算法 8 的原因——但驗證是另一回事，而一個檢查不了 RSA 的驗證器，也就檢查不了根。

- **封鎖清單拒答碼與供應商輪替。** 一份 DNSxL 回應「已列入」與抱怨**發問者**用的是同一種方式——`127.0.0.0/8` 之下的一筆 `A` 記錄——而唯一能區分兩者的就是位址本身：`zen.spamhaus.org` 用 `127.0.0.2` 說「已列入」，用 `127.255.255.254` 說「你正透過公用解析器發問」。因此，把任何 `A` 記錄都讀成「已列入」，就等於在封鎖清單決定不再回答我們的那一刻，把**每一個**對照該供應商檢查過的名稱都變成 NXDOMAIN，而這會在查詢量越過該供應商門檻時開始發生——可能是一次看起來一切正常的部署之後好幾小時或好幾週。Spamhaus 講得很直接：那些碼「不應被解讀為任何形式的信譽評價」。

  現在拒答碼會被辨識出來，而一次拒答既不是列入也不是否定——什麼都不會被快取，因為我們對被查詢的名稱什麼都沒學到。同一個回應中，任何位置的拒答都勝過同一個回應中的列入：一個正在抱怨的供應商不會同時在回報信譽，而往這個方向犯錯會**失效放行**，反過來的順序則會在每一個名稱上失效阻斷。接著該供應商會被移出查詢輪替一段冷卻時間（`refusal_cooldown_secs`，預設 3600 秒，可逐供應商覆寫），而不是每次請求都再去問它一遍；輪替只跳過新的查找，已快取的判定維持不變；它會自行失效，因此短暫的超額配額期不需要維運人員動手就會自癒；而 `FlushCache` 或任何 `SetRblConfig`/`SetDnsblConfig` 都會清除它——重新設定往往正是那次拒答的修正動作。

  內建集合涵蓋了常見供應商已載明的那些碼：`127.255.255.0/24`（整個 Spamhaus 錯誤範圍，而不是今天的那三個碼，這樣明天的第四個碼才不會被悄悄讀成列入）、`127.0.1.255` 與 `127.0.2.255`（Spamhaus DBL/ZRD 的「不支援 IP 查詢」），以及 `127.0.0.1`／`127.0.0.255`（URIBL/SURBL 的「查詢已被封鎖」）。空值代表內建集合——它不可能代表「沒有任何碼」，因為在這項功能存在之前寫的每一份設定都是空的——而單一項目 `none` 則為那些真實列入值恰好與其中之一相撞的私有封鎖清單停用偵測。明確列出的清單是取代而非擴充預設值，所以把它寫出來的維運人員也可以藉此縮小範圍。無法剖析的碼會在啟動時被拒絕，或由 RPC 回以 `InvalidArgument`，絕不跳過：一個悄悄失效的碼，就是一個會被讀成「已列入」的拒答，而且設定當時還回報了成功。

  可透過 proto、CLI、Go 用戶端與設定檔逐供應商及全清單設定，且 RBL 與 DNSBL 兩份清單各自帶有獨立的冷卻預設值。`GetRblConfig`/`GetDnsblConfig` 回報的是**實際生效**的解析後結果（一份設定為空的清單會讀回內建集合，而不是讀回空值），外加目前有哪些供應商被移出輪替、以及還剩多久；`rolodex_dns_blocklist_refusals_total{kind}` 與 `rolodex_dns_blocklist_rotated_out` 把同樣的資訊輸出給 Prometheus。少了它們，「封鎖清單安靜了」與「封鎖清單很乾淨」從外面看起來一模一樣，而維運人員預設會認為是第二種。把冷卻設為 `0` 代表「使用預設值」，而不是「不冷卻」——零冷卻等於去重問那個剛剛叫我們別問了的供應商。

### 錯誤修正

- **現在每一次封鎖清單命中都會是 NXDOMAIN，而允許清單項目是唯一能壓下它的東西。** 三個漏洞，每一個都是「某份清單說了『已列入』而查詢照樣解析成功」，或是「維運人員的逃生口構不到」的情況：

  - DNSBL 允許清單只守住正向名稱檢查（步驟 7），所以針對**位址**的誤判是解不掉的——一個被錯誤列入的 IP 會讓一台運作正常的主機 `dig -x` 失敗，除了停用該供應商之外別無他法。現在它同時守住反向／IP 路徑（步驟 2），且兩種寫法皆可：`in-addr.arpa`／`ip6.arpa` 名稱，像任何 DNS 名稱一樣以後綴比對，因此一個項目就能解除整個 /24；或是 IP 字面值，以**精確**比對——位址是最高位在前的，所以 `1.100` 不是 `192.168.1.100` 的父節點，把它當成父節點會豁免掉沒有人指名過的位址。
  - 逐範圍的 RBL 供應商被儲存了、能被 API 列出來，卻從來沒被查詢過。查詢路徑呼叫的是單純的 `is_listed`，所以來自某個範圍自有清單的命中照樣正常解析。
  - 寫成反向名稱的本地 RBL 項目什麼都比對不到——IP 那道關卡只查 `ip.to_string()`。`dig -x` 印出的是反向名稱，所以那正是維運人員會貼進去的東西，而一個讀起來像封鎖、實際上什麼都沒比對到的項目，比一個被拒絕的項目更糟。現在兩種寫法都能比對。

  兩處反向呼叫點都匯流進同一道關卡（`ip_blocklist_kind`），而每一道關卡都匯流進同一個豁免判斷（`blocklist_exempt`），因此日後新增的出口不會偏離。當對外的明文 `:53` 不可達時，逐範圍供應商會被跳過——那個旗標不是政策開關，它說的是這次查找只可能逾時。本地記錄仍然勝過封鎖清單，這是刻意的：反過來會讓第三方的列入弄掉一個內部服務，而且有測試釘住它。

- **區域簽章產生出無法被驗證的簽章，以及讀不出來的已發佈金鑰。** DNSKEY、DS 與 RRSIG 過去是以承載儲存字串的 **TXT 記錄**提供的，所以一次 DNSKEY 查詢拿回來的是 TXT，而每一個已發佈的簽章都無法使用。現在它們以自己的型別碼提供，RDATA 來自簽章者拿去做雜湊的同一個正規編碼器，因此送上線路的東西與被簽的東西逐位元組相同；URI 與 ZONEMD 也以同樣的方式編碼。被簽的位元組本身現在是 RFC 4034 §3.1.8.1 的正規形式——正規化的擁有者名稱、原始 TTL、RFC 4034 §6.3 的排序並去除重複，因此記錄從 SQLite 出來的順序無法改變簽章——並具備 RFC 4035 §2.1 的 KSK/ZSK 角色分工、以標籤邊界界定的區域範圍，以及「取代而非累積」的重新簽章（包括在那些自上次執行以來記錄已被刪除的名稱處）。無法簽章的型別（NSEC、NSEC3、NSEC3PARAM、ANAME，以及格式錯誤的值）會被跳過並在回應中指名，而不是用一種杜撰出來的編碼去簽——對一串沒有別人重現得出來的位元組所做的簽章，會在每一個驗證器上失效阻斷，那比讓那個名稱維持未簽章更糟。

- **RSA/SHA-256（演算法 8）在產生金鑰時就被拒絕**，而不是先接受再偷偷替換。`ring` 無法產生 RSA 金鑰；一個宣稱演算法 13 卻承載 Ed25519 金鑰材料的 DNSKEY，會產出彼此都對不上的 DS、DNSKEY 與一整組 RRSIG，而那個失敗會在某個做驗證的解析器上浮現，而不是在本地。相關地，若某個已儲存金鑰的位元組無法以它被歸檔的演算法載入，簽章時會跳過它並記錄警告，而不是拿它去簽。

- **受管區域的權威判定過去要求區域**頂點**有記錄。** 一個記錄全都位於子網域的區域——例如有 `www.example.com` 而 `example.com` 上什麼都沒有，也就是區域的正常樣貌——永遠不會變成權威，於是它的落空查詢被轉送到上游，而內部的表述也就不再優先。現在區域內任何位置的記錄都算數，而 `remove_records` 會在該區域及其之下什麼都不剩時把 `managed_zones` 快取剪除，因此信任這份快取是安全的（過期的項目並非無害——它會持續為一個已經不存在的區域回答 NXDOMAIN）。反向樹被排除在外：最後兩個標籤的啟發式永遠會推導出 `in-addr.arpa.` 本身，所以一筆儲存的 PTR 就會宣告整棵全球反向樹——而在 `dns.auto_ptr` 開啟時，單單一筆 A 記錄就會產生那筆 PTR。`home.arpa`（RFC 8375）則刻意**不**排除，因為那套啟發式在那裡是對的，而把落空查詢轉送出去正是 RFC 8375 §4 所禁止的。

- **區域後綴比對在三個地方用了裸 `ends_with`**，把 `notexample.com` 算進 `example.com` 裡——分別發生在憑證列表、範圍內權威判定與區域簽章。已提取為 `db::name_in_zone`，改以標籤邊界比對。

- **`SetDns64Config` 是個樁函式**，它只把參數記錄下來、什麼都不儲存、然後回報成功；而 `GetDns64Config` 回傳的是寫死的預設值。現在啟用旗標與前綴會被獨立儲存，因此停用合成不會丟棄已設定的前綴，而一個剖析不出來的前綴會被拒絕而不是被替換掉。

- **`GetTtlDriftConfig` 無法往返。** 以 `1h30m` 設定的調整值讀回來是 `5400s`。那個回覆正是維運人員用來確認自己設定了什麼的東西，而讀取－修改－寫回的自動化也需要它能往返，所以現在它會以設定時所用的同一種複合寫法格式化（`ttl_drift::format_duration_secs`，剖析器的反函式）。

- **`add-scope-rbl --enabled` 形同虛設。** 作為帶 `default_value = "true"` 的裸 `bool`，clap 會給這個欄位 `SetTrue` 行為；clap 4.6 會套用該預設值，所以這個旗標永遠表達不了 `false`，而一個被停用的逐範圍供應商也就無從設定。現在它接受一個值（`--enabled false`），並且在兩個方向上都被釘住，因為這個旗標在兩個方向上都曾經是錯的。

### 變更

- 文件與範例中移除了 `dnsbl.sorbs.net`——SORBS 已經關閉。

### 測試

- **`tests/signed_hierarchy/mod.rs`**——一套在真實 UDP socket 之上、經過 DNSSEC 簽章的模擬階層：已簽章的根 → 已簽章的 TLD → 已簽章的區域，每一層持有自己的 Ed25519 金鑰、發佈一組 DNSKEY RRset，並為每個已簽章的子節點交付一筆 DS。`tests/mock_hierarchy` 證明的是查詢**次數**，而這一套證明的是**判定**，因為幾乎每一種把 DNSSEC 做錯的方式，仍然會回傳正確的記錄——一個略過過期檢查、或相信未簽章 NSEC、或接受任何簽章者名稱的驗證器，會把整個網際網路解析得好好的，直到有人來攻擊它為止。它的 `Tamper` 列舉會在回應**被序列化**時、也就是區域已被正確建構之後，才施加每一種攻擊，因此每個測試都是「一個有效的部署，遭到攻擊」，而不是「一個無效的部署，被拒絕」。

  `tests/dnssec_validation_test.rs` 涵蓋那些必須持續正常運作的路徑（完整的鏈驗證為 Secure、由 NSEC 證明的非安全委派、經證明的 NXDOMAIN 與 NODATA、金鑰快取讓已預熱的區域省下對根的第二次查詢、不做驗證的解析器回報 Insecure 而非 Secure）；`tests/security_dnssec_test.rs` 涵蓋那些攻擊（被剝除的簽章、既無 DS 也無其不存在證明的委派、已過期與尚未生效的簽章、來自未發佈金鑰的簽章、外來的簽章者名稱、簽章後被竄改的資料、未經證明的否定回應、對不上任何根金鑰的信任錨點、格式錯誤的錨點）。兩者同等重要，且理由相同：一個什麼都拒絕的驗證器能通過所有攻擊測試，一個什麼都接受的驗證器能通過所有正常路徑測試。

- **`tests/dnssec_signing_test.rs`** 釘住的是簽章**可被檢查**，而不只是存在——核心測試會從**已發佈的 DNSKEY RRset** 重新推導出簽章輸入，絕不從私鑰資料列，因為驗證器手上只有 DNSKEY。

- **`tests/blocklist_nxdomain_test.rs`** 以維運人員的方式驅動封鎖清單契約：先做一次 gRPC 變更，再透過真實的 UDP 或 TCP socket 發出查詢。五份清單、兩道關卡、兩條程式碼路徑，每一項都有自己的對照組——一道什麼都封鎖的關卡滿足契約的一半，一道什麼都不封鎖的關卡滿足另一半。

- **`tests/rbl_refusal_test.rs`** 以真實 UDP DNS 對著一個模擬封鎖清單區域驅動拒答碼，經過轉送器後備與分類邏輯一路進到 `handle_query`，因為中間的每一層都是「列入／拒答」的區分可能遺失的地方。每個測試都配有一個對照組：一筆貨真價實的 `127.0.0.2` 列入沿著完全相同的路徑走，仍然必須回傳 NXDOMAIN。

- **`tests/doq_test.rs`、`tests/proxy_test.rs`、`tests/tls_reload_test.rs`** 涵蓋三個先前只有編譯冒煙測試或設定剖析單元測試的介面：一個真正的 `quinn` 用戶端對上 `serve_doq`；會**剖析**伺服器所送內容而不是回以罐頭回應的模擬 HTTP CONNECT、SOCKS5 與 DoH 代理（並釘住「連不上的代理**不會**退回直連」）；以及透過真實 TLS 交握觀察到的 `TlsManager::reload()`。

- **`tests/zonemd_test.rs`** 逐欄位釘住 RFC 8976 的 RDATA 佈局，而且是手寫出來的而不是從編碼器取得——拿編碼器跟自己比對只會把錯誤蓋章認可。

- **`tests/acme_admin_test.rs`** 以性質而非 `success` 旗標來涵蓋那五個管理 RPC：`EnsureZoneCa` 的冪等性（重新鑄造會弄壞每一張已經鏈到舊中繼憑證的憑證，以及隨之而來的已發佈 DANE-TA 記錄）、同一個共用根之下的逐區域中繼憑證、EAB 的範圍限定與金鑰往返、誠實的移除行為，以及以標籤邊界進行的憑證列表。

- **`tests/cli_integration_test.rs`** 現在會走訪每個子指令的 `--help`。clap 在建構剖析器時就會驗證短選項，並在重複時**panic**，所以一個重用了全域選項字母的子指令會在讀到任何一個參數之前就中止——`generate-dnssec-key`（`-a`／`--algorithm`）與 `set-ttl-drift`（`-a`／`--adjustment`）就是這樣雙雙與全域的 `--address` 相撞，變成根本無法執行。現在兩者都只保留長選項。

## v0.4.3（2026-08-07）

### 安全性

- **Do53 轉送器接受任何抵達的資料包。** `forward_udp` 送出查詢後就把回來的任何東西當成答案：交易 ID 從未被比對、池中的 socket 未經 connect 因此核心會遞送來自**任何**來源的資料包、回應中的問題從未與查詢比對，而唯一存在的那道檢查——0x20 QNAME 大小寫——只發出 `warn!` 然後照樣回傳該回應。`security.qname_case_randomization` 預設開啟，且文件記載它是快取汙染的防護；它其實什麼都沒強制。由於那 8 個池中 socket 綁定一次後就在整個行程生命週期內重複使用，它們的來源連接埠只要被觀察一次就固定了，因此路徑外的汙染攻擊根本不需要猜測——而 `cache_from_wire` 接著會把偽造的內容持久化。

  現在一個回覆必須來自被查詢的位址、帶有送出時的交易 ID，並回答同一個問題——名稱、型別與類別，且以**大小寫敏感**方式比對，這才終於讓 0x20 發揮它的作用——才會被接受。被拒絕的資料包會被丟棄，並且仍會等到期限為止以取得真正的答案，而不是由第一個封包決定這次查詢：這既能避免單一注入封包否決掉查找，也能把池中 socket 上遲到的、屬於較早查詢的回覆排掉。

  維運上的影響：一個不會逐字回送問題的上游解析器現在會失敗，而不是悄悄解析成功。行為良好的解析器會依規定回送，但如果你指向的是一個會把大小寫正規化的解析器，請為它關掉 `security.qname_case_randomization`。經由代理（`proxy.mode`）的轉送路徑不受影響——它的回應是透過一條到已知代理的 TCP 連線抵達的。

- **迭代解析器接受來自任何來源、回答任何問題的回覆。** `query_one` 綁定了它的 UDP socket 卻從未 connect，所以核心會遞送來自**任何**對端的資料包——那道通常會迫使路徑外攻擊者必須偽冒權威伺服器 IP 的來源位址檢查根本不存在，只剩下交易 ID 與臨時連接埠。另外，`validate_question` 只比對回應問題的**名稱**而不比對其他，所以一個回答了解析器從未問過的問題的回覆——不同的記錄型別，或不同的 DNS 類別——會通過驗證，而它的記錄會被採納、快取並提供出去。

  現在查詢用的 socket 會在查詢送出前先 connect 到該名稱伺服器，這在核心中恢復了來源檢查且不增加每封包成本；而 `validate_question` 會比對完整的問題：名稱（大小寫不敏感，因為解析器不做 0x20 隨機化）、型別與類別。這條路徑本來就是兩條上游路徑中防護較好的——它確實檢查了交易 ID——但現在兩個缺口在 UDP 與 TCP 上都補上了。

- **迭代解析器接受超出管轄範圍的轉介與黏合記錄。** `classify` 從權威段中排在最前面的那筆 NS 記錄的擁有者名稱取得轉介區域，從不去對照正在解析的名稱或作答的區域，而 `walk` 直接把它寫進委派快取。任何解析器曾經對話過的名稱伺服器——一個廣告網域、一個連結——都能在回答關於它自己區域的查詢時附上 `AUTHORITY: com. NS ns.attacker.example` 加上黏合記錄，而 `best_match` 會走訪後綴，於是往後每一次 `.com` 查找都從攻擊者的伺服器開始。若某個轉介的 TTL 超過 `resolution.delegation_persist_min_ttl`，它會被寫進 `delegation_cache` 資料表並在開機時重新載入，於是這場劫持能活過重啟。`.` 的情況一模一樣。另外，`cache_glue` 把附加段的位址以它們自己的擁有者名稱快取起來，所以同一個回應還能為一個外來的名稱伺服器主機名稱植入位址。

  現在只有當一個轉介**嚴格往下**離開作答的區域**並且**涵蓋正在解析的名稱時（`referral_in_bailiwick`），它才會被跟隨與快取；其他情況會讓這次查找失敗，而不是被默默忽略，因此一個惡意委派無法偽裝成進展。黏合記錄被過濾到**作答**區域之內的名稱——刻意不是被委派的那個區域，因為一個關於 `com.` 的根轉介理所當然會帶著 `a.gtld-servers.net.` 的黏合記錄，它在 `com.` 之外，卻穩穩地在 `.` 之內。被丟棄的數量由 `rolodex_dns_resolver_out_of_bailiwick_total` 計數。

- **DHCP 主機名稱未經驗證就被插進 DNS 名稱。** `register_dns_hostname` 用 `format!("{}.lan.{}.", hostname, tld)` 從 DHCP 選項 12 建出記錄名稱——那是由區網上任何未認證裝置逐字提供的。一個把自己命名為 `*` 的用戶端會讓 `*.lan.<tld>.` 被註冊進去，而 `make_wildcard_name` 讓那成為一個真正的萬用字元：`lookup_scoped` 在精確比對落空時就會退回到它，於是一台筆電替它所在範圍中每一個未註冊的名稱作答。點號讓用戶端能把自己放到一個它從未被分配到的深度，而且沒有任何長度檢查，所以超過 63 位元組線路限制的標籤會進到資料庫，並在稍後序列化時失敗，而不是在抵達的當下一次擋掉。

  現在主機名稱會被驗證為單一 LDH 標籤（RFC 1123 §2.1）——1 至 63 位元組、字母／數字／連字號、開頭與結尾不得為連字號——並轉為小寫，且是**被拒絕而非被清洗**，所以一個送出了規則不允許之名稱的用戶端，不會被悄悄指派成另一個名稱。註冊會被跳過並記錄警告；註銷會執行同一套驗證，因此被移除的名稱就是當初被加入的名稱。

- **DNS-over-TCP 與 DoT 連線既無上限也無計時。** `serve_tcp` 與 `serve_dot` 為每條連線生成一個任務且沒有上限，而兩者的讀取迴圈都在沒有逾時的情況下 await `read_exact`，因此一個連上來卻什麼都不送的用戶端——或送半個長度前綴、或宣告了 65535 位元組卻從不交付的用戶端——會無限期地佔住一個任務與一個檔案描述子。`dns.bind` 預設是 `0.0.0.0:53`：在可路由的介面上，那就是一個認證前的遠端資源耗盡問題，而一旦描述子耗盡，`accept` 對每一個正當用戶端也都會失敗。DoT 更糟：`acceptor.accept()` 同樣沒有計時，所以一個裸的 `connect()`、完全不需要任何 TLS 實作，就能在任何 DNS 被交換之前佔住一個任務，而那個位置是任何針對 DNS 讀取迴圈的修正都構不到的。

  現在兩個監聽器都會在訊息之間套用 10 秒閒置逾時、對已宣告訊息的主體套用 5 秒期限，並為每個監聽器設下 1024 條連線的上限；DoT 另加 10 秒的 TLS 交握逾時。閒置逾時是從最後一次活動起算，因此 RFC 7766 的連線重用仍然可行——這在 DoT 上更重要，因為重新連線要付出一次全新的交握代價。DoQ 本來就有 `max_idle_timeout`，維持不變。

- **gRPC 共用密鑰是用 `==` 比對的，且失敗不受節流。** `String: PartialEq` 先比長度，再交給 `memcmp`，而 `memcmp` 在第一個相異位元組處就返回——所以所花的時間洩漏了呼叫方猜對了密鑰前面幾個位元組，把對整個密鑰的搜尋變成逐位元組的搜尋。另外，沒有任何東西限制失敗嘗試：一個連得到 gRPC 連接埠的用戶端，能以它開得動連線的速度不斷猜測，而這正是讓一個弱的共用密鑰從「不明智」升級為「致命」的原因。

  現在比對使用 `subtle::ConstantTimeEq`。（`ring` 的 `verify_slices_are_equal` 已被棄用，且文件註明僅供內部使用、不對側通道做任何承諾，所以本來就已在依賴樹中的 `subtle` 現在成為直接依賴。）認證失敗會**依來源位址**節流：連續 5 次失敗會把該來源鎖定 30 秒，每次連續鎖定加倍直到 15 分鐘的上限；而在鎖定期間，每一次嘗試都會被以 `ResourceExhausted` 拒絕，且**完全不去比對權杖**，因此鎖定本身不會變成「剛剛那次猜對了嗎？」的預言機。

  它以來源為鍵而非全域，因此一個攻擊者無法把維運人員鎖在自己的管理平面之外；計數器算的是**失敗**而不是請求，且一次成功會清除該來源的歷史紀錄，所以迴圈呼叫的正當自動化永遠不會被節流；一連串失敗若安靜 5 分鐘就會重置，所以偶爾打錯一次權杖絕不會累積起來。這張表上限為 65536 個來源——超過上限時會剪除閒置且未鎖定的項目，再多的新來源則不予追蹤，而不是讓一場分散式洪水把它無界地撐大。

- **gRPC Unix socket 對全世界都可連。** Unix 連線完全跳過認證——那是刻意的，而這也讓 socket 的檔案模式成為管理平面上**唯一**的存取控制。`main.rs` 做的是裸的 `UnixListener::bind`，從未對結果 chmod，所以它落在 umask 之下、通常是 `0755`，於是這台機器上的每一個本地使用者都握有未認證的管理權限：改寫任何 DNS 記錄、鑄造 EAB 憑據、建立區域憑證機構。現在這個 socket 以 `0660` 建立——先綁定在一個暫時的同層路徑上、限制權限、再改名到位，因為在已發佈路徑上「先綁定後 chmod」會留下一個 socket 已存在且全世界可連的空窗，而原子性的改名保留同一個 inode，所以監聽器不受影響。用 `0660` 而不是 `0600`，是為了讓部署可以透過對 socket 執行 chgrp 來授權給一個專屬的管理群組。

- **資料庫對全世界可讀。** 它就是金鑰庫：根憑證機構的私鑰、每個區域的中繼憑證金鑰、DNSSEC 私鑰與 EAB HMAC 密鑰，都是它裡面的普通資料列。沒有任何地方呼叫 `set_permissions`，所以 SQLite 在裸 umask 之下建立它——常見預設下是 `0644`——而任何本地使用者都能讀出根憑證機構的金鑰，並為任何名稱偽造一張每個已註冊用戶端都信任的憑證。現在 `Database::open` 會把它限制為 `0600`，而且是在啟用 WAL **之前**做，因為 SQLite 會把主檔的模式複製到它建立的 `-wal`／`-shm` 附屬檔上；既有的附屬檔也會一併收緊。

- **一個可路由的 gRPC 綁定搭配沒有共用密鑰的組合會靜默啟動。** 空的 `grpc.shared_secret` 會讓 `check_auth` 對每一個 TCP RPC 提早回傳 `Ok(())`。那在 loopback 上是文件記載的開發設定，在其他任何位址上則是管理平面的徹底暴露——而伺服器會看起來健康地啟動，日誌裡也沒有任何異狀。這個組合現在會在啟動時被拒絕（`config::check_grpc_exposure`）：loopback 的情況仍會啟動，`0.0.0.0` 與 `::` 正確地不算 loopback，而一個 `interface:port` 綁定只要該介面上有單一個可路由位址就會被判定不合格。

- **預設部署就是一台開放遞迴解析器。** `dns.bind` 預設為 `0.0.0.0:53`，而來源分類把 `security.overlay_cidrs` 之外的一切都當成受信任的本地用戶端，所以在可路由的介面上，網際網路上的每一台主機都取得完整的遞迴服務。那就是經典的反射／放大攻擊資產——一個小的偽冒查詢會回傳一個大的答案打向被偽冒的受害者，而對外的解析流量算在這台機器頭上——而且沒有任何速率限制，所以洪水來得多快就服務得多快。

  新的 `security.recursion_cidrs` 決定誰可以驅動**上游**解析，預設為每一個從網際網路不可路由的範圍：loopback、RFC 1918、link-local、ULA 與 CGNAT。那涵蓋了區域網路、容器橋接與 WireGuard 疊加網路（`10.64.0.0/10` 在 `10.0.0.0/8` 之內），因此任何正當使用這台伺服器的東西都不會失去服務。它與 `overlay_cidrs` 是分開的一個軸向，後者決定的是誰會被**強制套用範圍**。

  這道檢查位於本地／遠端的邊界上——在所有「從這台伺服器持有的資料作答」的路徑之後，在所有「去取得它沒有的資料」的路徑之前——因此一個陌生人仍然拿得到這台伺服器的權威答案與 NXDOMAIN，卻沒辦法讓它代為解析網際網路。它在回應快取**之前**執行，因為一個被快取的答案放大效果跟新鮮的一樣好，而把快取預熱正是這種攻擊的準備手法；而拒絕的形式是 REFUSED 搭配空的答案段，它永遠不會比引發它的問題更大。DoH 現在會帶著連線資訊提供服務，讓它的對端位址能進到分類邏輯；沒有那一步，`:443` 會把 `:53` 關上的東西重新打開。

- **IPv4-mapped 的來源位址繞過了範圍強制。** 來源分類是完全按照 socket 回報的位址去測試的，而 `IpCidr::contains` 刻意不跨位址族比對。在雙協議堆疊監聽器上——`[::]:53`，一種文件記載的綁定形式，也是 Linux 在 `net.ipv6.bindv6only=0` 下的預設——一個 IPv4 的疊加網路對等節點會以 `::ffff:10.64.0.1` 抵達，那是一個 `IpAddr::V6`，所以 `is_overlay_peer` 回傳 false，而該對等節點被歸類為**受信任的本機來源**。分割視域的兩半都因此壞掉：一個沒有加入任何網路的疊加對等節點不再被 REFUSED，並進到了它本應被分隔開來的全域命名空間；而一個**確實**呼叫過 `JoinNetwork` 的對等節點會悄悄失去它的範圍，因為那筆關聯是以純 IPv4 形式儲存的。一個 WireGuard 對等節點究竟會不會被強制套用範圍，最後取決於監聽器碰巧是怎麼綁的。

  現在 `handle_query_on` 會在任何分類發生之前，先把來源位址（以及本地監聽器 IP）正規化一次，因此一個位址的 mapped 寫法與純寫法在下游所有地方都是同一個位址——CIDR 測試、關聯查找，以及入口監聽器比對。IPv4-compatible 位址（`::1.2.3.4`，已棄用）刻意不折疊；只折疊真正的 IPv4-mapped 位址。

- **註冊入口網站會成為網際網路上任何區域的憑證機構。** `create_account_inner` 逐字接受 `zone` 字串、對它呼叫 `ensure_zone_intermediate`，並鑄造一個以它為範圍的 EAB。沒有任何東西把那個區域與這台伺服器實際管理的東西綁在一起，所以一個連得上的用戶端可以為 `windowsupdate.com` 建立一張中繼憑證——以 DANE-TA 記錄發佈進本地 DNS，並鏈到每個已註冊用戶端都信任的那個根——然後據以簽發憑證。「任何連得上的人都可以註冊」是刻意的設計決策且維持不變；它從來就不該意味著「可以成為整個命名空間的憑證機構」。註冊現在被限制在這台伺服器與之有關係的區域上：某個範圍以 TLD 形式擁有它（這涵蓋了範圍隱含的 `.home` 網域）、它在本地資料庫中有記錄、它是一個已宣告的權威區域，或它已經因為維運人員執行過 `EnsureZoneCa` 而有了中繼憑證機構。每一項都是後綴比對，所以受管區域的子區域也能註冊，而 `acme.issuance_scope: any` 會完全解除這項限制——那也正是這個設定對簽發者本來就代表的意思。

- **入口網站對「路過式 CSRF」門戶洞開。** `POST /api/account` 把主體讀成位元組並剖析 JSON，既不要求 JSON 內容型別，也不檢查 `Origin`。一個帶 `Content-Type: text/plain` 的跨來源表單 POST 是 CORS 的**簡單請求**——不會有預檢，所以瀏覽器會送出它，副作用也就落地了。攻擊者讀不回被鑄造的 EAB，但憑證機構的建立與 DNS 發佈照樣發生：任何區網使用者瀏覽器中的一個頁面，就能重塑本地的 PKI。現在這個端點要求 `application/json`（這會強制一次入口網站不予回應的預檢），並拒絕任何不是這台伺服器的 `Origin`，比對是以 authority 進行的，因此終止 TLS 的反向代理仍然可用。瀏覽器擴充功能的來源被豁免——隨附的擴充功能是一級用戶端，而它的來源只有在使用者授予主機權限之後才會被附上。非瀏覽器用戶端（CLI、`js/` 的入口網站用戶端、本地 UI 代理）不送 `Origin`，因此不受影響。

- **ACME 簽發者會把 CSR 要求的任何名稱都簽下去。** `ca::issue_leaf` 把用戶端的 CSR 直接交給 rcgen，而 rcgen 會把它的 `dNSName` SAN 與主體複製到簽發出的憑證裡。沒有任何東西把它們與該訂單實際驗證過的識別碼做比對，所以對某一個名稱的控制權證明，可以換到**任何**名稱的憑證——包括另一個租戶區域裡的名稱——由一張鏈到每個已註冊用戶端都信任的 Rolodex 根的中繼憑證簽發，並被當作 DANE-TA 錨點發佈。現在 `issue_leaf` 會接收該訂單已驗證的識別碼，並拒絕任何要求超出範圍的 CSR（`badCSR`）。主體通用名稱也會被檢查，但只在它讀起來像主機名稱時，因為 rcgen 會把 `"rcgen self signed cert"` 蓋進任何沒有明確 DN 就建出來的 CSR。

- **ACME 端點認證了呼叫者，卻從不授權他們。** `order`、`authz`、`challenge`、`finalize` 與 `cert` 都以 id 查出物件，卻從不檢查它是否屬於發出請求的帳號（RFC 8555 §7.4）。把別人帳號中已就緒的訂單 finalize 掉，會拿到一張受害者名稱、由攻擊者持有金鑰的憑證；而憑證是以連續的 rowid 定址的，所以任何帳號都能列舉並下載這個憑證機構簽發過的每一張憑證。這五個現在都會驗證所有權，憑證路徑則透過簽發它的訂單來驗證。

- **`url` 綁定是選用的。** `verify_request` 只在 JWS 受保護標頭中的 `url` 存在時才檢查它，所以省略它就等於這個請求沒有綁定到任何端點，並容許跨端點重放。現在它是必填的（RFC 8555 §6.4）。

- **EAB 憑據可以永久重複使用。** `used` 欄位由 `mark_eab_used` 寫入卻從未被讀取，所以一份外洩的註冊憑據可以在它的區域上註冊無限多個帳號。現在它會被強制執行。

- **訂單與授權的到期從未被檢查。** `expires_at` 被儲存了卻被忽略，所以一次執行過的驗證可以無限期有效——遠在維運人員移除那筆挑戰記錄或失去那個名稱之後仍然有效。現在 `finalize` 與挑戰回應路徑會強制執行它。

- **防重放的 nonce 無界地累積。** 每一個回應都會鑄造並持久化一個 nonce，包括未認證的 `GET /acme/directory`，而只有被用掉的才會被移除——一個未認證的遠端用戶端可以把這張表撐大到磁碟塞滿，並在 DNS 熱路徑所使用的那個互斥鎖上造成爭用。現在 nonce 會在一小時後過期，**而且**這張表被限制在 1024 個未使用項目，並以最舊優先淘汰。光靠 TTL 是限制不住的：一秒之內的一場洪水完全落在那個時間窗之內。

- **`revoke-cert` 在沒有撤銷的情況下回報成功。** 它在驗證 JWS 之後回傳 `200 OK`，告訴用戶端一張已遭破解的憑證已經作廢，而其實什麼都沒變。現在它會回傳 `501`，直到撤銷功能實作為止。

### 功能

- **Prometheus 指標。** 一個選用的 `metrics` 設定區段會啟動一個純 HTTP 的 `/metrics` 端點（預設 `127.0.0.1:9153`），輸出橫跨整台伺服器的 66 個指標系列：依傳輸方式／rcode／型別分類的查詢、逐傳輸方式的端到端延遲直方圖、訊息大小直方圖、回應快取的命中／未命中／過期／清空帳目、封鎖清單封鎖數與供應商查詢結果、`auto` 層級鏈（目前層級、逐層級的嘗試／勝出／失敗、已提交的切換、復原探測、逐上游的交換次數）、迭代解析器的內部狀態（轉介、CNAME 跳數、額度耗盡、TCP 重試、根伺服器預熱、逐名稱伺服器的 EMA 延遲）、分割視域狀態（記錄、範圍、關聯、專屬 TLD、入口監聽器）、位址族抑制、依狀態分類的 DHCP 訊息與租約、ACME 簽發與 dns-01 驗證，以及逐方法的 gRPC 呼叫次數。

  這個區段**預設不存在**，所以升級不會開出新的連接埠；預設綁定在 loopback，因為這個端點不做認證。它只承載彙總計數——沒有查詢名稱，沒有記錄值。

  這個登錄表是手工打造的（`src/metrics.rs`），建立在伺服器其餘部分本來就在用的同一批無鎖原語上，**不新增任何依賴**：熱路徑上的一次計數器遞增，是對一個預先配置好的系列做一次 relaxed `fetch_add`，而文字輸出格式是直接寫出來的。每個標籤維度要嘛是固定列舉、要嘛由設定所限制——唯一可能被用戶端撐爆的那個，也就是查詢型別，會把無法辨識的型別折疊進 `OTHER`，因此一場 `TYPE4242` 查詢的洪水無法產生無界的系列。

  `rolodex_dns_answers_total{source}` 指標回報的是解析順序中的哪個階段作答——快取、本地記錄、範圍、LAN→範圍後備、TLD 對等節點、封鎖清單、RBL、DNS64、上游、權威 NXDOMAIN、被拒絕——這正是讓分割視域管線從外面看得懂的關鍵。它以一個預設值為「上游路徑」的標記，貫穿 `resolve_query` 的約三十個出口，並記錄在一個由每個傳輸方式共用的、受檢測的單一出口上，因此新增的提早返回不可能悄悄逃出指標之外。

- **DNSBL 允許清單——特定主機可以被排除在封鎖清單檢查之外。** 一個對某個名稱誤判的封鎖清單供應商，過去讓維運人員除了停用該供應商或整個 DNSBL 之外別無他法。位於允許清單上的名稱（儲存在 `dnsbl_allowlist` 資料表中，附帶一個理由）會完全跳過解析的步驟 7。

  一個項目是**以標籤邊界進行後綴比對**的，所以把 `example.com` 加進允許清單也會豁免 `www.example.com`，但不會豁免 `notexample.com`；而且它是正規化儲存的（小寫、結尾帶點），因此任何寫法都會新增或移除同一個項目。這道檢查會整個短路掉以名稱為基礎的那一步——不管是已設定的 DNSBL 供應商還是本地 RBL 封鎖清單，都封鎖不了一個被豁免的名稱——而且它在供應商查詢**之前**執行，所以一個被豁免的名稱根本不會發出任何封鎖清單查詢。反向 DNS 的 IP 封鎖不受影響。查找是對一個從資料表鏡射而來、並在開機時重新載入的 `DashSet` 做 O(標籤數) 的操作，所以在沒有設定任何允許清單時，熱路徑的代價只是一次空集合檢查。

  透過 `AddDnsblAllowlistEntry`／`RemoveDnsblAllowlistEntry`／`ListDnsblAllowlistEntries`（gRPC 與 Go 用戶端），或 CLI 上的 `add-dnsbl-allow`／`remove-dnsbl-allow`／`list-dnsbl-allow` 管理。

## v0.4.2（2026-08-06）

### 錯誤修正

- **上游回應快取幾乎完全是個空操作。** 兩個彼此獨立的鍵值錯誤，使得答案被寫在任何查找都重現不出來的鍵之下，於是幾乎每一次查詢都要付出完整的上游往返代價，無論同一個名稱多久之前才剛解析過。

  `cache_answers` 是以第一筆**答案記錄**而不是以問題來作為每個項目的鍵。對任何位於 CNAME 之後的名稱來說那都是錯的：`index.crates.io A` 回來的是一條鏈——`index.crates.io CNAME` → `fastly-index.crates.io CNAME` → 第三個名稱上的 A 記錄——它的第一筆記錄是 CNAME，所以項目落在 `index.crates.io.:CNAME` 之下，而每一次查找問的都是 `index.crates.io.:A`。這兩個鍵永遠碰不到，所以那個名稱**永久**無法快取。那涵蓋了 CDN 前置的網際網路的大部分（Fastly、CloudFront、S3）。它之所以沒被注意到，是因為像 `example.com` 這樣的名稱會用它自己的 A 記錄作答，讓錯誤的鍵碰巧變成正確的——而每一個測試用的都是那種形狀的名稱。

  `cache_key` 則另外信任呼叫方已經把名稱正規化了。快取的兩側看到的大小寫並不相同：讀取用的是用戶端送來的大小寫，寫入用的是問題回來時的大小寫，而在 0x20 編碼下（`security.qname_case_randomization`，預設開啟）一個轉送出去的查詢會以 `eXaMpLe.CoM` 送出，回應也照樣回送。因此每一個上游答案都被歸檔在一個隨機大小寫的鍵之下，這等於把快取整個停用了——是每一個名稱，而不只是有鏈的那些。

  現在兩條路徑都以問題為鍵，`question.name()` 加上問題的型別，那正是讀取路徑會去查的鍵，也是 `cache_negative` 本來就遵循的規則。整組答案，包含 CNAME 鏈在內，都存在那一個鍵之下。一個不帶問題的回應無法被正確地建鍵，因此現在它根本不會被快取，而不是被快取到一個沒人會去看的地方。`cache_key` 會在填入鍵的同時轉小寫，這不增加額外配置，因為那個 `String` 本來就要配置。

### 建置

- **兩種架構都在執行 `make` 的那台主機上交叉編譯；建置用的虛擬機沒了。** 多架構映像先前是在主機無法執行外來架構時，啟動一台 x86_64 QEMU 虛擬機（`make/amd64-vm.sh`）來建置的——用一整台虛擬機來代替一套工具鏈。現在發佈出去的兩個映像，只差在它們的目標三元組，而不是差在它們是怎麼被產生的。

  `cargo-zigbuild`（`make/cross.sh`）提供了 `rusqlite`（隨附的 SQLite 原始碼）與 `ring` 所需要的 C 交叉編譯器——光靠 `rustup target add` 會在 `cc` 那一步就死掉——並鏈結到一個釘住的 glibc（`GLIBC_VERSION`，預設 `2.36`，與 `debian:bookworm` 相符），因此無論建置主機自己的 glibc 是哪個版本，產出的執行檔都能在執行期的基礎映像上跑。整套工具鏈都不需要 root 就能安裝：`rustup target add`、`cargo install cargo-zigbuild`，以及解壓到 `.cache/zig/` 底下的一個 zig 壓縮檔，因此 `make deps` 可以在任何地方佈建它，而不必依賴各發行版各異的交叉 gcc 套件。

  執行期的 `Containerfile` 現在只做 `COPY`——複製交叉編譯出的執行檔與一份取自建置主機的 CA 憑證包。由於完全沒有 `RUN` 步驟，`podman build --platform linux/<arch>` 從來不需要**執行**任何目標架構的東西，因此一個外來架構的映像根本不需要任何使用者模式模擬。那才是真正把虛擬機拿掉，而不是把問題搬個位置：在像 Fedora Asahi 這樣的主機上，x86 模擬要經過 FEX + `binfmt-dispatcher` + `muvm`，在 `podman build` 的沙箱裡根本用不了。`Containerfile.build` 也隨之移除，而 `--network=host` 不再預設傳入，因為映像建置過程中已經沒有任何東西需要解析 DNS。

- `TARGET` 現在為**每一個**容器目標（`image`、`push-arch`、`push-rc`、`push-release`）選擇架構，比照 `install` 儲存庫所採用的模型，好讓同一個 `TARGET=` 值能在各個 town-os 儲存庫之間通用。開發板風味（`rpi`、`rg35xxpro`、`anbernic` 等）會解析到它們對應的架構；無法辨識的值在解析階段就是硬性錯誤。任何主機都能建置任何架構——`CROSS` 是推導出來的，不是使用者旋鈕。`push-rc-all`／`push-release-all` 可以從任一架構的單一主機發佈兩種架構並組出資訊清單。

## v0.4.1（2026-08-06）

### 效能

- **UDP 監聽器改為跨 `SO_REUSEPORT` socket 分片。** 單一 UDP socket 把整個監聽器序列化了：一個任務用 `recv_from` 把它抽乾，而每一個回覆也都從同一個 socket 送出去，所以接收是單執行緒的，而核心在兩個方向上都要拿一把逐 socket 的鎖。為每個查詢生成一個任務，把**處理**擴散到各核心，卻從未擴散那個 socket，於是吞吐量在機器閒置的情況下就打平了——在一台 16 核主機上，伺服器在約 104k qps 處到頂，而 CPU 用不到一核，超過那個點之後，增加用戶端並行度只會拉高延遲（32 個並行用戶端時 p99 為 1.70ms）而不會增加吞吐量。現在每個 UDP 監聽位址會把 `dns.udp_shards` 個 socket 綁到同一個 `addr:port`，每個都有自己的接收迴圈與自己用來回覆的 socket，並由核心把抵達的資料包雜湊分散到它們之間。在 16 核上針對一筆本地記錄實測，前 → 後：4 個並行用戶端時 68k → 101k qps、16 個時 99k → 168k、32 個時 104k → 257k（p99 1.70ms → 432µs）。舊路徑在 32 個用戶端時就已經打平；分片後的版本到 128 個時仍在爬升（477k qps），而伺服器只用了 16 核中的 2.87 核，所以剩下的天花板是負載產生器而不是伺服器。

  `SO_REUSEPORT` **只**在請求超過一個分片時才設定。Linux 只有在綁到某個連接埠的每一個 socket 都帶著這個選項時才會共享該埠，所以單分片的監聽器在忙碌的連接埠上仍然會相撞——而那正是入口綁定失敗處理所依賴的行為。無條件設定它，會讓一次失敗的入口綁定開始對一個屬於其他行程的連接埠「成功」，並悄悄分走它的一部分流量。分片由 `serve_udp` 這個 future 擁有（放在一個 `JoinSet` 裡）而不是由呼叫方擁有，所以 `stop_ingress_listener` 仍然能透過它那唯一的 abort handle 把它們全部拆掉；而綁定到連接埠 `0` 時會被強制為單一分片，因為核心會給每個 socket 一個**不同的**臨時連接埠，那樣沒有任何一致的東西可以共享。

### 設定

- `dns.udp_shards`（預設 `0`）——每個 UDP 監聽位址所綁定的 `SO_REUSEPORT` socket 數量。`0` 代表每個可用核心一個分片；`1` 恢復先前的單一 socket 監聽器。

### 文件

- 並行模型章節先前把 DNS UDP 查詢描述為「在單一任務上循序處理」，那自從查詢改為一個查詢一個任務之後就已經過時了；現在它描述的是分片扇出與上述三項限制。

## v0.4.0（2026-08-05）

### 變更

- **入口 DNS 監聽器現在解析整個命名空間，而不只是它自己的 TLD。** 範圍選擇過去是以被查詢的**名稱**為鍵：`ingress_target` 從 `find_tld_owner(qname)` 開始，所以一個不屬於任何專屬 TLD 的名稱會回傳 `None`，於是這次查詢就失去了它與抵達時所在監聽器的關聯。它接著落入來源 IP 分支，而在那裡，一個 WireGuard 對等節點位於 `security.overlay_cidrs` 之內卻從來沒有被 `JoinNetwork` 過（只有這台機器自己的入口位址會被加入，對等節點從來不會）——於是被當成未關聯的疊加對等節點而 REFUSED。因此那個監聽器只回答它自己的 TLD 而別無其他：網路上的一個用戶端解析得了 `gitea.default.fart`，卻對 `google.com` 得到 REFUSED，所以這個網路的 DNS 伺服器沒辦法解析它所服務的那個網際網路。

  現在範圍是從**監聽器**選出的（`db::scope_for_ingress_ip`，逐 TLD 入口映射的反向查找），而不是從名稱。一個抵達入口監聽器的查詢，無論名稱是什麼，都屬於該監聽器的擁有範圍：專屬 TLD 仍然保持分隔（兄弟網路的 TLD 仍然是權威 NXDOMAIN），而其他每一個名稱都落入全域解析與上游轉送。答案**改寫**成入口 IP 這件事仍然是依名稱把關的，因此一個穿透過去的名稱會保留它解析出來的值。入口監聽器之外的範圍強制維持不變。

### 文件

- `CLAUDE.md` 與 `README.md` 更新至 v0.3.x 所落地的解析器工作：`auto` 層級鏈、迭代解析器的伺服器選擇／退避／界限、委派與記錄快取（包含 `flush_cache` 與 `flush_upstream_state` 各自清掉什麼），TTL 語意，以及上面那些入口監聽器範圍規則。

## v0.3.3（2026-07-14）

### 錯誤修正

- **失敗的入口綁定不再讓它的 IP 在整個行程生命週期內作廢。** `spawn_ingress_listener` 無條件地、在兩個任務都還沒嘗試綁定之前就記下 UDP+TCP 的 abort handle，所以一個綁定失敗的監聽器會留下一個宣稱該位址已被服務的項目，而其實沒有任何東西在監聽——而那個只看「是否存在」的保護判斷，會讓往後每一次重新加入都在這具屍體上提早返回。這打中的正是常見情況：一個網路 TLD 的入口 IP 是 WireGuard 疊加位址，而 `sync_ingress_listeners` 會在啟動時從資料庫重放它，那時通道介面**還不存在**，於是兩個任務都以 `EADDRNOTAVAIL` 失敗並退出；一台在已設定好網路的情況下重開機的機器，就再也無法在那個疊加位址上提供 DNS 了。現在一個任務都已結束的項目會被視為不存在——丟掉並重新生成——因此重新加入確實會在介面起來之後重試綁定。

## v0.3.2（2026-07-11）

### 效能

- **迭代解析器的委派快取**——從根伺服器往下走訪時學到的「區域 → 名稱伺服器」委派現在會被快取，因此一個冷名稱不再每次查找都重走一遍根 → TLD。長壽的委派（根與 TLD 的 NS 集合帶有數天的 TTL）會被持久化到新的 `delegation_cache` 資料表，由 `resolution.delegation_persist_min_ttl`（預設 300 秒）把關，因此重啟後回來時是預熱過的。
- **整個遞迴過程都被快取，而不只是委派**——黏合記錄、無黏合記錄的 NS 名稱查找，以及 CNAME 跳轉，這些都帶著 TTL 抵達，先前用過一次就被丟棄。現在它們會以 `(name, type)` 為鍵保留，並以它們**剩餘的**壽命交還，因此一筆被提供出去的記錄絕不會在上游被以完整 TTL 重新快取。權威的 NXDOMAIN／NODATA 答案被快取在一個獨立的否定映射中，讓正面路徑得以繼續把「沒有記錄」當成未命中。
- **負載分散到各根伺服器之間**——伺服器選擇現在以 `hits * ema_latency` 評分，而不是純粹依 RTT 排序，後者會把每一次查詢都趕到單一最快的根伺服器上。這個乘積會朝向整個群組的相等靠攏，把查詢分配成 `hits ∝ 1/latency`：快的伺服器承擔較多，每一台健康的伺服器都承擔一些。一台未被查詢過的伺服器評分為 0，會被優先嘗試，並從一次本來就得發生的查詢中學到它的延遲——不需要預先量測，也不需要探索機率的分支。
- **根伺服器預熱**——實際的根 NS 集合會在啟動時取得一次，並以它真正的 TTL 快取為 `.` 的委派。寫死的 `ROOT_HINTS` 因此回到它們應有的角色：一個啟動引導，以及預熱失敗時的後備。

### 錯誤修正

- **失敗的上游伺服器現在真的會復原。** 失敗現在被追蹤為一個明確的指數退避（2 秒、加倍、上限 300 秒、首次成功時清除），而不是被折進延遲 EMA 當成一個 10 秒的合成 RTT。把它們折進去，會讓復原取決於健康的同儕碰巧有多快——面對一個 0.3ms 的同儕，10 秒的懲罰讓一台死掉的伺服器只分到三萬三千分之一的份額，於是它永遠不會被重試，也就永遠回不來。處於退避中的伺服器在同一位址族內會排在健康的伺服器之後，但絕不會被移除，因此即使所有東西都在失敗，解析仍會繼續。
- **DNS 快取的開機載入永遠載入 0 個項目**——它呼叫 `cache_lookup` 時給了空名稱；新的 `cache_load_all` 會執行它本來就該做的那個查詢。
- **重複的 `dns_cache` 資料列**——重新快取一個回應時是附加一列而不是更新它。既有的重複列會被合併，而一個唯一索引讓 `cache_insert` 變成 upsert。
- **`flush_cache` 不再丟棄委派。** 每一次 gRPC 記錄變更都會呼叫它，這等於在每一次本地記錄變動時都把上游狀態扔掉；層級切換改用獨立的 `flush_upstream_state`。

### 設定

- `resolution.delegation_persist_min_ttl`（預設 300）——一個委派要被跨重啟持久化所需的最低 TTL。
- `resolution.default_ttl`（預設 300）——在記錄或回應完全沒有提供自己的 TTL 時所套用的值（沒有 SOA 的 NXDOMAIN／NODATA、TTL 為零的委派或黏合記錄）。一個**確實存在**的 TTL 一律照原樣採用，包括 SOA 的否定 TTL，它從不被夾限。

## v0.3.1（2026-07-10）

### 新功能

- **LAN → 擁有範圍的解析後備**——一個受信任的本機來源（loopback／區域網路，未關聯任何範圍）現在會從擁有它的範圍解析**每一個**專屬網路 TLD，因此即使記錄是以範圍方式儲存的，所有網路 TLD 在區域網路上都看得到。這個後備在全域查找之後執行（一個雙棲名稱面向區域網路的全域記錄仍然勝出），並在上游轉送之前：僅存在於範圍中的名稱（例如某個網路的區域頂點）會以其儲存值從擁有範圍提供，接著會諮詢該 TLD 的對等轉送器，若全部落空則回傳一個**權威 NXDOMAIN**——一個私有擁有的 TLD 絕不會從區域網路被轉送到上游。疊加對等節點不受影響，仍然嚴格分隔（一個加入某個網路的對等節點只看得到自己的 TLD，對兄弟網路的 TLD 得到 NXDOMAIN），這使得一個範圍可以純粹為了**擁有**某個 TLD 而建立（與對等節點分隔、可從區域網路解析），而完全不需要為它綁定任何 WireGuard 疊加網路。

### 建置

- **即時的 `go test` 輸出**——`go-test` 與 `go-integration-test` 這兩個 Makefile 目標現在以本地目錄模式執行 `go test`（去掉 `./...` 套件樣式），讓詳細（`-v`）輸出在測試執行時串流出來，而不是逐套件緩衝到結束為止。所有 Go 測試都位於根部的 `go/` 套件中，因此涵蓋範圍不變。

## v0.3.0（2026-07-02）

### 新功能

- **具韌性的 `auto` 上游解析，並以 DoH 為首選的安全層級**——解析器現在遵循一條分層的後備鏈（根伺服器 → DoH/DoT → 本地轉送器 → 公用 :53）而不是只走根伺服器，因此在會過濾對外 :53（以及 DoT 的 :853）的網路上仍能持續解析。:443 上的 DoH 是首選。黏著的目前層級避免了在死掉的層級上逐查詢逾時，定期的復原探測會在某個層級回來時把它取回，層級切換被一段失敗寬限期把關，而每一次已提交的切換都會清空 DNS 快取作為跨層級的快取汙染防護。透過 `resolution` 區段設定（`mode = auto`、`secure_upstreams`、`public_fallback`、`switch_grace_failures`、`recovery_probe_secs`）。
- **位址族可路由性探測**——新的 `address_family` 設定區段與背景探測會定期以 TCP 連到 :443 上的公用 v4/v6 目標，測試真實的網際網路可達性。在 `auto` 模式（預設）下，伺服器會停止為主機構不到的那一族回傳 A 或 AAAA 答案，因此用戶端會退到可用的協議堆疊，而不是卡在一個死掉的位址族上。`off` 一律兩族都回答（舊行為）；`force4`／`force6` 則不做探測直接釘住一族。一個失敗門檻用來抑制抖動，復原則在首次成功時立即生效，而開機時的第一次探測是決定性的（沒有寬限），因此開機到一個死掉位址族的連線上時，從第一次查詢起就會抑制它。

### 錯誤修正

- **RBL/DNSBL 查找不再繞回本地存根。** 封鎖清單查詢（`<name>.<zone>`）先前是透過系統解析器送出的，而在典型主機上那會指回 rolodex 自己——重新進入查詢處理器、再被 DNSBL 檢查一次、再附加一次區域，然後永遠迴圈下去（`<name>.<zone>.<zone>…`），把所有解析都卡死。現在 `RecursiveRblResolver` 會用 rolodex 解析其他所有東西的同一種方式來解析封鎖清單名稱：在它自己的 socket 上從根伺服器開始遞迴（絕不走本地存根），並在根伺服器不可達時退回以 UDP 走已設定的轉送器。徹底失敗時失效放行（視為未列入）。
- **當對外 :53 被過濾時（由背景探測偵測），RBL/DNSBL 檢查會跳過由解析器支撐的供應商**，因此受過濾的網路不會拖住解析；由本地資料庫支撐的 RBL 項目不受影響。

### 效能

- **RBL/DNSBL 檢查改用射後不理的非同步快取填充**——封鎖清單的快取填充不再阻塞解析路徑。

### 基礎設施

- **可重現映像的保證**——`make/build.sh` 會計算 `SOURCE_REV`（git 短 HEAD，工作目錄有修改時附上 `+dirty`）並把它貫穿進兩個映像建置：建置階段在 `cargo build` 之前就引用它，因此變更了的提交一定會讓編譯層失效（不會從重用的快取層送出過期的執行檔），而執行期映像會蓋上 `org.opencontainers.image.revision`，因此推送出去的映像能透過 `skopeo inspect` 自我識別。

### 程式碼品質

- 實際連線的 DoH/DoT 上游測試現在是以可達性把關，而不是無條件忽略，因此在網路允許的情況下它真的會操練到安全層級。

## v0.2.4（2026-06-28）

### 新功能

- **DNSBL（網域封鎖清單）**——一套網域名稱封鎖機制，與以 IP 為基礎的 RBL 分開。DNSBL 供應商（例如 Spamhaus DBL、SURBL、URIBL）是以把被查找的名稱前置到區域之前（`<name>.<zone>`）來查詢的，相對於 RBL 所使用的反轉 IP 形式。DNSBL 的列入**優先於任何外部解析出來的答案**——轉送的、從根伺服器迭代的，或上游快取的——而本地記錄與受管／權威區域一律勝出。可在啟動時透過 `dnsbl` 設定區段設定，並在執行期透過新的 `SetDnsblConfig`/`GetDnsblConfig` gRPC 端點、`set-dnsbl-config`/`get-dnsbl-config` CLI 子指令，以及 Go 用戶端的 `SetDnsblConfig`/`GetDnsblConfig` 方法（`DnsblConfig`/`DnsblStatus` 型別）設定。
- **RBL/DNSBL 進入解析快取管線**——本地 RBL 與 DNSBL 檢查現在會在本地／區域解析之後、但在上游快取與轉送器之前，套用到正向網域名稱上，因此即使先前已快取了一個上游答案，被列入封鎖的名稱仍會被以 NXDOMAIN 拒絕。DNS 快取的「本地記錄優先」階段現在只提供權威的本地項目（透過 `lookup_local_only`）；上游快取的項目則在封鎖清單關卡之後才提供。

### 變更

- **RBL 與 DNSBL 的供應商清單現在預設為空。** 先前 RBL 隨附五個預設區域；在維運人員設定供應商之前（透過設定檔或 `Set{Rbl,Dnsbl}Config`），不會查詢任何外部封鎖清單。一個已啟用但為空的封鎖清單是空操作。

### 程式碼品質

- `make lint` 現在執行 `cargo clippy --all-targets`，除了函式庫之外也 lint 測試與 benches。修正了因此浮現的既有問題：benchmark 中橫跨 `await` 持有的 `RefCell` 借用、測試中對 `Copy` 型別的 clone、`assert!(true)` 佔位，以及設定測試中的 `field_reassign_with_default`。
- 新增涵蓋 DNSBL 解析與優先順序（包含優先於上游快取答案）、空封鎖清單的空操作行為，以及 gRPC/CLI/Go 可程式化端點的單元、整合、CLI 與 Go 測試。

## v0.2.3（2026-06-28）

### 新功能

- **從根伺服器開始的迭代解析**現在是預設的上游模式——查詢會從根伺服器開始遞迴解析，而先前的 `forward` 模式仍可選用。
- **自動反向 PTR 記錄**——選用的 `dns.auto_ptr` 讓透過 gRPC 管理介面新增的 A/AAAA 記錄自動維護一筆對應的 `in-addr.arpa`／`ip6.arpa` PTR 記錄。
- **ACME 簽發者／憑證機構**（RFC 8555，伺服器端）——Rolodex 自己就是憑證機構：一個自簽根簽署逐區域的中繼憑證，而中繼憑證透過 ACME 簽發終端憑證，並對照 Rolodex 自己的 DNS 資料進行 dns-01 驗證。包含 External Account Binding、自動的 DANE-TA TLSA 發佈、一個受信任網路的網頁註冊入口網站、一個瀏覽器擴充功能，以及一個具備 DANE 取得能力的 JavaScript 用戶端函式庫。
- **透過 DNS 散佈憑證機構**——根與中繼憑證鏈會被發佈進 DNS 資料庫，形式為 CERT（RFC 4398）與分塊的 TXT 記錄，因此任何解析得到該區域的用戶端都能取得並信任該憑證機構，而不需要存取入口網站。

### 基礎設施

- 原生的多架構（amd64/arm64）容器建置，發佈到 `quay.io/town/rolodex`，包含供 arm64 主機使用的 amd64 建置虛擬機，以及以 `uname -m` 為後綴的逐架構映像標籤。
- 統一的 DNS 綁定設定，具備 `primary`（自動偵測對外 IP）與 `interface:port`（綁定指定介面上的每一個位址）關鍵字。
- 在 `Cargo.toml` 中新增 `repository` 欄位。

## v0.2.0-alpha（2026-03-27）

### 新功能

- **DHCP 伺服器**，具備整合的 IPAM、自動的 DNS 主機名稱註冊（A + PTR 範圍內記錄）、黏著的 MAC 對 IP 綁定、背景租約清掃，以及完整的 DISCOVER/OFFER/REQUEST/ACK 流程
- **逐範圍的 RBL 供應商**——網路範圍可以在全域設定之外額外選用 RBL 供應商
- **DHCP 憑證交付**——透過站台專用的 DHCP 選項（代碼 224-254）把憑證交付給用戶端
- 上游 DNS 轉送的 **SOCKS5 代理**支援（RFC 1928），與既有的 HTTP CONNECT 及 DoH 代理模式並存
- DNS-over-HTTPS 的 **HTTP/3（QUIC）傳輸**，透過 `enable_h3` 設定旗標啟用
- **Criterion 效能基準測試**（`make bench`），涵蓋 QNAME 隨機化、快取操作、資料庫查找、區域比對、查詢管線，以及 UDP/TCP 往返

### 效能

- **原地 QNAME 大小寫隨機化**——直接在 DNS 線路格式的位元組上操作（切換 0x20 位元），而不是剖析、複製、重建並重新序列化整個 DNS 訊息；每次轉送查詢省下約 6 次配置
- **批次資料庫查找**——`lookup_with_fallbacks()` 把精確、萬用字元、CNAME 與 ANAME 查找合併成單一 SQL `UNION ALL` 查詢，把每次查詢的互斥鎖取得次數從 4 次以上降到 1 次
- **O(標籤數) 的區域比對**——`find_managed_zone()` 與 `find_authoritative_zone()` 會對一個 `DashSet` 走訪 DNS 標籤後綴，而不是用 `ends_with()` 做 O(區域數) 的線性迭代
- **以 Arc 包裝的快取記錄**——DNS 快取儲存 `Arc<Vec<DnsRecord>>`，以消除快取插入與本地快取命中時的複製
- **批次快取持久化**——有界的 `mpsc` 通道（容量 1024）搭配單一背景工作者，取代逐次插入的 `tokio::spawn`，每次最多抽取 64 筆寫入
- **最佳化的快取鍵**——預先配置大小的 `String::with_capacity`，且不做多餘的 `to_lowercase()`（名稱已正規化）
- **UDP 緩衝區大小**——接收緩衝區在迴圈外配置一次；複製時透過 `Vec::with_capacity` + `extend_from_slice` 依實際封包長度取大小
- **DoH 代理連線池**——透過逐代理位址的 `DashMap` 池（每個位址最多 8 條）與 HTTP/1.1 keep-alive 重用 TCP 連線

### 程式碼品質

- 消除了整個程式碼庫中所有的 `unwrap()` 呼叫、`let _ = expr;` 壓抑寫法與死碼
- 新增 `lint` Makefile 目標（`cargo fmt --check` + `cargo clippy -D warnings`）
- 在 lib.rs 與 main.rs 兩處以 crate 層級強制 `#![deny(dead_code)]` 與 `#![deny(unsafe_code)]`
- 40 個新的單元測試，涵蓋所有經過效能最佳化的程式碼路徑
- 全面更新的 CLAUDE.md 規格書，含所有新功能、效能模式與 benchmark 文件

### 基礎設施

- 容器映像切換到 `quay.io/town/rolodex`
- 所有依賴更新到最新的相容版本

## v0.1.0

首次發行。

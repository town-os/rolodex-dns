# Rolodex DNS 開発ルール

> 言語：[English](CLAUDE.md) ｜ [繁體中文](CLAUDE.zh-TW.md) ｜ [简体中文](CLAUDE.zh-CN.md) ｜ [Español (España)](CLAUDE.es-ES.md) ｜ [Español (México)](CLAUDE.es-MX.md) ｜ **日本語**

Rolodex DNS は、gRPC によるリモート管理を備えたスプリットホライズン DNS サーバー兼再帰／フォワーディングリゾルバーであり、Rust で書かれ、AGPL-3.0-only でライセンスされています。

このファイルは、それに手を入れるためのルールです。意図的に短く保たれています。**ソフトウェアが何をするか**は `DESIGN.md` にあり、挙動・アーキテクチャ・API 表面に関することは一切ここには置きません。

## どこに何が書かれているか

| ドキュメント | 内容 |
| ------------ | ---- |
| `DESIGN.md` | 機能仕様書：アーキテクチャ、解決順序、すべての管理面（gRPC、CLI、Go クライアント、JavaScript クライアント、メトリクス、設定）、テストスイートの設計、そしてビルドシステム。挙動を変更する前にこれを読むこと。 |
| `README.md` | 利用者向けリファレンス。PromQL クックブックを含む。 |
| `CONFIGURATION.md` | タスク指向の設定ガイド——具体的なデプロイ形態、再起動が必要なもの、症状別のトラブルシューティング。 |
| `CHANGELOG.md` | リリース履歴。 |
| `CLAUDE.md` | このファイル。開発ルールのみ。 |
| `TOWNOS_CONTRACT.md` | Town OS、インストールイメージ、ttyforce との境界を双方向に越えるすべて。`make check-townos-sync` が検証します。gRPC の面、フォワーダーの文法、あるいは複数のリポジトリに書かれたアドレスを変える前に読んでください。 |

この六つそれぞれに、繁体字中国語（`.zh-TW.md`）、簡体字中国語（`.zh-CN.md`）、欧州スペイン語（`.es-ES.md`）、メキシコスペイン語（`.es-MX.md`）、日本語（`.ja-JP.md`）の訳が並んでいます。**英語版が唯一の正典です**：まず英語版を変更し、訳文は後から追随させるべきものとして扱ってください。第二の編集場所ではありません。両者が一致していることを検証する仕組みはありません——`tests/promql_docs_test.rs` が読むのは英語版の `README.md` と `DESIGN.md` だけなので、訳文中の PromQL ブロックやファミリー数はドキュメントであって、検査されるアサーションではありません。

## ルール

- 指示がない限り make タスクを実行しないでください
- deny(dead_code) と deny(unsafe) が先頭にあり、かつ守られていることを確認すること
- すべての std::result::Result を適切に処理すること
- unwrap を使わないこと
- unsafe なコードを使わないこと
- **自分でテストを実行しないこと。** これは `make` に限った話ではありません：`cargo test`、`go test`、`npm test` その他あらゆる直接の呼び出しも同様に禁止で、一件に絞ってあるかどうかは関係ありません。バッテリーを走らせてログを返すのはオペレーターの役目です。`cargo check`、`cargo clippy`、`cargo fmt` は構いません——これらはコンパイルと静的検査だけで、何も実行しないからです
- 統合テストや実地テストも含め、すべてに対してテストを書くこと
- 変更の検証には make test を使うこと
- 統合テストがホストを改変してはならない、絶対に
- テスト：特に断りがない限り、シミュレートされた入力で動作し、実行されるはずの操作について出力を生成します。稼働中のシステムに影響を与えることは決してありません。
- テストの実行：毎回 make タスクを使うこと。
- テストには常に lint チェックを含めること
- lint チェックは Rust コミュニティ標準のリンター群とし、`lint` の make タスクとして実行すること
- 未使用変数の警告を抑えるためや借用チェッカーを回避するために `let _ = expr;` を使わないこと。実際の問題を直すこと：その変数を使う、引数を削除する、あるいはコードを組み直す。
- `#![deny(dead_code)]` と `#![deny(unsafe_code)]` は lib.rs と main.rs の両方でクレートレベルに設定されています。これらを回避するために `#[allow(dead_code)]` や `#[allow(unsafe_code)]` を追加しないこと——デッドコードは削除し、unsafe の代わりに安全な抽象（例：nix クレート）を使うこと。
- ハードウェアの設定を超えてシステムを変更しないこと
- 明示的に指示されない限り、git タグを削除・移動・変更しないこと

## 変更を検証する

`make test` がゲートであり、それを実行するのはオペレーターの役目です——上記のルールを参照。次の順で実行されます：`lint`（`translation-check`、`cargo fmt -- --check`、`cargo clippy --all-targets -- -D warnings`）、Go の統合テストとユニットテスト、`prometheus-test`、Rust の各統合テストファイルを明示的に、`cargo test`、そして JavaScript の lint／統合／ユニットテスト。`make test-log` は実行全体をタイムスタンプ付きのログファイルに記録するので、実行が長くなるときはそちらのほうが適しています。

より絞り込んだターゲットも存在し、`DESIGN.md` の Build System の項に一覧があります——`make lint`、`make rust-test`、`make go-test`、`make js-test`、`make bench`。

テストを実行する人ではなく、テストを追加する人に課される義務が二つあります：

- **新しい Rust 統合テストファイルは、Makefile の `rust-integration-test` レシピに追加しなければなりません。** このレシピは各ファイルを明示的に列挙しています。末尾の `cargo test` にだけ拾われるファイルも実行はされますが、独立したステップとして見えなくなり、その中の失敗が全体の失敗として読めてしまいます。
- **テストはホストに触れてはなりません。** 一時ディレクトリ、エフェメラルポート、そしてインメモリまたはテストごとの SQLite ファイルのみ。作業ツリーへの書き込み、固定の特権ポートへのバインド、公開インターネットへの到達はいずれも行いません——上流解決を動かすスイート群がルートを死んだループバックアドレスやインプロセスのモック階層に向けているのは、まさにグリーンな実行がネットワークに依存しないようにするためです。

## テストの書き方

このリポジトリのテストスイートは一つの考えの上に組み立てられています。各ファイル冒頭のモジュールドキュメントに書かれており、ここで繰り返す価値があります：**対照のないアサーションは何も証明しない。** すべてをブロックするブロックリストは「リストされた名前が拒否される」を満たし、何もブロックしないブロックリストは「許可リストの名前が解決する」を満たします。すべてを拒否する DNSSEC バリデーターはあらゆる攻撃テストに通り、すべてを受理するバリデーターはあらゆる正常系テストに通ります。対で書いてください。

- **テストを通すためにアサーションを弱めてはいけません。** これは `tests/security_*.rs` のスイート群にとりわけ強く当てはまります：それぞれが、ある一つのセキュリティ指摘が要求する挙動を固定しており、そこでの失敗は壊れたテストではなく、その指摘そのものです。
- 呼び出しが `success` を返したことを示すより、性質そのものを示すほうを選んでください。キャッシュのバグとその修正を分けるのはクエリの*回数*であり、バリデーターとパーサーを分けるのは*判定*であり、検証可能な署名と保存された blob を分けるのは導出し直した署名です。
- エンコーダーを自分自身と比較しないこと。ワイヤーフォーマットの期待値は手で書き下します。
- パイプラインが繋がっていること自体が要点である箇所では、変更は本物のコントロールプレーン（gRPC）を通して起こし、結果は本物のソケット越しに読み戻してください。ユニットテストは、ゲートをレスポンスキャッシュに対して動かしてしまったリグレッションをすり抜けてグリーンのままになります。

## メトリクス

- **すべてのラベル次元は有界でなければなりません**——固定の列挙型か、設定によって有界であること。クライアントが制御できるものは、すべてまとめ役へ畳み込みます（クエリタイプは `OTHER`、TLD は `other`）。クエリ名がラベルになることは決してありません。
- **新しいラベル値は末尾に追加するのであって、挿入してはいけません。** `BLOCK_*` 形式の定数は事前確保された配列の位置です。挿入は既存のカウンターすべてを黙って付け替えてしまいます。
- メトリクスの追加や改名は、`README.md` と `DESIGN.md` のファミリー数と該当クエリの更新を意味します——`tests/promql_docs_test.rs` は両方を読み、文書化されたファミリー数をレジストリが出力する値に対して固定し、文書化された各 PromQL クエリを実際のエクスポジション出力に対して解決します。続いて `tests/prometheus_integration_test.rs` が同じクエリを本物の Prometheus に通します。

## ドキュメント

- 挙動の変更は、それを行う変更と同じ変更のなかで `DESIGN.md` に反映します。これは仕様書であって、後から書かれる要約ではありません。
- 新しいトップレベルのドキュメントファイルは、`Cargo.toml` の `include` リストに追加しなければなりません。このパッケージは自身のテストを同梱しており、テストを運びながらその入力を運ばないパッケージは実行できないテストです——それはまさに `tests/promql_docs_test.rs` が `README.md` および `DESIGN.md` に対して持っている関係です。
- ```promql ブロックが走査されるのは `README.md` と `DESIGN.md` の二つのファイルです。別の言語にラベルを付け替えられたブロックは、チェックされなくなります。

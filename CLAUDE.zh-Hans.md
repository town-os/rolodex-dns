# Rolodex DNS 开发规则

Rolodex DNS 是一套分割视域（split-horizon）DNS 服务器与递归／转发解析器，通过 gRPC 进行远程管理，用 Rust 编写，许可证为 AGPL-3.0-only。

本文件是开发工作的规则。它有意保持简短：**软件做什么**写在 `DESIGN.md`，任何关于行为、架构或 API 界面的内容都不属于这里。

> 语言：[English](CLAUDE.md) ｜ [繁體中文](CLAUDE.zh-Hant.md) ｜ **简体中文**

## 文档分工

| 文档 | 内容 |
| ---- | ---- |
| `DESIGN.md` | 功能规格：架构、解析顺序、所有管理界面（gRPC、CLI、Go 客户端、JavaScript 客户端、指标、配置）、测试套件设计，以及构建系统。改动行为前先读这份。 |
| `README.md` | 面向用户的参考文档，含 PromQL 查询集。 |
| `CONFIGURATION.md` | 任务导向的配置指南——实际部署形态、哪些配置需要重启、按症状查找的故障排查。 |
| `CHANGELOG.md` | 版本历史。 |
| `CLAUDE.md` | 本文件。只放开发规则。 |

这五份文档每一份都各有一个繁体（`.zh-Hant.md`）与简体（`.zh-Hans.md`）中文译本并列。**英文版是唯一的真实来源**：请先改英文版，并把译本视为需要后续补上的工作，而不是第二个可以编辑的地方。没有任何东西会验证它们彼此一致——`tests/promql_docs_test.rs` 只读英文的 `README.md` 与 `DESIGN.md`，因此译本中的 PromQL 块或系列数量是文档，而不是一项受检查的断言。

## 规则

- 除非被要求，否则请不要执行 make 任务
- 确保 deny(dead_code) 与 deny(unsafe) 置于文件顶部并被切实遵守
- 以适当方式处理所有 std::result::Result
- 不要使用 unwrap
- 不要使用 unsafe 代码
- 绝不自行运行测试
- 为所有东西编写测试，包括集成测试与真实测试
- 使用 make test 验证任何改动
- 集成测试绝不可改动主机
- 测试：除非另有说明，测试以模拟输入运行，并就“原本会执行的操作”产生输出。它们绝不影响运行中的系统。
- 运行测试：每次都使用 make 任务。
- 测试应始终包含 lint 检查
- lint 检查应采用 Rust 社区标准的检查工具，通过 `lint` make 任务运行
- 绝不使用 `let _ = expr;` 来压制未使用变量的警告或绕过借用检查器。请修正真正的问题：使用该变量、移除该参数，或重构代码。
- `#![deny(dead_code)]` 与 `#![deny(unsafe_code)]` 在 lib.rs 与 main.rs 两处以 crate 级别设置。绝不用 `#[allow(dead_code)]` 或 `#[allow(unsafe_code)]` 绕过——请删除死代码，并改用安全的抽象（例如 nix crate）取代 unsafe。
- 除配置硬件外，不要改动系统
- 除非明确被告知，否则绝不删除、移动或修改 git 标签

## 验证改动

`make test` 是把关的关卡，且按上述规则，它应由运维人员来运行。它会依次执行：`lint`（`cargo fmt -- --check` 与 `cargo clippy --all-targets -- -D warnings`）、Go 集成测试与单元测试、`prometheus-test`、逐一列出的每个 Rust 集成测试文件、`cargo test`，以及 JavaScript 的 lint／集成／单元测试。`make test-log` 会把整轮运行写进带时间戳的日志文件，运行时间长时用它更合适。

更小范围的目标也存在，列在 `DESIGN.md` 的“构建系统”一节——`make lint`、`make rust-test`、`make go-test`、`make js-test`、`make bench`。

有两项义务落在“新增测试的人”身上，而不是运行测试的人：

- **新的 Rust 集成测试文件必须加入 Makefile 的 `rust-integration-test` 规则。** 该规则逐一列出每个文件；只被末尾那条 `cargo test` 带到的文件虽然仍会运行，却不再是可见的独立步骤，其中的失败读起来会像是整体的失败。
- **测试不得触碰主机。** 只能使用临时目录、临时端口，以及内存中或逐测试独立的 SQLite 文件。任何测试都不得写入工作目录、绑定固定的特权端口，或连上公网——会走上游解析的那些套件之所以把根服务器指向一个没有响应的 loopback 地址或指向进程内的模拟层级，正是为了让“测试通过”永远不取决于网络。

## 编写测试

本仓库的测试套件围绕一个想法建立，它写在每个文件开头的模块注释里，也值得在此重述：**没有对照组的断言什么都证明不了。** 一个“什么都封锁”的封锁列表同样能通过“已列入的名称被拒绝”；一个“什么都不封锁”的封锁列表同样能通过“在允许列表上的名称可以解析”。一个“什么都拒绝”的 DNSSEC 验证器能通过所有攻击测试，一个“什么都接受”的验证器能通过所有正常路径测试。请成对地写。

- **绝不为了让测试通过而弱化断言。** 这一点对 `tests/security_*.rs` 套件尤其成立：每一个都钉住某项安全发现所要求的行为，那里的失败就是那项发现本身，而不是测试坏了。
- 优先证明性质，而不是证明调用返回了 `success`。查询**次数**才能区分缓存缺陷与其修复；**判定**才能区分验证器与解析器；重新推导出的签名才能区分“可被检查的签名”与“存起来的一团字节”。
- 不要拿编码器跟自己比对。线路格式的预期值一律逐项手写。
- 当重点在于整条流水线确实接好时，变更要走真正的控制平面（gRPC），结果要从真实的套接字读回来。若某个回归把某道关卡相对于响应缓存的位置移动了，单元测试仍会是绿的。

## 指标

- **每个标签维度都必须有界**——要么是固定的枚举，要么由配置所限制。任何由客户端控制的值都折叠进兜底值（查询类型用 `OTHER`，TLD 用 `other`）。查询名称永远不会成为标签。
- **新的标签值只能追加在末尾，绝不可插入中间。** 那些 `BLOCK_*` 之类的常量是预分配数组中的位置；插入会悄悄地把已有的每个计数器重新贴上别的标签。
- 新增或改名一个指标，就意味着要更新 `README.md` 与 `DESIGN.md` 中的系列数量与受影响的查询——`tests/promql_docs_test.rs` 会读取这两份文档，把文档中声称的系列数量钉在注册表实际输出的数量上，并将每一条文档中的 PromQL 查询对照实际输出解析。接着 `tests/prometheus_integration_test.rs` 会把同一批查询送进真正的 Prometheus 执行。

## 文档

- 行为改动要在做出该改动的同一次提交中写进 `DESIGN.md`。它是规格，不是事后补写的摘要。
- 新的顶层文档文件必须加进 `Cargo.toml` 的 `include` 列表。这个包会连同自己的测试一起发布，而一个带着测试却没带测试输入的包，等于带了一个跑不起来的测试——`tests/promql_docs_test.rs` 与 `README.md`、`DESIGN.md` 之间正是这种关系。
- `README.md` 与 `DESIGN.md` 是被扫描 ```promql 块的两份文件。块若被改标成别的语言，就不再受检查。

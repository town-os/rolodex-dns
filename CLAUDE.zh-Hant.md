# Rolodex DNS 開發規則

Rolodex DNS 是一套分割視域（split-horizon）DNS 伺服器與遞迴／轉送解析器，透過 gRPC 進行遠端管理，以 Rust 撰寫，授權為 AGPL-3.0-only。

本檔案是開發工作的規則。它刻意保持精簡：**軟體做什麼**寫在 `DESIGN.md`，任何關於行為、架構或 API 介面的內容都不屬於這裡。

> 語言：[English](CLAUDE.md) ｜ **繁體中文** ｜ [简体中文](CLAUDE.zh-Hans.md)

## 文件分工

| 文件 | 內容 |
| ---- | ---- |
| `DESIGN.md` | 功能規格書：架構、解析順序、所有管理介面（gRPC、CLI、Go 用戶端、JavaScript 用戶端、指標、設定）、測試套件設計，以及建置系統。變更行為前請先讀這份。 |
| `README.md` | 面向使用者的參考文件，含 PromQL 查詢集。 |
| `CONFIGURATION.md` | 任務導向的設定指南——實際部署形態、哪些設定需要重啟、依症狀查找的疑難排解。 |
| `CHANGELOG.md` | 版本歷史。 |
| `CLAUDE.md` | 本檔案。只放開發規則。 |

這五份文件每一份都各有一個繁體（`.zh-Hant.md`）與簡體（`.zh-Hans.md`）中文譯本並列。**英文版是唯一的真實來源**：請先改英文版，並把譯本視為需要後續補上的工作，而不是第二個可以編輯的地方。沒有任何東西會驗證它們彼此一致——`tests/promql_docs_test.rs` 只讀英文的 `README.md` 與 `DESIGN.md`，因此譯本中的 PromQL 區塊或系列數量是文件，而不是一項受檢查的斷言。

## 規則

- 除非被要求，否則請不要執行 make 任務
- 確保 deny(dead_code) 與 deny(unsafe) 置於檔案頂端並被確實遵守
- 以適當方式處理所有 std::result::Result
- 不要使用 unwrap
- 不要使用 unsafe 程式碼
- 絕不自行執行測試
- 為所有東西撰寫測試，包含整合測試與真實測試
- 使用 make test 驗證任何變更
- 整合測試絕不可更動主機
- 測試：除非另有說明，測試以模擬輸入執行，並就「原本會執行的操作」產生輸出。它們絕不影響執行中的系統。
- 執行測試：每次都使用 make 任務。
- 測試應一律包含 lint 檢查
- lint 檢查應採用 Rust 社群標準的檢查工具，透過 `lint` make 任務執行
- 絕不使用 `let _ = expr;` 來壓抑未使用變數的警告或繞過借用檢查器。請修正真正的問題：使用該變數、移除該參數，或重構程式碼。
- `#![deny(dead_code)]` 與 `#![deny(unsafe_code)]` 在 lib.rs 與 main.rs 兩處以 crate 層級設定。絕不以 `#[allow(dead_code)]` 或 `#[allow(unsafe_code)]` 繞過——請移除死碼，並改用安全的抽象（例如 nix crate）取代 unsafe。
- 除設定硬體外，不要更動系統
- 除非明確被告知，否則絕不刪除、移動或修改 git 標籤

## 驗證變更

`make test` 是把關的關卡，且依照上述規則，它應由維運人員執行。它會依序執行：`lint`（`cargo fmt -- --check` 與 `cargo clippy --all-targets -- -D warnings`）、Go 整合測試與單元測試、`prometheus-test`、逐一列出的每個 Rust 整合測試檔、`cargo test`，以及 JavaScript 的 lint／整合／單元測試。`make test-log` 會把整輪執行寫進帶時間戳的紀錄檔，執行時間長時用它更合適。

更小範圍的目標也存在，列於 `DESIGN.md` 的「建置系統」一節——`make lint`、`make rust-test`、`make go-test`、`make js-test`、`make bench`。

有兩項義務落在「新增測試的人」身上，而非執行測試的人：

- **新的 Rust 整合測試檔必須加入 Makefile 的 `rust-integration-test` 規則。** 該規則逐一列出每個檔案；只被結尾的 `cargo test` 帶到的檔案雖然仍會執行，卻不再是可見的獨立步驟，其中的失敗會讀起來像是整體的失敗。
- **測試不得碰觸主機。** 只能使用暫存目錄、臨時連接埠，以及記憶體內或逐測試獨立的 SQLite 檔案。任何測試都不得寫入工作目錄、綁定固定的特權連接埠，或連上公開網際網路——會走上游解析的那些套件之所以把根伺服器指向一個沒有回應的 loopback 位址或指向行程內的模擬階層，正是為了讓「測試通過」永遠不取決於網路。

## 撰寫測試

本儲存庫的測試套件圍繞一個想法建立，它寫在每個檔案開頭的模組註解裡，也值得在此重述：**沒有對照組的斷言什麼都證明不了。** 一個「什麼都封鎖」的封鎖清單同樣能通過「已列入的名稱被拒絕」；一個「什麼都不封鎖」的封鎖清單同樣能通過「在允許清單上的名稱可以解析」。一個「什麼都拒絕」的 DNSSEC 驗證器能通過所有攻擊測試，一個「什麼都接受」的驗證器能通過所有正常路徑測試。請成對地寫。

- **絕不為了讓測試通過而弱化斷言。** 這一點對 `tests/security_*.rs` 套件尤其成立：每一個都釘住某項安全發現所要求的行為，那裡的失敗就是那項發現本身，而不是測試壞了。
- 優先證明性質，而不是證明呼叫回傳了 `success`。查詢**次數**才能區分快取錯誤與其修正；**判定**才能區分驗證器與剖析器；重新推導出的簽章才能區分「可被檢查的簽章」與「存起來的一團位元組」。
- 不要拿編碼器跟自己比對。線路格式的預期值一律逐項手寫。
- 當重點在於整條管線確實接好時，變更要走真正的控制平面（gRPC），結果要從真實的 socket 讀回來。若某個回歸把某道關卡相對於回應快取的位置移動了，單元測試仍會是綠的。

## 指標

- **每個標籤維度都必須有界**——要嘛是固定的列舉，要嘛由設定所限制。任何由用戶端控制的值都折疊進兜底值（查詢型別用 `OTHER`，TLD 用 `other`）。查詢名稱永遠不會成為標籤。
- **新的標籤值只能附加在後面，絕不可插入中間。** 那些 `BLOCK_*` 之類的常數是預先配置陣列中的位置；插入會悄悄地把既有的每個計數器重新貼上別的標籤。
- 新增或改名一個指標，就意味著要更新 `README.md` 與 `DESIGN.md` 中的系列數量與受影響的查詢——`tests/promql_docs_test.rs` 會讀取這兩份文件，把文件中宣稱的系列數量釘在登錄表實際輸出的數量上，並將每一條文件中的 PromQL 查詢對照實際輸出解析。接著 `tests/prometheus_integration_test.rs` 會把同一批查詢送進真正的 Prometheus 執行。

## 文件

- 行為變更要在做出該變更的同一次改動中寫進 `DESIGN.md`。它是規格書，不是事後補寫的摘要。
- 新的頂層文件檔必須加進 `Cargo.toml` 的 `include` 清單。這個套件會連同自己的測試一起發佈，而一個帶著測試卻沒帶測試輸入的套件，等於帶了一個跑不起來的測試——`tests/promql_docs_test.rs` 與 `README.md`、`DESIGN.md` 之間正是這種關係。
- `README.md` 與 `DESIGN.md` 是被掃描 ```promql 區塊的兩份檔案。區塊若被改標成別的語言，就不再受檢查。

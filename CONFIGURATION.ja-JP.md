# Rolodex DNS 設定ガイド

> 言語：[English](CONFIGURATION.md) ｜ [繁體中文](CONFIGURATION.zh-TW.md) ｜ [简体中文](CONFIGURATION.zh-CN.md) ｜ [Español (España)](CONFIGURATION.es-ES.md) ｜ [Español (México)](CONFIGURATION.es-MX.md) ｜ **日本語**

これはタスク指向の手引きです。まず動くサーバーを手に入れ、そのうえで各サブシステムをどう有効にするか、そしてなぜそうしたいのかを説明します。網羅的なフィールド一覧は README の[設定オプション](README.ja-JP.md#設定オプション)を参照してください。

- [設定はどう読み込まれるか](#設定はどう読み込まれるか)
- [動く最小の設定](#動く最小の設定)
- [バインドアドレス](#バインドアドレス)
- [デプロイの形](#デプロイの形) — 四つの実例
- [サブシステム](#サブシステム) — 各一節
- [実行中の変更と再起動](#実行中の変更と再起動)
- [サーバーが起動を拒む条件](#サーバーが起動を拒む条件)
- [トラブルシューティング](#トラブルシューティング)

## 設定はどう読み込まれるか

サーバーは YAML ファイルを一つ読みます。既定では `rolodex-dns.yml` です：

```bash
rolodex-dns                        # ./rolodex-dns.yml を読む
rolodex-dns -c /etc/rolodex-dns/config.yml
```

**ファイルが無いことはエラーではありません。** サーバーは `No config file found, using defaults` をログに出し、組み込みの既定値で起動します——それは実際に使える本物の設定です：DNS は `0.0.0.0:53`、ルートからの反復解決と DNSSEC 検証、gRPC はループバックと Unix ソケット、ブロックリストは無効、暗号化トランスポートは無効。

すべてのセクションは省略可能で、その中のすべてのフィールドに既定値があります。したがって設定ファイルには、変更するものだけを書けば足ります。*存在すること自体*がスイッチであるセクション——`dot`、`doh`、`doq`、`proxy`、`dhcp`、`acme`、`metrics`——は、省略すると何も起動しません。

ログ出力は設定ファイルではなく `RUST_LOG` 環境変数で制御します：

```bash
RUST_LOG=rolodex_dns=debug rolodex-dns -c /etc/rolodex-dns/config.yml
```

## 動く最小の設定

```yaml
database_path: /var/lib/rolodex-dns/rolodex-dns.db

dns:
  bind:
    - udp: "0.0.0.0:53"
    - tcp: "0.0.0.0:53"

grpc:
  unix_socket: /var/run/rolodex-dns.sock
  tcp_bind: ""          # 管理はソケット経由のみ
```

これで、ローカルレコードデータベースを備えた検証付き再帰リゾルバーになります。レコードはソケット越しに追加します：

```bash
rolodex-dns-cli -u /var/run/rolodex-dns.sock add-record \
  --name nas.example.com --record-type a --value 192.168.1.10
```

ポート 53 には特権が要ります。開発では高位ポートを使い——`make dev` は `dev.yml` により `127.0.0.1:5300` で動きます——本番ではバイナリに `CAP_NET_BIND_SERVICE` を与えるか、root で走らせるのではなくサービスマネージャー経由でバインドさせてください。

## バインドアドレス

アドレスを受け取るすべての場所（`dns.bind`、`dot.bind`、`doh.bind`、`doq.bind`、`grpc.tcp_bind`、`dhcp.bind`、`acme.bind`、`acme.portal_bind`、`metrics.bind`）で、四つの書き方が受け付けられます：

（`dns.bind` はプロトコルとアドレスの組の一覧を、`dot.bind`、`doh.bind`、`doq.bind` は**単一のアドレスまたは一覧**を受け取ります —— 一覧は、一つのリスナーが両方のアドレスファミリを覆うための手立てです。`0.0.0.0` は IPv4 だけを指し、`[::]` のソケットは同じポートでそれと衝突するからです。残りは単一のアドレスだけを受け取ります。）

| 書き方 | 例 | 結果 |
| ------ | -- | ---- |
| `ip:ポート` | `192.168.1.1:53` | そのアドレスにリスナー一つ |
| `[ipv6]:ポート` | `[::1]:53` | リスナー一つ。角括弧は必須 |
| `primary:ポート` | `primary:53` | OS の既定ルートの送信元 IP。起動時に検出 |
| `インターフェース:ポート` | `eth0:53` | **そのインターフェースの IP ごとにリスナー一つ** |

`primary` は `8.8.8.8:53` に向けた「何も送らない」UDP connect で解決されます——どの送信元アドレスが使われるかをルーティングテーブルに尋ねるだけで、何も送信しません。`インターフェース:ポート` はそのインターフェースに割り当てられた全アドレスに展開されるので、デュアルスタックのホストでは `eth0:53` は二つのリスナーを作ります。

`dns.bind` はキーが一つだけのマップのリストです。リスナーとはプロトコル*と*アドレスの組だからです：

```yaml
dns:
  bind:
    - udp: "eth0:53"
    - tcp: "eth0:53"
    - udp: "127.0.0.1:53"
    - tcp: "127.0.0.1:53"
```

インターフェース指定のバインドは**起動時に**解決され、後から再解決されることはありません。起動後にアドレスを得たインターフェース（たとえば立ち上がってきた WireGuard トンネル）は再起動まで拾われません——TLD ごとのイングレスリスナーが実行中に再登録できるのは、まさにそのためです。[所有 TLD とイングレス](#所有-tld-とイングレス)を参照。

## デプロイの形

### 1. 家庭／小規模オフィスのリゾルバー

検証あり、広告とマルウェアをブロックし、ローカル名をいくつか提供し、LAN からのみ到達可能。

```yaml
database_path: /var/lib/rolodex-dns/rolodex-dns.db

dns:
  bind:
    - udp: "0.0.0.0:53"
    - tcp: "0.0.0.0:53"
  auto_ptr: true                # 逆引き PTR を A/AAAA と同期させる

resolution:
  mode: auto                    # まずルート、次に暗号化、最後に ISP のリゾルバー

dnssec:
  validate: true                # 既定値。重要なのでここに明記する

dnsbl:
  enabled: true
  providers:
    - zone: dbl.spamhaus.org
      enabled: true

security:
  # 既定値はすでに RFC 1918 を含む。LAN がその部分集合なら絞ってよい
  recursion_cidrs: ["127.0.0.0/8", "192.168.0.0/16", "::1/128"]

grpc:
  unix_socket: /var/run/rolodex-dns.sock
  tcp_bind: ""

metrics:
  bind: "127.0.0.1:9153"
```

選択の理由：`resolution.mode: auto` は、ルートに到達できる間はあなたのクエリを第三者が一切見ないことを意味し、それでいて `:53` を遮断するネットワークでも解決は生き延びます。`recursion_cidrs` こそが `0.0.0.0:53` バインドをオープンリゾルバーにしないための要です——既定の一覧はすでに安全であり、自分のレンジまで絞るのは改善であって必須ではありません。

### 2. 純粋な権威サーバー

上流解決を一切行いません。すべての答えはローカルデータベースから来て、見つからないものは権威ある NXDOMAIN になります。

```yaml
database_path: /var/lib/rolodex-dns/auth.db

forwarders: []
resolution:
  mode: forward                 # フォワーダー無しの forward モード = 上流なし

dnssec:
  validate: false               # 上流で解決するものが無いので、検証するものも無い

security:
  recursion_cidrs: []           # 念のため：再帰を誰にも開かない

dns:
  bind:
    - udp: "0.0.0.0:53"
    - tcp: "0.0.0.0:53"
```

スイッチは `mode: forward` と `forwarders: []` の組み合わせです。`recursion_cidrs: []` はそれと重複しますが、意図を文書化し、ブロックリストプロバイダー経路とキャッシュ暖機の経路も閉じます。

権威を持つゾーンを宣言しておけば、その内側での不一致は、どこにも行き着かない検索ではなく NXDOMAIN になります：

```bash
rolodex-dns-cli -u /var/run/rolodex-dns.sock add-auth-zone --zone example.com.
```

（すでにレコードを*持っている*ゾーンは自動的に権威として扱われます——[管理ゾーンと権威ゾーン](#管理ゾーンと権威ゾーン)を参照。）

### 3. スプリットホライズンのオーバーレイノード（Town OS の形）

このマシンは LAN と WireGuard オーバーレイの両方にいます。オーバーレイのピアはネットワークスコープに分割され、LAN からはすべてが見えます。

```yaml
database_path: /var/lib/rolodex-dns/rolodex-dns.db

dns:
  bind:
    - udp: "0.0.0.0:53"
    - tcp: "0.0.0.0:53"
  ingress_listen_port: 53

security:
  overlay_cidrs: ["10.64.0.0/10"]     # これらの送信元はスコープを強制される
  recursion_cidrs:                    # これらの送信元は上流解決を行える
    - "127.0.0.0/8"
    - "10.0.0.0/8"                    # オーバーレイを含む
    - "192.168.0.0/16"
    - "::1/128"

grpc:
  unix_socket: /var/run/rolodex-dns.sock
  tcp_bind: ""
```

二つの CIDR リストは**別々の問い**であり、独立に設定されることを意図しています：

- `overlay_cidrs` — 「誰にスコープを強制するか？」ここに含まれる送信元はネットワークに参加（`JoinNetwork`）していなければ REFUSED となり、自分のスコープの TLD しか見えません。
- `recursion_cidrs` — 「誰がこのサーバーに他所へ問い合わせさせてよいか？」ここから外れる送信元も権威データは受け取れます。ただ上流検索を駆動できないだけです。

スコープは設定ファイルではなく実行中に組み立てます——データベースの中に存在するからです：

```bash
CLI="rolodex-dns-cli -u /var/run/rolodex-dns.sock"
$CLI create-scope --name office                       # office.home. を含意する
$CLI add-scope-tld -s office --tld office. --listen-ip 10.64.0.1
$CLI add-scoped-record -s office --name git.office. --record-type a --value 10.64.0.5
$CLI join-network --ip 10.64.0.7 --scope office --ttl 300
```

### 4. 敵対的／フィルタされたネットワーク上のリゾルバー

外向きの `:53` を落とし、`:853` の DoT ハンドシェイクを DPI するネットワークがあります。`auto` チェーンはまさにこのために作られており、変える価値があるのはどの暗号化上流を使うかだけです：

```yaml
resolution:
  mode: auto
  secure_upstreams:
    - transport: https            # :443 の DoH — ふつうの HTTPS に見える
      addr: "1.1.1.1:443"         # IP で接続するので事前の DNS を必要としない
      hostname: cloudflare-dns.com
      path: /dns-query
    - transport: https
      addr: "8.8.8.8:443"
      hostname: dns.google
      path: /dns-query
  switch_grace_failures: 3        # 降格が確定するまでに許す逸脱クエリ数
  recovery_probe_secs: 60         # 降格したチェーンが上位を再試行する間隔
```

セキュア上流は **IP** で接続し、証明書は `hostname` に対して検証されるので、この階層は自前の DNS 無しで立ち上がります。なお、階層 0 より下に降格した `auto` チェーンは **DNSSEC 検証されていません**（転送された答えは誰か他人の要約です）。そのことは AD ビットを立てないことで示されます。

## サブシステム

### 上流解決

```yaml
forwarders:                       # 「ローカル」階層。forward モードでは唯一の上流
  - "8.8.8.8:53"
  - "8.8.4.4:53"

resolution:
  mode: auto                      # auto | recursive | forward
  root_hints: []                  # 組み込みの IANA ルートを上書きする
  public_fallback: ["1.1.1.1:53", "8.8.8.8:53"]
  delegation_persist_min_ttl: 300 # この TTL を超える学習済み委任を永続化する
  default_ttl: 300                # TTL を持たないものに対して「のみ」使われる
```

| モード | こんなときに |
| ------ | ------------ |
| `auto`（既定） | プライバシーを最優先したいが、フィルタされたネットワークでも解決は生き延びてほしい |
| `recursive` | ルートか、さもなくば何もなし——上流リゾルバーには決して接触しない |
| `forward` | 素朴なフォワーダーが欲しい（あるいは `forwarders: []` で上流を完全に断つ） |

**ここの `mode` は起動時の種であって、いま効いている設定ではありません。** これは
起動時に一度だけ読まれます。そこから先、モードは `SetResolutionMode` が最後に設定した
ものであり、`GetResolutionMode` が報告するのは実際に問い合わせを解決しているほうです
—— つまり両者は食い違いうるのであって、拠り所は動いているサーバーです。
`rolodex-dns-cli set-resolution-mode -m <mode>` / `get-resolution-mode` が、この二つの
呼び出しをシェルから行うものです。ファイルを書き換えて再起動しても構いませんが、機体で
唯一のリゾルバーの再起動は、その上のすべてにとっての DNS の停止です —— この RPC が
存在する理由はまさにそこにあります。ファイルと違い、この RPC は認識できないモードを
警告して `auto` へ落とすのではなく**拒否**します。

`default_ttl` は**フォールバックであって下限ではありません**。存在する TTL は、ゾーンの SOA ネガティブ TTL も含め、送られたとおりに厳密に尊重されます。生きている TTL を短くしたり長くしたりしたいのなら、それは [TTL ドリフト](#dns64-と-ttl-ドリフトとアドレスファミリー)であって、これではありません。

### DNSSEC

独立した二つの半分があります。**検証**は既定で有効で、設定を必要としません：

```yaml
dnssec:
  validate: true
  trust_anchors: []        # 空 = IANA のルート鍵
```

これは反復経路にのみ適用されるので（`recursive` モード、および `auto` のルート階層）、`forward` モードでは何もしません。不正（bogus）なデータは SERVFAIL になり、キャッシュされることは決してありません。無効にするのは、直せない壊れた上流や、まだアンカーを設定していない私的な階層など、はっきりした理由があるときだけにしてください。

`trust_anchors` は DNSKEY のプレゼンテーション形式、すなわち `dig DNSKEY .` が表示するとおりの四つの RDATA フィールドを取ります。そして上書きは IANA の鍵に**加える**のではなく**置き換えます**：

```yaml
dnssec:
  trust_anchors:
    - "257 3 15 <base64 の鍵>"     # 私的なルート。IANA は信頼されなくなる
```

不正な形式のアンカーは、IANA へフォールバックするのではなく起動を失敗させます——実在の DNSKEY に一致しえないアンカーは、原因がアンカーだと指し示すものが何も無いまま、署名済みゾーンをすべて失敗させてしまうからです。

**署名**のほうは YAML ではまったく設定しません。ゾーンに対する実行時操作です：

```bash
CLI="rolodex-dns-cli -u /var/run/rolodex-dns.sock"
$CLI generate-dnssec-key --zone example.com. --algorithm ED25519 --key-type KSK
$CLI generate-dnssec-key --zone example.com. --algorithm ED25519 --key-type ZSK
$CLI sign-zone --zone example.com.
```

レコードを変更したら `sign-zone` を再実行してください。署名は置き換えられるのであって、積み上がりません。RSA（アルゴリズム 8）は鍵生成の時点で拒否されます——`ring` は RSA 鍵を生成できません——また、認証付き否認（NSEC/NSEC3）は検証はしますが生成は決してしません。

### セキュリティ：二つの CIDR リスト

```yaml
security:
  qname_case_randomization: true      # 転送クエリへの 0x20 エンコーディング
  overlay_cidrs: ["10.64.0.0/10"]     # スコープを強制される送信元
  recursion_cidrs: [ ... ]            # 上流解決を許される送信元
```

この二つを取り違えるのが最もありがちな設定ミスなので、単刀直入に：

| | `overlay_cidrs` | `recursion_cidrs` |
| --- | --- | --- |
| 問い | この送信元にはどの*ビュー*が見えるか？ | この送信元は我々に上流へ問わせてよいか？ |
| リストの内側 | ネットワークに参加していなければ REFUSED。自分のスコープしか見えない | 上流解決を駆動できる |
| リストの外側 | 信頼されたローカル送信元。グローバルな名前空間 | ローカル／権威の答えは受け取れる。マシン外のものはすべて REFUSED |
| 既定値 | `10.64.0.0/10` | ループバック、RFC 1918、リンクローカル、ULA、CGNAT |

`recursion_cidrs` は*狭める*のでない限り触らないでください。公開インターネットのほうへ広げると、このマシンはオープンリゾルバー、すなわち今この瞬間に誰かが悪用しているかどうかに関わらず、リフレクション／増幅攻撃の資産になります。

`qname_case_randomization` は有効のままにしておくべきです。無効にするのは、返してくる質問の大文字小文字を正規化してしまう上流が相手のときだけにしてください。そうでない場合、そのリゾルバーはすべてのクエリを失敗させます。大文字小文字の比較こそが 0x20 に実際の防御力を与えているからです。

### ブロックリスト（DNSBL）

**DNSBL は名前でブロックし**、外部解決の前に照会します。既定では無効でプロバイダー一覧も空なので、あなたがプロバイダーを追加するまでは何も問い合わせられず、いかなる名前もブロックリスト運営者の手に渡りません。

```yaml
dnsbl:
  enabled: true
  refusal_cooldown_secs: 3600
  providers:
    - zone: dbl.spamhaus.org
      enabled: true
```

アドレスをブロックするのはプロバイダーではなく**ローカルリスト**です。プロバイダーに問い合わせられるのは解決中の名前であり、逆引きにおいてそれは誰も評判情報を公開していない名前だからです。下記のローカルエントリーを参照してください。

有効にする前に知っておく価値のあることが三つあります：

1. **ローカルレコードが常に勝ちます。** ブロックリストはローカルレコードと管理ゾーンの後に走るので、第三者のリスト掲載が内部サービスを潰すことはありえません。一方でレスポンスキャッシュとリゾルバーの*前*に走るので、以前にキャッシュされた名前に対しても掲載は効きます。
2. **ブロックは問い合わせられた名前ごとであって、サフィックス単位ではありません。** `doubleclick.net` が掲載されていても `stats.g.doubleclick.net` はブロックされません——プロバイダーがそちらも掲載する必要があります。許可リストの*ほうは*サフィックス一致します。サブドメインを取りこぼす抜け道は抜け道の名に値しないからです。
3. **量が増えると拒否コードが効いてきます。** ブロックリストは「クォータ超過です」を、「掲載あり」に使うのと同じ種類の `A` レコードで伝えてきます。拒否の処理は既定で有効で、組み込みのコード集合を持っています。`refusal_codes` を設定する唯一の理由は、私的なブロックリストの本物の掲載値がそのうちの一つと衝突する場合（`refusal_codes: ["none"]`）か、集合を絞りたい場合です。[拒否コードとプロバイダーのローテーション](README.ja-JP.md#拒否コードとプロバイダーのローテーション)を参照。

ローカルエントリーと許可リストは設定ではなく実行時の状態です：

```bash
CLI="rolodex-dns-cli -u /var/run/rolodex-dns.sock"
$CLI add-local-blocklist --name 10.0.0.5 --reason "known spam source"
$CLI add-dnsbl-allow --name vendor.example.com --reason "false positive"
$CLI add-dnsbl-allow --name 192.168.1.100 --reason "our own relay"   # IP でも動く
```

### 管理ゾーンと権威ゾーン

設定ファイルにゾーンの一覧はありません。ゾーンが権威になる道は二つです：

- **暗黙的に**、レコードを持つことによって。ゾーン内のどこであれレコードが一つあれば、このサーバーはそのゾーン全体に対して権威になります——つまり `foo.example.com` をローカルの上書きとして追加すると、`www.example.com` はインターネットから解決されるのではなく NXDOMAIN を返します。それがスプリットホライズンの取引であり、意識的にやる価値があります：公開ドメインを上書きするのは、それを自分のものにするつもりのときだけに。
- **明示的に**、`add-auth-zone` で。まだレコードの無いゾーンや、逆引きゾーンを主張するのはこの方法です（暗黙のルールは `in-addr.arpa`／`ip6.arpa` を意図的に飛ばします。そこでこのヒューリスティックを働かせると、グローバルな逆引きツリー全体を主張してしまうからです）。

### 暗号化トランスポート

各セクションは存在すること自体がスイッチであり、それぞれ TLS 資材を必要とします：

```yaml
dot:
  bind: "0.0.0.0:853"
  tls:
    cert_path: /etc/rolodex-dns/cert.pem
    key_path: /etc/rolodex-dns/key.pem
    auto_self_signed: false

doh:
  bind: "0.0.0.0:443"
  tls: { cert_path: /etc/rolodex-dns/cert.pem, key_path: /etc/rolodex-dns/key.pem, auto_self_signed: false }
  enable_h3: false

doq:
  bind: "0.0.0.0:8853"
  tls:
    auto_self_signed: true            # 信頼できるネットワークなら問題ない
    self_signed_sans:                 # LAN のクライアントがこの機器を呼ぶ名前
      - dns.home
      - town-os.local
```

`auto_self_signed: true`（既定）は、証明書が設定されていなければ起動時に生成します。信頼できるネットワークでは便利です。

**更新した証明書に再起動は要りません。** `cert_path`／`key_path` を設定したリスナーは 30 秒ごとにそのファイルを読み直し、その窓の内に新しい組を提供しはじめます。すでに開いている接続は、ハンドシェイクしたときの証明書のまま終わり、次に到着した接続が新しいものを受け取ります。何かにシグナルを送る必要も、ファイルを書く側と歩調を合わせる必要もありません。ACME クライアントの二回の書き込みの間に落ちたポーリングは、証明書と対応しない鍵を見て、それを拒否し、古い組を提供したまま、次のティックで再試行します。生成された（`auto_self_signed`）証明書はポーリングされません。背後にファイルが無く、タイマーで再生成すれば毎分二回、クライアントごとに違う証明書を手渡すことになるからです。

**まだ発行されていない証明書を名指せます。** `cert_path`／`key_path` を存在しないファイルに向けることが強い失敗になるのは、`auto_self_signed` が切れているときだけです。入れておけば、リスナーは生成された材料で起動し、上の巡回が、本物の組が着いたその時にそれを引き継ぎます。これによって、証明書を発行する当のものが動く前にこれらの経路を書いておけます —— CA がリゾルバーの起動のあとに作られる機体では、それがごく普通の場合であり、代わりの道はファイルが現れてから機体で唯一のリゾルバーを再起動することです。

**DoT、DoH、DoQ は実行時に設定し直せます**。`SetDotConfig`／`SetDohConfig`／`SetDoqConfig` を通じてです。バインドアドレス、証明書の経路、SAN の一覧は、いずれも動いているサーバー上で変更でき、`Get*Config` は実際にバインドされているものを報告します。下の YAML は起動時の設定です。それが唯一の入口ではありませんし、サーバーが立ち上がったあとは権威でもありません。

**DoT クライアントが証明書の名前不一致を報告したら、これがその設定です。** 生成された証明書は `localhost`、`127.0.0.1`、`::1`、そしてリスナー自身のバインドアドレスを覆います。つまり `192.168.1.5:853` のリスナーは、そのアドレスへ接続するクライアントに対しては何も設定しなくてもすでに使えます。覆えないのは、この機器が応答する他のすべて——ホスト名、mDNS の `.local` 名、LAN がそれを呼ぶ CNAME、あるいは NAT が公開しているアドレス——です。それらを `self_signed_sans` に入れます。**ワイルドカード**にバインドしたリスナー（`0.0.0.0:853`、既定値）は何一つ導出しません。`0.0.0.0` は誰も接続先に指定する識別子ではないからです。ワイルドカードのバインドでは、このリストだけがその機器を名乗ります。

これは名前の確認であって信頼の判断ではなく、しかも先に失敗します。クライアントには依然としてその証明書を信頼するよう伝える必要があります——ピン留めするか、DANE/TLSA で公開して確認するか——自己署名証明書には連鎖が無いからです。何も検証しないクライアント（`kdig +tls`、日和見モードの systemd-resolved）はいずれにせよ影響を受けません。

### gRPC 管理

```yaml
grpc:
  tcp_bind: "127.0.0.1:50051"       # "" で TCP を無効化
  unix_socket: /var/run/rolodex-dns.sock   # "" でソケットを無効化
  shared_secret: ""                 # ループバック以外の tcp_bind には必須
```

- **Unix ソケットは認証を完全にバイパスします**。したがって、そのファイルモード*こそが*アクセス制御です。ソケットは（umask に影響されず）`0660` で作られるので、モードを緩めるのではなく管理グループへ `chgrp` することでアクセスを与えてください。
- **TCP は共有秘密を要求します**。比較は定数時間で行われ、失敗が続くと送信元ごとにロックアウトされます。空の秘密は「認証なし」を意味し、ループバックでは問題ありませんが、ルーティング可能なアドレスでは起動時に拒否されます。
- ソケットを優先してください。単一ホストのデプロイでは、ソケットパスを指定して `tcp_bind: ""` とするのが推奨される形です。

### DHCP

セクションの存在が有効化のスイッチです。`tld` は必須で、ホスト名はそこに着地します：

```yaml
dhcp:
  bind: "0.0.0.0:67"
  tld: example.com          # "laptop" というクライアントは laptop.lan.example.com. として登録される
  default_lease_duration: 3600
  reclaim_timeout: 86400
  sweep_interval: 60
```

プールはネットワークスコープごとの実行時状態です：

```bash
rolodex-dns-cli -u /var/run/rolodex-dns.sock add-dhcp-pool -s office \
  --range-start 10.0.0.100 --range-end 10.0.0.200 \
  --gateway 10.0.0.1 --subnet-mask 255.255.255.0 --dns-servers 10.0.0.1
```

プールは連続した単一のレンジであり、使い切ると割り当ては失敗します——プールをまたぐ集約はありません。MAC と IP の結び付きは固定的です。クライアントが提示したホスト名は妥当な単一 DNS ラベル（RFC 1123）でなければならず、そうでなければ警告付きで登録がスキップされます。サニタイズではなく拒否なので、クライアントが送っていない名前で黙って登録されることはありません。

### ACME 発行者とポータル

セクションの存在により、起動時にルート CA が作られ、二つのリスナーが立ち上がります：クライアント向けの ACME エンドポイントと、登録ポータルです。

```yaml
acme:
  bind: "0.0.0.0:8555"
  portal_bind: "127.0.0.1:8500"                       # 信頼できるネットワークのみ
  directory_url: "https://dns.example.com:8555/acme"  # これは設定すること — クライアントが見る
  root_ca_cn: "Rolodex Root CA"
  leaf_validity_days: 90
  require_eab: true
  issuance_scope: managed_zones                       # または "any"
  tls: { auto_self_signed: true }
```

`directory_url` は ACME クライアントに「ここへ話しかけよ」と伝えるものなので、`localhost` ではなく外部から到達可能な URL でなければなりません。**`portal_bind` は信頼できるアドレスに置いたままにしてください**——ポータルに到達できる者は誰でも登録できます。登録は `issuance_scope: any` にしない限りこのサーバーが実際に管理しているゾーンに限定され、`require_eab: true` はアカウント登録を発行済み資格情報の後ろに置きます。

### メトリクス

```yaml
metrics:
  bind: "127.0.0.1:9153"
```

既定では存在しないので、アップグレードで新しいポートが開くことはありません。平文 HTTP かつ認証なしで——運ぶのは集計値だけであり、クエリ名やレコード値は決して運びません——プライベートなアドレスにバインドしてください。まず見る価値のある系列は `rolodex_dns_answers_total{source}`（どの段が答えたか）、`rolodex_dns_dnssec_verdicts_total{verdict}`、そして `rolodex_dns_blocklist_rotated_out` です。

### DNS64 と TTL ドリフトとアドレスファミリー

```yaml
dns64:
  enabled: false
  prefix: "64:ff9b::"       # よく知られたプレフィックス

ttl_drift:
  mode: disabled            # disabled | fixed | logarithmic
  fixed_adjustment: "5m"    # "5m", "-30s", "1h30m", "2d12h"
  log_multiplier: 0.1

address_family:
  mode: auto                # auto | off | force4 | force6
  probe_interval_secs: 30
  fail_threshold: 2
```

`address_family: auto` が既定であり、たいていはこれが望むものです：公開リゾルバーの `:443` へ TCP 接続してファミリーごとの*実際の*到達性を試し、ホストが経路を持たないファミリーの A または AAAA 応答を抑制するので、クライアントは行き詰まる代わりにもう一方へフォールバックします。探査せずにファミリーを固定するなら `force4`／`force6`、常に両方を答えるなら `off` を使ってください。

### 所有 TLD とイングレス

これらは設定ではなく——データベースに存在し、実行中に管理されます——それでも二つの設定フィールドが関わってきます：

- `dns.ingress_listen_port`（既定 53）は、TLD ごとのイングレスリスナーがバインドするポートです。IP は TLD ごとで、`add-scope-tld --listen-ip` で与えます。
- イングレスリスナーは起動時にデータベースから再生されます。オーバーレイのインターフェースがまだ存在しなければバインドは失敗し、そのエントリーは不在として扱われるので、トンネルが立ち上がってから TLD を追加し直せば、再起動なしでバインドが再試行されます。

## 実行中の変更と再起動

設定に見えるものの多くは SQLite 上の実行時状態であり、再起動なしに gRPC 経由で変更できます：

| 実行中に変更（gRPC/CLI） | 再起動が必要 |
| ---- | ---- |
| レコード、スコープ付きレコード、スコープ、関連付け | `dns.bind` およびその他すべてのバインドアドレス |
| 権威ゾーン、所有 TLD、イングレスリスナー | `mode` **以外**の `resolution.*` と `forwarders`（初期値。`set-forwarders` は稼働中に変更する） |
| DNSBL 設定、ローカルエントリー、許可リスト | `dnssec.*` |
| DNS64、TTL ドリフト、プロキシ、DoT/DoH/DoQ 設定 | `security.*` |
| DHCP プール、リース、証明書オプション | `database_path`、`dhcp.*`、`acme.*`、`metrics.*` |
| DNSSEC 鍵とゾーン署名、ACME の CA と EAB 資格情報 | `<transport>.tls.*` — パスと SAN のリストであって、証明書そのものではない |
| TLS 証明書**ファイル** — その場で書き換えれば 30 秒以内に拾われる | — |
| `resolution.mode` —— `set-resolution-mode` が切り替え、`get-resolution-mode` が実際に効いているものを読む | — |

レコードとブロックリストの変更は次のクエリから効きます——レコードの変更はレスポンスキャッシュを自動的にフラッシュします。

## サーバーが起動を拒む条件

以下は警告ではなく意図的な即時失敗です。どれも、そうしなければ「健全に見えるのに間違ったことをしているサーバー」を生んでしまうからです：

- **ルーティング可能な `grpc.tcp_bind` と空の `shared_secret` の組み合わせ。** これは到達可能なポート上の無認証の管理プレーンです。ループバックなら問題なく、それが文書化された開発用の形です。`0.0.0.0` と `::` はループバックではありません。
- **不正な形式の DNSSEC トラストアンカー。** IANA の鍵へフォールバックすれば、私的なルートを設定したはずの運用者が間違ったものにアンカーされたまま、気持ちよく検証を続けてしまいます。
- **解析できないブロックリスト拒否コード。** 黙って適用されないコードは、掲載として読まれてしまう拒否です——そのプロバイダーに照会されたすべての名前が NXDOMAIN になります。
- **解決できないバインドアドレス**——アドレスを一つも持たないインターフェース、あるいは IP でもインターフェースでもない名前。これは DNS、DoT、DoH、DoQ、gRPC、DHCP、メトリクスの各リスナーにとって致命的です。ACME の二つのリスナーはエラーをログに出し、サーバーの残りは動き続けます。

YAML の解析エラーもまた致命的です。存在しないファイルは致命的ではありません。

**解決はできたが OS レベルで失敗するバインド**——ポートが使用中、あるいはアドレスがまだ存在しない——は致命的ではありません。リスナーごとにログへ出て、サーバーの残りは動きます。したがって `:53` の `EADDRINUSE` は起動失敗ではなく一行のエラーとして現れます。きれいに起動したからといって全リスナーが立ち上がったと決めつけず、ログを確認してください。

## トラブルシューティング

| 症状 | ありそうな原因 |
| ---- | -------------- |
| LAN 外のクライアントが、あなた自身のゾーン以外すべてで REFUSED になる | 意図どおりの動作：`security.recursion_cidrs`。再帰を許すべきならそのレンジを追加する |
| オーバーレイのピアがあらゆる名前で REFUSED になる | `security.overlay_cidrs` の内側にいるのに `JoinNetwork` を呼んでいないか、関連付けの TTL が切れている |
| 上書きしたドメイン配下の公開名が NXDOMAIN を返す | レコードを一つ追加したことで、このサーバーがゾーン全体の権威になった。その名前をローカルに追加するか、上書きを自分が所有する名前へ移す |
| 他のどこでも解決する名前が、ここでだけ SERVFAIL になる | DNSSEC 検証が拒否している。`rolodex_dns_dnssec_verdicts_total{verdict="bogus"}` を確認し、`dig +cd`（検証無効）で裏を取る |
| **すべての**名前が SERVFAIL になり、チェーンが暗号化上流へ降格すらしない | ルートゾーン自体が検証を通っていない：このビルドが知らないトラストアンカー（KSK ロールオーバー）、誤った `dnssec.trust_anchors`、あるいは `:53` にいる何かが自前の資材で DNSKEY クエリに答えている。これは意図的です——検証を通らないルートは階層の失敗ではなく判定なので、検証しない上流へこっそり問い直すのではなくクエリを拒否します。アンカーを直すまでの避難口が `dnssec.validate: false` です |
| `arpa.` 配下の名前が REFUSED になる（`ipv4only.arpa`、保持していないアドレスへの `dig -x`） | 意図どおりの動作：`arpa.` とその配下すべては、どの解決モードであれローカルデータから答えるか、さもなくば答えません。この部分木は一切上流へ送られません。レコードをローカルに追加するか、逆引きゾーンの作業を待ってください |
| `rolodex_dns_dnssec_blamed_roots` が非ゼロ | あるルートサーバーが、あなたのアンカーに対して検証を通らない DNSSEC を返したため、15 分間ルート集合から外されています（違反のたびに倍増）。**すべて**が外されているなら、サーバーではなくアンカーかルートゾーンを疑ってください——ログがそう明言します。この責任追及はメモリ上だけで、再起動でリセットされます |
| あるブロックリストに照会した名前がすべて NXDOMAIN になり始めた | 拒否処理より前の挙動。ローテーションで外れたプロバイダーがないか `get-dnsbl-config` を確認し、そのプロバイダーのクォータも見る |
| DHCP クライアントのホスト名が DNS に現れない | 妥当な単一 DNS ラベルではない——ホスト名はサニタイズされず拒否されます。警告がその名前を挙げています |
| 問題のないホストに対して `dig -x` が失敗する | ローカルブロックリストのエントリーがそのアドレスに一致しています。`add-dnsbl-allow --name <ip>` で解除できます |
| 更新した証明書が提供されない | 30 秒待ってください。それでも続くならログが理由を告げます——失敗したリロードはポーリングのたびに記録されます。よくある原因は証明書と鍵が対応していないことで、それは書き込みが途中で止まった姿でもあります。永久に途中で止まった更新は決して完了しません。`auto_self_signed` のリスナーはそもそもポーリングされません。ファイルが無いからです |
| DoT クライアントが、この機器のホスト名や LAN アドレスについて証明書の名前不一致を報告する | 生成された証明書はループバックの集合とリスナーのバインドアドレスしか名乗らず、ワイルドカードのバインドは何も寄与しません。その名前を `dot.tls.self_signed_sans` に加えて再起動してください。これはその証明書を信頼するかどうかとは別の話で、自己署名なら信頼は依然として必要です |
| DoT クライアントが `no_application_protocol` でハンドシェイクに失敗する | `dot` 以外の ALPN プロトコルを提示しています。リスナーは `dot` を広告し、他のものしか提示しないクライアントを拒否します。ALPN をまったく提示しないクライアントは通常どおり提供されます |
| イングレスリスナーが一度も立ち上がらなかった | その IP が起動時に存在しなかった。インターフェースが上がってから TLD を追加し直す |

網羅的なフィールドリファレンスは[設定オプション](README.ja-JP.md#設定オプション)を参照してください。

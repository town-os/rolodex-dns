# Town OS 契約

> 言語：[English](TOWNOS_CONTRACT.md) ｜ [繁體中文](TOWNOS_CONTRACT.zh-TW.md) ｜ [简体中文](TOWNOS_CONTRACT.zh-CN.md) ｜ [Español (España)](TOWNOS_CONTRACT.es-ES.md) ｜ [Español (México)](TOWNOS_CONTRACT.es-MX.md) ｜ **日本語**

これは、rolodex と Town OS のあいだの境界を双方向に越えるすべてについての、正式な一覧です。

**向きは gfeh とは逆です。** gfeh は Town OS のクライアントですが、rolodex は Town OS が駆動する側です。Town OS の systemcontroller が rolodex の gRPC クライアントであり、`../install` のイメージが rolodex のブートストラップ設定ファイルを書き、ttyforce が rolodex に何を発見できるかを決めるネットワーク設定を書きます。したがって以下の大半は、**その三者が rolodex について前提にしてよいこと**であり、最後に rolodex の側から求めることを少しだけ記します。

**ここには、あるリビジョンに固定されたものは一つもありません。** `make check-townos-sync` は、走るその瞬間にこのマシン上に実際にあるチェックアウトを解決します。どのスクリプトも読まない記録されたリビジョンは、誰も維持していない主張であり、固定は rolodex が依存する何も変えていない Town OS のコミットで大声で失敗します——両方の最悪の側です。

| コマンド | 何に対して検査するか | スキップする？ |
|---|---|---|
| `make check-townos-sync` | ローカルのチェックアウト（`TOWNOS_DIR=`、`INSTALL_DIR=`） | する。無ければ |

これは `make lint` の一部として走るので、通常の開発はこの検査をただで得られ、しかもこのリポジトリしか無いマシンでも動き続けます。

### この検査が実際に検証しているもの

名前だけでは足りません——まだ存在しているが場所が動いた定数こそ、ここでは緑のままでマシン上で壊れる、まさにその失敗です。この検査が比べるのは：

- **Town OS の `Client` インターフェースが宣言するすべてのメソッドが、rolodex 自身の Go クライアント（`go/client.go`）に存在すること。** proto ではなくそちらが、Town OS が実際に結び付いている面です。Town OS 自身の `client` 構造体は、このリポジトリの Go パッケージへそのまま委譲します。それらのメソッドのいくつかは独立した rpc ではなく便宜のラッパーであり（`AddScopeTldWithListener` は `listen_ip` を設定した `AddScopeTld` です）、proto だけを見る検査は存在しないズレを報告し、同時に取り除かれたラッパーを見逃します。後者こそが本当のズレです。
- **二つのパーサーにおけるフォワーダーの scheme の集合が同一であること**——こちらの `src/forwarder.rs` と、あちらの `src/rolodex/forwarder.go` です。互いを見ることのできない二つのリポジトリで、同じ文法を手書きの二つのパーサーで解析することは、この文書の中で最も新しく、最も守られていないものです。
- **三つのリポジトリにまたがって固定アドレスが一致すること**：DoH のバックエンド、metrics のリスナー、rolodex がバインドするループバック、そして TLS のディレクトリ。それぞれ Go の定数として、インストールスクリプト内のリテラルとして、そしてここでの既定値として。

## 範囲

三つの相手方があり、それらは入れ替えできません：

1. **Town OS（`../town-os`）**——systemcontroller。gRPC 越しに rolodex の*設定*をプログラムし、その metrics をスクレイプします。設定ファイルは一切書きません。
2. **インストールイメージ（`../install`）**——`scripts/rolodex-config.sh` が `rolodex.yml` を書き、他には誰も書きません。動いている rolodex に対して設定できないものだけを載せます。
3. **ttyforce（`~/src/github.com/erikh/ttyforce`）**——networkd のユニットを書きます。ここに現れるのは、その選択の一つ（`UseDNS=no`）が Town OS のフォワーダー発見に何を見つけられるかを決めているからだけであり、それはどちらの側から見ても自明ではありません。

これ以外に境界を越えるものはありません。とりわけ：

- **rolodex が Town OS を呼ぶことは決してありません。** HTTP クライアントも、アカウントの参照も、ストレージ呼び出しもありません。すべては流れ込んでくる側です。
- **rolodex は Town OS が読むファイルを一切書きません。** そのデータベースは rolodex 自身のものであり、gRPC ソケットと metrics エンドポイントが外向きの面のすべてです。

## `rolodex.yml` はブートストラップ専用であり、二つのリポジトリは一緒に動く

`../install` の `scripts/rolodex-config.sh` が唯一の書き手です。動いているサーバーに対して設定できないものだけを、ちょうど載せています：

| キー | なぜプログラムできないか |
|---|---|
| `dns.bind` | リスナーは、どの API 呼び出しが届くよりも前に存在していなければならない |
| `metrics.bind` | rolodex はその節の存在を見て、起動時に一度だけそのリスナーを開く |
| `doh` / `dot` / `doq` | それぞれの節の存在から、起動時に一度だけ開かれる |
| `database_path`、`grpc` | サーバーが存在するより前に読まれる |
| `forwarders`、`resolution.mode` | 起動時の**既定値**のみ——運用者の本当の選択は systemcontroller が gRPC 越しにプログラムする |

**serde は未知のフィールドも欠けたフィールドもそのまま拒否します。** そのイメージのリビジョンで必須でありながらファイルに無いフィールド——あるいはファイルにあってイメージが知らないフィールド——は、起動時の硬い `failed to parse config file` であり、`Restart=always` のもとではそれはクラッシュループで、そのマシン上のすべてから DNS が失われます。これは既に一度、`rbl` → `dnsbl` の改名で起きています。

そこから従う規則：**インストールリポジトリの `rolodex-config.sh` と、公開された rolodex イメージは一緒に動く。** ここで設定キーの名前を変え、あちらに対応する変更が無ければ、得られるのは壊れたマシンであって、失敗したテストではありません。Town OS の `TestRolodexDohBackendMatchesTheInstallScript` はこのうちちょうど一方向だけを捕まえ、しかも `../install` がチェックアウトされている場合に限られます。

## 設定はメモリー上にしか存在しない

rolodex は gRPC 越しに設定されたものを**何も**永続化しません。起動時に `rolodex.yml` から種を得て、残りはメモリーに保持するので、`Restart=always` のもとでのクラッシュ、そのユニットを跳ねさせる DHCP リースの変更、あるいは運用者が手で再起動することのいずれもが、Town OS が押し込んだすべての設定を起動時の既定値へ戻します。

したがって Town OS の義務は：**再起動のたびに押し直すこと。** `ProgramRolodex` は 15 秒ごとに走り、`Manager.Generation`——rolodex が起動時にバインドする gRPC ソケットの同一性（デバイス、inode、更新時刻）——を通じて再起動に気づきます。rolodex の中に再起動を告げるものは何もありません。ソケットの同一性がその合図です。

はっきり述べておく価値のある帰結が二つあります：

- **同一の押し直しは、ただでなければならない。** `SetForwarders` とブロックリストのセッターは素の格納であり——キャッシュのフラッシュも、上流への再接続もありません——まさにそのために、あのティックは差分を取らずに無条件で押せます。`SetResolutionMode` は*ただではなく*（`auto` へ切り替えると階層の探索がやり直されます）、だからこそ Town OS はそれだけを `GetResolutionMode` と突き合わせ、変わったときにだけ押します。
- **フォワーダーごとの健全性は、そのティックを生き延びなければならない。** 押し込まれた一覧が所有するサーキットブレーカーは 15 秒ごとにリセットされ——3 回の失敗がそれを落とすよりも速く——なので `forwarder::carry_health` がラベルで健全性を置き換え後の一覧へ移します。これは完全に Town OS の押し込みの間隔が生んだ rolodex 側の義務であり、フォワーダーのラベルが飾りではなく安定している理由でもあります。

## フォワーダー指定の文法

**手書きのパーサーが二つ、文法は一つ、そしてそのあいだに生成されたコードは無い。** こちらの `src/forwarder.rs` と Town OS の `src/rolodex/forwarder.go` は同じ文字列を受け付けます。リポジトリは互いを見ることができず、ビルド時に両者を結び付けるものもありません。`make check-townos-sync` が scheme の集合を比べ、双方のユニットテストが意図的に同じケースを固定します。それが唯一の守りだと思ってください。

`SetForwarders` は変わらず `repeated string` を取るので、この文法は既存のワイヤー型の上に乗ります：

| 指定 | トランスポート |
|---|---|
| `8.8.8.8:53` | 平文 UDP（Do53） |
| `tcp://8.8.8.8:53` | 平文 TCP（RFC 7766） |
| `tls://cloudflare-dns.com@1.1.1.1:853` | DoT（RFC 7858） |
| `https://cloudflare-dns.com@1.1.1.1/dns-query` | DoH（RFC 8484） |
| `quic://dns.adguard.com@94.140.14.14:853` | DoQ（RFC 9250） |

Town OS 側がまさに依存している性質：

- **裸の `ip:port` は平文 UDP です。** トランスポートが名指しできるようになる前に書かれたすべての呼び出し側が動き続け、scheme は別のものを求めるときに呼び出し側が足すものです。`udp://` と裸の形は同じフォワーダーへ解析され、同じ metrics ラベルを持ちます。
- **アドレスは常にリテラルであり、決してホスト名ではありません。** `name@ip` は、ダイヤルするアドレスと証明書を検証する名前を一つの文字列に載せます。これがブートストラップの性質です：先に解決しなければ使えない暗号化上流は、動く DNS の無いマシンを直すものにはなり得ません。
- **フォワーダーがどの階層に入るかは rolodex の決定であり、Town OS のものではありません。** それはフォワーダー自身から導かれ——暗号化、次に平文かつプライベート、次に平文かつパブリック——なので Town OS は優先度を表すために一覧を並べ替えてはならず、送った順序が試される順序だと仮定してもなりません。
- **検証は全部か無かです。** `SetForwarders` は一覧を置き換えるので、rolodex はどれか一つを適用する前にすべての項目を解析し、Town OS は押し込む前に検証します。一項目が落とされたまま受け入れられた一覧は、誰も求めなかったものをリゾルバーに持たせます。

**暗号化された上流は、この一覧を通してしかプログラムできません。** `rolodex.yml` の `resolution.secure_upstreams` には gRPC のセッターが無く、起動時に一度だけ読まれます。一覧が型付けされる前は、それは、外向きの `:53` を濾すネットワークで唯一動く階層が、同時に、そのマシンの唯一のリゾルバーを再起動せずには何も再設定できない唯一の階層でもあった、ということでした——一方で*プログラム可能だった*階層は、そういうネットワークがまさに落とす平文アドレスしか載せられませんでした。

## 固定アドレス

以下のそれぞれは複数のリポジトリに書かれており、どの組も少なくとも一度は間違っていました：

| 値 | rolodex | Town OS | `../install` |
|---|---|---|---|
| `127.0.0.2` | `dns.bind` の最初の項目 | `rolodex.DNSLoopback` | `add_bind 127.0.0.2` |
| `127.0.0.2:9153` | `metrics.bind` | `rolodex.DefaultMetricsPort` | `metrics.bind` のリテラル |
| `127.0.0.2:4443` | `doh.bind` | `systemcontroller.RolodexDohBackend` | `doh.bind` のリテラル |
| `/data/tls/dot` | `dot`／`doq` の `cert_path` | `systemcontroller.RolodexTLSSubdir` | `ENC_CERT` / `ENC_KEY` |
| `/data/rolodex.sock` | `grpc.unix_socket` | `Config.UnixSocketPath` | `unix_socket` のリテラル |

`443` ではなく `4443` であることは効いています：ingress は `0.0.0.0:443` で公開され、rolodex は `--net host` で動くので、一つの名前空間の中でワイルドカードの `:443` と特定の `127.0.0.2:443` が並ぶことは、後にバインドした方にとって `EADDRINUSE` です——起動順に応じて、DNS か ingress のどちらかが落ちます。

`127.0.0.1` ではなく `127.0.0.2` であることは、systemd-resolved の `127.0.0.53` のスタブと、`127.0.0.1` 上の他の何かを避けます。それは同時に `bootstrap-dns.sh` が resolved を向けるアドレスでもあるので、そのマシン自身の解決がそれ無しでは成り立たない唯一のバインドです。

### DoH のバックエンドが落ちているあいだ、ingress が何を返すか

Town OS は `127.0.0.2:4443` を、ふつうの ingress の vhost 上の path backend として前に置きます。その ingress は、到達できないバックエンド —— あるいは `5xx` を返したバックエンド —— に対して、Caddy の素の `502` ではなく自前のリトライページで答えます。サービスが利用できないことを告げ、五秒ごとに自分で読み込み直す `503` です。rolodex の再起動のあいだこそ、それが出てくる場面です。

**それは要求を門にしており、DoH のクライアントがその門に当たることは決してありません。** ページが配られるのは、`Accept` に `text/html` を含む `GET`／`HEAD` の要求に対してだけです。RFC 8484 のクライアントが送るのは `application/dns-message` で、しかも `POST` であることも同じくらい多いので:

- rolodex が返した `5xx` はそのままクライアントに届きます —— 状態・本体・ヘッダが写されます。
- listen していない rolodex は、`502` ではなく `Retry-After` の付いた `503` を ingress から返します。

後者がこの経路で観測できる唯一の変化であり、しかも rolodex 自身の側からは見えない変化です —— rolodex が動いていないときに起きるからです。ここに記すのは、「リゾルバが再起動しているあいだ DoH のクライアントが何を受け取るか」が Town OS の決定でありながら、それが姿を現すのは rolodex の不具合報告としてだからです。

**契約であるのはその門であって、その後ろのページではありません**。Town OS 側の変更が `Accept` の判定を落とせば、`/dns-query` は HTML のページで答えはじめ、この機械の DoH クライアントはどれも、送られてもいない DNS メッセージを解析しようとして失敗します。

## Metrics

rolodex は `127.0.0.2:9153` で Prometheus のテキスト形式を提供し、`metrics` の節の存在から起動時に一度だけ開かれます。Town OS はスクレイプ先を既定値から組み直すのではなく `rolodex.Manager.MetricsAddr()` から設定するので、ターゲットとバインドがずれることはありえません。

Town OS の監視が依存する二つの性質：

- **すべてのラベルの次元が有界であること。** 固定の列挙か、設定によって有界か。クライアントが制御できるものはすべて総括値に畳まれます（クエリ種別は `OTHER`、TLD は `other`）。**クエリ名がラベルになることは決してありません。** `upstream_queries_total{server}` と `upstream_skipped_total{server}` は設定されたフォワーダー一覧によって有界です。
- **新しいラベル値は追加されるだけで、決して挿入されません。** `BLOCK_*` 形式の定数は事前に確保された配列の位置であり、挿入は既存のすべてのカウンターを黙って貼り替えます。

メトリクスを追加あるいは改名することは、`README.md` と `DESIGN.md` の中のファミリー数と影響を受けるクエリを更新することを意味します——`tests/promql_docs_test.rs` が、文書に記された数をレジストリが実際に出すものに対して固定します。

## Town OS に求めること：並べ替えないこと、そして Do53 だと仮定しないこと

Town OS が*してはならない*ことが二つあり、そのどちらもかつては安全でした：

- **優先度を表すためにフォワーダー一覧を並べ替えたりソートしたりしないこと。** 階層の中での順序は尊重されます——それは rolodex が試す順番です——が、階層そのものは導かれます。Town OS が「暗号化を先に」で並べた一覧は、よくても冗長であり、そのソートが rolodex の導出と食い違えばログの中で誤解を招きます。
- **フォワーダーが `ip:port` だと仮定しないこと。** `Manager.Forwarders` は scheme 付きの指定を返しうります。ホストとポートを取り戻すためにフォワーダーを `:` で切るものは何であれ `tls://name@ip:853` に対して誤りであり、IPv6 リテラルに対しては壊滅的に誤りです。

## Town OS に求めること：DHCP のリゾルバーは resolv.conf からは発見できない

これは、Town OS／ttyforce の選択が rolodex 側の機能を黙って無効にしてしまう唯一の箇所であり、どちらの側もそれ単体では間違っていないので、ここに記録します。

- ttyforce は networkd のユニットに `[DHCPv4] UseDNS=no`（と v6 の対応物）を書くので、DHCP が配ったリゾルバーが rolodex を上回るリンクごとのリゾルバーになることは決してありません。
- `../install` の `bootstrap-dns.sh` は、rolodex が動いているあいだ systemd-resolved を `127.0.0.2` に向けます。
- `/etc/resolv.conf` は resolved 自身の `127.0.0.53` のスタブです。

三つともループバックか不在であり、三つとも正しくクエリのループとして捨てられます。したがって Town OS の `HostResolversFrom` は動いているマシン上で**何も**見つけられず、そのローカルフォワーダーの発見は、何かを見つけるためには `/proc/net/route` から**既定のゲートウェイ**を読まなければなりません。ゲートウェイが生き延びるのは、それが DHCP リースの DNS オプションではなく *router* オプションから来るからです。

その三つの選択のどれかを変えるものは何であれ、発見が何を見つけられるかを変えます。一緒に変えるか、まったく変えないかです。

## 既知の相違

デバッグして気づくことのないよう、記録しておきます：

- **rolodex の gRPC の面は、Town OS が使う分よりずっと大きい。** proto は管理 API の全体を宣言しており、Town OS の `Client` インターフェースはその部分集合です。この検査が確かめるのは、Town OS が宣言するすべてがここに存在することであって、その逆ではありません——どの Town OS クライアントも呼ばない rpc はズレではありません。
- **`shared_secret` は空で、認証はファイルシステムのパーミッションです。** インストールスクリプトは `grpc.tcp_bind: ""` と Unix ソケットを書くので、そのソケットのモードがアクセス制御のすべてです。TCP バインドならその秘密が要りますが、Town OS の中でそれを設定するものはありません。
- **`GetForwarders` は存在しません。** Town OS は無条件に押し込み、rolodex が実際に持っているものを読み戻すことはできません。だからこそ `GET /dns/status` は、rolodex が持っているものではなく、Town OS が*プログラムするであろう*ものを報告します。
- **スコープ／TLD のフォワーダーは別の一覧です。** `SetScopeTldForwarders` はスコープごとのピア転送であってグローバルなフォワーダー一覧ではなく、素の `ip:port` であり、上のトランスポート文法は取りません。

## 同期を保つ

Town OS はセマンティックなバージョンを持たないアーキテクチャごとのコンテナイメージとして出荷されるので、同期の正確な単位はコミットのリビジョンだけです——そして意図的に**固定はありません**。

**gRPC の面、フォワーダーの文法、あるいはいずれかの固定アドレスに触れるすべての変更について：**

1. `TOWNOS_DIR` と `INSTALL_DIR` をチェックアウトに向けて `make check-townos-sync` を走らせる。
2. 失敗はすべて、反対側**と**この文書を一緒に更新して解消する——決して片方だけにしないこと。
3. その変更が `rolodex.yml` のキーを改名あるいは削除するなら、インストールスクリプトと公開イメージは一緒に出荷しなければなりません。それを捕まえるバージョンの握手はありません。
